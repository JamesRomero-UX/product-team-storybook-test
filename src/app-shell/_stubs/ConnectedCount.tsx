// ConnectedCount renders an inline count next to a nav item. With no data
// loaded, return null — same as the production behaviour for empty results.
import type { FC } from 'react';

const ConnectedCount: FC<any> = () => null;
export type TCountOptions = string;
export default ConnectedCount;
