{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "tsc": {
      "dependsOn": ["^tsc", "generate-graphql", "db:pull", "//#sst:types"],
      "outputLogs": "full"
    }
  }
}
