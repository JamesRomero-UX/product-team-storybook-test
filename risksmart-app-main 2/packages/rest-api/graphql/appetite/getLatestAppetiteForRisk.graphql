query getLatestAppetitesForRisk($riskId: uuid!) {
  appetite(
    where: { parents: { ParentId: { _eq: $riskId } } }
    order_by: { CreatedAtTimestamp: desc_nulls_last }
  ) {
    Id
    ImpactId
    parents {
      ParentId
      risk {
        Id
      }
    }
  }
}
