mutation deleteControls($Ids: [uuid!]!) {
  delete_test_result(where: { ParentControlId: { _in: $Ids } }) {
    affected_rows
  }
  delete_control(where: { Id: { _in: $Ids } }) {
    returning {
      ...ControlParts
    }
  }
}
