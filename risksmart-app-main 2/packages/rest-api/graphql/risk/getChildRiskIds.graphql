query getChildRiskIds($riskId: uuid!) {
  risk(
    where: {
      _or: [
        { ParentRiskId: { _eq: $riskId } }
        { parent: { ParentRiskId: { _eq: $riskId } } }
      ]
    }
  ) {
    Id
  }
}
