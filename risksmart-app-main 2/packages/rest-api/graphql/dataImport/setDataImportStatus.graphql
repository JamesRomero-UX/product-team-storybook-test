mutation setDataImportStatus(
  $dataImportId: uuid!
  $status: data_import_status_enum
) {
  update_data_import(
    where: { Id: { _eq: $dataImportId } }
    _set: { Status: $status }
  ) {
    affected_rows
  }
}
