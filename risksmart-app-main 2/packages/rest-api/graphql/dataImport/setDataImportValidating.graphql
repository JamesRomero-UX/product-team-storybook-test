mutation setDataImportValidating($dataImportId: uuid!) {
  delete_data_import_error(where: { DataImportId: { _eq: $dataImportId } }) {
    affected_rows
  }

  update_data_import(
    where: { Id: { _eq: $dataImportId } }
    _set: { Status: validating }
  ) {
    affected_rows
  }
}
