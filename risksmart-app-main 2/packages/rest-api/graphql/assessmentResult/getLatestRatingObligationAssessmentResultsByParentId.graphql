query getLatestRatingObligationAssessmentResultsByParentId($Id: uuid) {
  obligation_assessment_result(
    where: {
      RatingType: { _in: ["assessment", "rating"] }
      parents: { ParentId: { _eq: $Id } }
    }
    order_by: { TestDate: desc }
    limit: 1
  ) {
    Id
    TestDate
  }
}
