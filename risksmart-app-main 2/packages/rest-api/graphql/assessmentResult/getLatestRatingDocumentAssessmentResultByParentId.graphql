query getLatestRatingDocumentAssessmentResultByParentId($Id: uuid) {
  document_assessment_result(
    where: {
      RatingType: { _in: ["assessment", "rating"] }
      parents: { ParentId: { _eq: $Id } }
    }
    limit: 1
    order_by: { TestDate: desc }
  ) {
    Id
    TestDate
  }
}
