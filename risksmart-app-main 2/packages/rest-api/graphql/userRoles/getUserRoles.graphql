query GetUserRoles($userId: String!, $orgKey: String!) {
  auth_user_role(
    where: { UserId: { _eq: $userId }, OrgKey: { _eq: $orgKey } }
  ) {
    Id
    RoleKey
    role_type {
      RoleKey
      Name
      Description
    }
  }
}
