query getAncestorContributorsForObject($Id: uuid!) {
  ancestor_contributor(where: { Id: { _eq: $Id }, user: { Status: { _neq: "archived" } } }) {
    AncestorId
    Id
    user {
      UserName
      Id
      Email
      Status
    }
    user_group {
      Id
      Email
      Name
    }
  }
}
