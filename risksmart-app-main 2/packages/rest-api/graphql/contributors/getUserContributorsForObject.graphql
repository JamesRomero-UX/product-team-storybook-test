query getUserContributorsForObject($Id: uuid!) {
  contributor(where: { ParentId: { _eq: $Id }, user: { Status: { _neq: "archived" } } }) {
    user {
      Id
      Email
      UserName
      Status
    }
  }
}
