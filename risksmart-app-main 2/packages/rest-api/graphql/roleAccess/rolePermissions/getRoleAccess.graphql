query getRoleAccess(
  $ObjectType: parent_type_enum
  $AccessType: access_type_enum
) {
  role_access(
    where: {
      ObjectType: { _eq: $ObjectType }
      AccessType: { _eq: $AccessType }
    }
  ) {
    ContributorType
  }
}
