mutation insertIngestionConfig($IngestionConfig: jsonb, $SecretArn: String) {
  insert_ingestion_config_one(
    object: { IngestionConfig: $IngestionConfig, SecretArn: $SecretArn }
  ) {
    Id
  }
}
