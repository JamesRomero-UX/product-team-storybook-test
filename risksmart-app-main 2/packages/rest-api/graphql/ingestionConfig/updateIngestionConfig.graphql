mutation updateIngestionConfig(
  $Id: uuid!
  $IngestionConfig: jsonb
  $SecretArn: String
) {
  update_ingestion_config_by_pk(
    pk_columns: { Id: $Id }
    _set: { IngestionConfig: $IngestionConfig, SecretArn: $SecretArn }
  ) {
    Id
    ModifiedAtTimestamp
  }
}
