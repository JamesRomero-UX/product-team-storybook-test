mutation updateThirdPartyContactRevoked($Id: uuid!, $IsRevoked: Boolean!) {
  update_third_party_contact_by_pk(
    pk_columns: { Id: $Id }
    _set: { IsRevoked: $IsRevoked }
  ) {
    Id
    IsRevoked
    ModifiedAtTimestamp
  }
}
