mutation insertActionParent(
  $ActionId: uuid!
  $ParentId: uuid!
  $ParentType: parent_type_enum!
) {
  insert_action_parent_one(
    object: {
      ActionId: $ActionId
      ParentId: $ParentId
      ParentType: $ParentType
    }
  ) {
    ActionId
    ParentId
    ParentType
  }
}
