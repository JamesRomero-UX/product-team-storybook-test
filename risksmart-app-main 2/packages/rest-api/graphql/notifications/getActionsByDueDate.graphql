query getActionByDateDue($minDateDue: timestamptz!, $maxDateDue: timestamptz!) {
  action(
    where: {
      Status: { _neq: closed }
      DateDue: { _gte: $minDateDue, _lt: $maxDateDue }
    }
  ) {
    Id
    OrgKey
    CreatedAtTimestamp
    DateDue
    Title
    SequentialId
  }
}
