mutation linkEnterpriseRisk(
  $objects: [enterprise_risk_instance_insert_input!]!
) {
  insert_enterprise_risk_instance(
    objects: $objects
    on_conflict: {
      constraint: enterprise_risk_instance_pkey
      update_columns: [EnterpriseRiskId, EntityId]
    }
  ) {
    affected_rows
  }
}
