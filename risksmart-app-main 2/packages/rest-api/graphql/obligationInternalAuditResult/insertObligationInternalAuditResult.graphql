mutation insertObligationInternalAuditResults(
  $results: [obligation_internal_audit_result_insert_input!]!
) {
  insert_obligation_internal_audit_result(objects: $results) {
    affected_rows
  }
}
