mutation deleteDepartmentTypes($DepartmentTypeIds: [uuid!]) {
  delete_department(where: { DepartmentTypeId: { _in: $DepartmentTypeIds } }) {
    affected_rows
  }
  delete_department_type(
    where: { DepartmentTypeId: { _in: $DepartmentTypeIds } }
  ) {
    affected_rows
  }
}
