{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "tsc": {
      "dependsOn": ["^tsc", "generate-graphql"],
      "outputLogs": "full"
    },
    "build": {
      "dependsOn": ["^build", "generate-graphql"]
    }
  }
}
