# property-filter

These files are taken from https://github.com/cloudscape-design/collection-hooks as they are not exposed in the package itself, so I had to copy them across.

eslint is disabled in all these files as they break a lot of our rules.
