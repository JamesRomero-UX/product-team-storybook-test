{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "test:unit": {
      "dependsOn": ["^build"],
      "outputLogs": "new-only",
      "outputs": ["coverage/**", "html/**"]
    }
  }
}
