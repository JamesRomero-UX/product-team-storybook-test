mutation instatiateEnterpriseRisk(
  $EnterpriseRiskIds: [uuid!]!
  $Entities: [uuid!]!
) {
  instantiateChildEnterpriseRisk(
    object: { EnterpriseRiskIds: $EnterpriseRiskIds, Entities: $Entities }
  ) {
    affected_rows
  }
}
