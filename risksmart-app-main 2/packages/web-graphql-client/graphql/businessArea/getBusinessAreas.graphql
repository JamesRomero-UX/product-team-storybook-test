query getBusinessAreas {
  business_area(order_by: { Title: asc }) {
    Id
    Title
    SequentialId
    CreatedAtTimestamp
    ModifiedAtTimestamp
    createdByUser {
      FriendlyName
    }
    modifiedByUser {
      FriendlyName
    }
  }
}
