query getObligationImpactAuditById($id: uuid!) {
  obligation_impact_audit(where: { Id: { _eq: $id } }) {
    Id
    Description
    ImpactRating
    CustomAttributeData
    CreatedByUser
    CreatedAtTimestamp
    ModifiedByUser
    ModifiedAtTimestamp
  }
}
