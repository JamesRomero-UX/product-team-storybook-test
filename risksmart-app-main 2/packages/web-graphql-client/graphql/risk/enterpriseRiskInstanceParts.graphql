# Enterprise risk instance entity reference (flat). Path is built client-side for unlimited depth.
fragment EnterpriseRiskInstanceWithEntityHierarchyParts on enterprise_risk_instance {
  EntityId
  EnterpriseRiskId
  entity {
    Id
    Name
    ParentId
  }
}
