query getRiskAuditById($Id: uuid) {
  risk_audit(where: { Id: { _eq: $Id } }) {
    Id
    Title
    Tier
    Description
    ParentRiskId
    CreatedByUser
    Treatment
    Status
    ModifiedByUser
    CreatedAtTimestamp
    ModifiedAtTimestamp
    CustomAttributeData
    SequentialId
  }
}
