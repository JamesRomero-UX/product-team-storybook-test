mutation deleteAppetites($Ids: [uuid!]) {
  delete_file(where: { relationFile: { ParentId: { _in: $Ids } } }) {
    affected_rows
  }

  delete_relation_file(where: { ParentId: { _in: $Ids } }) {
    affected_rows
  }

  delete_appetite(where: { Id: { _in: $Ids } }) {
    affected_rows
  }
}
