mutation updateControlledRiskInternalAuditResult(
  $Id: uuid!
  $Impact: Int
  $Likelihood: Int
  $Rating: Int
  $Rationale: String
  $TestDate: timestamptz
  $CustomAttributeData: jsonb
) {
  update_risk_controlled_internal_audit_result(
    where: { Id: { _eq: $Id } }
    _set: {
      CustomAttributeData: $CustomAttributeData
      Rating: $Rating
      Rationale: $Rationale
      TestDate: $TestDate
      Likelihood: $Likelihood
      Impact: $Impact
    }
  ) {
    affected_rows
  }
}

mutation updateUncontrolledRiskInternalAuditResult(
  $Id: uuid!
  $Impact: Int
  $Likelihood: Int
  $Rating: Int
  $Rationale: String
  $TestDate: timestamptz
  $CustomAttributeData: jsonb
) {
  update_risk_uncontrolled_internal_audit_result(
    where: { Id: { _eq: $Id } }
    _set: {
      CustomAttributeData: $CustomAttributeData
      Rating: $Rating
      Rationale: $Rationale
      TestDate: $TestDate
      Likelihood: $Likelihood
      Impact: $Impact
    }
  ) {
    affected_rows
  }
}
