query getCausesByParentIssueId($_eq: uuid!) {
  cause(where: { ParentIssueId: { _eq: $_eq } }) {
    ModifiedByUser
    CreatedByUser
    Title
    ModifiedAtTimestamp
    CreatedAtTimestamp
    Significance
    ParentIssueId
    Id
    Description
    CustomAttributeData
  }
}
