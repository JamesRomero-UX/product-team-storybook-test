query getRiskAssessmentResultImpactAuditById($id: uuid!) {
  risk_assessment_result_impact_audit(where: { Id: { _eq: $id } }) {
    Id
    RiskAssessmentResultId
    Label
    Value
    CreatedByUser
    CreatedAtTimestamp
    ModifiedByUser
    ModifiedAtTimestamp
  }
}
