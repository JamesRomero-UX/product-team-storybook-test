query GetUserGroupById($Id: uuid!) {
  user_group(where: { Id: { _eq: $Id } }) {
    Id
    Name
    Description
    Email
    OwnerContributor
    ModifiedAtTimestamp
    approvers_aggregate {
      aggregate {
        count
      }
    }
  }
}
