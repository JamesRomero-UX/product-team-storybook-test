query getApprovalAuditById($Id: uuid!) {
  approval_audit(where: { Id: { _eq: $Id } }, order_by: {ModifiedAtTimestamp: desc}) {
    Id
    Workflow
    ModifiedAtTimestamp
    ModifiedByUser
    CreatedAtTimestamp
    CreatedByUser
    InFlightEditRule
  }
}
