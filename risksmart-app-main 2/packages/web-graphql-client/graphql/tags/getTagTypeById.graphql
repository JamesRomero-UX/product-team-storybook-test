query GetTagTypeById($Id: uuid) {
  tag_type(where: { TagTypeId: { _eq: $Id } }) {
    TagTypeId
    Name
    Description
    ModifiedAtTimestamp
    TagTypeGroupId
    tag_type_group {
      Id
      Name
    }
  }
}
