mutation resetTabPreferences($ObjectType: parent_type_enum!) {
  delete_organisation_tab_preference(
    where: { ObjectType: { _eq: $ObjectType } }
  ) {
    affected_rows
  }
  delete_user_tab_preference(where: { ObjectType: { _eq: $ObjectType } }) {
    affected_rows
  }
}
