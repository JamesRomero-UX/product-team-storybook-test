query getImpactRatingById($id: uuid!) {
  impact_rating(where: { Id: { _eq: $id } }) {
    ...ImpactRatingParts
    createdByUser {
      FriendlyName
    }
  }
}

query getInternalAuditImpactRatingById($id: uuid!) {
  impact_internal_audit_rating(where: { Id: { _eq: $id } }) {
    ...ImpactInternalAuditRatingParts
    createdByUser {
      FriendlyName
    }
  }
}

query getSecondLineImpactRatingById($id: uuid!) {
  impact_second_line_rating(where: { Id: { _eq: $id } }) {
    ...ImpactSecondLineRatingParts
    createdByUser {
      FriendlyName
    }
  }
}
