mutation updateDocumentSecondLineResult(
  $Id: uuid!
  $Rating: Int
  $Rationale: String
  $TestDate: timestamptz
  $CustomAttributeData: jsonb
) {
  update_document_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: {
      CustomAttributeData: $CustomAttributeData
      Rating: $Rating
      Rationale: $Rationale
      TestDate: $TestDate
    }
  ) {
    affected_rows
  }
}
