query getIssueAssessmentHistory($where: issue_assessment_audit_bool_exp) {
  issue_assessment_audit(
    where: $where
    order_by: { ModifiedAtTimestamp: asc }
  ) {
    Status
    ParentIssueId
    ModifiedAtTimestamp
    Action
  }
}
