query GetAttestationConfig($id: uuid!) {
  attestation_config(where: { ParentId: { _eq: $id } }, limit: 1) {
    ...AttestationConfigParts
  }

  document_file(where: { ParentDocumentId: { _eq: $id } }, limit: 1) {
    Id
    Version
    Status
    PublishedDate
  }
}
