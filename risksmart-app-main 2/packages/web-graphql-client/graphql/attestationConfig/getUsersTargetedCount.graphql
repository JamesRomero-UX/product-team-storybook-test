query getGlobalUsersAndGroups {
  globalUsers: user_aggregate(where: { IsCustomerSupport: { _eq: false } }) {
    aggregate {
      count
    }
    nodes {
      Id
    }
  }
  userGroups: user_group {
    Id
    Name
    users {
      UserId
    }
  }
}
