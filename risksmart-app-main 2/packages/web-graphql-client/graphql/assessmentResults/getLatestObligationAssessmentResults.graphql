query getLatestObligationAssessmentResults {
  obligation_assessment_result(
    where: { RatingType: { _in: ["assessment", "rating"] } }
    order_by: [
      { TestDate: desc_nulls_last }
      { CreatedAtTimestamp: desc_nulls_last }
    ]
  ) {
    ...ObligationAssessmentResultParts
    parents {
      ParentId
      assessment {
        ...AssessmentParts
      }
    }
  }
}
