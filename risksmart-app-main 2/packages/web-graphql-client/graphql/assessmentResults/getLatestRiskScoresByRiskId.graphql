subscription getLatestRiskScoresByRiskId($RiskId: uuid!) {
  risk_score(where: { RiskId: { _eq: $RiskId } }) {
    ResidualScore
    InherentScore
    ResidualRating
    InherentRating
    ResidualImpact
    ResidualLikelihood
    InherentImpact
    InherentLikelihood
    ModifiedAtTimestamp
  }
}
