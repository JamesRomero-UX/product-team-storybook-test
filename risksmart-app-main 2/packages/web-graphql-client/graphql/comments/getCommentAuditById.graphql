query getCommentAuditById($Id: uuid!) {
  comment_audit(where: { Id: { _eq: $Id } }, order_by: {ModifiedAtTimestamp: desc}) {
    Id
    Content
    ModifiedAtTimestamp
    CreatedAtTimestamp
    CreatedByUser
    ModifiedByUser
  }
}
