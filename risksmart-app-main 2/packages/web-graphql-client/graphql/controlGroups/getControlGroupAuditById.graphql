query getControlGroupAuditById($_eq: uuid!) {
  control_group_audit(where: { Id: { _eq: $_eq } }, order_by: {ModifiedAtTimestamp: desc}) {
    Description
    Id
    Owner
    ModifiedAtTimestamp
    CreatedAtTimestamp
    Title
    ModifiedByUser
    CreatedByUser
    CustomAttributeData
  }
}
