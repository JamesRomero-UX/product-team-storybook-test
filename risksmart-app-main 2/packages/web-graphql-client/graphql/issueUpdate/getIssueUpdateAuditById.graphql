query getIssueUpdateAuditById($Id: uuid!) {
  issue_update_audit(where: { Id: { _eq: $Id } }) {
    Description
    Id
    ParentIssueId
    CreatedAtTimestamp
    ModifiedAtTimestamp
    Title
    CreatedByUser
    ModifiedByUser
    CustomAttributeData
  }
}
