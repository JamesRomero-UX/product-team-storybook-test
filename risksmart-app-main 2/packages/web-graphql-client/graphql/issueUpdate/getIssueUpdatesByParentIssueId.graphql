query getIssueUpdatesByParentIssueId($_eq: uuid!) {
  issue_update(where: { ParentIssueId: { _eq: $_eq } }) {
    ...IssueUpdateParts
    createdByUser {
      FriendlyName
    }
    files {
      ...RelationFileParts
    }
  }
}
