mutation upsertRecentUsers($RecentUserIds: [String!]!) {
  insert_user_search_preferences(
    objects: [{ RecentUserIds: $RecentUserIds }]
    on_conflict: {
      constraint: recent_users_pkey
      update_columns: [RecentUserIds]
    }
  ) {
    affected_rows
  }
}
