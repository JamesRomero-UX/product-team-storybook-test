query getLinkedItemAudit(
  $Id: uuid!
) {
  risksmart_linked_item_audit(where: { Id: { _eq: $Id } }) {
    Id
    Source
    Target
    ModifiedAtTimestamp
    ModifiedByUser
    CreatedAtTimestamp
    CreatedByUser
  }
}
