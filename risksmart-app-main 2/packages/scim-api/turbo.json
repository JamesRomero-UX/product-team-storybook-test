{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "tsc": {
      "dependsOn": ["^tsc", "generate-graphql", "//#sst:types"],
      "outputLogs": "full"
    }
  }
}
