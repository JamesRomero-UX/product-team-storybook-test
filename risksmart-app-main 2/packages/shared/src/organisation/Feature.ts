/**
 * @deprecated Use `OrgFeature` from `@risksmart-app/modules` instead.
 * This type is kept for backwards compatibility with existing consumers.
 */
export type { OrgFeature as Feature } from '@risksmart-app/modules/src/index';
