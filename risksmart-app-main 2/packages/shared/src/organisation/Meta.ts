export interface Meta {
  /** Comma separated list of features */
  features?: string;
  isRisksmartOrg?: string;
  /** Optional per-organisation base URL for deep links (e.g. https://app.customer.com) */
  baseUrl?: string;
}
