mutation updateRisk(
  $Id: uuid!
  $Title: String!
  $Tier: Int
  $ParentRiskId: uuid
) {
  update_risk(
    where: { Id: { _eq: $Id } }
    _set: { Title: $Title, Tier: $Tier, ParentRiskId: $ParentRiskId }
  ) {
    affected_rows
  }
}
