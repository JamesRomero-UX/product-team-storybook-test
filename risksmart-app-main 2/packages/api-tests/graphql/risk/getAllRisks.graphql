query getAllRisks {
  risk(order_by: { Title: asc }) {
    Description
    Id
    ParentRiskId
    Tier
    CreatedAtTimestamp
    ModifiedAtTimestamp
    Title
    CreatedByUser
    ModifiedByUser
  }
}
