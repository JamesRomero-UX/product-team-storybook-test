mutation updateInternalAudit(
  $Id: uuid!
  $Title: String!
  $Description: String
) {
  update_internal_audit_entity(
    where: { Id: { _eq: $Id } }
    _set: { Title: $Title, Description: $Description }
  ) {
    affected_rows
  }
}
