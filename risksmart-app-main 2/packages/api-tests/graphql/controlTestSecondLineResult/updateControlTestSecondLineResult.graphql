mutation updateControlTestSecondLineResult($Id: uuid!, $Title: String!) {
  update_control_test_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: { Title: $Title }
  ) {
    affected_rows
  }
}
