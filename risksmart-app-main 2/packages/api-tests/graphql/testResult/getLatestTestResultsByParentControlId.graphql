query getLatestTestResultsByParentControlId($Id: uuid!) {
  test_result(
    where: {
      ParentControlId: { _eq: $Id }
      RatingType: { _in: ["assessment", "rating"] }
    }
    order_by: { TestDate: desc }
  ) {
    TestDate
    Id
  }
}
