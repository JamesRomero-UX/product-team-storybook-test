query getTestResultsByControlId($_eq: uuid = "") {
  test_result(where: { ParentControlId: { _eq: $_eq } }) {
    Title
    Submitter
    Description
    ParentControlId
    TestType
    DesignEffectiveness
    PerformanceEffectiveness
    OverallEffectiveness
    TestDate
    CustomAttributeData
    submitter {
      UserName
    }
  }
}
