query getLatestControlTestInternalAuditResultByParentControlId($Id: uuid!) {
  control_test_internal_audit_result(
    where: { ParentControlId: { _eq: $Id } }
    order_by: { TestDate: desc }
  ) {
    TestDate
    Id
  }
}
