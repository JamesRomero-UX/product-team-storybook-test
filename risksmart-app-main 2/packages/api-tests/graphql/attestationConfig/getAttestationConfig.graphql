query getAttestationConfigByParentId($ParentId: uuid!) {
  attestation_config_by_pk(ParentId: $ParentId) {
    ParentId
    RequireGlobalAttestation
    AttestationTimeLimit
    CreatedAtTimestamp
    CreatedByUser
    ModifiedAtTimestamp
    ModifiedByUser
  }
}
