mutation updateDocumentSecondLineResult($Id: uuid!, $Rating: Int!) {
  update_document_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: { Rating: $Rating }
  ) {
    affected_rows
  }
}

mutation updateObligationSecondLineResult($Id: uuid!, $Rating: Int!) {
  update_obligation_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: { Rating: $Rating }
  ) {
    affected_rows
  }
}

mutation updateRiskControlledSecondLineResult($Id: uuid!, $Rating: Int!) {
  update_risk_controlled_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: { Rating: $Rating }
  ) {
    affected_rows
  }
}

mutation updateRiskUncontrolledSecondLineResult($Id: uuid!, $Rating: Int!) {
  update_risk_uncontrolled_second_line_result(
    where: { Id: { _eq: $Id } }
    _set: { Rating: $Rating }
  ) {
    affected_rows
  }
}
