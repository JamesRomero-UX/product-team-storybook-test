{
  "blocks": [
    {
      "type": "header",
      "text": {
        "type": "plain_text",
        "text": "{{ orgName }}",
        "emoji": true
      }
    },
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "A change request has been created for {{ objectParent.title }} ({{ objectParent.sequenceId }}) that requires your approval"
      }
    },
    {
      "type": "context",
      "elements": [
        {
          "type": "mrkdwn",
          "text": "Change Request ID: {{ objectSequenceId }} has been created by {{ actor.email }}\nCreated at: {{ objectTimeStamp }}"
        }
      ]
    },
    {% if data.requesterComment and data.requesterComment != "" %}
    {
      "type": "section",
      "text": { "type": "mrkdwn", "text": "*Reason for request:* {{ data.requesterComment }}" }
    },
    {% endif %}
    {
      "type": "actions",
      "elements": [
        {
          "type": "button",
          "text": {
            "type": "plain_text",
            "text": "View change requests",
            "emoji": true
          },
          "url": "{{ data.deepLinkBaseUrl | default: vars.app_base_url }}{{ objectParent.url }}?organization={{ data.deepLinkOrgId | default: data.org_id }}{% if recipient.connection %}&connection={{ recipient.connection }}{% endif %}"
        }
      ]
    }
  ]
}
