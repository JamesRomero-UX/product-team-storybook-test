{
	"blocks": [
		{
      "type": "header",
      "text": {
        "type": "plain_text",
        "text": "{{ orgName }}",
        "emoji": true
      }
    },
    {
			"type": "section",
			"text": {
				"type": "mrkdwn",
        "text": "Reminder to attest to the following policy: *{{ objectTitle }}*"
			}
    },
    {
      "type": "context",
      "elements": [
        {
          "type": "mrkdwn",
          "text": "You have {{ daysRemaining }} {{ daysRemaining | pluralize: 'day', 'days' }} remaining to attest. Expires on {{ expiresAtDate }}."
        }
      ]
		},
		{
			"type": "actions",
			"elements": [
				{
					"type": "button",
					"text": {
						"type": "plain_text",
						"text": "View policy",
						"emoji": true
					},
					"url": "{{ data.deepLinkBaseUrl | default: vars.app_base_url }}/public-policies/{{ parentObjectId }}/files/{{ objectId }}?organization={{ data.deepLinkOrgId | default: data.org_id }}{% if recipient.connection %}&connection={{ recipient.connection }}{% endif %}"
				}
			]
		}
	]
}
