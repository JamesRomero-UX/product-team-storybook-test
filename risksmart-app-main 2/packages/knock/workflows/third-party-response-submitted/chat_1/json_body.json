{
	"blocks": [
		{
			"type": "header",
			"text": {
				"type": "plain_text",
				"text": "{{ orgName }}",
				"emoji": true
			}
		},
		{
			"type": "section",
			"text": {
				"type": "mrkdwn",
				"text": "Response submitted: {{ questionnaireTitle }} - {{ questionnaireVersion }}"
			}
		},
		{
			"type": "context",
			"elements": [
				{
					"type": "mrkdwn",
					"text": "{{ thirdPartyTitle }} has submitted a response for {{ questionnaireTitle }} - {{ questionnaireVersion }}."
				}
			]
		},
		{
			"type": "actions",
			"elements": [
				{
					"type": "button",
					"text": {
						"type": "plain_text",
						"text": "View risk",
						"emoji": true
					},
					"url": "{{ data.deepLinkBaseUrl | default: vars.app_base_url }}/third-party/{{ thirdPartyId }}/questionnaire-responses/{{ objectId }}?organization={{ data.deepLinkOrgId | default: data.org_id }}{% if recipient.connection %}&connection={{ recipient.connection }}{% endif %}"
				}
			]
		}
	]
}