bucket         = "${var.environment}-${var.region}-risksmart-terraform-state"
key            = "terraform/external-api/${var.environment}/${var.region}/terraform.tfstate"
region         = var.region
encrypt        = true
dynamodb_table = "${var.environment}-${var.region}-risksmart-terraform-lock"
