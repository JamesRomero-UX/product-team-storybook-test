table:
  name: approver_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: approver_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - CreatedByUser
        - ModifiedByUser
        - CreatedAtTimestamp
        - ModifiedAtTimestamp
        - Id
        - OwnerApprover
        - UserId
        - UserGroupId
        - LevelId
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
