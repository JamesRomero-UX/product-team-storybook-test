table:
  name: ingestion_config_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: ingestion_config_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - Id
        - IngestionConfig
        - SecretArn
        - CreatedByUser
        - CreatedAtTimestamp
        - ModifiedByUser
        - ModifiedAtTimestamp
        - Action
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
