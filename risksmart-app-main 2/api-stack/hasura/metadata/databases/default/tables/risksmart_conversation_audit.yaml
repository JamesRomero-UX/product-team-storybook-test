table:
  name: conversation_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: conversation_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - Id
        - IsResolved
        - ModifiedAtTimestamp
        - ModifiedByUser
        - CreatedByUser
        - CreatedAtTimestamp
        - ParentId
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
