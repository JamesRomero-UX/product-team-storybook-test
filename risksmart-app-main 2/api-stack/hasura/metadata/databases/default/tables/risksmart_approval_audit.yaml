table:
  name: approval_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: approval_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - CreatedByUser
        - ModifiedByUser
        - CreatedAtTimestamp
        - ModifiedAtTimestamp
        - Id
        - ParentId
        - Workflow
        - InFlightEditRule
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
