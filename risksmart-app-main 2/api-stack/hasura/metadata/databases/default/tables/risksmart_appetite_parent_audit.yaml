table:
  name: appetite_parent_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: appetite_parent_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - ParentId
        - Id
        - ModifiedAtTimestamp
        - ModifiedByUser
        - CreatedByUser
        - CreatedAtTimestamp
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
      allow_aggregations: true
