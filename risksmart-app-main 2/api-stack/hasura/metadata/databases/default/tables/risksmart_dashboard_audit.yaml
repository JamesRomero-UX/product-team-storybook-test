table:
  name: dashboard_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: dashboard_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - Id
        - Name
        - Sharing
        - Description
        - Content
        - CreatedByUser
        - CreatedAtTimestamp
        - ModifiedByUser
        - ModifiedAtTimestamp
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
