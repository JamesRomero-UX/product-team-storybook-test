table:
  name: colour_palette_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: colour_palette_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - Id
        - Name
        - Settings
        - CreatedByUser
        - ModifiedByUser
        - CreatedAtTimestamp
        - ModifiedAtTimestamp
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
