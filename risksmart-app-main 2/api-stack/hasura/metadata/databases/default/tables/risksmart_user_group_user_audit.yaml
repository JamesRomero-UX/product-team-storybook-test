table:
  name: user_group_user_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: user_group_user_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - UserId
        - UserGroupId
        - CreatedAtTimestamp
        - CreatedByUser
        - ModifiedAtTimestamp
        - ModifiedByUser
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
