table:
  name: action_parent_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: action_parent_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - ParentId
        - ActionId
        - ModifiedAtTimestamp
        - ModifiedByUser
        - CreatedByUser
        - CreatedAtTimestamp
        - ParentType
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
