table:
  name: owner_group_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: owner_group_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - ParentId
        - UserGroupId
        - CreatedAtTimestamp
        - CreatedByUser
        - ModifiedAtTimestamp
        - ModifiedByUser
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
