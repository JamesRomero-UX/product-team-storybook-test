table:
  name: custom_attribute_schema_audit
  schema: risksmart
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: custom_attribute_schema_audit
  custom_root_fields: {}
select_permissions:
  - role: read:audit
    permission:
      columns:
        - CreatedAtTimestamp
        - CreatedByUser
        - ModifiedAtTimestamp
        - ModifiedByUser
        - Id
        - Title
        - Schema
        - UiSchema
      filter:
        OrgKey:
          _eq: X-Hasura-org-id
