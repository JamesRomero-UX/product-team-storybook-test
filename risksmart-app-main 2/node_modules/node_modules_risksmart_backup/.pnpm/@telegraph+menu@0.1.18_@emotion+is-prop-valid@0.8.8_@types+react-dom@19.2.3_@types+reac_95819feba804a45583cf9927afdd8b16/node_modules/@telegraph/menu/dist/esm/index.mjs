import { jsx as C, jsxs as _t } from "react/jsx-runtime";
import * as Ft from "@radix-ui/react-menu";
import { useControllableState as Es } from "@radix-ui/react-use-controllable-state";
import { RefToTgphRef as ce } from "@telegraph/helpers";
import { Stack as mn, Box as Ls } from "@telegraph/layout";
import gn, { createContext as nt, useRef as lt, useLayoutEffect as Ns, useEffect as le, useState as Bs, useContext as E, useMemo as Dt, Fragment as yn, createElement as Ks, useCallback as Ws, useInsertionEffect as $s, forwardRef as js } from "react";
import { Button as dt } from "@telegraph/button";
import { Check as _s } from "lucide-react";
const Gs = nt({});
function Hs(t) {
  const e = lt(null);
  return e.current === null && (e.current = t()), e.current;
}
const fe = typeof window < "u", Us = fe ? Ns : le, bn = /* @__PURE__ */ nt(null);
function zs(t, e) {
  t.indexOf(e) === -1 && t.push(e);
}
function Ys(t, e) {
  const n = t.indexOf(e);
  n > -1 && t.splice(n, 1);
}
const z = (t, e, n) => n > e ? e : n < t ? t : n;
function Gt(t, e) {
  return e ? `${t}. For more information and steps for solving, visit https://motion.dev/troubleshooting/${e}` : t;
}
let gt = () => {
}, Y = () => {
};
process.env.NODE_ENV !== "production" && (gt = (t, e, n) => {
  !t && typeof console < "u" && console.warn(Gt(e, n));
}, Y = (t, e, n) => {
  if (!t)
    throw new Error(Gt(e, n));
});
const K = {}, vn = (t) => /^-?(?:\d+(?:\.\d+)?|\.\d+)$/u.test(t);
function Xs(t) {
  return typeof t == "object" && t !== null;
}
const Tn = (t) => /^0[^.\s]+$/u.test(t);
// @__NO_SIDE_EFFECTS__
function de(t) {
  let e;
  return () => (e === void 0 && (e = t()), e);
}
const st = /* @__NO_SIDE_EFFECTS__ */ (t) => t, qs = (t, e) => (n) => e(t(n)), Ot = (...t) => t.reduce(qs), wn = /* @__NO_SIDE_EFFECTS__ */ (t, e, n) => {
  const s = e - t;
  return s === 0 ? 1 : (n - t) / s;
};
class Vn {
  constructor() {
    this.subscriptions = [];
  }
  add(e) {
    return zs(this.subscriptions, e), () => Ys(this.subscriptions, e);
  }
  notify(e, n, s) {
    const i = this.subscriptions.length;
    if (i)
      if (i === 1)
        this.subscriptions[0](e, n, s);
      else
        for (let r = 0; r < i; r++) {
          const o = this.subscriptions[r];
          o && o(e, n, s);
        }
  }
  getSize() {
    return this.subscriptions.length;
  }
  clear() {
    this.subscriptions.length = 0;
  }
}
const N = /* @__NO_SIDE_EFFECTS__ */ (t) => t * 1e3, B = /* @__NO_SIDE_EFFECTS__ */ (t) => t / 1e3;
function Sn(t, e) {
  return e ? t * (1e3 / e) : 0;
}
const Ne = /* @__PURE__ */ new Set();
function xn(t, e, n) {
  t || Ne.has(e) || (console.warn(Gt(e, n)), Ne.add(e));
}
const An = (t, e, n) => (((1 - 3 * n + 3 * e) * t + (3 * n - 6 * e)) * t + 3 * e) * t, Zs = 1e-7, Js = 12;
function Qs(t, e, n, s, i) {
  let r, o, a = 0;
  do
    o = e + (n - e) / 2, r = An(o, s, i) - t, r > 0 ? n = o : e = o;
  while (Math.abs(r) > Zs && ++a < Js);
  return o;
}
function yt(t, e, n, s) {
  if (t === e && n === s)
    return st;
  const i = (r) => Qs(r, 0, 1, t, n);
  return (r) => r === 0 || r === 1 ? r : An(i(r), e, s);
}
const Mn = (t) => (e) => e <= 0.5 ? t(2 * e) / 2 : (2 - t(2 * (1 - e))) / 2, Cn = (t) => (e) => 1 - t(1 - e), Pn = /* @__PURE__ */ yt(0.33, 1.53, 0.69, 0.99), he = /* @__PURE__ */ Cn(Pn), Fn = /* @__PURE__ */ Mn(he), Dn = (t) => (t *= 2) < 1 ? 0.5 * he(t) : 0.5 * (2 - Math.pow(2, -10 * (t - 1))), pe = (t) => 1 - Math.sin(Math.acos(t)), ti = Cn(pe), On = Mn(pe), ei = /* @__PURE__ */ yt(0.42, 0, 1, 1), ni = /* @__PURE__ */ yt(0, 0, 0.58, 1), In = /* @__PURE__ */ yt(0.42, 0, 0.58, 1), si = (t) => Array.isArray(t) && typeof t[0] != "number", Rn = (t) => Array.isArray(t) && typeof t[0] == "number", Be = {
  linear: st,
  easeIn: ei,
  easeInOut: In,
  easeOut: ni,
  circIn: pe,
  circInOut: On,
  circOut: ti,
  backIn: he,
  backInOut: Fn,
  backOut: Pn,
  anticipate: Dn
}, ii = (t) => typeof t == "string", Ke = (t) => {
  if (Rn(t)) {
    Y(t.length === 4, "Cubic bezier arrays must contain four numerical values.", "cubic-bezier-length");
    const [e, n, s, i] = t;
    return yt(e, n, s, i);
  } else if (ii(t))
    return Y(Be[t] !== void 0, `Invalid easing type '${t}'`, "invalid-easing-type"), Be[t];
  return t;
}, vt = [
  "setup",
  // Compute
  "read",
  // Read
  "resolveKeyframes",
  // Write/Read/Write/Read
  "preUpdate",
  // Compute
  "update",
  // Compute
  "preRender",
  // Compute
  "render",
  // Write
  "postRender"
  // Compute
];
function ri(t, e) {
  let n = /* @__PURE__ */ new Set(), s = /* @__PURE__ */ new Set(), i = !1, r = !1;
  const o = /* @__PURE__ */ new WeakSet();
  let a = {
    delta: 0,
    timestamp: 0,
    isProcessing: !1
  };
  function c(u) {
    o.has(u) && (l.schedule(u), t()), u(a);
  }
  const l = {
    /**
     * Schedule a process to run on the next frame.
     */
    schedule: (u, f = !1, d = !1) => {
      const y = d && i ? n : s;
      return f && o.add(u), y.has(u) || y.add(u), u;
    },
    /**
     * Cancel the provided callback from running on the next frame.
     */
    cancel: (u) => {
      s.delete(u), o.delete(u);
    },
    /**
     * Execute all schedule callbacks.
     */
    process: (u) => {
      if (a = u, i) {
        r = !0;
        return;
      }
      i = !0, [n, s] = [s, n], n.forEach(c), n.clear(), i = !1, r && (r = !1, l.process(u));
    }
  };
  return l;
}
const oi = 40;
function kn(t, e) {
  let n = !1, s = !0;
  const i = {
    delta: 0,
    timestamp: 0,
    isProcessing: !1
  }, r = () => n = !0, o = vt.reduce((g, x) => (g[x] = ri(r), g), {}), { setup: a, read: c, resolveKeyframes: l, preUpdate: u, update: f, preRender: d, render: h, postRender: y } = o, b = () => {
    const g = K.useManualTiming ? i.timestamp : performance.now();
    n = !1, K.useManualTiming || (i.delta = s ? 1e3 / 60 : Math.max(Math.min(g - i.timestamp, oi), 1)), i.timestamp = g, i.isProcessing = !0, a.process(i), c.process(i), l.process(i), u.process(i), f.process(i), d.process(i), h.process(i), y.process(i), i.isProcessing = !1, n && e && (s = !1, t(b));
  }, T = () => {
    n = !0, s = !0, i.isProcessing || t(b);
  };
  return { schedule: vt.reduce((g, x) => {
    const v = o[x];
    return g[x] = (S, P = !1, w = !1) => (n || T(), v.schedule(S, P, w)), g;
  }, {}), cancel: (g) => {
    for (let x = 0; x < vt.length; x++)
      o[vt[x]].cancel(g);
  }, state: i, steps: o };
}
const { schedule: L, cancel: Ht, state: St } = /* @__PURE__ */ kn(typeof requestAnimationFrame < "u" ? requestAnimationFrame : st, !0);
let wt;
function ai() {
  wt = void 0;
}
const k = {
  now: () => (wt === void 0 && k.set(St.isProcessing || K.useManualTiming ? St.timestamp : performance.now()), wt),
  set: (t) => {
    wt = t, queueMicrotask(ai);
  }
}, En = (t) => (e) => typeof e == "string" && e.startsWith(t), Ln = /* @__PURE__ */ En("--"), ui = /* @__PURE__ */ En("var(--"), me = (t) => ui(t) ? ci.test(t.split("/*")[0].trim()) : !1, ci = /var\(--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)$/iu, it = {
  test: (t) => typeof t == "number",
  parse: parseFloat,
  transform: (t) => t
}, ht = {
  ...it,
  transform: (t) => z(0, 1, t)
}, Tt = {
  ...it,
  default: 1
}, ft = (t) => Math.round(t * 1e5) / 1e5, ge = /-?(?:\d+(?:\.\d+)?|\.\d+)/gu;
function li(t) {
  return t == null;
}
const fi = /^(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))$/iu, ye = (t, e) => (n) => !!(typeof n == "string" && fi.test(n) && n.startsWith(t) || e && !li(n) && Object.prototype.hasOwnProperty.call(n, e)), Nn = (t, e, n) => (s) => {
  if (typeof s != "string")
    return s;
  const [i, r, o, a] = s.match(ge);
  return {
    [t]: parseFloat(i),
    [e]: parseFloat(r),
    [n]: parseFloat(o),
    alpha: a !== void 0 ? parseFloat(a) : 1
  };
}, di = (t) => z(0, 255, t), Lt = {
  ...it,
  transform: (t) => Math.round(di(t))
}, _ = {
  test: /* @__PURE__ */ ye("rgb", "red"),
  parse: /* @__PURE__ */ Nn("red", "green", "blue"),
  transform: ({ red: t, green: e, blue: n, alpha: s = 1 }) => "rgba(" + Lt.transform(t) + ", " + Lt.transform(e) + ", " + Lt.transform(n) + ", " + ft(ht.transform(s)) + ")"
};
function hi(t) {
  let e = "", n = "", s = "", i = "";
  return t.length > 5 ? (e = t.substring(1, 3), n = t.substring(3, 5), s = t.substring(5, 7), i = t.substring(7, 9)) : (e = t.substring(1, 2), n = t.substring(2, 3), s = t.substring(3, 4), i = t.substring(4, 5), e += e, n += n, s += s, i += i), {
    red: parseInt(e, 16),
    green: parseInt(n, 16),
    blue: parseInt(s, 16),
    alpha: i ? parseInt(i, 16) / 255 : 1
  };
}
const Ut = {
  test: /* @__PURE__ */ ye("#"),
  parse: hi,
  transform: _.transform
}, bt = /* @__NO_SIDE_EFFECTS__ */ (t) => ({
  test: (e) => typeof e == "string" && e.endsWith(t) && e.split(" ").length === 1,
  parse: parseFloat,
  transform: (e) => `${e}${t}`
}), W = /* @__PURE__ */ bt("deg"), Q = /* @__PURE__ */ bt("%"), m = /* @__PURE__ */ bt("px"), pi = /* @__PURE__ */ bt("vh"), mi = /* @__PURE__ */ bt("vw"), We = {
  ...Q,
  parse: (t) => Q.parse(t) / 100,
  transform: (t) => Q.transform(t * 100)
}, Z = {
  test: /* @__PURE__ */ ye("hsl", "hue"),
  parse: /* @__PURE__ */ Nn("hue", "saturation", "lightness"),
  transform: ({ hue: t, saturation: e, lightness: n, alpha: s = 1 }) => "hsla(" + Math.round(t) + ", " + Q.transform(ft(e)) + ", " + Q.transform(ft(n)) + ", " + ft(ht.transform(s)) + ")"
}, M = {
  test: (t) => _.test(t) || Ut.test(t) || Z.test(t),
  parse: (t) => _.test(t) ? _.parse(t) : Z.test(t) ? Z.parse(t) : Ut.parse(t),
  transform: (t) => typeof t == "string" ? t : t.hasOwnProperty("red") ? _.transform(t) : Z.transform(t),
  getAnimatableNone: (t) => {
    const e = M.parse(t);
    return e.alpha = 0, M.transform(e);
  }
}, gi = /(?:#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\))/giu;
function yi(t) {
  var e, n;
  return isNaN(t) && typeof t == "string" && (((e = t.match(ge)) == null ? void 0 : e.length) || 0) + (((n = t.match(gi)) == null ? void 0 : n.length) || 0) > 0;
}
const Bn = "number", Kn = "color", bi = "var", vi = "var(", $e = "${}", Ti = /var\s*\(\s*--(?:[\w-]+\s*|[\w-]+\s*,(?:\s*[^)(\s]|\s*\((?:[^)(]|\([^)(]*\))*\))+\s*)\)|#[\da-f]{3,8}|(?:rgb|hsl)a?\((?:-?[\d.]+%?[,\s]+){2}-?[\d.]+%?\s*(?:[,/]\s*)?(?:\b\d+(?:\.\d+)?|\.\d+)?%?\)|-?(?:\d+(?:\.\d+)?|\.\d+)/giu;
function pt(t) {
  const e = t.toString(), n = [], s = {
    color: [],
    number: [],
    var: []
  }, i = [];
  let r = 0;
  const a = e.replace(Ti, (c) => (M.test(c) ? (s.color.push(r), i.push(Kn), n.push(M.parse(c))) : c.startsWith(vi) ? (s.var.push(r), i.push(bi), n.push(c)) : (s.number.push(r), i.push(Bn), n.push(parseFloat(c))), ++r, $e)).split($e);
  return { values: n, split: a, indexes: s, types: i };
}
function Wn(t) {
  return pt(t).values;
}
function $n(t) {
  const { split: e, types: n } = pt(t), s = e.length;
  return (i) => {
    let r = "";
    for (let o = 0; o < s; o++)
      if (r += e[o], i[o] !== void 0) {
        const a = n[o];
        a === Bn ? r += ft(i[o]) : a === Kn ? r += M.transform(i[o]) : r += i[o];
      }
    return r;
  };
}
const wi = (t) => typeof t == "number" ? 0 : M.test(t) ? M.getAnimatableNone(t) : t;
function Vi(t) {
  const e = Wn(t);
  return $n(t)(e.map(wi));
}
const rt = {
  test: yi,
  parse: Wn,
  createTransformer: $n,
  getAnimatableNone: Vi
};
function Nt(t, e, n) {
  return n < 0 && (n += 1), n > 1 && (n -= 1), n < 1 / 6 ? t + (e - t) * 6 * n : n < 1 / 2 ? e : n < 2 / 3 ? t + (e - t) * (2 / 3 - n) * 6 : t;
}
function Si({ hue: t, saturation: e, lightness: n, alpha: s }) {
  t /= 360, e /= 100, n /= 100;
  let i = 0, r = 0, o = 0;
  if (!e)
    i = r = o = n;
  else {
    const a = n < 0.5 ? n * (1 + e) : n + e - n * e, c = 2 * n - a;
    i = Nt(c, a, t + 1 / 3), r = Nt(c, a, t), o = Nt(c, a, t - 1 / 3);
  }
  return {
    red: Math.round(i * 255),
    green: Math.round(r * 255),
    blue: Math.round(o * 255),
    alpha: s
  };
}
function xt(t, e) {
  return (n) => n > 0 ? e : t;
}
const It = (t, e, n) => t + (e - t) * n, Bt = (t, e, n) => {
  const s = t * t, i = n * (e * e - s) + s;
  return i < 0 ? 0 : Math.sqrt(i);
}, xi = [Ut, _, Z], Ai = (t) => xi.find((e) => e.test(t));
function je(t) {
  const e = Ai(t);
  if (gt(!!e, `'${t}' is not an animatable color. Use the equivalent color code instead.`, "color-not-animatable"), !e)
    return !1;
  let n = e.parse(t);
  return e === Z && (n = Si(n)), n;
}
const _e = (t, e) => {
  const n = je(t), s = je(e);
  if (!n || !s)
    return xt(t, e);
  const i = { ...n };
  return (r) => (i.red = Bt(n.red, s.red, r), i.green = Bt(n.green, s.green, r), i.blue = Bt(n.blue, s.blue, r), i.alpha = It(n.alpha, s.alpha, r), _.transform(i));
}, zt = /* @__PURE__ */ new Set(["none", "hidden"]);
function Mi(t, e) {
  return zt.has(t) ? (n) => n <= 0 ? t : e : (n) => n >= 1 ? e : t;
}
function Ci(t, e) {
  return (n) => It(t, e, n);
}
function be(t) {
  return typeof t == "number" ? Ci : typeof t == "string" ? me(t) ? xt : M.test(t) ? _e : Di : Array.isArray(t) ? jn : typeof t == "object" ? M.test(t) ? _e : Pi : xt;
}
function jn(t, e) {
  const n = [...t], s = n.length, i = t.map((r, o) => be(r)(r, e[o]));
  return (r) => {
    for (let o = 0; o < s; o++)
      n[o] = i[o](r);
    return n;
  };
}
function Pi(t, e) {
  const n = { ...t, ...e }, s = {};
  for (const i in n)
    t[i] !== void 0 && e[i] !== void 0 && (s[i] = be(t[i])(t[i], e[i]));
  return (i) => {
    for (const r in s)
      n[r] = s[r](i);
    return n;
  };
}
function Fi(t, e) {
  const n = [], s = { color: 0, var: 0, number: 0 };
  for (let i = 0; i < e.values.length; i++) {
    const r = e.types[i], o = t.indexes[r][s[r]], a = t.values[o] ?? 0;
    n[i] = a, s[r]++;
  }
  return n;
}
const Di = (t, e) => {
  const n = rt.createTransformer(e), s = pt(t), i = pt(e);
  return s.indexes.var.length === i.indexes.var.length && s.indexes.color.length === i.indexes.color.length && s.indexes.number.length >= i.indexes.number.length ? zt.has(t) && !i.values.length || zt.has(e) && !s.values.length ? Mi(t, e) : Ot(jn(Fi(s, i), i.values), n) : (gt(!0, `Complex values '${t}' and '${e}' too different to mix. Ensure all colors are of the same type, and that each contains the same quantity of number and color values. Falling back to instant transition.`, "complex-values-different"), xt(t, e));
};
function _n(t, e, n) {
  return typeof t == "number" && typeof e == "number" && typeof n == "number" ? It(t, e, n) : be(t)(t, e);
}
const Oi = (t) => {
  const e = ({ timestamp: n }) => t(n);
  return {
    start: (n = !0) => L.update(e, n),
    stop: () => Ht(e),
    /**
     * If we're processing this frame we can use the
     * framelocked timestamp to keep things in sync.
     */
    now: () => St.isProcessing ? St.timestamp : k.now()
  };
}, Gn = (t, e, n = 10) => {
  let s = "";
  const i = Math.max(Math.round(e / n), 2);
  for (let r = 0; r < i; r++)
    s += Math.round(t(r / (i - 1)) * 1e4) / 1e4 + ", ";
  return `linear(${s.substring(0, s.length - 2)})`;
}, At = 2e4;
function ve(t) {
  let e = 0;
  const n = 50;
  let s = t.next(e);
  for (; !s.done && e < At; )
    e += n, s = t.next(e);
  return e >= At ? 1 / 0 : e;
}
function Ii(t, e = 100, n) {
  const s = n({ ...t, keyframes: [0, e] }), i = Math.min(ve(s), At);
  return {
    type: "keyframes",
    ease: (r) => s.next(i * r).value / e,
    duration: /* @__PURE__ */ B(i)
  };
}
const Ri = 5;
function Hn(t, e, n) {
  const s = Math.max(e - Ri, 0);
  return Sn(n - t(s), e - s);
}
const A = {
  // Default spring physics
  stiffness: 100,
  damping: 10,
  mass: 1,
  velocity: 0,
  // Default duration/bounce-based options
  duration: 800,
  // in ms
  bounce: 0.3,
  visualDuration: 0.3,
  // in seconds
  // Rest thresholds
  restSpeed: {
    granular: 0.01,
    default: 2
  },
  restDelta: {
    granular: 5e-3,
    default: 0.5
  },
  // Limits
  minDuration: 0.01,
  // in seconds
  maxDuration: 10,
  // in seconds
  minDamping: 0.05,
  maxDamping: 1
}, Kt = 1e-3;
function ki({ duration: t = A.duration, bounce: e = A.bounce, velocity: n = A.velocity, mass: s = A.mass }) {
  let i, r;
  gt(t <= /* @__PURE__ */ N(A.maxDuration), "Spring duration must be 10 seconds or less", "spring-duration-limit");
  let o = 1 - e;
  o = z(A.minDamping, A.maxDamping, o), t = z(A.minDuration, A.maxDuration, /* @__PURE__ */ B(t)), o < 1 ? (i = (l) => {
    const u = l * o, f = u * t, d = u - n, h = Yt(l, o), y = Math.exp(-f);
    return Kt - d / h * y;
  }, r = (l) => {
    const f = l * o * t, d = f * n + n, h = Math.pow(o, 2) * Math.pow(l, 2) * t, y = Math.exp(-f), b = Yt(Math.pow(l, 2), o);
    return (-i(l) + Kt > 0 ? -1 : 1) * ((d - h) * y) / b;
  }) : (i = (l) => {
    const u = Math.exp(-l * t), f = (l - n) * t + 1;
    return -Kt + u * f;
  }, r = (l) => {
    const u = Math.exp(-l * t), f = (n - l) * (t * t);
    return u * f;
  });
  const a = 5 / t, c = Li(i, r, a);
  if (t = /* @__PURE__ */ N(t), isNaN(c))
    return {
      stiffness: A.stiffness,
      damping: A.damping,
      duration: t
    };
  {
    const l = Math.pow(c, 2) * s;
    return {
      stiffness: l,
      damping: o * 2 * Math.sqrt(s * l),
      duration: t
    };
  }
}
const Ei = 12;
function Li(t, e, n) {
  let s = n;
  for (let i = 1; i < Ei; i++)
    s = s - t(s) / e(s);
  return s;
}
function Yt(t, e) {
  return t * Math.sqrt(1 - e * e);
}
const Ni = ["duration", "bounce"], Bi = ["stiffness", "damping", "mass"];
function Ge(t, e) {
  return e.some((n) => t[n] !== void 0);
}
function Ki(t) {
  let e = {
    velocity: A.velocity,
    stiffness: A.stiffness,
    damping: A.damping,
    mass: A.mass,
    isResolvedFromDuration: !1,
    ...t
  };
  if (!Ge(t, Bi) && Ge(t, Ni))
    if (t.visualDuration) {
      const n = t.visualDuration, s = 2 * Math.PI / (n * 1.2), i = s * s, r = 2 * z(0.05, 1, 1 - (t.bounce || 0)) * Math.sqrt(i);
      e = {
        ...e,
        mass: A.mass,
        stiffness: i,
        damping: r
      };
    } else {
      const n = ki(t);
      e = {
        ...e,
        ...n,
        mass: A.mass
      }, e.isResolvedFromDuration = !0;
    }
  return e;
}
function Mt(t = A.visualDuration, e = A.bounce) {
  const n = typeof t != "object" ? {
    visualDuration: t,
    keyframes: [0, 1],
    bounce: e
  } : t;
  let { restSpeed: s, restDelta: i } = n;
  const r = n.keyframes[0], o = n.keyframes[n.keyframes.length - 1], a = { done: !1, value: r }, { stiffness: c, damping: l, mass: u, duration: f, velocity: d, isResolvedFromDuration: h } = Ki({
    ...n,
    velocity: -/* @__PURE__ */ B(n.velocity || 0)
  }), y = d || 0, b = l / (2 * Math.sqrt(c * u)), T = o - r, p = /* @__PURE__ */ B(Math.sqrt(c / u)), V = Math.abs(T) < 5;
  s || (s = V ? A.restSpeed.granular : A.restSpeed.default), i || (i = V ? A.restDelta.granular : A.restDelta.default);
  let g;
  if (b < 1) {
    const v = Yt(p, b);
    g = (S) => {
      const P = Math.exp(-b * p * S);
      return o - P * ((y + b * p * T) / v * Math.sin(v * S) + T * Math.cos(v * S));
    };
  } else if (b === 1)
    g = (v) => o - Math.exp(-p * v) * (T + (y + p * T) * v);
  else {
    const v = p * Math.sqrt(b * b - 1);
    g = (S) => {
      const P = Math.exp(-b * p * S), w = Math.min(v * S, 300);
      return o - P * ((y + b * p * T) * Math.sinh(w) + v * T * Math.cosh(w)) / v;
    };
  }
  const x = {
    calculatedDuration: h && f || null,
    next: (v) => {
      const S = g(v);
      if (h)
        a.done = v >= f;
      else {
        let P = v === 0 ? y : 0;
        b < 1 && (P = v === 0 ? /* @__PURE__ */ N(y) : Hn(g, v, S));
        const w = Math.abs(P) <= s, O = Math.abs(o - S) <= i;
        a.done = w && O;
      }
      return a.value = a.done ? o : S, a;
    },
    toString: () => {
      const v = Math.min(ve(x), At), S = Gn((P) => x.next(v * P).value, v, 30);
      return v + "ms " + S;
    },
    toTransition: () => {
    }
  };
  return x;
}
Mt.applyToOptions = (t) => {
  const e = Ii(t, 100, Mt);
  return t.ease = e.ease, t.duration = /* @__PURE__ */ N(e.duration), t.type = "keyframes", t;
};
function Xt({ keyframes: t, velocity: e = 0, power: n = 0.8, timeConstant: s = 325, bounceDamping: i = 10, bounceStiffness: r = 500, modifyTarget: o, min: a, max: c, restDelta: l = 0.5, restSpeed: u }) {
  const f = t[0], d = {
    done: !1,
    value: f
  }, h = (w) => a !== void 0 && w < a || c !== void 0 && w > c, y = (w) => a === void 0 ? c : c === void 0 || Math.abs(a - w) < Math.abs(c - w) ? a : c;
  let b = n * e;
  const T = f + b, p = o === void 0 ? T : o(T);
  p !== T && (b = p - f);
  const V = (w) => -b * Math.exp(-w / s), g = (w) => p + V(w), x = (w) => {
    const O = V(w), I = g(w);
    d.done = Math.abs(O) <= l, d.value = d.done ? p : I;
  };
  let v, S;
  const P = (w) => {
    h(d.value) && (v = w, S = Mt({
      keyframes: [d.value, y(d.value)],
      velocity: Hn(g, w, d.value),
      // TODO: This should be passing * 1000
      damping: i,
      stiffness: r,
      restDelta: l,
      restSpeed: u
    }));
  };
  return P(0), {
    calculatedDuration: null,
    next: (w) => {
      let O = !1;
      return !S && v === void 0 && (O = !0, x(w), P(w)), v !== void 0 && w >= v ? S.next(w - v) : (!O && x(w), d);
    }
  };
}
function Wi(t, e, n) {
  const s = [], i = n || K.mix || _n, r = t.length - 1;
  for (let o = 0; o < r; o++) {
    let a = i(t[o], t[o + 1]);
    if (e) {
      const c = Array.isArray(e) ? e[o] || st : e;
      a = Ot(c, a);
    }
    s.push(a);
  }
  return s;
}
function $i(t, e, { clamp: n = !0, ease: s, mixer: i } = {}) {
  const r = t.length;
  if (Y(r === e.length, "Both input and output ranges must be the same length", "range-length"), r === 1)
    return () => e[0];
  if (r === 2 && e[0] === e[1])
    return () => e[1];
  const o = t[0] === t[1];
  t[0] > t[r - 1] && (t = [...t].reverse(), e = [...e].reverse());
  const a = Wi(e, s, i), c = a.length, l = (u) => {
    if (o && u < t[0])
      return e[0];
    let f = 0;
    if (c > 1)
      for (; f < t.length - 2 && !(u < t[f + 1]); f++)
        ;
    const d = /* @__PURE__ */ wn(t[f], t[f + 1], u);
    return a[f](d);
  };
  return n ? (u) => l(z(t[0], t[r - 1], u)) : l;
}
function ji(t, e) {
  const n = t[t.length - 1];
  for (let s = 1; s <= e; s++) {
    const i = /* @__PURE__ */ wn(0, e, s);
    t.push(It(n, 1, i));
  }
}
function _i(t) {
  const e = [0];
  return ji(e, t.length - 1), e;
}
function Gi(t, e) {
  return t.map((n) => n * e);
}
function Hi(t, e) {
  return t.map(() => e || In).splice(0, t.length - 1);
}
function J({ duration: t = 300, keyframes: e, times: n, ease: s = "easeInOut" }) {
  const i = si(s) ? s.map(Ke) : Ke(s), r = {
    done: !1,
    value: e[0]
  }, o = Gi(
    // Only use the provided offsets if they're the correct length
    // TODO Maybe we should warn here if there's a length mismatch
    n && n.length === e.length ? n : _i(e),
    t
  ), a = $i(o, e, {
    ease: Array.isArray(i) ? i : Hi(e, i)
  });
  return {
    calculatedDuration: t,
    next: (c) => (r.value = a(c), r.done = c >= t, r)
  };
}
const Ui = (t) => t !== null;
function Te(t, { repeat: e, repeatType: n = "loop" }, s, i = 1) {
  const r = t.filter(Ui), a = i < 0 || e && n !== "loop" && e % 2 === 1 ? 0 : r.length - 1;
  return !a || s === void 0 ? r[a] : s;
}
const zi = {
  decay: Xt,
  inertia: Xt,
  tween: J,
  keyframes: J,
  spring: Mt
};
function Un(t) {
  typeof t.type == "string" && (t.type = zi[t.type]);
}
class we {
  constructor() {
    this.updateFinished();
  }
  get finished() {
    return this._finished;
  }
  updateFinished() {
    this._finished = new Promise((e) => {
      this.resolve = e;
    });
  }
  notifyFinished() {
    this.resolve();
  }
  /**
   * Allows the animation to be awaited.
   *
   * @deprecated Use `finished` instead.
   */
  then(e, n) {
    return this.finished.then(e, n);
  }
}
const Yi = (t) => t / 100;
class Ve extends we {
  constructor(e) {
    super(), this.state = "idle", this.startTime = null, this.isStopped = !1, this.currentTime = 0, this.holdTime = null, this.playbackSpeed = 1, this.stop = () => {
      var s, i;
      const { motionValue: n } = this.options;
      n && n.updatedAt !== k.now() && this.tick(k.now()), this.isStopped = !0, this.state !== "idle" && (this.teardown(), (i = (s = this.options).onStop) == null || i.call(s));
    }, this.options = e, this.initAnimation(), this.play(), e.autoplay === !1 && this.pause();
  }
  initAnimation() {
    const { options: e } = this;
    Un(e);
    const { type: n = J, repeat: s = 0, repeatDelay: i = 0, repeatType: r, velocity: o = 0 } = e;
    let { keyframes: a } = e;
    const c = n || J;
    process.env.NODE_ENV !== "production" && c !== J && Y(a.length <= 2, `Only two keyframes currently supported with spring and inertia animations. Trying to animate ${a}`, "spring-two-frames"), c !== J && typeof a[0] != "number" && (this.mixKeyframes = Ot(Yi, _n(a[0], a[1])), a = [0, 100]);
    const l = c({ ...e, keyframes: a });
    r === "mirror" && (this.mirroredGenerator = c({
      ...e,
      keyframes: [...a].reverse(),
      velocity: -o
    })), l.calculatedDuration === null && (l.calculatedDuration = ve(l));
    const { calculatedDuration: u } = l;
    this.calculatedDuration = u, this.resolvedDuration = u + i, this.totalDuration = this.resolvedDuration * (s + 1) - i, this.generator = l;
  }
  updateTime(e) {
    const n = Math.round(e - this.startTime) * this.playbackSpeed;
    this.holdTime !== null ? this.currentTime = this.holdTime : this.currentTime = n;
  }
  tick(e, n = !1) {
    const { generator: s, totalDuration: i, mixKeyframes: r, mirroredGenerator: o, resolvedDuration: a, calculatedDuration: c } = this;
    if (this.startTime === null)
      return s.next(0);
    const { delay: l = 0, keyframes: u, repeat: f, repeatType: d, repeatDelay: h, type: y, onUpdate: b, finalKeyframe: T } = this.options;
    this.speed > 0 ? this.startTime = Math.min(this.startTime, e) : this.speed < 0 && (this.startTime = Math.min(e - i / this.speed, this.startTime)), n ? this.currentTime = e : this.updateTime(e);
    const p = this.currentTime - l * (this.playbackSpeed >= 0 ? 1 : -1), V = this.playbackSpeed >= 0 ? p < 0 : p > i;
    this.currentTime = Math.max(p, 0), this.state === "finished" && this.holdTime === null && (this.currentTime = i);
    let g = this.currentTime, x = s;
    if (f) {
      const w = Math.min(this.currentTime, i) / a;
      let O = Math.floor(w), I = w % 1;
      !I && w >= 1 && (I = 1), I === 1 && O--, O = Math.min(O, f + 1), !!(O % 2) && (d === "reverse" ? (I = 1 - I, h && (I -= h / a)) : d === "mirror" && (x = o)), g = z(0, 1, I) * a;
    }
    const v = V ? { done: !1, value: u[0] } : x.next(g);
    r && (v.value = r(v.value));
    let { done: S } = v;
    !V && c !== null && (S = this.playbackSpeed >= 0 ? this.currentTime >= i : this.currentTime <= 0);
    const P = this.holdTime === null && (this.state === "finished" || this.state === "running" && S);
    return P && y !== Xt && (v.value = Te(u, this.options, T, this.speed)), b && b(v.value), P && this.finish(), v;
  }
  /**
   * Allows the returned animation to be awaited or promise-chained. Currently
   * resolves when the animation finishes at all but in a future update could/should
   * reject if its cancels.
   */
  then(e, n) {
    return this.finished.then(e, n);
  }
  get duration() {
    return /* @__PURE__ */ B(this.calculatedDuration);
  }
  get time() {
    return /* @__PURE__ */ B(this.currentTime);
  }
  set time(e) {
    var n;
    e = /* @__PURE__ */ N(e), this.currentTime = e, this.startTime === null || this.holdTime !== null || this.playbackSpeed === 0 ? this.holdTime = e : this.driver && (this.startTime = this.driver.now() - e / this.playbackSpeed), (n = this.driver) == null || n.start(!1);
  }
  get speed() {
    return this.playbackSpeed;
  }
  set speed(e) {
    this.updateTime(k.now());
    const n = this.playbackSpeed !== e;
    this.playbackSpeed = e, n && (this.time = /* @__PURE__ */ B(this.currentTime));
  }
  play() {
    var i, r;
    if (this.isStopped)
      return;
    const { driver: e = Oi, startTime: n } = this.options;
    this.driver || (this.driver = e((o) => this.tick(o))), (r = (i = this.options).onPlay) == null || r.call(i);
    const s = this.driver.now();
    this.state === "finished" ? (this.updateFinished(), this.startTime = s) : this.holdTime !== null ? this.startTime = s - this.holdTime : this.startTime || (this.startTime = n ?? s), this.state === "finished" && this.speed < 0 && (this.startTime += this.calculatedDuration), this.holdTime = null, this.state = "running", this.driver.start();
  }
  pause() {
    this.state = "paused", this.updateTime(k.now()), this.holdTime = this.currentTime;
  }
  complete() {
    this.state !== "running" && this.play(), this.state = "finished", this.holdTime = null;
  }
  finish() {
    var e, n;
    this.notifyFinished(), this.teardown(), this.state = "finished", (n = (e = this.options).onComplete) == null || n.call(e);
  }
  cancel() {
    var e, n;
    this.holdTime = null, this.startTime = 0, this.tick(0), this.teardown(), (n = (e = this.options).onCancel) == null || n.call(e);
  }
  teardown() {
    this.state = "idle", this.stopDriver(), this.startTime = this.holdTime = null;
  }
  stopDriver() {
    this.driver && (this.driver.stop(), this.driver = void 0);
  }
  sample(e) {
    return this.startTime = 0, this.tick(e, !0);
  }
  attachTimeline(e) {
    var n;
    return this.options.allowFlatten && (this.options.type = "keyframes", this.options.ease = "linear", this.initAnimation()), (n = this.driver) == null || n.stop(), e.observe(this);
  }
}
function Xi(t) {
  for (let e = 1; e < t.length; e++)
    t[e] ?? (t[e] = t[e - 1]);
}
const G = (t) => t * 180 / Math.PI, qt = (t) => {
  const e = G(Math.atan2(t[1], t[0]));
  return Zt(e);
}, qi = {
  x: 4,
  y: 5,
  translateX: 4,
  translateY: 5,
  scaleX: 0,
  scaleY: 3,
  scale: (t) => (Math.abs(t[0]) + Math.abs(t[3])) / 2,
  rotate: qt,
  rotateZ: qt,
  skewX: (t) => G(Math.atan(t[1])),
  skewY: (t) => G(Math.atan(t[2])),
  skew: (t) => (Math.abs(t[1]) + Math.abs(t[2])) / 2
}, Zt = (t) => (t = t % 360, t < 0 && (t += 360), t), He = qt, Ue = (t) => Math.sqrt(t[0] * t[0] + t[1] * t[1]), ze = (t) => Math.sqrt(t[4] * t[4] + t[5] * t[5]), Zi = {
  x: 12,
  y: 13,
  z: 14,
  translateX: 12,
  translateY: 13,
  translateZ: 14,
  scaleX: Ue,
  scaleY: ze,
  scale: (t) => (Ue(t) + ze(t)) / 2,
  rotateX: (t) => Zt(G(Math.atan2(t[6], t[5]))),
  rotateY: (t) => Zt(G(Math.atan2(-t[2], t[0]))),
  rotateZ: He,
  rotate: He,
  skewX: (t) => G(Math.atan(t[4])),
  skewY: (t) => G(Math.atan(t[1])),
  skew: (t) => (Math.abs(t[1]) + Math.abs(t[4])) / 2
};
function Jt(t) {
  return t.includes("scale") ? 1 : 0;
}
function Qt(t, e) {
  if (!t || t === "none")
    return Jt(e);
  const n = t.match(/^matrix3d\(([-\d.e\s,]+)\)$/u);
  let s, i;
  if (n)
    s = Zi, i = n;
  else {
    const a = t.match(/^matrix\(([-\d.e\s,]+)\)$/u);
    s = qi, i = a;
  }
  if (!i)
    return Jt(e);
  const r = s[e], o = i[1].split(",").map(Qi);
  return typeof r == "function" ? r(o) : o[r];
}
const Ji = (t, e) => {
  const { transform: n = "none" } = getComputedStyle(t);
  return Qt(n, e);
};
function Qi(t) {
  return parseFloat(t.trim());
}
const ot = [
  "transformPerspective",
  "x",
  "y",
  "z",
  "translateX",
  "translateY",
  "translateZ",
  "scale",
  "scaleX",
  "scaleY",
  "rotate",
  "rotateX",
  "rotateY",
  "rotateZ",
  "skew",
  "skewX",
  "skewY"
], at = new Set(ot), Ye = (t) => t === it || t === m, tr = /* @__PURE__ */ new Set(["x", "y", "z"]), er = ot.filter((t) => !tr.has(t));
function nr(t) {
  const e = [];
  return er.forEach((n) => {
    const s = t.getValue(n);
    s !== void 0 && (e.push([n, s.get()]), s.set(n.startsWith("scale") ? 1 : 0));
  }), e;
}
const H = {
  // Dimensions
  width: ({ x: t }, { paddingLeft: e = "0", paddingRight: n = "0" }) => t.max - t.min - parseFloat(e) - parseFloat(n),
  height: ({ y: t }, { paddingTop: e = "0", paddingBottom: n = "0" }) => t.max - t.min - parseFloat(e) - parseFloat(n),
  top: (t, { top: e }) => parseFloat(e),
  left: (t, { left: e }) => parseFloat(e),
  bottom: ({ y: t }, { top: e }) => parseFloat(e) + (t.max - t.min),
  right: ({ x: t }, { left: e }) => parseFloat(e) + (t.max - t.min),
  // Transform
  x: (t, { transform: e }) => Qt(e, "x"),
  y: (t, { transform: e }) => Qt(e, "y")
};
H.translateX = H.x;
H.translateY = H.y;
const U = /* @__PURE__ */ new Set();
let te = !1, ee = !1, ne = !1;
function zn() {
  if (ee) {
    const t = Array.from(U).filter((s) => s.needsMeasurement), e = new Set(t.map((s) => s.element)), n = /* @__PURE__ */ new Map();
    e.forEach((s) => {
      const i = nr(s);
      i.length && (n.set(s, i), s.render());
    }), t.forEach((s) => s.measureInitialState()), e.forEach((s) => {
      s.render();
      const i = n.get(s);
      i && i.forEach(([r, o]) => {
        var a;
        (a = s.getValue(r)) == null || a.set(o);
      });
    }), t.forEach((s) => s.measureEndState()), t.forEach((s) => {
      s.suspendedScrollY !== void 0 && window.scrollTo(0, s.suspendedScrollY);
    });
  }
  ee = !1, te = !1, U.forEach((t) => t.complete(ne)), U.clear();
}
function Yn() {
  U.forEach((t) => {
    t.readKeyframes(), t.needsMeasurement && (ee = !0);
  });
}
function sr() {
  ne = !0, Yn(), zn(), ne = !1;
}
class Se {
  constructor(e, n, s, i, r, o = !1) {
    this.state = "pending", this.isAsync = !1, this.needsMeasurement = !1, this.unresolvedKeyframes = [...e], this.onComplete = n, this.name = s, this.motionValue = i, this.element = r, this.isAsync = o;
  }
  scheduleResolve() {
    this.state = "scheduled", this.isAsync ? (U.add(this), te || (te = !0, L.read(Yn), L.resolveKeyframes(zn))) : (this.readKeyframes(), this.complete());
  }
  readKeyframes() {
    const { unresolvedKeyframes: e, name: n, element: s, motionValue: i } = this;
    if (e[0] === null) {
      const r = i == null ? void 0 : i.get(), o = e[e.length - 1];
      if (r !== void 0)
        e[0] = r;
      else if (s && n) {
        const a = s.readValue(n, o);
        a != null && (e[0] = a);
      }
      e[0] === void 0 && (e[0] = o), i && r === void 0 && i.set(e[0]);
    }
    Xi(e);
  }
  setFinalKeyframe() {
  }
  measureInitialState() {
  }
  renderEndStyles() {
  }
  measureEndState() {
  }
  complete(e = !1) {
    this.state = "complete", this.onComplete(this.unresolvedKeyframes, this.finalKeyframe, e), U.delete(this);
  }
  cancel() {
    this.state === "scheduled" && (U.delete(this), this.state = "pending");
  }
  resume() {
    this.state === "pending" && this.scheduleResolve();
  }
}
const ir = (t) => t.startsWith("--");
function rr(t, e, n) {
  ir(e) ? t.style.setProperty(e, n) : t.style[e] = n;
}
const or = /* @__PURE__ */ de(() => window.ScrollTimeline !== void 0), ar = {};
function ur(t, e) {
  const n = /* @__PURE__ */ de(t);
  return () => ar[e] ?? n();
}
const Xn = /* @__PURE__ */ ur(() => {
  try {
    document.createElement("div").animate({ opacity: 0 }, { easing: "linear(0, 1)" });
  } catch {
    return !1;
  }
  return !0;
}, "linearEasing"), ct = ([t, e, n, s]) => `cubic-bezier(${t}, ${e}, ${n}, ${s})`, Xe = {
  linear: "linear",
  ease: "ease",
  easeIn: "ease-in",
  easeOut: "ease-out",
  easeInOut: "ease-in-out",
  circIn: /* @__PURE__ */ ct([0, 0.65, 0.55, 1]),
  circOut: /* @__PURE__ */ ct([0.55, 0, 1, 0.45]),
  backIn: /* @__PURE__ */ ct([0.31, 0.01, 0.66, -0.59]),
  backOut: /* @__PURE__ */ ct([0.33, 1.53, 0.69, 0.99])
};
function qn(t, e) {
  if (t)
    return typeof t == "function" ? Xn() ? Gn(t, e) : "ease-out" : Rn(t) ? ct(t) : Array.isArray(t) ? t.map((n) => qn(n, e) || Xe.easeOut) : Xe[t];
}
function cr(t, e, n, { delay: s = 0, duration: i = 300, repeat: r = 0, repeatType: o = "loop", ease: a = "easeOut", times: c } = {}, l = void 0) {
  const u = {
    [e]: n
  };
  c && (u.offset = c);
  const f = qn(a, i);
  Array.isArray(f) && (u.easing = f);
  const d = {
    delay: s,
    duration: i,
    easing: Array.isArray(f) ? "linear" : f,
    fill: "both",
    iterations: r + 1,
    direction: o === "reverse" ? "alternate" : "normal"
  };
  return l && (d.pseudoElement = l), t.animate(u, d);
}
function Zn(t) {
  return typeof t == "function" && "applyToOptions" in t;
}
function lr({ type: t, ...e }) {
  return Zn(t) && Xn() ? t.applyToOptions(e) : (e.duration ?? (e.duration = 300), e.ease ?? (e.ease = "easeOut"), e);
}
class fr extends we {
  constructor(e) {
    if (super(), this.finishedTime = null, this.isStopped = !1, !e)
      return;
    const { element: n, name: s, keyframes: i, pseudoElement: r, allowFlatten: o = !1, finalKeyframe: a, onComplete: c } = e;
    this.isPseudoElement = !!r, this.allowFlatten = o, this.options = e, Y(typeof e.type != "string", `Mini animate() doesn't support "type" as a string.`, "mini-spring");
    const l = lr(e);
    this.animation = cr(n, s, i, l, r), l.autoplay === !1 && this.animation.pause(), this.animation.onfinish = () => {
      if (this.finishedTime = this.time, !r) {
        const u = Te(i, this.options, a, this.speed);
        this.updateMotionValue ? this.updateMotionValue(u) : rr(n, s, u), this.animation.cancel();
      }
      c == null || c(), this.notifyFinished();
    };
  }
  play() {
    this.isStopped || (this.animation.play(), this.state === "finished" && this.updateFinished());
  }
  pause() {
    this.animation.pause();
  }
  complete() {
    var e, n;
    (n = (e = this.animation).finish) == null || n.call(e);
  }
  cancel() {
    try {
      this.animation.cancel();
    } catch {
    }
  }
  stop() {
    if (this.isStopped)
      return;
    this.isStopped = !0;
    const { state: e } = this;
    e === "idle" || e === "finished" || (this.updateMotionValue ? this.updateMotionValue() : this.commitStyles(), this.isPseudoElement || this.cancel());
  }
  /**
   * WAAPI doesn't natively have any interruption capabilities.
   *
   * In this method, we commit styles back to the DOM before cancelling
   * the animation.
   *
   * This is designed to be overridden by NativeAnimationExtended, which
   * will create a renderless JS animation and sample it twice to calculate
   * its current value, "previous" value, and therefore allow
   * Motion to also correctly calculate velocity for any subsequent animation
   * while deferring the commit until the next animation frame.
   */
  commitStyles() {
    var e, n;
    this.isPseudoElement || (n = (e = this.animation).commitStyles) == null || n.call(e);
  }
  get duration() {
    var n, s;
    const e = ((s = (n = this.animation.effect) == null ? void 0 : n.getComputedTiming) == null ? void 0 : s.call(n).duration) || 0;
    return /* @__PURE__ */ B(Number(e));
  }
  get time() {
    return /* @__PURE__ */ B(Number(this.animation.currentTime) || 0);
  }
  set time(e) {
    this.finishedTime = null, this.animation.currentTime = /* @__PURE__ */ N(e);
  }
  /**
   * The playback speed of the animation.
   * 1 = normal speed, 2 = double speed, 0.5 = half speed.
   */
  get speed() {
    return this.animation.playbackRate;
  }
  set speed(e) {
    e < 0 && (this.finishedTime = null), this.animation.playbackRate = e;
  }
  get state() {
    return this.finishedTime !== null ? "finished" : this.animation.playState;
  }
  get startTime() {
    return Number(this.animation.startTime);
  }
  set startTime(e) {
    this.animation.startTime = e;
  }
  /**
   * Attaches a timeline to the animation, for instance the `ScrollTimeline`.
   */
  attachTimeline({ timeline: e, observe: n }) {
    var s;
    return this.allowFlatten && ((s = this.animation.effect) == null || s.updateTiming({ easing: "linear" })), this.animation.onfinish = null, e && or() ? (this.animation.timeline = e, st) : n(this);
  }
}
const Jn = {
  anticipate: Dn,
  backInOut: Fn,
  circInOut: On
};
function dr(t) {
  return t in Jn;
}
function hr(t) {
  typeof t.ease == "string" && dr(t.ease) && (t.ease = Jn[t.ease]);
}
const qe = 10;
class pr extends fr {
  constructor(e) {
    hr(e), Un(e), super(e), e.startTime && (this.startTime = e.startTime), this.options = e;
  }
  /**
   * WAAPI doesn't natively have any interruption capabilities.
   *
   * Rather than read commited styles back out of the DOM, we can
   * create a renderless JS animation and sample it twice to calculate
   * its current value, "previous" value, and therefore allow
   * Motion to calculate velocity for any subsequent animation.
   */
  updateMotionValue(e) {
    const { motionValue: n, onUpdate: s, onComplete: i, element: r, ...o } = this.options;
    if (!n)
      return;
    if (e !== void 0) {
      n.set(e);
      return;
    }
    const a = new Ve({
      ...o,
      autoplay: !1
    }), c = /* @__PURE__ */ N(this.finishedTime ?? this.time);
    n.setWithVelocity(a.sample(c - qe).value, a.sample(c).value, qe), a.stop();
  }
}
const Ze = (t, e) => e === "zIndex" ? !1 : !!(typeof t == "number" || Array.isArray(t) || typeof t == "string" && // It's animatable if we have a string
(rt.test(t) || t === "0") && // And it contains numbers and/or colors
!t.startsWith("url("));
function mr(t) {
  const e = t[0];
  if (t.length === 1)
    return !0;
  for (let n = 0; n < t.length; n++)
    if (t[n] !== e)
      return !0;
}
function gr(t, e, n, s) {
  const i = t[0];
  if (i === null)
    return !1;
  if (e === "display" || e === "visibility")
    return !0;
  const r = t[t.length - 1], o = Ze(i, e), a = Ze(r, e);
  return gt(o === a, `You are trying to animate ${e} from "${i}" to "${r}". "${o ? r : i}" is not an animatable value.`, "value-not-animatable"), !o || !a ? !1 : mr(t) || (n === "spring" || Zn(n)) && s;
}
function se(t) {
  t.duration = 0, t.type;
}
const yr = /* @__PURE__ */ new Set([
  "opacity",
  "clipPath",
  "filter",
  "transform"
  // TODO: Could be re-enabled now we have support for linear() easing
  // "background-color"
]), br = /* @__PURE__ */ de(() => Object.hasOwnProperty.call(Element.prototype, "animate"));
function vr(t) {
  var u;
  const { motionValue: e, name: n, repeatDelay: s, repeatType: i, damping: r, type: o } = t;
  if (!(((u = e == null ? void 0 : e.owner) == null ? void 0 : u.current) instanceof HTMLElement))
    return !1;
  const { onUpdate: c, transformTemplate: l } = e.owner.getProps();
  return br() && n && yr.has(n) && (n !== "transform" || !l) && /**
   * If we're outputting values to onUpdate then we can't use WAAPI as there's
   * no way to read the value from WAAPI every frame.
   */
  !c && !s && i !== "mirror" && r !== 0 && o !== "inertia";
}
const Tr = 40;
class wr extends we {
  constructor({ autoplay: e = !0, delay: n = 0, type: s = "keyframes", repeat: i = 0, repeatDelay: r = 0, repeatType: o = "loop", keyframes: a, name: c, motionValue: l, element: u, ...f }) {
    var y;
    super(), this.stop = () => {
      var b, T;
      this._animation && (this._animation.stop(), (b = this.stopTimeline) == null || b.call(this)), (T = this.keyframeResolver) == null || T.cancel();
    }, this.createdAt = k.now();
    const d = {
      autoplay: e,
      delay: n,
      type: s,
      repeat: i,
      repeatDelay: r,
      repeatType: o,
      name: c,
      motionValue: l,
      element: u,
      ...f
    }, h = (u == null ? void 0 : u.KeyframeResolver) || Se;
    this.keyframeResolver = new h(a, (b, T, p) => this.onKeyframesResolved(b, T, d, !p), c, l, u), (y = this.keyframeResolver) == null || y.scheduleResolve();
  }
  onKeyframesResolved(e, n, s, i) {
    this.keyframeResolver = void 0;
    const { name: r, type: o, velocity: a, delay: c, isHandoff: l, onUpdate: u } = s;
    this.resolvedAt = k.now(), gr(e, r, o, a) || ((K.instantAnimations || !c) && (u == null || u(Te(e, s, n))), e[0] = e[e.length - 1], se(s), s.repeat = 0);
    const d = {
      startTime: i ? this.resolvedAt ? this.resolvedAt - this.createdAt > Tr ? this.resolvedAt : this.createdAt : this.createdAt : void 0,
      finalKeyframe: n,
      ...s,
      keyframes: e
    }, h = !l && vr(d) ? new pr({
      ...d,
      element: d.motionValue.owner.current
    }) : new Ve(d);
    h.finished.then(() => this.notifyFinished()).catch(st), this.pendingTimeline && (this.stopTimeline = h.attachTimeline(this.pendingTimeline), this.pendingTimeline = void 0), this._animation = h;
  }
  get finished() {
    return this._animation ? this.animation.finished : this._finished;
  }
  then(e, n) {
    return this.finished.finally(e).then(() => {
    });
  }
  get animation() {
    var e;
    return this._animation || ((e = this.keyframeResolver) == null || e.resume(), sr()), this._animation;
  }
  get duration() {
    return this.animation.duration;
  }
  get time() {
    return this.animation.time;
  }
  set time(e) {
    this.animation.time = e;
  }
  get speed() {
    return this.animation.speed;
  }
  get state() {
    return this.animation.state;
  }
  set speed(e) {
    this.animation.speed = e;
  }
  get startTime() {
    return this.animation.startTime;
  }
  attachTimeline(e) {
    return this._animation ? this.stopTimeline = this.animation.attachTimeline(e) : this.pendingTimeline = e, () => this.stop();
  }
  play() {
    this.animation.play();
  }
  pause() {
    this.animation.pause();
  }
  complete() {
    this.animation.complete();
  }
  cancel() {
    var e;
    this._animation && this.animation.cancel(), (e = this.keyframeResolver) == null || e.cancel();
  }
}
const Vr = (
  // eslint-disable-next-line redos-detector/no-unsafe-regex -- false positive, as it can match a lot of words
  /^var\(--(?:([\w-]+)|([\w-]+), ?([a-zA-Z\d ()%#.,-]+))\)/u
);
function Sr(t) {
  const e = Vr.exec(t);
  if (!e)
    return [,];
  const [, n, s, i] = e;
  return [`--${n ?? s}`, i];
}
const xr = 4;
function Qn(t, e, n = 1) {
  Y(n <= xr, `Max CSS variable fallback depth detected in property "${t}". This may indicate a circular fallback dependency.`, "max-css-var-depth");
  const [s, i] = Sr(t);
  if (!s)
    return;
  const r = window.getComputedStyle(e).getPropertyValue(s);
  if (r) {
    const o = r.trim();
    return vn(o) ? parseFloat(o) : o;
  }
  return me(i) ? Qn(i, e, n + 1) : i;
}
function ts(t, e) {
  return (t == null ? void 0 : t[e]) ?? (t == null ? void 0 : t.default) ?? t;
}
const es = /* @__PURE__ */ new Set([
  "width",
  "height",
  "top",
  "left",
  "right",
  "bottom",
  ...ot
]), Ar = {
  test: (t) => t === "auto",
  parse: (t) => t
}, ns = (t) => (e) => e.test(t), ss = [it, m, Q, W, mi, pi, Ar], Je = (t) => ss.find(ns(t));
function Mr(t) {
  return typeof t == "number" ? t === 0 : t !== null ? t === "none" || t === "0" || Tn(t) : !0;
}
const Cr = /* @__PURE__ */ new Set(["brightness", "contrast", "saturate", "opacity"]);
function Pr(t) {
  const [e, n] = t.slice(0, -1).split("(");
  if (e === "drop-shadow")
    return t;
  const [s] = n.match(ge) || [];
  if (!s)
    return t;
  const i = n.replace(s, "");
  let r = Cr.has(e) ? 1 : 0;
  return s !== n && (r *= 100), e + "(" + r + i + ")";
}
const Fr = /\b([a-z-]*)\(.*?\)/gu, ie = {
  ...rt,
  getAnimatableNone: (t) => {
    const e = t.match(Fr);
    return e ? e.map(Pr).join(" ") : t;
  }
}, Qe = {
  ...it,
  transform: Math.round
}, Dr = {
  rotate: W,
  rotateX: W,
  rotateY: W,
  rotateZ: W,
  scale: Tt,
  scaleX: Tt,
  scaleY: Tt,
  scaleZ: Tt,
  skew: W,
  skewX: W,
  skewY: W,
  distance: m,
  translateX: m,
  translateY: m,
  translateZ: m,
  x: m,
  y: m,
  z: m,
  perspective: m,
  transformPerspective: m,
  opacity: ht,
  originX: We,
  originY: We,
  originZ: m
}, xe = {
  // Border props
  borderWidth: m,
  borderTopWidth: m,
  borderRightWidth: m,
  borderBottomWidth: m,
  borderLeftWidth: m,
  borderRadius: m,
  radius: m,
  borderTopLeftRadius: m,
  borderTopRightRadius: m,
  borderBottomRightRadius: m,
  borderBottomLeftRadius: m,
  // Positioning props
  width: m,
  maxWidth: m,
  height: m,
  maxHeight: m,
  top: m,
  right: m,
  bottom: m,
  left: m,
  // Spacing props
  padding: m,
  paddingTop: m,
  paddingRight: m,
  paddingBottom: m,
  paddingLeft: m,
  margin: m,
  marginTop: m,
  marginRight: m,
  marginBottom: m,
  marginLeft: m,
  // Misc
  backgroundPositionX: m,
  backgroundPositionY: m,
  ...Dr,
  zIndex: Qe,
  // SVG
  fillOpacity: ht,
  strokeOpacity: ht,
  numOctaves: Qe
}, Or = {
  ...xe,
  // Color props
  color: M,
  backgroundColor: M,
  outlineColor: M,
  fill: M,
  stroke: M,
  // Border props
  borderColor: M,
  borderTopColor: M,
  borderRightColor: M,
  borderBottomColor: M,
  borderLeftColor: M,
  filter: ie,
  WebkitFilter: ie
}, is = (t) => Or[t];
function rs(t, e) {
  let n = is(t);
  return n !== ie && (n = rt), n.getAnimatableNone ? n.getAnimatableNone(e) : void 0;
}
const Ir = /* @__PURE__ */ new Set(["auto", "none", "0"]);
function Rr(t, e, n) {
  let s = 0, i;
  for (; s < t.length && !i; ) {
    const r = t[s];
    typeof r == "string" && !Ir.has(r) && pt(r).values.length && (i = t[s]), s++;
  }
  if (i && n)
    for (const r of e)
      t[r] = rs(n, i);
}
class kr extends Se {
  constructor(e, n, s, i, r) {
    super(e, n, s, i, r, !0);
  }
  readKeyframes() {
    const { unresolvedKeyframes: e, element: n, name: s } = this;
    if (!n || !n.current)
      return;
    super.readKeyframes();
    for (let c = 0; c < e.length; c++) {
      let l = e[c];
      if (typeof l == "string" && (l = l.trim(), me(l))) {
        const u = Qn(l, n.current);
        u !== void 0 && (e[c] = u), c === e.length - 1 && (this.finalKeyframe = l);
      }
    }
    if (this.resolveNoneKeyframes(), !es.has(s) || e.length !== 2)
      return;
    const [i, r] = e, o = Je(i), a = Je(r);
    if (o !== a)
      if (Ye(o) && Ye(a))
        for (let c = 0; c < e.length; c++) {
          const l = e[c];
          typeof l == "string" && (e[c] = parseFloat(l));
        }
      else H[s] && (this.needsMeasurement = !0);
  }
  resolveNoneKeyframes() {
    const { unresolvedKeyframes: e, name: n } = this, s = [];
    for (let i = 0; i < e.length; i++)
      (e[i] === null || Mr(e[i])) && s.push(i);
    s.length && Rr(e, s, n);
  }
  measureInitialState() {
    const { element: e, unresolvedKeyframes: n, name: s } = this;
    if (!e || !e.current)
      return;
    s === "height" && (this.suspendedScrollY = window.pageYOffset), this.measuredOrigin = H[s](e.measureViewportBox(), window.getComputedStyle(e.current)), n[0] = this.measuredOrigin;
    const i = n[n.length - 1];
    i !== void 0 && e.getValue(s, i).jump(i, !1);
  }
  measureEndState() {
    var a;
    const { element: e, name: n, unresolvedKeyframes: s } = this;
    if (!e || !e.current)
      return;
    const i = e.getValue(n);
    i && i.jump(this.measuredOrigin, !1);
    const r = s.length - 1, o = s[r];
    s[r] = H[n](e.measureViewportBox(), window.getComputedStyle(e.current)), o !== null && this.finalKeyframe === void 0 && (this.finalKeyframe = o), (a = this.removedTransforms) != null && a.length && this.removedTransforms.forEach(([c, l]) => {
      e.getValue(c).set(l);
    }), this.resolveNoneKeyframes();
  }
}
function Er(t, e, n) {
  if (t instanceof EventTarget)
    return [t];
  if (typeof t == "string") {
    let s = document;
    const i = (n == null ? void 0 : n[t]) ?? s.querySelectorAll(t);
    return i ? Array.from(i) : [];
  }
  return Array.from(t);
}
const os = (t, e) => e && typeof t == "number" ? e.transform(t) : t;
function Lr(t) {
  return Xs(t) && "offsetHeight" in t;
}
const tn = 30, Nr = (t) => !isNaN(parseFloat(t));
class Br {
  /**
   * @param init - The initiating value
   * @param config - Optional configuration options
   *
   * -  `transformer`: A function to transform incoming values with.
   */
  constructor(e, n = {}) {
    this.canTrackVelocity = null, this.events = {}, this.updateAndNotify = (s) => {
      var r;
      const i = k.now();
      if (this.updatedAt !== i && this.setPrevFrameValue(), this.prev = this.current, this.setCurrent(s), this.current !== this.prev && ((r = this.events.change) == null || r.notify(this.current), this.dependents))
        for (const o of this.dependents)
          o.dirty();
    }, this.hasAnimated = !1, this.setCurrent(e), this.owner = n.owner;
  }
  setCurrent(e) {
    this.current = e, this.updatedAt = k.now(), this.canTrackVelocity === null && e !== void 0 && (this.canTrackVelocity = Nr(this.current));
  }
  setPrevFrameValue(e = this.current) {
    this.prevFrameValue = e, this.prevUpdatedAt = this.updatedAt;
  }
  /**
   * Adds a function that will be notified when the `MotionValue` is updated.
   *
   * It returns a function that, when called, will cancel the subscription.
   *
   * When calling `onChange` inside a React component, it should be wrapped with the
   * `useEffect` hook. As it returns an unsubscribe function, this should be returned
   * from the `useEffect` function to ensure you don't add duplicate subscribers..
   *
   * ```jsx
   * export const MyComponent = () => {
   *   const x = useMotionValue(0)
   *   const y = useMotionValue(0)
   *   const opacity = useMotionValue(1)
   *
   *   useEffect(() => {
   *     function updateOpacity() {
   *       const maxXY = Math.max(x.get(), y.get())
   *       const newOpacity = transform(maxXY, [0, 100], [1, 0])
   *       opacity.set(newOpacity)
   *     }
   *
   *     const unsubscribeX = x.on("change", updateOpacity)
   *     const unsubscribeY = y.on("change", updateOpacity)
   *
   *     return () => {
   *       unsubscribeX()
   *       unsubscribeY()
   *     }
   *   }, [])
   *
   *   return <motion.div style={{ x }} />
   * }
   * ```
   *
   * @param subscriber - A function that receives the latest value.
   * @returns A function that, when called, will cancel this subscription.
   *
   * @deprecated
   */
  onChange(e) {
    return process.env.NODE_ENV !== "production" && xn(!1, 'value.onChange(callback) is deprecated. Switch to value.on("change", callback).'), this.on("change", e);
  }
  on(e, n) {
    this.events[e] || (this.events[e] = new Vn());
    const s = this.events[e].add(n);
    return e === "change" ? () => {
      s(), L.read(() => {
        this.events.change.getSize() || this.stop();
      });
    } : s;
  }
  clearListeners() {
    for (const e in this.events)
      this.events[e].clear();
  }
  /**
   * Attaches a passive effect to the `MotionValue`.
   */
  attach(e, n) {
    this.passiveEffect = e, this.stopPassiveEffect = n;
  }
  /**
   * Sets the state of the `MotionValue`.
   *
   * @remarks
   *
   * ```jsx
   * const x = useMotionValue(0)
   * x.set(10)
   * ```
   *
   * @param latest - Latest value to set.
   * @param render - Whether to notify render subscribers. Defaults to `true`
   *
   * @public
   */
  set(e) {
    this.passiveEffect ? this.passiveEffect(e, this.updateAndNotify) : this.updateAndNotify(e);
  }
  setWithVelocity(e, n, s) {
    this.set(n), this.prev = void 0, this.prevFrameValue = e, this.prevUpdatedAt = this.updatedAt - s;
  }
  /**
   * Set the state of the `MotionValue`, stopping any active animations,
   * effects, and resets velocity to `0`.
   */
  jump(e, n = !0) {
    this.updateAndNotify(e), this.prev = e, this.prevUpdatedAt = this.prevFrameValue = void 0, n && this.stop(), this.stopPassiveEffect && this.stopPassiveEffect();
  }
  dirty() {
    var e;
    (e = this.events.change) == null || e.notify(this.current);
  }
  addDependent(e) {
    this.dependents || (this.dependents = /* @__PURE__ */ new Set()), this.dependents.add(e);
  }
  removeDependent(e) {
    this.dependents && this.dependents.delete(e);
  }
  /**
   * Returns the latest state of `MotionValue`
   *
   * @returns - The latest state of `MotionValue`
   *
   * @public
   */
  get() {
    return this.current;
  }
  /**
   * @public
   */
  getPrevious() {
    return this.prev;
  }
  /**
   * Returns the latest velocity of `MotionValue`
   *
   * @returns - The latest velocity of `MotionValue`. Returns `0` if the state is non-numerical.
   *
   * @public
   */
  getVelocity() {
    const e = k.now();
    if (!this.canTrackVelocity || this.prevFrameValue === void 0 || e - this.updatedAt > tn)
      return 0;
    const n = Math.min(this.updatedAt - this.prevUpdatedAt, tn);
    return Sn(parseFloat(this.current) - parseFloat(this.prevFrameValue), n);
  }
  /**
   * Registers a new animation to control this `MotionValue`. Only one
   * animation can drive a `MotionValue` at one time.
   *
   * ```jsx
   * value.start()
   * ```
   *
   * @param animation - A function that starts the provided animation
   */
  start(e) {
    return this.stop(), new Promise((n) => {
      this.hasAnimated = !0, this.animation = e(n), this.events.animationStart && this.events.animationStart.notify();
    }).then(() => {
      this.events.animationComplete && this.events.animationComplete.notify(), this.clearAnimation();
    });
  }
  /**
   * Stop the currently active animation.
   *
   * @public
   */
  stop() {
    this.animation && (this.animation.stop(), this.events.animationCancel && this.events.animationCancel.notify()), this.clearAnimation();
  }
  /**
   * Returns `true` if this value is currently animating.
   *
   * @public
   */
  isAnimating() {
    return !!this.animation;
  }
  clearAnimation() {
    delete this.animation;
  }
  /**
   * Destroy and clean up subscribers to this `MotionValue`.
   *
   * The `MotionValue` hooks like `useMotionValue` and `useTransform` automatically
   * handle the lifecycle of the returned `MotionValue`, so this method is only necessary if you've manually
   * created a `MotionValue` via the `motionValue` function.
   *
   * @public
   */
  destroy() {
    var e, n;
    (e = this.dependents) == null || e.clear(), (n = this.events.destroy) == null || n.notify(), this.clearListeners(), this.stop(), this.stopPassiveEffect && this.stopPassiveEffect();
  }
}
function Ct(t, e) {
  return new Br(t, e);
}
const { schedule: Kr } = /* @__PURE__ */ kn(queueMicrotask, !1), Wr = {
  y: !1
};
function $r() {
  return Wr.y;
}
function as(t, e) {
  const n = Er(t), s = new AbortController(), i = {
    passive: !0,
    ...e,
    signal: s.signal
  };
  return [n, i, () => s.abort()];
}
function en(t) {
  return !(t.pointerType === "touch" || $r());
}
function jr(t, e, n = {}) {
  const [s, i, r] = as(t, n), o = (a) => {
    if (!en(a))
      return;
    const { target: c } = a, l = e(c, a);
    if (typeof l != "function" || !c)
      return;
    const u = (f) => {
      en(f) && (l(f), c.removeEventListener("pointerleave", u));
    };
    c.addEventListener("pointerleave", u, i);
  };
  return s.forEach((a) => {
    a.addEventListener("pointerenter", o, i);
  }), r;
}
const us = (t, e) => e ? t === e ? !0 : us(t, e.parentElement) : !1, _r = (t) => t.pointerType === "mouse" ? typeof t.button != "number" || t.button <= 0 : t.isPrimary !== !1, Gr = /* @__PURE__ */ new Set([
  "BUTTON",
  "INPUT",
  "SELECT",
  "TEXTAREA",
  "A"
]);
function Hr(t) {
  return Gr.has(t.tagName) || t.tabIndex !== -1;
}
const Vt = /* @__PURE__ */ new WeakSet();
function nn(t) {
  return (e) => {
    e.key === "Enter" && t(e);
  };
}
function Wt(t, e) {
  t.dispatchEvent(new PointerEvent("pointer" + e, { isPrimary: !0, bubbles: !0 }));
}
const Ur = (t, e) => {
  const n = t.currentTarget;
  if (!n)
    return;
  const s = nn(() => {
    if (Vt.has(n))
      return;
    Wt(n, "down");
    const i = nn(() => {
      Wt(n, "up");
    }), r = () => Wt(n, "cancel");
    n.addEventListener("keyup", i, e), n.addEventListener("blur", r, e);
  });
  n.addEventListener("keydown", s, e), n.addEventListener("blur", () => n.removeEventListener("keydown", s), e);
};
function sn(t) {
  return _r(t) && !0;
}
function zr(t, e, n = {}) {
  const [s, i, r] = as(t, n), o = (a) => {
    const c = a.currentTarget;
    if (!sn(a))
      return;
    Vt.add(c);
    const l = e(c, a), u = (h, y) => {
      window.removeEventListener("pointerup", f), window.removeEventListener("pointercancel", d), Vt.has(c) && Vt.delete(c), sn(h) && typeof l == "function" && l(h, { success: y });
    }, f = (h) => {
      u(h, c === window || c === document || n.useGlobalTarget || us(c, h.target));
    }, d = (h) => {
      u(h, !1);
    };
    window.addEventListener("pointerup", f, i), window.addEventListener("pointercancel", d, i);
  };
  return s.forEach((a) => {
    (n.useGlobalTarget ? window : a).addEventListener("pointerdown", o, i), Lr(a) && (a.addEventListener("focus", (l) => Ur(l, i)), !Hr(a) && !a.hasAttribute("tabindex") && (a.tabIndex = 0));
  }), r;
}
const D = (t) => !!(t && t.getVelocity), Yr = [...ss, M, rt], Xr = (t) => Yr.find(ns(t)), cs = nt({
  transformPagePoint: (t) => t,
  isStatic: !1,
  reducedMotion: "never"
}), Ae = nt({ strict: !1 }), rn = {
  animation: [
    "animate",
    "variants",
    "whileHover",
    "whileTap",
    "exit",
    "whileInView",
    "whileFocus",
    "whileDrag"
  ],
  exit: ["exit"],
  drag: ["drag", "dragControls"],
  focus: ["whileFocus"],
  hover: ["whileHover", "onHoverStart", "onHoverEnd"],
  tap: ["whileTap", "onTap", "onTapStart", "onTapCancel"],
  pan: ["onPan", "onPanStart", "onPanSessionStart", "onPanEnd"],
  inView: ["whileInView", "onViewportEnter", "onViewportLeave"],
  layout: ["layout", "layoutId"]
}, et = {};
for (const t in rn)
  et[t] = {
    isEnabled: (e) => rn[t].some((n) => !!e[n])
  };
function on(t) {
  for (const e in t)
    et[e] = {
      ...et[e],
      ...t[e]
    };
}
function qr({ children: t, features: e, strict: n = !1 }) {
  const [, s] = Bs(!$t(e)), i = lt(void 0);
  if (!$t(e)) {
    const { renderer: r, ...o } = e;
    i.current = r, on(o);
  }
  return le(() => {
    $t(e) && e().then(({ renderer: r, ...o }) => {
      on(o), i.current = r, s(!0);
    });
  }, []), C(Ae.Provider, { value: { renderer: i.current, strict: n }, children: t });
}
function $t(t) {
  return typeof t == "function";
}
const Zr = /* @__PURE__ */ new Set([
  "animate",
  "exit",
  "variants",
  "initial",
  "style",
  "values",
  "variants",
  "transition",
  "transformTemplate",
  "custom",
  "inherit",
  "onBeforeLayoutMeasure",
  "onAnimationStart",
  "onAnimationComplete",
  "onUpdate",
  "onDragStart",
  "onDrag",
  "onDragEnd",
  "onMeasureDragConstraints",
  "onDirectionLock",
  "onDragTransitionEnd",
  "_dragX",
  "_dragY",
  "onHoverStart",
  "onHoverEnd",
  "onViewportEnter",
  "onViewportLeave",
  "globalTapTarget",
  "ignoreStrict",
  "viewport"
]);
function Pt(t) {
  return t.startsWith("while") || t.startsWith("drag") && t !== "draggable" || t.startsWith("layout") || t.startsWith("onTap") || t.startsWith("onPan") || t.startsWith("onLayout") || Zr.has(t);
}
let ls = (t) => !Pt(t);
function Jr(t) {
  typeof t == "function" && (ls = (e) => e.startsWith("on") ? !Pt(e) : t(e));
}
try {
  Jr(require("@emotion/is-prop-valid").default);
} catch {
}
function Qr(t, e, n) {
  const s = {};
  for (const i in t)
    i === "values" && typeof t.values == "object" || (ls(i) || n === !0 && Pt(i) || !e && !Pt(i) || // If trying to use native HTML drag events, forward drag listeners
    t.draggable && i.startsWith("onDrag")) && (s[i] = t[i]);
  return s;
}
const Rt = /* @__PURE__ */ nt({});
function kt(t) {
  return t !== null && typeof t == "object" && typeof t.start == "function";
}
function mt(t) {
  return typeof t == "string" || Array.isArray(t);
}
const Me = [
  "animate",
  "whileInView",
  "whileFocus",
  "whileHover",
  "whileTap",
  "whileDrag",
  "exit"
], Ce = ["initial", ...Me];
function Et(t) {
  return kt(t.animate) || Ce.some((e) => mt(t[e]));
}
function fs(t) {
  return !!(Et(t) || t.variants);
}
function to(t, e) {
  if (Et(t)) {
    const { initial: n, animate: s } = t;
    return {
      initial: n === !1 || mt(n) ? n : void 0,
      animate: mt(s) ? s : void 0
    };
  }
  return t.inherit !== !1 ? e : {};
}
function eo(t) {
  const { initial: e, animate: n } = to(t, E(Rt));
  return Dt(() => ({ initial: e, animate: n }), [an(e), an(n)]);
}
function an(t) {
  return Array.isArray(t) ? t.join(" ") : t;
}
const no = {};
function ds(t, { layout: e, layoutId: n }) {
  return at.has(t) || t.startsWith("origin") || (e || n !== void 0) && (!!no[t] || t === "opacity");
}
const so = {
  x: "translateX",
  y: "translateY",
  z: "translateZ",
  transformPerspective: "perspective"
}, io = ot.length;
function ro(t, e, n) {
  let s = "", i = !0;
  for (let r = 0; r < io; r++) {
    const o = ot[r], a = t[o];
    if (a === void 0)
      continue;
    let c = !0;
    if (typeof a == "number" ? c = a === (o.startsWith("scale") ? 1 : 0) : c = parseFloat(a) === 0, !c || n) {
      const l = os(a, xe[o]);
      if (!c) {
        i = !1;
        const u = so[o] || o;
        s += `${u}(${l}) `;
      }
      n && (e[o] = l);
    }
  }
  return s = s.trim(), n ? s = n(e, i ? "" : s) : i && (s = "none"), s;
}
function Pe(t, e, n) {
  const { style: s, vars: i, transformOrigin: r } = t;
  let o = !1, a = !1;
  for (const c in e) {
    const l = e[c];
    if (at.has(c)) {
      o = !0;
      continue;
    } else if (Ln(c)) {
      i[c] = l;
      continue;
    } else {
      const u = os(l, xe[c]);
      c.startsWith("origin") ? (a = !0, r[c] = u) : s[c] = u;
    }
  }
  if (e.transform || (o || n ? s.transform = ro(e, t.transform, n) : s.transform && (s.transform = "none")), a) {
    const { originX: c = "50%", originY: l = "50%", originZ: u = 0 } = r;
    s.transformOrigin = `${c} ${l} ${u}`;
  }
}
const Fe = () => ({
  style: {},
  transform: {},
  transformOrigin: {},
  vars: {}
});
function hs(t, e, n) {
  for (const s in e)
    !D(e[s]) && !ds(s, n) && (t[s] = e[s]);
}
function oo({ transformTemplate: t }, e) {
  return Dt(() => {
    const n = Fe();
    return Pe(n, e, t), Object.assign({}, n.vars, n.style);
  }, [e]);
}
function ao(t, e) {
  const n = t.style || {}, s = {};
  return hs(s, n, t), Object.assign(s, oo(t, e)), s;
}
function uo(t, e) {
  const n = {}, s = ao(t, e);
  return t.drag && t.dragListener !== !1 && (n.draggable = !1, s.userSelect = s.WebkitUserSelect = s.WebkitTouchCallout = "none", s.touchAction = t.drag === !0 ? "none" : `pan-${t.drag === "x" ? "y" : "x"}`), t.tabIndex === void 0 && (t.onTap || t.onTapStart || t.whileTap) && (n.tabIndex = 0), n.style = s, n;
}
const co = {
  offset: "stroke-dashoffset",
  array: "stroke-dasharray"
}, lo = {
  offset: "strokeDashoffset",
  array: "strokeDasharray"
};
function fo(t, e, n = 1, s = 0, i = !0) {
  t.pathLength = 1;
  const r = i ? co : lo;
  t[r.offset] = m.transform(-s);
  const o = m.transform(e), a = m.transform(n);
  t[r.array] = `${o} ${a}`;
}
function ps(t, {
  attrX: e,
  attrY: n,
  attrScale: s,
  pathLength: i,
  pathSpacing: r = 1,
  pathOffset: o = 0,
  // This is object creation, which we try to avoid per-frame.
  ...a
}, c, l, u) {
  if (Pe(t, a, l), c) {
    t.style.viewBox && (t.attrs.viewBox = t.style.viewBox);
    return;
  }
  t.attrs = t.style, t.style = {};
  const { attrs: f, style: d } = t;
  f.transform && (d.transform = f.transform, delete f.transform), (d.transform || f.transformOrigin) && (d.transformOrigin = f.transformOrigin ?? "50% 50%", delete f.transformOrigin), d.transform && (d.transformBox = (u == null ? void 0 : u.transformBox) ?? "fill-box", delete f.transformBox), e !== void 0 && (f.x = e), n !== void 0 && (f.y = n), s !== void 0 && (f.scale = s), i !== void 0 && fo(f, i, r, o, !1);
}
const ms = () => ({
  ...Fe(),
  attrs: {}
}), gs = (t) => typeof t == "string" && t.toLowerCase() === "svg";
function ho(t, e, n, s) {
  const i = Dt(() => {
    const r = ms();
    return ps(r, e, gs(s), t.transformTemplate, t.style), {
      ...r.attrs,
      style: { ...r.style }
    };
  }, [e]);
  if (t.style) {
    const r = {};
    hs(r, t.style, t), i.style = { ...r, ...i.style };
  }
  return i;
}
const po = [
  "animate",
  "circle",
  "defs",
  "desc",
  "ellipse",
  "g",
  "image",
  "line",
  "filter",
  "marker",
  "mask",
  "metadata",
  "path",
  "pattern",
  "polygon",
  "polyline",
  "rect",
  "stop",
  "switch",
  "symbol",
  "svg",
  "text",
  "tspan",
  "use",
  "view"
];
function De(t) {
  return (
    /**
     * If it's not a string, it's a custom React component. Currently we only support
     * HTML custom React components.
     */
    typeof t != "string" || /**
     * If it contains a dash, the element is a custom HTML webcomponent.
     */
    t.includes("-") ? !1 : (
      /**
       * If it's in our list of lowercase SVG tags, it's an SVG component
       */
      !!(po.indexOf(t) > -1 || /**
       * If it contains a capital letter, it's an SVG component
       */
      /[A-Z]/u.test(t))
    )
  );
}
function mo(t, e, n, { latestValues: s }, i, r = !1) {
  const a = (De(t) ? ho : uo)(e, s, i, t), c = Qr(e, typeof t == "string", r), l = t !== yn ? { ...c, ...a, ref: n } : {}, { children: u } = e, f = Dt(() => D(u) ? u.get() : u, [u]);
  return Ks(t, {
    ...l,
    children: f
  });
}
function un(t) {
  const e = [{}, {}];
  return t == null || t.values.forEach((n, s) => {
    e[0][s] = n.get(), e[1][s] = n.getVelocity();
  }), e;
}
function Oe(t, e, n, s) {
  if (typeof e == "function") {
    const [i, r] = un(s);
    e = e(n !== void 0 ? n : t.custom, i, r);
  }
  if (typeof e == "string" && (e = t.variants && t.variants[e]), typeof e == "function") {
    const [i, r] = un(s);
    e = e(n !== void 0 ? n : t.custom, i, r);
  }
  return e;
}
function go(t) {
  return D(t) ? t.get() : t;
}
function yo({ scrapeMotionValuesFromProps: t, createRenderState: e }, n, s, i) {
  return {
    latestValues: bo(n, s, i, t),
    renderState: e()
  };
}
function bo(t, e, n, s) {
  const i = {}, r = s(t, {});
  for (const d in r)
    i[d] = go(r[d]);
  let { initial: o, animate: a } = t;
  const c = Et(t), l = fs(t);
  e && l && !c && t.inherit !== !1 && (o === void 0 && (o = e.initial), a === void 0 && (a = e.animate));
  let u = n ? n.initial === !1 : !1;
  u = u || o === !1;
  const f = u ? a : o;
  if (f && typeof f != "boolean" && !kt(f)) {
    const d = Array.isArray(f) ? f : [f];
    for (let h = 0; h < d.length; h++) {
      const y = Oe(t, d[h]);
      if (y) {
        const { transitionEnd: b, transition: T, ...p } = y;
        for (const V in p) {
          let g = p[V];
          if (Array.isArray(g)) {
            const x = u ? g.length - 1 : 0;
            g = g[x];
          }
          g !== null && (i[V] = g);
        }
        for (const V in b)
          i[V] = b[V];
      }
    }
  }
  return i;
}
const ys = (t) => (e, n) => {
  const s = E(Rt), i = E(bn), r = () => yo(t, e, s, i);
  return n ? r() : Hs(r);
};
function Ie(t, e, n) {
  var r;
  const { style: s } = t, i = {};
  for (const o in s)
    (D(s[o]) || e.style && D(e.style[o]) || ds(o, t) || ((r = n == null ? void 0 : n.getValue(o)) == null ? void 0 : r.liveStyle) !== void 0) && (i[o] = s[o]);
  return i;
}
const vo = /* @__PURE__ */ ys({
  scrapeMotionValuesFromProps: Ie,
  createRenderState: Fe
});
function bs(t, e, n) {
  const s = Ie(t, e, n);
  for (const i in t)
    if (D(t[i]) || D(e[i])) {
      const r = ot.indexOf(i) !== -1 ? "attr" + i.charAt(0).toUpperCase() + i.substring(1) : i;
      s[r] = t[i];
    }
  return s;
}
const To = /* @__PURE__ */ ys({
  scrapeMotionValuesFromProps: bs,
  createRenderState: ms
}), wo = Symbol.for("motionComponentSymbol");
function vs(t) {
  return t && typeof t == "object" && Object.prototype.hasOwnProperty.call(t, "current");
}
function Vo(t, e, n) {
  return Ws(
    (s) => {
      s && t.onMount && t.onMount(s), e && (s ? e.mount(s) : e.unmount()), n && (typeof n == "function" ? n(s) : vs(n) && (n.current = s));
    },
    /**
     * Only pass a new ref callback to React if we've received a visual element
     * factory. Otherwise we'll be mounting/remounting every time externalRef
     * or other dependencies change.
     */
    [e]
  );
}
const Re = (t) => t.replace(/([a-z])([A-Z])/gu, "$1-$2").toLowerCase(), So = "framerAppearId", Ts = "data-" + Re(So), xo = nt({});
function Ao(t, e, n, s, i) {
  var b, T;
  const { visualElement: r } = E(Rt), o = E(Ae), a = E(bn), c = E(cs).reducedMotion, l = lt(null);
  s = s || o.renderer, !l.current && s && (l.current = s(t, {
    visualState: e,
    parent: r,
    props: n,
    presenceContext: a,
    blockInitialAnimation: a ? a.initial === !1 : !1,
    reducedMotionConfig: c
  }));
  const u = l.current, f = E(xo);
  u && !u.projection && i && (u.type === "html" || u.type === "svg") && Mo(l.current, n, i, f);
  const d = lt(!1);
  $s(() => {
    u && d.current && u.update(n, a);
  });
  const h = n[Ts], y = lt(!!h && !((b = window.MotionHandoffIsComplete) != null && b.call(window, h)) && ((T = window.MotionHasOptimisedAnimation) == null ? void 0 : T.call(window, h)));
  return Us(() => {
    u && (d.current = !0, window.MotionIsMounted = !0, u.updateFeatures(), u.scheduleRenderMicrotask(), y.current && u.animationState && u.animationState.animateChanges());
  }), le(() => {
    u && (!y.current && u.animationState && u.animationState.animateChanges(), y.current && (queueMicrotask(() => {
      var p;
      (p = window.MotionHandoffMarkAsComplete) == null || p.call(window, h);
    }), y.current = !1), u.enteringChildren = void 0);
  }), u;
}
function Mo(t, e, n, s) {
  const { layoutId: i, layout: r, drag: o, dragConstraints: a, layoutScroll: c, layoutRoot: l, layoutCrossfade: u } = e;
  t.projection = new n(t.latestValues, e["data-framer-portal-id"] ? void 0 : ws(t.parent)), t.projection.setOptions({
    layoutId: i,
    layout: r,
    alwaysMeasureLayout: !!o || a && vs(a),
    visualElement: t,
    /**
     * TODO: Update options in an effect. This could be tricky as it'll be too late
     * to update by the time layout animations run.
     * We also need to fix this safeToRemove by linking it up to the one returned by usePresence,
     * ensuring it gets called if there's no potential layout animations.
     *
     */
    animationType: typeof r == "string" ? r : "both",
    initialPromotionConfig: s,
    crossfade: u,
    layoutScroll: c,
    layoutRoot: l
  });
}
function ws(t) {
  if (t)
    return t.options.allowProjection !== !1 ? t.projection : ws(t.parent);
}
function Co(t, { forwardMotionProps: e = !1 } = {}, n, s) {
  const i = De(t) ? To : vo;
  function r(a, c) {
    let l;
    const u = {
      ...E(cs),
      ...a,
      layoutId: Po(a)
    }, { isStatic: f } = u, d = eo(a), h = i(a, f);
    if (!f && fe) {
      Fo(u, n);
      const y = Do(u);
      l = y.MeasureLayout, d.visualElement = Ao(t, h, u, s, y.ProjectionNode);
    }
    return _t(Rt.Provider, { value: d, children: [l && d.visualElement ? C(l, { visualElement: d.visualElement, ...u }) : null, mo(t, a, Vo(h, d.visualElement, c), h, f, e)] });
  }
  r.displayName = `motion.${typeof t == "string" ? t : `create(${t.displayName ?? t.name ?? ""})`}`;
  const o = js(r);
  return o[wo] = t, o;
}
function Po({ layoutId: t }) {
  const e = E(Gs).id;
  return e && t !== void 0 ? e + "-" + t : t;
}
function Fo(t, e) {
  E(Ae).strict, process.env.NODE_ENV;
}
function Do(t) {
  const { drag: e, layout: n } = et;
  if (!e && !n)
    return {};
  const s = { ...e, ...n };
  return {
    MeasureLayout: e != null && e.isEnabled(t) || n != null && n.isEnabled(t) ? s.MeasureLayout : void 0,
    ProjectionNode: s.ProjectionNode
  };
}
function Oo({ top: t, left: e, right: n, bottom: s }) {
  return {
    x: { min: e, max: n },
    y: { min: t, max: s }
  };
}
function Io(t, e) {
  if (!e)
    return t;
  const n = e({ x: t.left, y: t.top }), s = e({ x: t.right, y: t.bottom });
  return {
    top: n.y,
    left: n.x,
    bottom: s.y,
    right: s.x
  };
}
function Ro(t, e) {
  return Oo(Io(t.getBoundingClientRect(), e));
}
const cn = () => ({ min: 0, max: 0 }), Vs = () => ({
  x: cn(),
  y: cn()
}), re = { current: null }, Ss = { current: !1 };
function ko() {
  if (Ss.current = !0, !!fe)
    if (window.matchMedia) {
      const t = window.matchMedia("(prefers-reduced-motion)"), e = () => re.current = t.matches;
      t.addEventListener("change", e), e();
    } else
      re.current = !1;
}
const Eo = /* @__PURE__ */ new WeakMap();
function Lo(t, e, n) {
  for (const s in e) {
    const i = e[s], r = n[s];
    if (D(i))
      t.addValue(s, i);
    else if (D(r))
      t.addValue(s, Ct(i, { owner: t }));
    else if (r !== i)
      if (t.hasValue(s)) {
        const o = t.getValue(s);
        o.liveStyle === !0 ? o.jump(i) : o.hasAnimated || o.set(i);
      } else {
        const o = t.getStaticValue(s);
        t.addValue(s, Ct(o !== void 0 ? o : i, { owner: t }));
      }
  }
  for (const s in n)
    e[s] === void 0 && t.removeValue(s);
  return e;
}
const ln = [
  "AnimationStart",
  "AnimationComplete",
  "Update",
  "BeforeLayoutMeasure",
  "LayoutMeasure",
  "LayoutAnimationStart",
  "LayoutAnimationComplete"
];
class No {
  /**
   * This method takes React props and returns found MotionValues. For example, HTML
   * MotionValues will be found within the style prop, whereas for Three.js within attribute arrays.
   *
   * This isn't an abstract method as it needs calling in the constructor, but it is
   * intended to be one.
   */
  scrapeMotionValuesFromProps(e, n, s) {
    return {};
  }
  constructor({ parent: e, props: n, presenceContext: s, reducedMotionConfig: i, blockInitialAnimation: r, visualState: o }, a = {}) {
    this.current = null, this.children = /* @__PURE__ */ new Set(), this.isVariantNode = !1, this.isControllingVariants = !1, this.shouldReduceMotion = null, this.values = /* @__PURE__ */ new Map(), this.KeyframeResolver = Se, this.features = {}, this.valueSubscriptions = /* @__PURE__ */ new Map(), this.prevMotionValues = {}, this.events = {}, this.propEventSubscriptions = {}, this.notifyUpdate = () => this.notify("Update", this.latestValues), this.render = () => {
      this.current && (this.triggerBuild(), this.renderInstance(this.current, this.renderState, this.props.style, this.projection));
    }, this.renderScheduledAt = 0, this.scheduleRender = () => {
      const d = k.now();
      this.renderScheduledAt < d && (this.renderScheduledAt = d, L.render(this.render, !1, !0));
    };
    const { latestValues: c, renderState: l } = o;
    this.latestValues = c, this.baseTarget = { ...c }, this.initialValues = n.initial ? { ...c } : {}, this.renderState = l, this.parent = e, this.props = n, this.presenceContext = s, this.depth = e ? e.depth + 1 : 0, this.reducedMotionConfig = i, this.options = a, this.blockInitialAnimation = !!r, this.isControllingVariants = Et(n), this.isVariantNode = fs(n), this.isVariantNode && (this.variantChildren = /* @__PURE__ */ new Set()), this.manuallyAnimateOnMount = !!(e && e.current);
    const { willChange: u, ...f } = this.scrapeMotionValuesFromProps(n, {}, this);
    for (const d in f) {
      const h = f[d];
      c[d] !== void 0 && D(h) && h.set(c[d]);
    }
  }
  mount(e) {
    var n;
    this.current = e, Eo.set(e, this), this.projection && !this.projection.instance && this.projection.mount(e), this.parent && this.isVariantNode && !this.isControllingVariants && (this.removeFromVariantTree = this.parent.addVariantChild(this)), this.values.forEach((s, i) => this.bindToMotionValue(i, s)), Ss.current || ko(), this.shouldReduceMotion = this.reducedMotionConfig === "never" ? !1 : this.reducedMotionConfig === "always" ? !0 : re.current, process.env.NODE_ENV !== "production" && xn(this.shouldReduceMotion !== !0, "You have Reduced Motion enabled on your device. Animations may not appear as expected.", "reduced-motion-disabled"), (n = this.parent) == null || n.addChild(this), this.update(this.props, this.presenceContext);
  }
  unmount() {
    var e;
    this.projection && this.projection.unmount(), Ht(this.notifyUpdate), Ht(this.render), this.valueSubscriptions.forEach((n) => n()), this.valueSubscriptions.clear(), this.removeFromVariantTree && this.removeFromVariantTree(), (e = this.parent) == null || e.removeChild(this);
    for (const n in this.events)
      this.events[n].clear();
    for (const n in this.features) {
      const s = this.features[n];
      s && (s.unmount(), s.isMounted = !1);
    }
    this.current = null;
  }
  addChild(e) {
    this.children.add(e), this.enteringChildren ?? (this.enteringChildren = /* @__PURE__ */ new Set()), this.enteringChildren.add(e);
  }
  removeChild(e) {
    this.children.delete(e), this.enteringChildren && this.enteringChildren.delete(e);
  }
  bindToMotionValue(e, n) {
    this.valueSubscriptions.has(e) && this.valueSubscriptions.get(e)();
    const s = at.has(e);
    s && this.onBindTransform && this.onBindTransform();
    const i = n.on("change", (o) => {
      this.latestValues[e] = o, this.props.onUpdate && L.preRender(this.notifyUpdate), s && this.projection && (this.projection.isTransformDirty = !0), this.scheduleRender();
    });
    let r;
    window.MotionCheckAppearSync && (r = window.MotionCheckAppearSync(this, e, n)), this.valueSubscriptions.set(e, () => {
      i(), r && r(), n.owner && n.stop();
    });
  }
  sortNodePosition(e) {
    return !this.current || !this.sortInstanceNodePosition || this.type !== e.type ? 0 : this.sortInstanceNodePosition(this.current, e.current);
  }
  updateFeatures() {
    let e = "animation";
    for (e in et) {
      const n = et[e];
      if (!n)
        continue;
      const { isEnabled: s, Feature: i } = n;
      if (!this.features[e] && i && s(this.props) && (this.features[e] = new i(this)), this.features[e]) {
        const r = this.features[e];
        r.isMounted ? r.update() : (r.mount(), r.isMounted = !0);
      }
    }
  }
  triggerBuild() {
    this.build(this.renderState, this.latestValues, this.props);
  }
  /**
   * Measure the current viewport box with or without transforms.
   * Only measures axis-aligned boxes, rotate and skew must be manually
   * removed with a re-render to work.
   */
  measureViewportBox() {
    return this.current ? this.measureInstanceViewportBox(this.current, this.props) : Vs();
  }
  getStaticValue(e) {
    return this.latestValues[e];
  }
  setStaticValue(e, n) {
    this.latestValues[e] = n;
  }
  /**
   * Update the provided props. Ensure any newly-added motion values are
   * added to our map, old ones removed, and listeners updated.
   */
  update(e, n) {
    (e.transformTemplate || this.props.transformTemplate) && this.scheduleRender(), this.prevProps = this.props, this.props = e, this.prevPresenceContext = this.presenceContext, this.presenceContext = n;
    for (let s = 0; s < ln.length; s++) {
      const i = ln[s];
      this.propEventSubscriptions[i] && (this.propEventSubscriptions[i](), delete this.propEventSubscriptions[i]);
      const r = "on" + i, o = e[r];
      o && (this.propEventSubscriptions[i] = this.on(i, o));
    }
    this.prevMotionValues = Lo(this, this.scrapeMotionValuesFromProps(e, this.prevProps, this), this.prevMotionValues), this.handleChildMotionValue && this.handleChildMotionValue();
  }
  getProps() {
    return this.props;
  }
  /**
   * Returns the variant definition with a given name.
   */
  getVariant(e) {
    return this.props.variants ? this.props.variants[e] : void 0;
  }
  /**
   * Returns the defined default transition on this component.
   */
  getDefaultTransition() {
    return this.props.transition;
  }
  getTransformPagePoint() {
    return this.props.transformPagePoint;
  }
  getClosestVariantNode() {
    return this.isVariantNode ? this : this.parent ? this.parent.getClosestVariantNode() : void 0;
  }
  /**
   * Add a child visual element to our set of children.
   */
  addVariantChild(e) {
    const n = this.getClosestVariantNode();
    if (n)
      return n.variantChildren && n.variantChildren.add(e), () => n.variantChildren.delete(e);
  }
  /**
   * Add a motion value and bind it to this visual element.
   */
  addValue(e, n) {
    const s = this.values.get(e);
    n !== s && (s && this.removeValue(e), this.bindToMotionValue(e, n), this.values.set(e, n), this.latestValues[e] = n.get());
  }
  /**
   * Remove a motion value and unbind any active subscriptions.
   */
  removeValue(e) {
    this.values.delete(e);
    const n = this.valueSubscriptions.get(e);
    n && (n(), this.valueSubscriptions.delete(e)), delete this.latestValues[e], this.removeValueFromRenderState(e, this.renderState);
  }
  /**
   * Check whether we have a motion value for this key
   */
  hasValue(e) {
    return this.values.has(e);
  }
  getValue(e, n) {
    if (this.props.values && this.props.values[e])
      return this.props.values[e];
    let s = this.values.get(e);
    return s === void 0 && n !== void 0 && (s = Ct(n === null ? void 0 : n, { owner: this }), this.addValue(e, s)), s;
  }
  /**
   * If we're trying to animate to a previously unencountered value,
   * we need to check for it in our state and as a last resort read it
   * directly from the instance (which might have performance implications).
   */
  readValue(e, n) {
    let s = this.latestValues[e] !== void 0 || !this.current ? this.latestValues[e] : this.getBaseTargetFromProps(this.props, e) ?? this.readValueFromInstance(this.current, e, this.options);
    return s != null && (typeof s == "string" && (vn(s) || Tn(s)) ? s = parseFloat(s) : !Xr(s) && rt.test(n) && (s = rs(e, n)), this.setBaseTarget(e, D(s) ? s.get() : s)), D(s) ? s.get() : s;
  }
  /**
   * Set the base target to later animate back to. This is currently
   * only hydrated on creation and when we first read a value.
   */
  setBaseTarget(e, n) {
    this.baseTarget[e] = n;
  }
  /**
   * Find the base target for a value thats been removed from all animation
   * props.
   */
  getBaseTarget(e) {
    var r;
    const { initial: n } = this.props;
    let s;
    if (typeof n == "string" || typeof n == "object") {
      const o = Oe(this.props, n, (r = this.presenceContext) == null ? void 0 : r.custom);
      o && (s = o[e]);
    }
    if (n && s !== void 0)
      return s;
    const i = this.getBaseTargetFromProps(this.props, e);
    return i !== void 0 && !D(i) ? i : this.initialValues[e] !== void 0 && s === void 0 ? void 0 : this.baseTarget[e];
  }
  on(e, n) {
    return this.events[e] || (this.events[e] = new Vn()), this.events[e].add(n);
  }
  notify(e, ...n) {
    this.events[e] && this.events[e].notify(...n);
  }
  scheduleRenderMicrotask() {
    Kr.render(this.render);
  }
}
class xs extends No {
  constructor() {
    super(...arguments), this.KeyframeResolver = kr;
  }
  sortInstanceNodePosition(e, n) {
    return e.compareDocumentPosition(n) & 2 ? 1 : -1;
  }
  getBaseTargetFromProps(e, n) {
    return e.style ? e.style[n] : void 0;
  }
  removeValueFromRenderState(e, { vars: n, style: s }) {
    delete n[e], delete s[e];
  }
  handleChildMotionValue() {
    this.childSubscription && (this.childSubscription(), delete this.childSubscription);
    const { children: e } = this.props;
    D(e) && (this.childSubscription = e.on("change", (n) => {
      this.current && (this.current.textContent = `${n}`);
    }));
  }
}
function As(t, { style: e, vars: n }, s, i) {
  const r = t.style;
  let o;
  for (o in e)
    r[o] = e[o];
  i == null || i.applyProjectionStyles(r, s);
  for (o in n)
    r.setProperty(o, n[o]);
}
function Bo(t) {
  return window.getComputedStyle(t);
}
class Ko extends xs {
  constructor() {
    super(...arguments), this.type = "html", this.renderInstance = As;
  }
  readValueFromInstance(e, n) {
    var s;
    if (at.has(n))
      return (s = this.projection) != null && s.isProjecting ? Jt(n) : Ji(e, n);
    {
      const i = Bo(e), r = (Ln(n) ? i.getPropertyValue(n) : i[n]) || 0;
      return typeof r == "string" ? r.trim() : r;
    }
  }
  measureInstanceViewportBox(e, { transformPagePoint: n }) {
    return Ro(e, n);
  }
  build(e, n, s) {
    Pe(e, n, s.transformTemplate);
  }
  scrapeMotionValuesFromProps(e, n, s) {
    return Ie(e, n, s);
  }
}
const Ms = /* @__PURE__ */ new Set([
  "baseFrequency",
  "diffuseConstant",
  "kernelMatrix",
  "kernelUnitLength",
  "keySplines",
  "keyTimes",
  "limitingConeAngle",
  "markerHeight",
  "markerWidth",
  "numOctaves",
  "targetX",
  "targetY",
  "surfaceScale",
  "specularConstant",
  "specularExponent",
  "stdDeviation",
  "tableValues",
  "viewBox",
  "gradientTransform",
  "pathLength",
  "startOffset",
  "textLength",
  "lengthAdjust"
]);
function Wo(t, e, n, s) {
  As(t, e, void 0, s);
  for (const i in e.attrs)
    t.setAttribute(Ms.has(i) ? i : Re(i), e.attrs[i]);
}
class $o extends xs {
  constructor() {
    super(...arguments), this.type = "svg", this.isSVGTag = !1, this.measureInstanceViewportBox = Vs;
  }
  getBaseTargetFromProps(e, n) {
    return e[n];
  }
  readValueFromInstance(e, n) {
    if (at.has(n)) {
      const s = is(n);
      return s && s.default || 0;
    }
    return n = Ms.has(n) ? n : Re(n), e.getAttribute(n);
  }
  scrapeMotionValuesFromProps(e, n, s) {
    return bs(e, n, s);
  }
  build(e, n, s) {
    ps(e, n, this.isSVGTag, s.transformTemplate, s.style);
  }
  renderInstance(e, n, s, i) {
    Wo(e, n, s, i);
  }
  mount(e) {
    this.isSVGTag = gs(e.tagName), super.mount(e);
  }
}
const jo = (t, e) => De(t) ? new $o(e) : new Ko(e, {
  allowProjection: t !== yn
});
function tt(t, e, n) {
  const s = t.getProps();
  return Oe(s, e, n !== void 0 ? n : s.custom, t);
}
const oe = (t) => Array.isArray(t);
function _o(t, e, n) {
  t.hasValue(e) ? t.getValue(e).set(n) : t.addValue(e, Ct(n));
}
function Go(t) {
  return oe(t) ? t[t.length - 1] || 0 : t;
}
function Ho(t, e) {
  const n = tt(t, e);
  let { transitionEnd: s = {}, transition: i = {}, ...r } = n || {};
  r = { ...r, ...s };
  for (const o in r) {
    const a = Go(r[o]);
    _o(t, o, a);
  }
}
function Uo(t) {
  return !!(D(t) && t.add);
}
function zo(t, e) {
  const n = t.getValue("willChange");
  if (Uo(n))
    return n.add(e);
  if (!n && K.WillChange) {
    const s = new K.WillChange("auto");
    t.addValue("willChange", s), s.add(e);
  }
}
function Yo(t) {
  return t.props[Ts];
}
const Xo = (t) => t !== null;
function qo(t, { repeat: e, repeatType: n = "loop" }, s) {
  const i = t.filter(Xo), r = e && n !== "loop" && e % 2 === 1 ? 0 : i.length - 1;
  return i[r];
}
const Zo = {
  type: "spring",
  stiffness: 500,
  damping: 25,
  restSpeed: 10
}, Jo = (t) => ({
  type: "spring",
  stiffness: 550,
  damping: t === 0 ? 2 * Math.sqrt(550) : 30,
  restSpeed: 10
}), Qo = {
  type: "keyframes",
  duration: 0.8
}, ta = {
  type: "keyframes",
  ease: [0.25, 0.1, 0.35, 1],
  duration: 0.3
}, ea = (t, { keyframes: e }) => e.length > 2 ? Qo : at.has(t) ? t.startsWith("scale") ? Jo(e[1]) : Zo : ta;
function na({ when: t, delay: e, delayChildren: n, staggerChildren: s, staggerDirection: i, repeat: r, repeatType: o, repeatDelay: a, from: c, elapsed: l, ...u }) {
  return !!Object.keys(u).length;
}
const sa = (t, e, n, s = {}, i, r) => (o) => {
  const a = ts(s, t) || {}, c = a.delay || s.delay || 0;
  let { elapsed: l = 0 } = s;
  l = l - /* @__PURE__ */ N(c);
  const u = {
    keyframes: Array.isArray(n) ? n : [null, n],
    ease: "easeOut",
    velocity: e.getVelocity(),
    ...a,
    delay: -l,
    onUpdate: (d) => {
      e.set(d), a.onUpdate && a.onUpdate(d);
    },
    onComplete: () => {
      o(), a.onComplete && a.onComplete();
    },
    name: t,
    motionValue: e,
    element: r ? void 0 : i
  };
  na(a) || Object.assign(u, ea(t, u)), u.duration && (u.duration = /* @__PURE__ */ N(u.duration)), u.repeatDelay && (u.repeatDelay = /* @__PURE__ */ N(u.repeatDelay)), u.from !== void 0 && (u.keyframes[0] = u.from);
  let f = !1;
  if ((u.type === !1 || u.duration === 0 && !u.repeatDelay) && (se(u), u.delay === 0 && (f = !0)), (K.instantAnimations || K.skipAnimations) && (f = !0, se(u), u.delay = 0), u.allowFlatten = !a.type && !a.ease, f && !r && e.get() !== void 0) {
    const d = qo(u.keyframes, a);
    if (d !== void 0) {
      L.update(() => {
        u.onUpdate(d), u.onComplete();
      });
      return;
    }
  }
  return a.isSync ? new Ve(u) : new wr(u);
};
function ia({ protectedKeys: t, needsAnimating: e }, n) {
  const s = t.hasOwnProperty(n) && e[n] !== !0;
  return e[n] = !1, s;
}
function Cs(t, e, { delay: n = 0, transitionOverride: s, type: i } = {}) {
  let { transition: r = t.getDefaultTransition(), transitionEnd: o, ...a } = e;
  s && (r = s);
  const c = [], l = i && t.animationState && t.animationState.getState()[i];
  for (const u in a) {
    const f = t.getValue(u, t.latestValues[u] ?? null), d = a[u];
    if (d === void 0 || l && ia(l, u))
      continue;
    const h = {
      delay: n,
      ...ts(r || {}, u)
    }, y = f.get();
    if (y !== void 0 && !f.isAnimating && !Array.isArray(d) && d === y && !h.velocity)
      continue;
    let b = !1;
    if (window.MotionHandoffAnimation) {
      const p = Yo(t);
      if (p) {
        const V = window.MotionHandoffAnimation(p, u, L);
        V !== null && (h.startTime = V, b = !0);
      }
    }
    zo(t, u), f.start(sa(u, f, d, t.shouldReduceMotion && es.has(u) ? { type: !1 } : h, t, b));
    const T = f.animation;
    T && c.push(T);
  }
  return o && Promise.all(c).then(() => {
    L.update(() => {
      o && Ho(t, o);
    });
  }), c;
}
function Ps(t, e, n, s = 0, i = 1) {
  const r = Array.from(t).sort((l, u) => l.sortNodePosition(u)).indexOf(e), o = t.size, a = (o - 1) * s;
  return typeof n == "function" ? n(r, o) : i === 1 ? r * s : a - r * s;
}
function ae(t, e, n = {}) {
  var c;
  const s = tt(t, e, n.type === "exit" ? (c = t.presenceContext) == null ? void 0 : c.custom : void 0);
  let { transition: i = t.getDefaultTransition() || {} } = s || {};
  n.transitionOverride && (i = n.transitionOverride);
  const r = s ? () => Promise.all(Cs(t, s, n)) : () => Promise.resolve(), o = t.variantChildren && t.variantChildren.size ? (l = 0) => {
    const { delayChildren: u = 0, staggerChildren: f, staggerDirection: d } = i;
    return ra(t, e, l, u, f, d, n);
  } : () => Promise.resolve(), { when: a } = i;
  if (a) {
    const [l, u] = a === "beforeChildren" ? [r, o] : [o, r];
    return l().then(() => u());
  } else
    return Promise.all([r(), o(n.delay)]);
}
function ra(t, e, n = 0, s = 0, i = 0, r = 1, o) {
  const a = [];
  for (const c of t.variantChildren)
    c.notify("AnimationStart", e), a.push(ae(c, e, {
      ...o,
      delay: n + (typeof s == "function" ? 0 : s) + Ps(t.variantChildren, c, s, i, r)
    }).then(() => c.notify("AnimationComplete", e)));
  return Promise.all(a);
}
function oa(t, e, n = {}) {
  t.notify("AnimationStart", e);
  let s;
  if (Array.isArray(e)) {
    const i = e.map((r) => ae(t, r, n));
    s = Promise.all(i);
  } else if (typeof e == "string")
    s = ae(t, e, n);
  else {
    const i = typeof e == "function" ? tt(t, e, n.custom) : e;
    s = Promise.all(Cs(t, i, n));
  }
  return s.then(() => {
    t.notify("AnimationComplete", e);
  });
}
function Fs(t, e) {
  if (!Array.isArray(e))
    return !1;
  const n = e.length;
  if (n !== t.length)
    return !1;
  for (let s = 0; s < n; s++)
    if (e[s] !== t[s])
      return !1;
  return !0;
}
const aa = Ce.length;
function Ds(t) {
  if (!t)
    return;
  if (!t.isControllingVariants) {
    const n = t.parent ? Ds(t.parent) || {} : {};
    return t.props.initial !== void 0 && (n.initial = t.props.initial), n;
  }
  const e = {};
  for (let n = 0; n < aa; n++) {
    const s = Ce[n], i = t.props[s];
    (mt(i) || i === !1) && (e[s] = i);
  }
  return e;
}
const ua = [...Me].reverse(), ca = Me.length;
function la(t) {
  return (e) => Promise.all(e.map(({ animation: n, options: s }) => oa(t, n, s)));
}
function fa(t) {
  let e = la(t), n = fn(), s = !0;
  const i = (c) => (l, u) => {
    var d;
    const f = tt(t, u, c === "exit" ? (d = t.presenceContext) == null ? void 0 : d.custom : void 0);
    if (f) {
      const { transition: h, transitionEnd: y, ...b } = f;
      l = { ...l, ...b, ...y };
    }
    return l;
  };
  function r(c) {
    e = c(t);
  }
  function o(c) {
    const { props: l } = t, u = Ds(t.parent) || {}, f = [], d = /* @__PURE__ */ new Set();
    let h = {}, y = 1 / 0;
    for (let T = 0; T < ca; T++) {
      const p = ua[T], V = n[p], g = l[p] !== void 0 ? l[p] : u[p], x = mt(g), v = p === c ? V.isActive : null;
      v === !1 && (y = T);
      let S = g === u[p] && g !== l[p] && x;
      if (S && s && t.manuallyAnimateOnMount && (S = !1), V.protectedKeys = { ...h }, // If it isn't active and hasn't *just* been set as inactive
      !V.isActive && v === null || // If we didn't and don't have any defined prop for this animation type
      !g && !V.prevProp || // Or if the prop doesn't define an animation
      kt(g) || typeof g == "boolean")
        continue;
      const P = da(V.prevProp, g);
      let w = P || // If we're making this variant active, we want to always make it active
      p === c && V.isActive && !S && x || // If we removed a higher-priority variant (i is in reverse order)
      T > y && x, O = !1;
      const I = Array.isArray(g) ? g : [g];
      let X = I.reduce(i(p), {});
      v === !1 && (X = {});
      const { prevResolvedValues: ke = {} } = V, Rs = {
        ...ke,
        ...X
      }, Ee = (F) => {
        w = !0, d.has(F) && (O = !0, d.delete(F)), V.needsAnimating[F] = !0;
        const R = t.getValue(F);
        R && (R.liveStyle = !1);
      };
      for (const F in Rs) {
        const R = X[F], $ = ke[F];
        if (h.hasOwnProperty(F))
          continue;
        let q = !1;
        oe(R) && oe($) ? q = !Fs(R, $) : q = R !== $, q ? R != null ? Ee(F) : d.add(F) : R !== void 0 && d.has(F) ? Ee(F) : V.protectedKeys[F] = !0;
      }
      V.prevProp = g, V.prevResolvedValues = X, V.isActive && (h = { ...h, ...X }), s && t.blockInitialAnimation && (w = !1);
      const Le = S && P;
      w && (!Le || O) && f.push(...I.map((F) => {
        const R = { type: p };
        if (typeof F == "string" && s && !Le && t.manuallyAnimateOnMount && t.parent) {
          const { parent: $ } = t, q = tt($, F);
          if ($.enteringChildren && q) {
            const { delayChildren: ks } = q.transition || {};
            R.delay = Ps($.enteringChildren, t, ks);
          }
        }
        return {
          animation: F,
          options: R
        };
      }));
    }
    if (d.size) {
      const T = {};
      if (typeof l.initial != "boolean") {
        const p = tt(t, Array.isArray(l.initial) ? l.initial[0] : l.initial);
        p && p.transition && (T.transition = p.transition);
      }
      d.forEach((p) => {
        const V = t.getBaseTarget(p), g = t.getValue(p);
        g && (g.liveStyle = !0), T[p] = V ?? null;
      }), f.push({ animation: T });
    }
    let b = !!f.length;
    return s && (l.initial === !1 || l.initial === l.animate) && !t.manuallyAnimateOnMount && (b = !1), s = !1, b ? e(f) : Promise.resolve();
  }
  function a(c, l) {
    var f;
    if (n[c].isActive === l)
      return Promise.resolve();
    (f = t.variantChildren) == null || f.forEach((d) => {
      var h;
      return (h = d.animationState) == null ? void 0 : h.setActive(c, l);
    }), n[c].isActive = l;
    const u = o(c);
    for (const d in n)
      n[d].protectedKeys = {};
    return u;
  }
  return {
    animateChanges: o,
    setActive: a,
    setAnimateFunction: r,
    getState: () => n,
    reset: () => {
      n = fn(), s = !0;
    }
  };
}
function da(t, e) {
  return typeof e == "string" ? e !== t : Array.isArray(e) ? !Fs(e, t) : !1;
}
function j(t = !1) {
  return {
    isActive: t,
    protectedKeys: {},
    needsAnimating: {},
    prevResolvedValues: {}
  };
}
function fn() {
  return {
    animate: j(!0),
    whileInView: j(),
    whileHover: j(),
    whileTap: j(),
    whileDrag: j(),
    whileFocus: j(),
    exit: j()
  };
}
class ut {
  constructor(e) {
    this.isMounted = !1, this.node = e;
  }
  update() {
  }
}
class ha extends ut {
  /**
   * We dynamically generate the AnimationState manager as it contains a reference
   * to the underlying animation library. We only want to load that if we load this,
   * so people can optionally code split it out using the `m` component.
   */
  constructor(e) {
    super(e), e.animationState || (e.animationState = fa(e));
  }
  updateAnimationControlsSubscription() {
    const { animate: e } = this.node.getProps();
    kt(e) && (this.unmountControls = e.subscribe(this.node));
  }
  /**
   * Subscribe any provided AnimationControls to the component's VisualElement
   */
  mount() {
    this.updateAnimationControlsSubscription();
  }
  update() {
    const { animate: e } = this.node.getProps(), { animate: n } = this.node.prevProps || {};
    e !== n && this.updateAnimationControlsSubscription();
  }
  unmount() {
    var e;
    this.node.animationState.reset(), (e = this.unmountControls) == null || e.call(this);
  }
}
let pa = 0;
class ma extends ut {
  constructor() {
    super(...arguments), this.id = pa++;
  }
  update() {
    if (!this.node.presenceContext)
      return;
    const { isPresent: e, onExitComplete: n } = this.node.presenceContext, { isPresent: s } = this.node.prevPresenceContext || {};
    if (!this.node.animationState || e === s)
      return;
    const i = this.node.animationState.setActive("exit", !e);
    n && !e && i.then(() => {
      n(this.id);
    });
  }
  mount() {
    const { register: e, onExitComplete: n } = this.node.presenceContext || {};
    n && n(this.id), e && (this.unmount = e(this.id));
  }
  unmount() {
  }
}
const ga = {
  animation: {
    Feature: ha
  },
  exit: {
    Feature: ma
  }
};
function dn(t, e, n, s = { passive: !0 }) {
  return t.addEventListener(e, n, s), () => t.removeEventListener(e, n);
}
function Os(t) {
  return {
    point: {
      x: t.pageX,
      y: t.pageY
    }
  };
}
function hn(t, e, n) {
  const { props: s } = t;
  t.animationState && s.whileHover && t.animationState.setActive("whileHover", n === "Start");
  const i = "onHover" + n, r = s[i];
  r && L.postRender(() => r(e, Os(e)));
}
class ya extends ut {
  mount() {
    const { current: e } = this.node;
    e && (this.unmount = jr(e, (n, s) => (hn(this.node, s, "Start"), (i) => hn(this.node, i, "End"))));
  }
  unmount() {
  }
}
class ba extends ut {
  constructor() {
    super(...arguments), this.isActive = !1;
  }
  onFocus() {
    let e = !1;
    try {
      e = this.node.current.matches(":focus-visible");
    } catch {
      e = !0;
    }
    !e || !this.node.animationState || (this.node.animationState.setActive("whileFocus", !0), this.isActive = !0);
  }
  onBlur() {
    !this.isActive || !this.node.animationState || (this.node.animationState.setActive("whileFocus", !1), this.isActive = !1);
  }
  mount() {
    this.unmount = Ot(dn(this.node.current, "focus", () => this.onFocus()), dn(this.node.current, "blur", () => this.onBlur()));
  }
  unmount() {
  }
}
function pn(t, e, n) {
  const { props: s } = t;
  if (t.current instanceof HTMLButtonElement && t.current.disabled)
    return;
  t.animationState && s.whileTap && t.animationState.setActive("whileTap", n === "Start");
  const i = "onTap" + (n === "End" ? "" : n), r = s[i];
  r && L.postRender(() => r(e, Os(e)));
}
class va extends ut {
  mount() {
    const { current: e } = this.node;
    e && (this.unmount = zr(e, (n, s) => (pn(this.node, s, "Start"), (i, { success: r }) => pn(this.node, i, r ? "End" : "Cancel")), { useGlobalTarget: this.node.props.globalTapTarget }));
  }
  unmount() {
  }
}
const ue = /* @__PURE__ */ new WeakMap(), jt = /* @__PURE__ */ new WeakMap(), Ta = (t) => {
  const e = ue.get(t.target);
  e && e(t);
}, wa = (t) => {
  t.forEach(Ta);
};
function Va({ root: t, ...e }) {
  const n = t || document;
  jt.has(n) || jt.set(n, {});
  const s = jt.get(n), i = JSON.stringify(e);
  return s[i] || (s[i] = new IntersectionObserver(wa, { root: t, ...e })), s[i];
}
function Sa(t, e, n) {
  const s = Va(e);
  return ue.set(t, n), s.observe(t), () => {
    ue.delete(t), s.unobserve(t);
  };
}
const xa = {
  some: 0,
  all: 1
};
class Aa extends ut {
  constructor() {
    super(...arguments), this.hasEnteredView = !1, this.isInView = !1;
  }
  startObserver() {
    this.unmount();
    const { viewport: e = {} } = this.node.getProps(), { root: n, margin: s, amount: i = "some", once: r } = e, o = {
      root: n ? n.current : void 0,
      rootMargin: s,
      threshold: typeof i == "number" ? i : xa[i]
    }, a = (c) => {
      const { isIntersecting: l } = c;
      if (this.isInView === l || (this.isInView = l, r && !l && this.hasEnteredView))
        return;
      l && (this.hasEnteredView = !0), this.node.animationState && this.node.animationState.setActive("whileInView", l);
      const { onViewportEnter: u, onViewportLeave: f } = this.node.getProps(), d = l ? u : f;
      d && d(c);
    };
    return Sa(this.node.current, o, a);
  }
  mount() {
    this.startObserver();
  }
  update() {
    if (typeof IntersectionObserver > "u")
      return;
    const { props: e, prevProps: n } = this.node;
    ["amount", "margin", "root"].some(Ma(e, n)) && this.startObserver();
  }
  unmount() {
  }
}
function Ma({ viewport: t = {} }, { viewport: e = {} } = {}) {
  return (n) => t[n] !== e[n];
}
const Ca = {
  inView: {
    Feature: Aa
  },
  tap: {
    Feature: va
  },
  focus: {
    Feature: ba
  },
  hover: {
    Feature: ya
  }
}, Pa = {
  renderer: jo,
  ...ga,
  ...Ca
};
function Fa(t, e) {
  return Co(t, e);
}
const Da = /* @__PURE__ */ Fa("span"), Oa = ({
  variant: t = "ghost",
  size: e = "2",
  px: n = "2",
  gap: s = "1_5",
  justify: i = "space-between",
  w: r = "auto",
  selected: o,
  icon: a,
  leadingIcon: c,
  leadingComponent: l,
  trailingIcon: u,
  trailingComponent: f,
  textProps: d,
  ...h
}) => /* @__PURE__ */ _t(
  dt.Root,
  {
    size: e,
    variant: t,
    gap: s,
    px: n,
    justify: i,
    w: r,
    ...h,
    children: [
      /* @__PURE__ */ _t(mn, { gap: s, align: "center", w: "full", children: [
        /* @__PURE__ */ C(
          Ia,
          {
            icon: a,
            selected: o,
            leadingIcon: c,
            leadingComponent: l
          }
        ),
        /* @__PURE__ */ C(
          dt.Text,
          {
            weight: (h == null ? void 0 : h.fontWeight) || "medium",
            w: "full",
            overflow: "hidden",
            textOverflow: "ellipsis",
            ...d,
            children: h.children
          }
        )
      ] }),
      /* @__PURE__ */ C(
        Ra,
        {
          trailingIcon: u,
          trailingComponent: f
        }
      )
    ]
  }
), Ia = ({
  icon: t,
  selected: e,
  leadingIcon: n,
  leadingComponent: s
}) => {
  if (e === !0 || e === !1)
    return /* @__PURE__ */ C(qr, { features: Pa, children: /* @__PURE__ */ C(
      dt.Icon,
      {
        as: Da,
        variant: "primary",
        icon: _s,
        "aria-hidden": !0,
        animate: e ? {
          opacity: 1,
          rotate: 0,
          scale: 1
        } : {
          opacity: 0,
          rotate: -45,
          scale: 0.3
        },
        transition: { duration: 0.15, type: "spring", bounce: 0 },
        style: { transformOrigin: "center" },
        display: "block"
      }
    ) });
  const r = n || t;
  if (r)
    return /* @__PURE__ */ C(dt.Icon, { variant: "primary", ...r });
  if (s)
    return s;
}, Ra = ({
  trailingIcon: t,
  trailingComponent: e
}) => {
  if (t)
    return /* @__PURE__ */ C(dt.Icon, { variant: "primary", ...t });
  if (e)
    return e;
}, Is = gn.createContext({
  open: !1,
  setOpen: () => {
  }
}), ka = ({
  open: t,
  onOpenChange: e,
  defaultOpen: n,
  modal: s = !0,
  children: i,
  ...r
}) => {
  const [o = !1, a] = Es({
    prop: t,
    defaultProp: n ?? !1,
    onChange: e
  });
  return /* @__PURE__ */ C(Is.Provider, { value: { open: o, setOpen: a }, children: /* @__PURE__ */ C(
    Ft.Root,
    {
      open: o,
      onOpenChange: a,
      modal: s,
      ...r,
      children: i
    }
  ) });
}, Ea = ({ asChild: t = !0, tgphRef: e, children: n, ...s }) => {
  const i = gn.useContext(Is);
  return /* @__PURE__ */ C(
    Ft.Anchor,
    {
      onClick: () => {
        i.setOpen(!i.open);
      },
      asChild: t,
      ...s,
      ref: e,
      children: /* @__PURE__ */ C(ce, { children: n })
    }
  );
}, La = ({
  direction: t = "column",
  gap: e = "1",
  rounded: n = "4",
  py: s = "1",
  border: i = "px",
  borderColor: r = "gray-8",
  shadow: o = "2",
  sideOffset: a = 4,
  children: c,
  onInteractOutside: l,
  onKeyDown: u,
  onCloseAutoFocus: f,
  tgphRef: d,
  ...h
}) => /* @__PURE__ */ C(
  Ft.Content,
  {
    onInteractOutside: l,
    onKeyDown: u,
    onCloseAutoFocus: f,
    asChild: !0,
    sideOffset: a,
    ...h,
    ref: d,
    children: /* @__PURE__ */ C(ce, { children: /* @__PURE__ */ C(
      mn,
      {
        bg: "surface-1",
        direction: t,
        gap: e,
        rounded: n,
        border: i,
        borderColor: r,
        py: s,
        shadow: o,
        style: {
          overflowY: "auto"
        },
        zIndex: "popover",
        children: c
      }
    ) })
  }
), Na = ({
  mx: t = "1",
  asChild: e = !0,
  icon: n,
  leadingIcon: s,
  trailingIcon: i,
  leadingComponent: r,
  trailingComponent: o,
  selected: a,
  tgphRef: c,
  onClick: l,
  ...u
}) => {
  const f = s || n;
  return /* @__PURE__ */ C(
    Ft.Item,
    {
      ...u,
      asChild: e,
      ref: c,
      children: /* @__PURE__ */ C(ce, { children: /* @__PURE__ */ C(
        Oa,
        {
          onClick: l,
          selected: a,
          leadingIcon: f,
          trailingIcon: i,
          leadingComponent: r,
          trailingComponent: o,
          "data-tgph-menu-button": !0,
          mx: t,
          style: {
            flexShrink: 0
          },
          ...u
        }
      ) })
    }
  );
}, Ba = ({
  w: t = "full",
  borderBottom: e = "px",
  ...n
}) => /* @__PURE__ */ C(Ls, { as: "hr", w: t, borderBottom: e, ...n }), Ka = {};
Object.assign(Ka, {
  Root: ka,
  Trigger: Ea,
  Content: La,
  Button: Na,
  Divider: Ba
});
export {
  Ka as Menu,
  Oa as MenuItem
};
//# sourceMappingURL=index.mjs.map
