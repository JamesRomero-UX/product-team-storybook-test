import type { SegmentItem, HiddenUnchangedLinesInfo } from './get-segments';
declare const findVisibleLines: (segments: Array<SegmentItem | HiddenUnchangedLinesInfo>, accTop: number[], viewportTop: number, viewportBottom: number, itemHeight: number, expandLineHeight: number) => number[];
export default findVisibleLines;
