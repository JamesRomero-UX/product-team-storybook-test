import * as http from 'node:http';
import { Readable } from 'node:stream';
import { MultipartParserOptions, MultipartPart } from './multipart.js';
/**
 * Returns true if the given request is a multipart request.
 */
export declare function isMultipartRequest(request: http.IncomingMessage): boolean;
/**
 * Parse a multipart node.js request and yield each part as a `MultipartPart` object.
 */
export declare function parseMultipartRequest(request: http.IncomingMessage, options?: MultipartParserOptions): AsyncGenerator<MultipartPart>;
/**
 * Parse a multipart Node.js `Buffer` or `Readable` stream and yield each part as a `MultipartPart` object.
 *
 * Note: This is a low-level API that requires manual handling of the stream and boundary. If you're
 * building a web server, consider using `parseMultipartRequest(request)` instead.
 */
export declare function parseMultipart(data: Readable | Buffer | Iterable<Buffer> | AsyncIterable<Buffer>, boundary: string, options?: MultipartParserOptions): AsyncGenerator<MultipartPart>;
