import { Component } from "../base";
import { IComponentOptions } from "../types";
export interface IProgressBarOptions extends IComponentOptions {
    progress: number;
    stages: Array<{
        name: string;
    }>;
    title?: string;
}
declare class ProgressBar extends Component {
    constructor(options?: IProgressBarOptions, componentType?: string);
}
export { ProgressBar };
