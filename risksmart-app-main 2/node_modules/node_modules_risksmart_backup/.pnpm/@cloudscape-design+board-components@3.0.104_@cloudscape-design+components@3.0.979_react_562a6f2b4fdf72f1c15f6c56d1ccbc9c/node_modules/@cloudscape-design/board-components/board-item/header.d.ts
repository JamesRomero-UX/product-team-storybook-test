import { ReactNode } from "react";
export interface WidgetContainerHeaderProps {
    handle: ReactNode;
    children?: ReactNode;
    settings?: ReactNode;
}
export default function WidgetContainerHeader({ handle, children, settings }: WidgetContainerHeaderProps): import("react/jsx-runtime").JSX.Element;
