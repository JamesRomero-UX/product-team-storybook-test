"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const useSchema_1 = __importDefault(require("../tests/useSchema"));
const useTestKnex_1 = __importDefault(require("../tests/useTestKnex"));
const extractEnum_1 = __importDefault(require("./extractEnum"));
const makePgType = (name, schemaName = "test") => ({
    schemaName,
    name,
    kind: "enum",
    comment: null,
});
(0, vitest_1.describe)("extractEnum", () => {
    const [getKnex] = (0, useTestKnex_1.default)();
    (0, useSchema_1.default)(getKnex, "test");
    (0, vitest_1.it)("should extract enum values", async () => {
        const db = getKnex();
        await db.raw("create type test.some_enum as enum('a', 'b', 'c')");
        const result = await (0, extractEnum_1.default)(db, makePgType("some_enum"));
        const expected = {
            name: "some_enum",
            schemaName: "test",
            kind: "enum",
            comment: null,
            values: ["a", "b", "c"],
        };
        (0, vitest_1.expect)(result).toStrictEqual(expected);
    });
});
