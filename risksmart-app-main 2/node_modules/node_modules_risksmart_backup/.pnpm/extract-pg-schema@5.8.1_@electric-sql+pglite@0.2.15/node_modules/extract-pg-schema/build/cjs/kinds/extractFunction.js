"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const parsePostgresArray_1 = require("./parsePostgresArray");
const parameterModeMap = {
    i: "IN",
    o: "OUT",
    b: "INOUT",
    v: "VARIADIC",
    t: "TABLE",
};
const volatilityMap = {
    i: "IMMUTABLE",
    s: "STABLE",
    v: "VOLATILE",
};
const parallelSafetyMap = {
    s: "SAFE",
    r: "RESTRICTED",
    u: "UNSAFE",
};
async function extractFunction(db, pgType) {
    const [informationSchemaValue] = await db
        .from("information_schema.routines")
        .where({
        routine_name: pgType.name,
        routine_schema: pgType.schemaName,
    })
        .select("*");
    const { rows } = await db.raw(`
    SELECT
      p.proname,
      p.prorettype::regtype::text AS return_type,
      l.lanname AS language,
      p.prosrc AS definition,
      p.proisstrict AS is_strict,
      p.prosecdef AS is_security_definer,
      p.proleakproof AS is_leak_proof,
      p.proretset AS returns_set,
      p.provolatile,
      p.proparallel,
      p.procost AS estimated_cost,
      CASE WHEN p.proretset THEN p.prorows ELSE NULL END AS estimated_rows,
      d.description AS comment,
      p.proargmodes,
      p.proargnames,
      array_to_string(COALESCE(p.proallargtypes::regtype[], p.proargtypes::regtype[]), ',') AS arg_types,
      p.pronargdefaults,
      pg_get_function_arguments(p.oid) AS arg_list,
      pg_get_function_identity_arguments(p.oid) AS identity_args,
      pg_get_function_result(p.oid) as full_return_type
    FROM pg_proc p
    LEFT JOIN pg_namespace n ON n.oid = p.pronamespace
    LEFT JOIN pg_description d ON d.objoid = p.oid
    LEFT JOIN pg_language l ON l.oid = p.prolang
    WHERE n.nspname = ? AND p.proname = ?`, [pgType.schemaName, pgType.name]);
    return rows.map((row) => {
        const argTypes = (row.arg_types ? row.arg_types.split(",") : []);
        const paramModes = row.proargmodes
            ? (0, parsePostgresArray_1.parsePostgresArray)(String(row.proargmodes))
            : argTypes.map(() => "i");
        const paramNames = row.proargnames
            ? (0, parsePostgresArray_1.parsePostgresArray)(String(row.proargnames))
            : argTypes.map((_, i) => `$${i + 1}`);
        const parameters = argTypes.map((type, i) => ({
            name: paramNames[i],
            type: type,
            mode: parameterModeMap[paramModes[i]],
            hasDefault: i >= argTypes.length - (row.pronargdefaults || 0),
            ordinalPosition: i + 1,
        }));
        let returnType = row.return_type;
        if (row.full_return_type &&
            row.full_return_type.toLowerCase().includes("table")) {
            const tableMatch = row.full_return_type.match(/TABLE\((.*)\)/i);
            if (tableMatch) {
                const columnDefs = tableMatch[1].split(",").map((col) => {
                    const [name, type] = col.trim().split(/\s+/);
                    return { name, type };
                });
                returnType = {
                    type: "table",
                    columns: columnDefs,
                };
            }
        }
        return {
            ...pgType,
            parameters,
            returnType,
            language: row.language,
            definition: row.definition,
            isStrict: row.is_strict,
            isSecurityDefiner: row.is_security_definer,
            isLeakProof: row.is_leak_proof,
            returnsSet: row.returns_set,
            volatility: volatilityMap[row.provolatile],
            parallelSafety: parallelSafetyMap[row.proparallel],
            estimatedCost: row.estimated_cost,
            estimatedRows: row.estimated_rows,
            comment: row.comment,
            informationSchemaValue,
        };
    });
}
exports.default = extractFunction;
