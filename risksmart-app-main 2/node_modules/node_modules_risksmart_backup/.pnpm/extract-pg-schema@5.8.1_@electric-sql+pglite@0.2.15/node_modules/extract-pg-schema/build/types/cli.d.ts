#!/usr/bin/env node
import type { ConnectionConfig } from "pg";
type ParsedArgs = {
    help: true;
} | {
    help: false;
    connectionConfig: ConnectionConfig;
    includePatterns: string[];
    excludePatterns: string[];
};
export declare function parseArgs(args: string[]): ParsedArgs;
export declare function createSchemaFilter(includePatterns: string[], excludePatterns: string[]): (schemaName: string) => boolean;
export {};
