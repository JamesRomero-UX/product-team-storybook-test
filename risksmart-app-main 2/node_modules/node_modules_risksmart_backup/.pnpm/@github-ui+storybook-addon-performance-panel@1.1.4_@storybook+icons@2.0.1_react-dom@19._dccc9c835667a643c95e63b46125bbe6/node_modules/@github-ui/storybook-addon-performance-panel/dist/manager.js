import React from "react";
import { addons, types, useChannel, useStorybookState } from "storybook/manager-api";
import { SyncIcon } from "@storybook/icons";
import { AddonPanel, Badge, Button, Code, Popover, WithTooltip } from "storybook/internal/components";
import { styled, useTheme } from "storybook/theming";

//#region core/performance-types.ts
/**
* Unique identifier for the Performance Monitor addon.
* Used for addon registration and channel event namespacing.
*/
const ADDON_ID = "primer-performance-monitor";
/**
* Panel identifier for the Performance Monitor UI.
* Used when registering the addon panel in Storybook's manager.
*/
const PANEL_ID = `${ADDON_ID}/panel`;
/**
* Channel event names for communication between preview and manager.
*
* @example
* // In decorator (preview)
* channel.emit(PERF_EVENTS.METRICS_UPDATE, metrics)
*
* // In panel (manager)
* channel.on(PERF_EVENTS.METRICS_UPDATE, handleMetrics)
*/
const PERF_EVENTS = {
	METRICS_UPDATE: `${ADDON_ID}/metrics-update`,
	RESET: `${ADDON_ID}/reset`,
	REQUEST_METRICS: `${ADDON_ID}/request-metrics`,
	INSPECT_ELEMENT: `${ADDON_ID}/inspect-element`,
	SELECT_PROFILER: `${ADDON_ID}/select-profiler`,
	PROFILER_UPDATE: `${ADDON_ID}/profiler-update`,
	PROFILERS_CHANGED: `${ADDON_ID}/profilers-changed`
};
/**
* Performance thresholds for metric color-coding and evaluation.
*
* Based on Web Vitals standards where applicable:
* - FPS/Frame timing: 60fps target (16.67ms frame budget)
* - Input latency: <100ms good, <300ms acceptable (RAIL model)
* - CLS: <0.1 good, <0.25 needs improvement (Core Web Vital)
* - INP: <200ms good, <500ms needs improvement (Core Web Vital)
* - TBT: <200ms good, <600ms needs improvement (Lighthouse)
*
* Naming convention:
* - `*_GOOD`: Green threshold (excellent performance)
* - `*_WARNING`: Yellow threshold (needs attention)
* - `*_DANGER`: Red threshold (poor performance)
*/
const THRESHOLDS = {
	FPS_GOOD: 55,
	FPS_WARNING: 30,
	FRAME_TIME_TARGET: 16.67,
	FRAME_TIME_WARNING: 32,
	DROPPED_FRAMES_WARNING: 10,
	INPUT_LATENCY_GOOD: 16,
	INPUT_LATENCY_WARNING: 50,
	INP_GOOD: 200,
	INP_WARNING: 500,
	LONG_TASKS_WARNING: 5,
	LONGEST_TASK_WARNING: 100,
	TBT_WARNING: 200,
	TBT_DANGER: 600,
	LOAF_COUNT_WARNING: 5,
	LOAF_COUNT_DANGER: 15,
	LOAF_DURATION_WARNING: 100,
	LOAF_DURATION_DANGER: 200,
	LOAF_BLOCKING_WARNING: 200,
	LOAF_BLOCKING_DANGER: 500,
	CLS_GOOD: .1,
	CLS_WARNING: .25,
	FORCED_REFLOW_WARNING: 5,
	FORCED_REFLOW_DANGER: 20,
	DOM_MUTATIONS_WARNING: 50,
	DOM_MUTATIONS_DANGER: 200,
	REACT_RENDER_GOOD: 8,
	REACT_RENDER_WARNING: 16,
	CASCADE_WARNING: 3,
	SLOW_UPDATES_WARNING: 3,
	SLOW_UPDATES_DANGER: 10,
	REACT_P95_WARNING: 8,
	REACT_P95_DANGER: 16,
	MEMORY_DELTA_WARNING: 5,
	MEMORY_DELTA_DANGER: 20,
	GC_PRESSURE_WARNING: 1,
	GC_PRESSURE_DANGER: 5,
	EVENT_LISTENERS_WARNING: 50,
	EVENT_LISTENERS_DANGER: 100,
	OBSERVERS_WARNING: 10,
	OBSERVERS_DANGER: 25,
	CSS_VAR_CHANGES_WARNING: 50,
	LAYERS_WARNING: 20,
	LAYERS_DANGER: 50
};
/**
* Default/initial metrics state (all zeros/nulls).
* Used when panel first loads or after reset.
*/
const DEFAULT_METRICS = {
	fps: 0,
	frameTime: 0,
	maxFrameTime: 0,
	droppedFrames: 0,
	frameJitter: 0,
	frameStability: 100,
	inputLatency: 0,
	maxInputLatency: 0,
	inputJitter: 0,
	eventTimingSupported: true,
	interactionCount: 0,
	inpMs: 0,
	firstInputDelay: null,
	firstInputType: null,
	lastInteraction: null,
	slowestInteraction: null,
	interactionsByType: {},
	paintTime: 0,
	maxPaintTime: 0,
	paintCount: 0,
	paintJitter: 0,
	memoryUsedMB: null,
	memoryDeltaMB: null,
	peakMemoryMB: null,
	gcPressure: 0,
	fpsHistory: [],
	frameTimeHistory: [],
	memoryHistory: [],
	longTasks: 0,
	longestTask: 0,
	totalBlockingTime: 0,
	loafSupported: true,
	loafCount: 0,
	totalLoafBlockingDuration: 0,
	longestLoafDuration: 0,
	longestLoafBlockingDuration: 0,
	avgLoafDuration: 0,
	p95LoafDuration: 0,
	loafsWithScripts: 0,
	lastLoaf: null,
	worstLoaf: null,
	styleWrites: 0,
	thrashingScore: 0,
	layoutShiftScore: 0,
	layoutShiftCount: 0,
	currentSessionCLS: 0,
	forcedReflowCount: 0,
	domMutationsPerFrame: 0,
	cssVarChanges: 0,
	reactRenderCount: 0,
	reactMountCount: 0,
	reactMountDuration: 0,
	reactPostMountUpdateCount: 0,
	reactPostMountMaxDuration: 0,
	reactP95Duration: 0,
	slowReactUpdates: 0,
	renderCascades: 0,
	domElements: null,
	scriptEvalTime: 0,
	eventListenerCount: 0,
	observerCount: 0,
	compositorLayers: null,
	elementTimingSupported: true,
	elementTimingCount: 0,
	largestElementRenderTime: 0,
	elementTimings: []
};
/**
* Determines status variant based on value and thresholds.
*
* @param value - Current metric value
* @param good - Threshold for "good" (green)
* @param warning - Threshold for "warning" (yellow)
* @param higherIsBetter - If true, higher values are better (e.g., FPS)
* @returns Status variant for styling
*/
function getStatusVariant(value, good, warning, higherIsBetter = false) {
	if (higherIsBetter) {
		if (value >= good) return "success";
		if (value >= warning) return "warning";
		return "error";
	}
	if (value <= good) return "success";
	if (value <= warning) return "warning";
	return "error";
}
/**
* Returns 'success' if value is zero, otherwise 'error'.
* Used for metrics where zero is the ideal (e.g., jitter, cascades).
*/
function getZeroIsGoodStatus(value) {
	return value === 0 ? "success" : "error";
}

//#endregion
//#region collectors/utils.ts
/**
* Computes a percentile using linear interpolation.
* More accurate than simple index lookup for small arrays.
*
* @param arr - Array of numbers
* @param p - Percentile (0-1), e.g., 0.95 for P95
* @returns The interpolated percentile value
*/
function computePercentile(arr, p) {
	if (arr.length === 0) return 0;
	if (arr.length === 1) return arr[0];
	const sorted = [...arr].sort((a, b) => a - b);
	const index = p * (sorted.length - 1);
	const lower = Math.floor(index);
	const upper = Math.ceil(index);
	const fraction = index - lower;
	if (lower === upper) return sorted[lower];
	return sorted[lower] + fraction * (sorted[upper] - sorted[lower]);
}
/**
* Computes the 95th percentile of a numeric array.
* Uses linear interpolation for accuracy.
*/
function computeP95(arr) {
	const value = computePercentile(arr, .95);
	return Math.round(value * 10) / 10;
}

//#endregion
//#region panel/components.tsx
/**
* Styled components using Storybook's theming system.
* All styles adapt to Storybook's light/dark theme automatically.
*
* Layout structure:
* - PanelWrapper: Flex container (content + sidebar)
* - ContentArea: Scrollable grid of metric sections
* - SideToolbar: Thin vertical strip with reset button
* - SectionsGrid: Auto-fit grid of Section cards
*/
/** Root container - horizontal flex with content area and sidebar */
const PanelWrapper = styled.div(({ theme }) => ({
	display: "flex",
	fontFamily: theme.typography.fonts.mono,
	fontSize: "11px",
	lineHeight: 1.4,
	color: theme.color.defaultText,
	height: "100%",
	background: theme.background.content
}));
const ContentArea = styled.div({
	flex: 1,
	overflow: "auto",
	padding: "4px"
});
const SideToolbar = styled.div(({ theme }) => ({
	display: "flex",
	flexDirection: "column",
	alignItems: "center",
	gap: "4px",
	padding: "4px",
	borderLeft: `1px solid ${theme.appBorderColor}`,
	background: theme.barBg
}));
const SectionsGrid = styled.div({
	display: "grid",
	gridTemplateColumns: "repeat(auto-fit, minmax(220px, 1fr))",
	gap: "4px"
});
const Section = styled.section(({ theme }) => ({
	background: theme.background.app,
	borderRadius: theme.appBorderRadius,
	border: `1px solid ${theme.appBorderColor}`
}));
const SectionHeader = styled.header(({ theme }) => ({
	padding: "4px 8px",
	background: theme.barBg,
	borderBottom: `1px solid ${theme.appBorderColor}`,
	display: "flex",
	alignItems: "center",
	gap: "4px"
}));
const SectionTitle = styled.h3(({ theme }) => ({
	margin: 0,
	fontSize: "10px",
	fontWeight: theme.typography.weight.bold,
	textTransform: "uppercase",
	letterSpacing: "0.5px",
	color: theme.color.defaultText
}));
const SectionIcon = styled.span({ fontSize: "12px" });
const MetricsList = styled.dl({
	margin: 0,
	padding: "2px 0"
});
/**
* Container for a single metric (dt + one or more dd elements).
* Uses CSS grid: label on left, values stacked on right.
* Fixed heights prevent layout shift when values/details appear.
*/
const MetricItem = styled.div(({ theme }) => ({
	display: "grid",
	gridTemplateColumns: "1fr auto",
	alignItems: "center",
	gap: "1px 6px",
	padding: "2px 8px",
	minHeight: "20px",
	borderBottom: `1px solid ${theme.appBorderColor}`,
	position: "relative",
	"&:last-child": { borderBottom: "none" }
}));
/** Variant of MetricItem that reserves space for a detail row */
const MetricItemWithDetail = styled(MetricItem)({
	minHeight: "36px",
	alignItems: "start"
});
const MetricLabel = styled.dt(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	gap: "4px",
	fontSize: "10px",
	color: theme.color.mediumdark,
	margin: 0,
	gridColumn: "1",
	gridRow: "1 / -1",
	alignSelf: "center",
	minHeight: "16px"
}));
/** Primary metric value - first dd element */
const MetricValue = styled.dd(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "flex-end",
	gap: "6px",
	fontSize: "11px",
	fontWeight: theme.typography.weight.bold,
	fontFamily: theme.typography.fonts.mono,
	color: theme.color.defaultText,
	margin: 0,
	textAlign: "right",
	gridColumn: "2",
	minWidth: "60px",
	minHeight: "16px"
}));
/** Secondary detail value - additional dd element for supplementary info */
const DetailValue = styled.dd(({ theme }) => ({
	display: "flex",
	alignItems: "center",
	justifyContent: "flex-end",
	gap: "4px",
	fontSize: "9px",
	fontWeight: "normal",
	fontFamily: theme.typography.fonts.mono,
	color: theme.color.mediumdark,
	margin: 0,
	textAlign: "right",
	gridColumn: "2",
	flexWrap: "wrap",
	minHeight: "14px",
	minWidth: "130px"
}));
const SecondaryValue = styled.span(({ theme }) => ({
	fontSize: "10px",
	fontWeight: "normal",
	color: theme.color.mediumdark
}));
const InfoIcon = styled.button(({ theme }) => ({
	display: "inline-flex",
	alignItems: "center",
	justifyContent: "center",
	width: "11px",
	height: "11px",
	fontSize: "8px",
	fontWeight: 600,
	fontStyle: "italic",
	fontFamily: "Georgia, serif",
	borderRadius: "50%",
	border: `1px solid ${theme.color.mediumdark}`,
	color: theme.color.mediumdark,
	background: "transparent",
	padding: 0,
	userSelect: "none",
	lineHeight: 1,
	cursor: "help",
	"&:focus": {
		outline: "none",
		boxShadow: `0 0 0 1px ${theme.color.secondary}`
	},
	"&:focus-visible": {
		outline: "none",
		boxShadow: `0 0 0 2px ${theme.color.secondary}`
	}
}));
const SparklineContainer = styled.div({
	display: "flex",
	alignItems: "center",
	height: "16px"
});
/** Container for sparkline when displayed on its own row above metric value */
const SparklineRow = styled.div({
	gridColumn: "1 / -1",
	display: "flex",
	justifyContent: "flex-end",
	paddingBottom: "1px"
});
const EmptyState = styled.div(({ theme }) => ({
	padding: "24px",
	textAlign: "center",
	color: theme.color.mediumdark
}));
const EmptyStateTitle = styled.p(({ theme }) => ({
	fontSize: "12px",
	color: theme.color.defaultText,
	marginBottom: "8px"
}));
const EmptyStateSubtitle = styled.p(({ theme }) => ({
	fontSize: "10px",
	color: theme.color.mediumdark,
	opacity: .7,
	margin: 0
}));
const EmptyStateHint = styled.p(({ theme }) => ({
	fontSize: "10px",
	color: theme.color.mediumdark,
	margin: 0
}));
const NoDataHint = styled(SecondaryValue)(() => ({ fontStyle: "italic" }));
/** Clickable button to inspect an element in the preview */
const InspectButton = styled.button(({ theme }) => ({
	display: "inline-flex",
	alignItems: "center",
	justifyContent: "center",
	padding: "1px 4px",
	fontSize: "9px",
	fontFamily: theme.typography.fonts.mono,
	borderRadius: "3px",
	border: `1px solid ${theme.color.mediumdark}`,
	color: theme.color.mediumdark,
	background: "transparent",
	cursor: "pointer",
	marginLeft: "4px",
	transition: "all 0.15s ease",
	"&:hover": {
		background: theme.color.secondary,
		borderColor: theme.color.secondary,
		color: theme.color.lightest
	},
	"&:focus": {
		outline: "none",
		boxShadow: `0 0 0 1px ${theme.color.secondary}`
	},
	"&:active": { transform: "scale(0.95)" }
}));
/** Web Vital badge - highlighted indicator for Core Web Vitals */
const WebVitalBadge = styled.span(() => ({
	display: "inline-flex",
	alignItems: "center",
	gap: "1px",
	padding: "1px 4px",
	fontSize: "7px",
	fontWeight: 700,
	textTransform: "uppercase",
	letterSpacing: "0.3px",
	borderRadius: "3px",
	background: "linear-gradient(135deg, #667eea 0%, #764ba2 100%)",
	color: "#fff",
	marginLeft: "4px",
	boxShadow: "0 1px 2px rgba(102, 126, 234, 0.3)",
	"&::before": {
		content: "\"⚡\"",
		fontSize: "7px"
	}
}));
/** Container for interaction timing breakdown with labeled phases */
const TimingBreakdown = styled.span(({ theme }) => ({
	display: "inline-flex",
	alignItems: "center",
	gap: "1px",
	fontSize: "8px",
	fontFamily: theme.typography.fonts.mono
}));
/** Individual phase in timing breakdown */
const TimingPhase = styled.span(({ theme, phase }) => {
	const colors = {
		delay: theme.color.warning,
		process: theme.color.secondary,
		paint: theme.color.positive
	};
	return {
		display: "inline-flex",
		alignItems: "center",
		gap: "1px",
		padding: "0px 2px",
		borderRadius: "2px",
		background: `${colors[phase]}22`,
		color: colors[phase],
		minWidth: "36px",
		"& > abbr": {
			textDecoration: "none",
			fontWeight: 600,
			fontSize: "7px",
			textTransform: "uppercase",
			marginRight: "1px",
			opacity: .8
		},
		"&::after": {
			content: "\"ms\"",
			fontSize: "6px",
			opacity: .7,
			marginLeft: "1px"
		}
	};
});
/** Arrow between timing phases */
const TimingArrow = styled.span(({ theme }) => ({
	color: theme.color.mediumdark,
	fontSize: "7px",
	padding: "0 1px"
}));

//#endregion
//#region panel/formatters.ts
/**
* Formatter for milliseconds with 1 decimal place.
*/
const msFormatter = new Intl.NumberFormat("en-US", {
	style: "unit",
	unit: "millisecond",
	unitDisplay: "narrow",
	minimumFractionDigits: 1,
	maximumFractionDigits: 1
});
/**
* Formats a number as milliseconds with 1 decimal place.
* @param value - Duration in milliseconds
* @returns Formatted string (e.g., "16.7ms")
*/
const formatMs = (value) => msFormatter.format(value);
/**
* Formatter for megabytes with 1 decimal place.
*/
const mbFormatter = new Intl.NumberFormat("en-US", {
	style: "unit",
	unit: "megabyte",
	unitDisplay: "narrow",
	minimumFractionDigits: 1,
	maximumFractionDigits: 1
});
/**
* Formats a number as megabytes with 1 decimal place.
* @param value - Size in megabytes
* @returns Formatted string (e.g., "45.2MB")
*/
const formatMb = (value) => mbFormatter.format(value);
/**
* Formatter for numbers with thousand separators.
*/
const numberFormatter = new Intl.NumberFormat("en-US");
/**
* Formats a number with thousand separators.
* @param value - Number to format
* @returns Formatted string (e.g., "1,234")
*/
const formatNumber = (value) => numberFormatter.format(value);
/**
* Formatter for scores with 3 decimal places.
*/
const scoreFormatter = new Intl.NumberFormat("en-US", {
	minimumFractionDigits: 3,
	maximumFractionDigits: 3
});
/**
* Formats a score with 3 decimal places.
* @param value - Score value (e.g., CLS)
* @returns Formatted string (e.g., "0.125")
*/
const formatScore = (value) => scoreFormatter.format(value);
/**
* Formats a number as a percentage (integer).
* @param value - Percentage value (0-100)
* @returns Formatted string (e.g., "48%")
*/
const formatPercent = (value) => `${String(Math.round(value))}%`;
/**
* Formatter for rate values with 2 decimal places.
*/
const rateFormatter = new Intl.NumberFormat("en-US", {
	minimumFractionDigits: 2,
	maximumFractionDigits: 2
});
/**
* Formats a rate with 2 decimal places and unit suffix.
* @param value - Rate value
* @param unit - Unit string (e.g., "MB/s")
* @returns Formatted string (e.g., "1.25 MB/s")
*/
const formatRate = (value, unit) => `${rateFormatter.format(value)} ${unit}`;

//#endregion
//#region performance-panel.tsx
/**
* @fileoverview Performance Monitor Addon - Panel Component
*
* This file implements the manager-side UI panel for the Performance Monitor
* Storybook addon. It displays real-time metrics received from the preview
* via the Storybook channel API.
*
* ## Architecture
*
* ```
* ┌─────────────────────────────────────────────────────────────────────────┐
* │                        Manager (Addon Panel)                             │
* │  ┌─────────────────────────────────────────────────────────────────┐    │
* │  │  PerformancePanel                                                │    │
* │  │  ├─ ContentArea (scrollable grid)                               │    │
* │  │  │  ├─ FrameTimingSection    [FPS, Frame Time, Dropped]         │    │
* │  │  │  ├─ InputSection          [Latency, INP, Jitter]             │    │
* │  │  │  ├─ MainThreadSection     [Long Tasks, TBT, Thrashing]       │    │
* │  │  │  ├─ LoAFSection           [LoAF Count, Blocking, Scripts]    │    │
* │  │  │  ├─ ReactSection          [Mounts, Updates, Cascades, P95]   │    │
* │  │  │  ├─ LayoutSection         [CLS, Reflows, DOM Mutations]      │    │
* │  │  │  └─ MemorySection         [Heap, Delta, Peak, GC]            │    │
* │  │  └─ SideToolbar (reset button)                                  │    │
* │  └─────────────────────────────────────────────────────────────────┘    │
* └─────────────────────────────────────────────────────────────────────────┘
* ```
*
* ## Metric Thresholds
*
* Metrics are color-coded based on Web Vitals standards:
* - 🟢 Green: Good performance (meets targets)
* - 🟡 Yellow: Needs improvement (may cause issues)
* - 🔴 Red: Poor performance (likely causing issues)
*
* ## Communication
*
* Uses Storybook's channel API to receive metrics:
* - `PERF_EVENTS.METRICS_UPDATE` - Receives metrics from decorator
* - `PERF_EVENTS.RESET` - Emits to reset all metrics
* - `PERF_EVENTS.REQUEST_METRICS` - Requests immediate metrics update
*
* @module performance-panel
* @see {@link ./performance-decorator.tsx} - The metrics collector
* @see {@link ./performance-types.ts} - Shared types and constants
*/
/** Static mapping - defined once outside component to avoid recreation */
const VARIANT_TO_BADGE_STATUS = {
	success: "positive",
	warning: "warning",
	error: "negative",
	neutral: "neutral"
};
/** Status badge component using Storybook's Badge */
const StatusBadge = React.memo(function StatusBadge({ variant, children }) {
	return /* @__PURE__ */ React.createElement(Badge, { status: VARIANT_TO_BADGE_STATUS[variant] }, children);
});
const Sparkline = React.memo(function Sparkline({ data, width = 80, height = 20, goodThreshold, badThreshold, higherIsBetter = false }) {
	const theme = useTheme();
	const { pathData, min, max, currentValue, getY } = React.useMemo(() => {
		if (data.length < 2) return {
			pathData: "",
			min: 0,
			max: 0,
			currentValue: 0,
			getY: () => height / 2
		};
		const padding = 2;
		const innerWidth = width - padding * 2;
		const innerHeight = height - padding * 2;
		const minVal = Math.min(...data);
		const maxVal = Math.max(...data);
		const range = maxVal - minVal || 1;
		const getX = (index) => padding + index / (data.length - 1) * innerWidth;
		const getYFn = (value) => padding + innerHeight - (value - minVal) / range * innerHeight;
		return {
			pathData: data.map((value, i) => `${i === 0 ? "M" : "L"} ${getX(i).toFixed(1)} ${getYFn(value).toFixed(1)}`).join(" "),
			min: minVal,
			max: maxVal,
			currentValue: data[data.length - 1] ?? NaN,
			getY: getYFn
		};
	}, [
		data,
		width,
		height
	]);
	if (data.length < 2) return /* @__PURE__ */ React.createElement(SparklineContainer, null, /* @__PURE__ */ React.createElement("svg", {
		width,
		height,
		"aria-hidden": "true"
	}, /* @__PURE__ */ React.createElement("line", {
		x1: 0,
		y1: height / 2,
		x2: width,
		y2: height / 2,
		stroke: theme.color.medium,
		strokeWidth: 1,
		strokeDasharray: "3,3"
	})));
	const padding = 2;
	const getX = (index) => padding + index / (data.length - 1) * (width - padding * 2);
	let lineColor = theme.color.secondary;
	if (goodThreshold !== void 0) {
		const isGood = higherIsBetter ? currentValue >= goodThreshold : currentValue <= goodThreshold;
		if (badThreshold !== void 0 && (higherIsBetter ? currentValue < badThreshold : currentValue > badThreshold)) lineColor = theme.color.negative;
		else if (isGood) lineColor = theme.color.positive;
		else lineColor = theme.color.warning;
	}
	return /* @__PURE__ */ React.createElement(SparklineContainer, null, /* @__PURE__ */ React.createElement("svg", {
		width,
		height,
		"aria-hidden": "true"
	}, goodThreshold !== void 0 && goodThreshold >= min && goodThreshold <= max && /* @__PURE__ */ React.createElement("line", {
		x1: padding,
		y1: getY(goodThreshold),
		x2: width - padding,
		y2: getY(goodThreshold),
		stroke: theme.color.medium,
		strokeWidth: 1,
		strokeDasharray: "2,2",
		opacity: .5
	}), /* @__PURE__ */ React.createElement("path", {
		d: pathData,
		fill: "none",
		stroke: lineColor,
		strokeWidth: 1.5,
		strokeLinecap: "round"
	}), /* @__PURE__ */ React.createElement("circle", {
		cx: getX(data.length - 1),
		cy: getY(currentValue),
		r: 2.5,
		fill: lineColor
	})));
});
/** Alias for getStatusVariant from performance-types */
const getStatus = getStatusVariant;
/** Alias for getZeroIsGoodStatus from performance-types */
const getZeroStatus = getZeroIsGoodStatus;
const Metric = React.memo(function Metric({ label, tooltip, sparkline, isWebVital, detail, reserveDetailSpace, children }) {
	const hasDetail = detail != null || reserveDetailSpace === true;
	const Container = hasDetail ? MetricItemWithDetail : MetricItem;
	return /* @__PURE__ */ React.createElement(Container, null, sparkline ? /* @__PURE__ */ React.createElement(SparklineRow, null, sparkline) : null, /* @__PURE__ */ React.createElement(MetricLabel, null, label, isWebVital && /* @__PURE__ */ React.createElement(WebVitalBadge, null, "Vital"), tooltip && /* @__PURE__ */ React.createElement(WithTooltip, {
		tooltip: /* @__PURE__ */ React.createElement(Popover, { hasChrome: false }, tooltip),
		closeOnOutsideClick: true
	}, /* @__PURE__ */ React.createElement(InfoIcon, {
		type: "button",
		"aria-label": `Info about ${label}`
	}, "i"))), /* @__PURE__ */ React.createElement(MetricValue, null, children), hasDetail ? /* @__PURE__ */ React.createElement(DetailValue, null, detail) : null);
});
const MetricsSection = React.memo(function MetricsSection({ icon, title, children }) {
	return /* @__PURE__ */ React.createElement(Section, null, /* @__PURE__ */ React.createElement(SectionHeader, null, /* @__PURE__ */ React.createElement(SectionIcon, null, icon), /* @__PURE__ */ React.createElement(SectionTitle, null, title)), /* @__PURE__ */ React.createElement(MetricsList, null, children));
});
const FrameTimingSection = React.memo(function FrameTimingSection({ fps, fpsHistory, frameTime, maxFrameTime, frameTimeHistory, droppedFrames, frameJitter, frameStability, paintTime, maxPaintTime, paintJitter }) {
	const fpsStatus = getStatus(fps, THRESHOLDS.FPS_GOOD, THRESHOLDS.FPS_WARNING, true);
	const droppedStatus = droppedFrames > THRESHOLDS.DROPPED_FRAMES_WARNING ? "error" : droppedFrames > 0 ? "warning" : "success";
	const frameJitterStatus = getZeroStatus(frameJitter);
	const stabilityStatus = frameStability >= 90 ? "success" : frameStability >= 70 ? "warning" : "error";
	const paintJitterStatus = getZeroStatus(paintJitter);
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "📊",
		title: "Frame Timing"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "FPS",
		tooltip: "Frames per second. Target: 60fps. Below 30 causes visible stuttering.",
		sparkline: /* @__PURE__ */ React.createElement(Sparkline, {
			data: fpsHistory,
			goodThreshold: THRESHOLDS.FPS_GOOD,
			badThreshold: THRESHOLDS.FPS_WARNING,
			higherIsBetter: true
		})
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: fpsStatus }, fps)), /* @__PURE__ */ React.createElement(Metric, {
		label: "Frame Time",
		tooltip: "Average time per frame. Target: ≤16.67ms for 60fps.",
		sparkline: /* @__PURE__ */ React.createElement(Sparkline, {
			data: frameTimeHistory,
			goodThreshold: THRESHOLDS.FRAME_TIME_TARGET,
			badThreshold: THRESHOLDS.FRAME_TIME_WARNING
		}),
		detail: /* @__PURE__ */ React.createElement(React.Fragment, null, "max ", formatMs(maxFrameTime))
	}, formatMs(frameTime)), /* @__PURE__ */ React.createElement(Metric, {
		label: "Dropped Frames",
		tooltip: "Frames taking >2× expected time. High count indicates stuttering."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: droppedStatus }, /* @__PURE__ */ React.createElement("span", null, droppedFrames), droppedFrames === 0 ? /* @__PURE__ */ React.createElement("span", null, " ✨") : /* @__PURE__ */ React.createElement("span", null, " 💧"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Frame Jitter",
		tooltip: "Sudden spikes in frame time vs recent baseline. Indicates inconsistent rendering."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: frameJitterStatus }, frameJitter === 0 ? "✨ Smooth" : `⚡ ${String(frameJitter)} spikes`)), /* @__PURE__ */ React.createElement(Metric, {
		label: "Frame Stability",
		tooltip: "Frame time consistency (0-100%). 100% = perfectly smooth, lower = choppy/variable frame pacing."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: stabilityStatus }, /* @__PURE__ */ React.createElement("span", null, frameStability >= 90 ? "🎯 " : frameStability >= 70 ? "📊 " : "📉 "), /* @__PURE__ */ React.createElement("span", null, frameStability, "%"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Paint Time",
		tooltip: "Browser rendering time via double-RAF technique."
	}, formatMs(paintTime), /* @__PURE__ */ React.createElement(SecondaryValue, null, "/ ", formatMs(maxPaintTime), " max")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Paint Jitter",
		tooltip: "Sudden spikes in paint time vs recent baseline. Indicates rendering inconsistency."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: paintJitterStatus }, paintJitter === 0 ? "✨ None" : `🎢 ${String(paintJitter)} spikes`)));
});
const InputSection = React.memo(function InputSection({ inputLatency, maxInputLatency, eventTimingSupported, inpMs, interactionCount, firstInputDelay, firstInputType, lastInteraction, slowestInteraction, onInspectElement }) {
	const inputStatus = getStatus(inputLatency, THRESHOLDS.INPUT_LATENCY_GOOD, THRESHOLDS.INPUT_LATENCY_WARNING);
	const inpStatus = getStatus(inpMs, THRESHOLDS.INP_GOOD, THRESHOLDS.INP_WARNING);
	const getInteractionStatus = (interaction) => {
		if (!interaction) return "neutral";
		return getStatus(interaction.duration, THRESHOLDS.INP_GOOD, THRESHOLDS.INP_WARNING);
	};
	const handleInspect = (selector) => {
		if (selector && selector !== "unknown" && onInspectElement) onInspectElement(selector);
	};
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "👆",
		title: "Input Responsiveness"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "INP",
		isWebVital: true,
		tooltip: "Interaction to Next Paint - p98 worst click/key latency. Core Web Vital. Good: ≤200ms, Poor: >500ms.",
		reserveDetailSpace: true,
		detail: eventTimingSupported && interactionCount > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, interactionCount, " interactions", slowestInteraction && slowestInteraction.targetSelector !== "unknown" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", null, "·"), /* @__PURE__ */ React.createElement("span", null, "worst:"), /* @__PURE__ */ React.createElement(Code, null, slowestInteraction.targetSelector.slice(0, 20)), /* @__PURE__ */ React.createElement(InspectButton, {
			onClick: () => {
				handleInspect(slowestInteraction.targetSelector);
			},
			title: "Inspect slowest interaction element"
		}, "🔍"))) : null
	}, !eventTimingSupported ? /* @__PURE__ */ React.createElement(NoDataHint, null, "Chrome/Edge only") : interactionCount > 0 ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: inpStatus }, Math.round(inpMs), "ms") : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Last Interaction",
		tooltip: "Most recent user interaction. Shows timing breakdown: input delay (waiting) → processing (JS) → paint (render).",
		reserveDetailSpace: true,
		detail: !eventTimingSupported ? null : lastInteraction ? /* @__PURE__ */ React.createElement(React.Fragment, null, lastInteraction.eventType, /* @__PURE__ */ React.createElement("span", null, "·"), /* @__PURE__ */ React.createElement(TimingBreakdown, null, /* @__PURE__ */ React.createElement(TimingPhase, { phase: "delay" }, /* @__PURE__ */ React.createElement("abbr", { title: "Input delay - time waiting for main thread" }, "wait"), Math.round(lastInteraction.inputDelay)), /* @__PURE__ */ React.createElement(TimingArrow, null, "→"), /* @__PURE__ */ React.createElement(TimingPhase, { phase: "process" }, /* @__PURE__ */ React.createElement("abbr", { title: "Processing time - event handler execution" }, "js"), Math.round(lastInteraction.processingTime)), /* @__PURE__ */ React.createElement(TimingArrow, null, "→"), /* @__PURE__ */ React.createElement(TimingPhase, { phase: "paint" }, /* @__PURE__ */ React.createElement("abbr", { title: "Presentation delay - render and paint" }, "paint"), Math.round(lastInteraction.presentationDelay))), lastInteraction.targetSelector !== "unknown" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", null, "·"), /* @__PURE__ */ React.createElement(Code, null, lastInteraction.targetSelector.slice(0, 18)), /* @__PURE__ */ React.createElement(InspectButton, {
			onClick: () => {
				handleInspect(lastInteraction.targetSelector);
			},
			title: "Highlight element in preview"
		}, "🔍"))) : null
	}, !eventTimingSupported ? /* @__PURE__ */ React.createElement(NoDataHint, null, "Chrome/Edge only") : lastInteraction ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: getInteractionStatus(lastInteraction) }, Math.round(lastInteraction.duration), "ms") : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Slowest",
		tooltip: "Slowest interaction observed during this session. Shows timing breakdown.",
		reserveDetailSpace: true,
		detail: !eventTimingSupported ? null : slowestInteraction ? /* @__PURE__ */ React.createElement(React.Fragment, null, slowestInteraction.eventType, /* @__PURE__ */ React.createElement("span", null, "·"), /* @__PURE__ */ React.createElement(TimingBreakdown, null, /* @__PURE__ */ React.createElement(TimingPhase, { phase: "delay" }, /* @__PURE__ */ React.createElement("abbr", { title: "Input delay - time waiting for main thread" }, "wait"), Math.round(slowestInteraction.inputDelay)), /* @__PURE__ */ React.createElement(TimingArrow, null, "→"), /* @__PURE__ */ React.createElement(TimingPhase, { phase: "process" }, /* @__PURE__ */ React.createElement("abbr", { title: "Processing time - event handler execution" }, "js"), Math.round(slowestInteraction.processingTime)), /* @__PURE__ */ React.createElement(TimingArrow, null, "→"), /* @__PURE__ */ React.createElement(TimingPhase, { phase: "paint" }, /* @__PURE__ */ React.createElement("abbr", { title: "Presentation delay - render and paint" }, "paint"), Math.round(slowestInteraction.presentationDelay))), slowestInteraction.targetSelector !== "unknown" && /* @__PURE__ */ React.createElement(React.Fragment, null, /* @__PURE__ */ React.createElement("span", null, "·"), /* @__PURE__ */ React.createElement(Code, null, slowestInteraction.targetSelector.slice(0, 18)), /* @__PURE__ */ React.createElement(InspectButton, {
			onClick: () => {
				handleInspect(slowestInteraction.targetSelector);
			},
			title: "Highlight element in preview"
		}, "🔍"))) : null
	}, !eventTimingSupported ? /* @__PURE__ */ React.createElement(NoDataHint, null, "Chrome/Edge only") : slowestInteraction ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: getInteractionStatus(slowestInteraction) }, Math.round(slowestInteraction.duration), "ms") : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—")), /* @__PURE__ */ React.createElement(Metric, {
		label: "FID",
		isWebVital: true,
		tooltip: "First Input Delay - latency of the very first interaction. Core Web Vital. Good: ≤100ms, Poor: >300ms.",
		reserveDetailSpace: true,
		detail: firstInputType ? /* @__PURE__ */ React.createElement(React.Fragment, null, firstInputType) : null
	}, firstInputDelay !== null ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: getStatus(firstInputDelay, 100, 300) }, Math.round(firstInputDelay), "ms") : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Pointer Latency",
		tooltip: "Time from pointer move to next frame. High values indicate main thread contention."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: inputStatus }, formatMs(inputLatency)), /* @__PURE__ */ React.createElement(SecondaryValue, null, "/ ", formatMs(maxInputLatency), " max")));
});
const MainThreadSection = React.memo(function MainThreadSection({ longTasks, longestTask, totalBlockingTime, thrashingScore, domMutationsPerFrame }) {
	const longTaskStatus = getStatus(longTasks, 0, THRESHOLDS.LONG_TASKS_WARNING);
	const tbtStatus = getStatus(totalBlockingTime, 0, THRESHOLDS.TBT_WARNING);
	const thrashingStatus = getZeroStatus(thrashingScore);
	const domMutationStatus = getStatus(domMutationsPerFrame, 0, THRESHOLDS.DOM_MUTATIONS_WARNING);
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "⏱️",
		title: "Main Thread"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "Long Tasks",
		tooltip: "Tasks blocking main thread >50ms. Target: 0 during interactions.",
		detail: longestTask > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "longest: ", Math.round(longestTask), "ms") : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: longTaskStatus }, /* @__PURE__ */ React.createElement("span", null, longTasks === 0 ? "✨ " : "🐢 "), /* @__PURE__ */ React.createElement("span", null, longTasks))), /* @__PURE__ */ React.createElement(Metric, {
		label: "TBT",
		isWebVital: true,
		tooltip: "Total Blocking Time - sum of time beyond 50ms for each long task. Correlates to TTI. Good: <200ms, Poor: >600ms."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: tbtStatus }, /* @__PURE__ */ React.createElement("span", null, totalBlockingTime === 0 ? "🚀 " : totalBlockingTime > 600 ? "🧱 " : "⏳ "), /* @__PURE__ */ React.createElement("span", null, totalBlockingTime, "ms"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Thrashing",
		tooltip: "Frame blocking >50ms near style writes. Indicates forced synchronous layout."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: thrashingStatus }, thrashingScore === 0 ? "✨ None" : `🔄 ${String(thrashingScore)} stalls`)), /* @__PURE__ */ React.createElement(Metric, {
		label: "DOM Churn",
		tooltip: "DOM mutations per sample period. High values indicate excessive re-rendering."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: domMutationStatus }, /* @__PURE__ */ React.createElement("span", null, domMutationsPerFrame === 0 ? "✨ " : domMutationsPerFrame > 10 ? "🌪️ " : "🔨 "), /* @__PURE__ */ React.createElement("span", null, domMutationsPerFrame)), /* @__PURE__ */ React.createElement(SecondaryValue, null, "/period")));
});
const LoAFSection = React.memo(function LoAFSection({ loafSupported, loafCount, totalLoafBlockingDuration, longestLoafDuration, longestLoafBlockingDuration, avgLoafDuration, p95LoafDuration, loafsWithScripts, worstLoaf }) {
	if (!loafSupported) return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🎞️",
		title: "Long Animation Frames"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "Status",
		tooltip: "Long Animation Frames API is only supported in Chrome 123+"
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: "neutral" }, /* @__PURE__ */ React.createElement("span", null, "⚠️ Not supported"))));
	const countStatus = getStatus(loafCount, 0, THRESHOLDS.LOAF_COUNT_WARNING);
	const blockingStatus = getStatus(totalLoafBlockingDuration, 0, THRESHOLDS.LOAF_BLOCKING_WARNING);
	const durationStatus = getStatus(longestLoafDuration, 0, THRESHOLDS.LOAF_DURATION_WARNING);
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🎞️",
		title: "Long Animation Frames"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "LoAF Count",
		tooltip: "Count of animation frames exceeding 50ms. More detailed than Long Tasks - includes rendering attribution.",
		detail: loafsWithScripts > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, loafsWithScripts, " with scripts") : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: countStatus }, /* @__PURE__ */ React.createElement("span", null, loafCount === 0 ? "✨ " : loafCount > 10 ? "🐢 " : "⚠️ "), /* @__PURE__ */ React.createElement("span", null, loafCount))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Blocking",
		tooltip: "Total blocking duration from all LoAFs (time beyond 50ms threshold). Good: <200ms, Poor: >500ms.",
		detail: longestLoafBlockingDuration > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "worst: ", longestLoafBlockingDuration, "ms") : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: blockingStatus }, /* @__PURE__ */ React.createElement("span", null, totalLoafBlockingDuration === 0 ? "🚀 " : totalLoafBlockingDuration > 500 ? "🧱 " : "⏳ "), /* @__PURE__ */ React.createElement("span", null, totalLoafBlockingDuration, "ms"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Longest",
		tooltip: "Duration of the longest long animation frame. Good: <100ms, Poor: >200ms.",
		detail: avgLoafDuration > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, "avg: ", avgLoafDuration, "ms") : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: durationStatus }, /* @__PURE__ */ React.createElement("span", null, longestLoafDuration === 0 ? "✨ " : longestLoafDuration > 200 ? "🐌 " : "⏱️ "), /* @__PURE__ */ React.createElement("span", null, longestLoafDuration, "ms"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "P95 Duration",
		tooltip: "95th percentile LoAF duration. Shows worst-case frame times."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: getStatus(p95LoafDuration, 0, THRESHOLDS.LOAF_DURATION_WARNING) }, /* @__PURE__ */ React.createElement("span", null, p95LoafDuration === 0 ? "✨ " : "📊 "), /* @__PURE__ */ React.createElement("span", null, p95LoafDuration, "ms"))), worstLoaf?.topScript && /* @__PURE__ */ React.createElement(Metric, {
		label: "Top Script",
		tooltip: `Worst LoAF caused by: ${worstLoaf.topScript.invokerType} (${worstLoaf.topScript.invoker})`
	}, /* @__PURE__ */ React.createElement(Code, { style: {
		fontSize: "10px",
		maxWidth: "150px",
		overflow: "hidden",
		textOverflow: "ellipsis"
	} }, worstLoaf.topScript.sourceFunctionName || worstLoaf.topScript.invoker), /* @__PURE__ */ React.createElement(SecondaryValue, null, Math.round(worstLoaf.topScript.duration), "ms")));
});
const ElementTimingSection = React.memo(function ElementTimingSection({ elementTimingSupported, elementTimingCount, largestElementRenderTime, elementTimings }) {
	if (!elementTimingSupported) return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🎯",
		title: "Element Timing"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "Status",
		tooltip: "Element Timing API is only supported in Chromium browsers"
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: "neutral" }, /* @__PURE__ */ React.createElement("span", null, "⚠️ Not supported"))));
	if (elementTimingCount === 0) return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🎯",
		title: "Element Timing"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "No elements tracked",
		tooltip: "Add `elementtiming` attribute to elements to track their render time"
	}, /* @__PURE__ */ React.createElement(Code, { style: { fontSize: "10px" } }, "elementtiming=\"name\"")));
	const sortedElements = [...elementTimings].sort((a, b) => b.renderTime - a.renderTime);
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🎯",
		title: "Element Timing"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "Elements",
		tooltip: "Number of elements with `elementtiming` attribute tracked",
		detail: sortedElements.length > 3 ? /* @__PURE__ */ React.createElement(React.Fragment, null, sortedElements.length, " total") : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: "success" }, /* @__PURE__ */ React.createElement("span", null, "📍 "), /* @__PURE__ */ React.createElement("span", null, elementTimingCount))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Largest",
		tooltip: "Slowest element to render. Similar concept to LCP but for tracked elements."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: getStatus(largestElementRenderTime, 100, 250) }, /* @__PURE__ */ React.createElement("span", null, largestElementRenderTime < 100 ? "⚡ " : largestElementRenderTime < 250 ? "⏱️ " : "🐌 "), /* @__PURE__ */ React.createElement("span", null, largestElementRenderTime, "ms"))), sortedElements.slice(0, 3).map((el, i) => /* @__PURE__ */ React.createElement(Metric, {
		key: el.identifier,
		label: el.identifier,
		tooltip: `Element: ${el.selector}\nRender time: ${String(el.renderTime)}ms`
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: getStatus(el.renderTime, 100, 250) }, /* @__PURE__ */ React.createElement("span", null, i === 0 ? "🥇 " : i === 1 ? "🥈 " : "🥉 "), /* @__PURE__ */ React.createElement("span", null, el.renderTime, "ms")))));
});
const LayoutAndInternalsSection = React.memo(function LayoutAndInternalsSection({ layoutShiftScore, layoutShiftCount, currentSessionCLS, forcedReflowCount, styleWrites, cssVarChanges, inputJitter }) {
	const clsStatus = getStatus(layoutShiftScore, THRESHOLDS.CLS_GOOD, THRESHOLDS.CLS_WARNING);
	const reflowStatus = getStatus(forcedReflowCount, 0, THRESHOLDS.FORCED_REFLOW_WARNING);
	const jitterStatus = getZeroStatus(inputJitter);
	const detailParts = [];
	if (layoutShiftCount > 0) detailParts.push(`${String(layoutShiftCount)} shifts`);
	if (currentSessionCLS > 0) detailParts.push(`session: ${formatScore(currentSessionCLS)}`);
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "📐",
		title: "Layout & Stability"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "CLS",
		isWebVital: true,
		tooltip: "Cumulative Layout Shift (max session window). Core Web Vital. Good: <0.1, Poor: >0.25. Uses session windowing per spec.",
		detail: detailParts.length > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, detailParts.join(" · ")) : null
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: clsStatus }, layoutShiftScore === 0 ? "🎯 0" : layoutShiftScore > .25 ? `📦 ${formatScore(layoutShiftScore)}` : formatScore(layoutShiftScore))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Forced Reflows",
		tooltip: "Layout reads after style writes force synchronous layout. Major perf killer during drag."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: reflowStatus }, /* @__PURE__ */ React.createElement("span", null, forcedReflowCount === 0 ? "✨ " : "💥 "), /* @__PURE__ */ React.createElement("span", null, forcedReflowCount))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Style Writes",
		tooltip: "Inline style mutations observed via MutationObserver.",
		detail: cssVarChanges > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, cssVarChanges, " CSS var changes") : null
	}, /* @__PURE__ */ React.createElement("span", null, "🎨 ", styleWrites)), /* @__PURE__ */ React.createElement(Metric, {
		label: "Input Jitter",
		tooltip: "Unexpected input latency spikes causing visible hitches during interaction."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: jitterStatus }, inputJitter === 0 ? "✨ None" : `😵 ${String(inputJitter)} hitches`)));
});
const ReactPerformancePanel = React.memo(function ReactPerformancePanel({ id, reactMountCount, reactMountDuration, reactRenderCount, reactPostMountUpdateCount, slowReactUpdates, reactP95Duration, renderCascades, memoizationEfficiency }) {
	const slowUpdateStatus = getStatus(slowReactUpdates, 0, THRESHOLDS.SLOW_UPDATES_WARNING);
	const p95Status = getStatus(reactP95Duration, 0, THRESHOLDS.REACT_P95_WARNING);
	const cascadeStatus = getStatus(renderCascades, 0, THRESHOLDS.CASCADE_WARNING);
	const workSaved = Math.max(0, Math.min(100, (1 - memoizationEfficiency) * 100));
	const savedStatus = workSaved >= 20 ? "success" : workSaved > 0 ? "neutral" : "warning";
	return /* @__PURE__ */ React.createElement(Section, null, /* @__PURE__ */ React.createElement(SectionHeader, null, /* @__PURE__ */ React.createElement(SectionIcon, null, "⚛️"), /* @__PURE__ */ React.createElement(SectionTitle, null, "React Performance")), /* @__PURE__ */ React.createElement(MetricsList, null, /* @__PURE__ */ React.createElement(Metric, {
		label: "ID",
		tooltip: "Profiler ID (React element ID or custom name)"
	}, id), /* @__PURE__ */ React.createElement(Metric, {
		label: "Mount",
		tooltip: "Initial render count and total duration.",
		detail: reactMountDuration > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, formatMs(reactMountDuration), " total") : null
	}, reactMountCount, "×"), /* @__PURE__ */ React.createElement(Metric, {
		label: "Slow Updates",
		tooltip: "React updates taking >16ms (one frame budget). These cause visible jank.",
		detail: reactRenderCount > 0 && reactPostMountUpdateCount > 0 ? /* @__PURE__ */ React.createElement(React.Fragment, null, reactPostMountUpdateCount, " total updates") : null
	}, reactRenderCount > 0 ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: slowUpdateStatus }, /* @__PURE__ */ React.createElement("span", null, slowReactUpdates === 0 ? "⚡ " : "🐌 "), /* @__PURE__ */ React.createElement("span", null, slowReactUpdates)) : /* @__PURE__ */ React.createElement(NoDataHint, null, "No renders")), /* @__PURE__ */ React.createElement(Metric, {
		label: "P95 Duration",
		tooltip: "95th percentile React update duration. Represents worst-case user experience."
	}, reactP95Duration > 0 ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: p95Status }, /* @__PURE__ */ React.createElement("span", null, reactP95Duration < THRESHOLDS.REACT_P95_WARNING ? "🎯 " : "🐢 "), /* @__PURE__ */ React.createElement("span", null, formatMs(reactP95Duration))) : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Cascades",
		tooltip: "Nested updates during commit phase. Often from setState in useLayoutEffect."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: cascadeStatus }, /* @__PURE__ */ React.createElement("span", null, renderCascades === 0 ? "✨ " : "🌀 "), /* @__PURE__ */ React.createElement("span", null, renderCascades))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Work Saved",
		tooltip: "How much render work is being skipped by memoization (React.memo, useMemo). Higher is better. 0% means everything re-renders every time."
	}, reactRenderCount > 0 ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: savedStatus }, /* @__PURE__ */ React.createElement("span", null, workSaved >= 20 ? "🎯 " : workSaved > 0 ? "" : "⚠️ "), /* @__PURE__ */ React.createElement("span", null, formatPercent(workSaved))) : /* @__PURE__ */ React.createElement(SecondaryValue, null, "—"))));
});
const EMPTY_PROFILERS = [];
const ReactSection = React.memo(function ReactSection({ profilers = EMPTY_PROFILERS }) {
	if (profilers.length === 0) return null;
	return /* @__PURE__ */ React.createElement(React.Fragment, null, profilers.map((profiler) => /* @__PURE__ */ React.createElement(ReactPerformancePanel, {
		key: profiler.id,
		id: profiler.id,
		reactMountCount: profiler.metrics.reactMountCount,
		reactMountDuration: profiler.metrics.reactMountDuration,
		reactRenderCount: profiler.metrics.reactRenderCount,
		reactPostMountUpdateCount: profiler.metrics.reactPostMountUpdateCount,
		slowReactUpdates: profiler.metrics.slowReactUpdates,
		reactP95Duration: computeP95(profiler.metrics.reactUpdateDurations),
		renderCascades: profiler.metrics.nestedUpdateCount,
		memoizationEfficiency: profiler.metrics.memoizationEfficiency
	})));
});
const MemoryAndRenderingSection = React.memo(function MemoryAndRenderingSection({ memoryUsedMB, memoryDeltaMB, peakMemoryMB, memoryHistory, gcPressure, domElements, paintCount, compositorLayers }) {
	const gcStatus = getStatus(gcPressure, 0, THRESHOLDS.GC_PRESSURE_WARNING);
	const layerStatus = compositorLayers === null ? "neutral" : getStatus(compositorLayers, 0, THRESHOLDS.LAYERS_WARNING);
	const deltaStatus = memoryDeltaMB === null ? "neutral" : memoryDeltaMB > THRESHOLDS.MEMORY_DELTA_DANGER ? "error" : memoryDeltaMB > THRESHOLDS.MEMORY_DELTA_WARNING ? "warning" : "success";
	const deltaText = memoryDeltaMB === null ? "" : memoryDeltaMB > .5 ? `+${formatMb(memoryDeltaMB)}` : memoryDeltaMB < -.5 ? formatMb(memoryDeltaMB) : "±0";
	if (memoryUsedMB === null) return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🧠",
		title: "Memory & Rendering"
	}, /* @__PURE__ */ React.createElement(Metric, { label: "Heap" }, /* @__PURE__ */ React.createElement(SecondaryValue, null, "Not available (Chrome only)")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Paint Count",
		tooltip: "Number of paint operations."
	}, paintCount), /* @__PURE__ */ React.createElement(Metric, {
		label: "Compositor Layers",
		tooltip: "Elements promoted to GPU layers."
	}, compositorLayers !== null ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: layerStatus }, compositorLayers) : "—"));
	return /* @__PURE__ */ React.createElement(MetricsSection, {
		icon: "🧠",
		title: "Memory & Rendering"
	}, /* @__PURE__ */ React.createElement(Metric, {
		label: "Heap",
		tooltip: "Current JS heap size. Watch for sustained growth indicating leaks.",
		sparkline: /* @__PURE__ */ React.createElement(Sparkline, { data: memoryHistory })
	}, /* @__PURE__ */ React.createElement("span", null, formatMb(memoryUsedMB), "MB", deltaText && /* @__PURE__ */ React.createElement(StatusBadge, { variant: deltaStatus }, " (", deltaText, ")"))), /* @__PURE__ */ React.createElement(Metric, {
		label: "Peak",
		tooltip: "Peak heap memory observed."
	}, peakMemoryMB !== null ? `${formatMb(peakMemoryMB)}MB` : "—"), /* @__PURE__ */ React.createElement(Metric, {
		label: "DOM Nodes",
		tooltip: "Current DOM element count in story container."
	}, domElements !== null ? formatNumber(domElements) : "—"), /* @__PURE__ */ React.createElement(Metric, {
		label: "GC Pressure",
		tooltip: "Memory allocation rate. High values cause GC pauses."
	}, /* @__PURE__ */ React.createElement(StatusBadge, { variant: gcStatus }, gcPressure > .01 ? `🗑️ ${formatRate(gcPressure, "MB/s")}` : "✨ Low")), /* @__PURE__ */ React.createElement(Metric, {
		label: "Paint / Layers",
		tooltip: "Paint operations and compositor layer count."
	}, /* @__PURE__ */ React.createElement("span", null, paintCount), /* @__PURE__ */ React.createElement(SecondaryValue, null, "/", " ", compositorLayers !== null ? /* @__PURE__ */ React.createElement(StatusBadge, { variant: layerStatus }, compositorLayers, " layers") : /* @__PURE__ */ React.createElement("span", null, "—"))));
});
const INITIAL_STATE = {
	status: "loading",
	metrics: DEFAULT_METRICS,
	profilersByStory: {},
	hasReactProfiler: false,
	errorMessage: null
};
function panelReducer(state, action) {
	switch (action.type) {
		case "METRICS_RECEIVED": return {
			...state,
			status: "connected",
			metrics: action.metrics,
			errorMessage: null
		};
		case "PROFILER_UPDATE": {
			const { storyId, id, metrics } = action;
			const storyProfilers = state.profilersByStory[storyId] ?? [];
			const existing = storyProfilers.findIndex((p) => p.id === id);
			const newProfiler = {
				id,
				metrics,
				lastUpdated: Date.now()
			};
			let updatedProfilers;
			if (existing >= 0) {
				updatedProfilers = [...storyProfilers];
				updatedProfilers[existing] = newProfiler;
			} else updatedProfilers = [...storyProfilers, newProfiler];
			return {
				...state,
				hasReactProfiler: true,
				profilersByStory: {
					...state.profilersByStory,
					[storyId]: updatedProfilers
				}
			};
		}
		case "CLEANUP_OLD_STORIES": {
			const currentProfilers = state.profilersByStory[action.currentStoryId];
			return {
				...state,
				hasReactProfiler: currentProfilers ? state.hasReactProfiler : false,
				profilersByStory: currentProfilers ? { [action.currentStoryId]: currentProfilers } : {}
			};
		}
		case "STORY_ERROR": return {
			...state,
			status: "error",
			errorMessage: action.message
		};
		case "NO_DECORATOR":
			if (state.status === "loading") return {
				...state,
				status: "no-decorator"
			};
			return state;
		case "RESET_METRICS": return {
			...state,
			metrics: DEFAULT_METRICS
		};
		default: return state;
	}
}
/**
* Connected panel content - only rendered when we have a valid storyId.
* Stores profilers by storyId to support multiple profilers per story
* and clean up old entries when story changes.
*
* @component
* @private
*/
function ConnectedPanelContent({ storyId }) {
	const [state, dispatch] = React.useReducer(panelReducer, INITIAL_STATE);
	const { previewInitialized } = useStorybookState();
	const currentProfilers = state.profilersByStory[storyId] ?? [];
	React.useEffect(() => {
		dispatch({
			type: "CLEANUP_OLD_STORIES",
			currentStoryId: storyId
		});
	}, [storyId]);
	const isConnected = () => state.status === "connected";
	const emit = useChannel({
		[PERF_EVENTS.METRICS_UPDATE]: (data) => {
			dispatch({
				type: "METRICS_RECEIVED",
				metrics: data
			});
		},
		[PERF_EVENTS.PROFILER_UPDATE]: (data) => {
			dispatch({
				type: "PROFILER_UPDATE",
				storyId: data.storyId,
				id: data.id,
				metrics: data.metrics
			});
		},
		storyRendered: () => {
			emit(PERF_EVENTS.REQUEST_METRICS);
		},
		storyFinished: () => {
			emit(PERF_EVENTS.REQUEST_METRICS);
		},
		storyErrored: () => {
			dispatch({
				type: "STORY_ERROR",
				message: "Story failed to render"
			});
		},
		storyMissing: () => {
			dispatch({
				type: "STORY_ERROR",
				message: "Story not found"
			});
		},
		storyThrewException: (error) => {
			dispatch({
				type: "STORY_ERROR",
				message: error.message || "Story threw an exception"
			});
		},
		playFunctionThrewException: (error) => {
			dispatch({
				type: "STORY_ERROR",
				message: `Play function error: ${error.message || "Unknown error"}`
			});
		},
		storyArgsUpdated: () => {
			if (isConnected()) {
				emit(PERF_EVENTS.RESET);
				dispatch({ type: "RESET_METRICS" });
			}
		}
	});
	React.useEffect(() => {
		if (previewInitialized) emit(PERF_EVENTS.REQUEST_METRICS);
	}, [previewInitialized, emit]);
	React.useEffect(() => {
		if (!previewInitialized || state.status !== "loading") return void 0;
		const timeoutId = setTimeout(() => {
			dispatch({ type: "NO_DECORATOR" });
		}, 500);
		return () => {
			clearTimeout(timeoutId);
		};
	}, [previewInitialized, state.status]);
	const handleReset = React.useCallback(() => {
		emit(PERF_EVENTS.RESET);
		dispatch({ type: "RESET_METRICS" });
	}, [emit]);
	const handleInspectElement = React.useCallback((selector) => {
		emit(PERF_EVENTS.INSPECT_ELEMENT, selector);
	}, [emit]);
	if (state.status !== "connected") {
		if (state.status === "error") return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Story error"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, state.errorMessage), /* @__PURE__ */ React.createElement(EmptyStateHint, null, /* @__PURE__ */ React.createElement("span", null, "Fix the error in your story to see performance metrics.")));
		if (state.status === "no-decorator") return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Performance monitoring not active for this story"), /* @__PURE__ */ React.createElement(EmptyStateHint, null, "Add the ", /* @__PURE__ */ React.createElement(Code, null, "withPerformanceMonitor"), " decorator to enable metrics collection."));
		return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Loading story…"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, "Waiting for performance metrics"));
	}
	const { metrics } = state;
	return /* @__PURE__ */ React.createElement(PanelWrapper, null, /* @__PURE__ */ React.createElement(ContentArea, null, /* @__PURE__ */ React.createElement(SectionsGrid, null, /* @__PURE__ */ React.createElement(FrameTimingSection, {
		fps: metrics.fps,
		fpsHistory: metrics.fpsHistory,
		frameTime: metrics.frameTime,
		maxFrameTime: metrics.maxFrameTime,
		frameTimeHistory: metrics.frameTimeHistory,
		droppedFrames: metrics.droppedFrames,
		frameJitter: metrics.frameJitter,
		frameStability: metrics.frameStability,
		paintTime: metrics.paintTime,
		maxPaintTime: metrics.maxPaintTime,
		paintJitter: metrics.paintJitter
	}), /* @__PURE__ */ React.createElement(InputSection, {
		inputLatency: metrics.inputLatency,
		maxInputLatency: metrics.maxInputLatency,
		eventTimingSupported: metrics.eventTimingSupported,
		inpMs: metrics.inpMs,
		interactionCount: metrics.interactionCount,
		firstInputDelay: metrics.firstInputDelay,
		firstInputType: metrics.firstInputType,
		lastInteraction: metrics.lastInteraction,
		slowestInteraction: metrics.slowestInteraction,
		onInspectElement: handleInspectElement
	}), /* @__PURE__ */ React.createElement(MainThreadSection, {
		longTasks: metrics.longTasks,
		longestTask: metrics.longestTask,
		totalBlockingTime: metrics.totalBlockingTime,
		thrashingScore: metrics.thrashingScore,
		domMutationsPerFrame: metrics.domMutationsPerFrame
	}), /* @__PURE__ */ React.createElement(LoAFSection, {
		loafSupported: metrics.loafSupported,
		loafCount: metrics.loafCount,
		totalLoafBlockingDuration: metrics.totalLoafBlockingDuration,
		longestLoafDuration: metrics.longestLoafDuration,
		longestLoafBlockingDuration: metrics.longestLoafBlockingDuration,
		avgLoafDuration: metrics.avgLoafDuration,
		p95LoafDuration: metrics.p95LoafDuration,
		loafsWithScripts: metrics.loafsWithScripts,
		lastLoaf: metrics.lastLoaf,
		worstLoaf: metrics.worstLoaf
	}), state.hasReactProfiler && /* @__PURE__ */ React.createElement(ReactSection, { profilers: currentProfilers }), /* @__PURE__ */ React.createElement(LayoutAndInternalsSection, {
		layoutShiftScore: metrics.layoutShiftScore,
		layoutShiftCount: metrics.layoutShiftCount,
		currentSessionCLS: metrics.currentSessionCLS,
		forcedReflowCount: metrics.forcedReflowCount,
		styleWrites: metrics.styleWrites,
		cssVarChanges: metrics.cssVarChanges,
		inputJitter: metrics.inputJitter
	}), /* @__PURE__ */ React.createElement(MemoryAndRenderingSection, {
		memoryUsedMB: metrics.memoryUsedMB,
		memoryDeltaMB: metrics.memoryDeltaMB,
		peakMemoryMB: metrics.peakMemoryMB,
		memoryHistory: metrics.memoryHistory,
		gcPressure: metrics.gcPressure,
		domElements: metrics.domElements,
		paintCount: metrics.paintCount,
		compositorLayers: metrics.compositorLayers
	}), /* @__PURE__ */ React.createElement(ElementTimingSection, {
		elementTimingSupported: metrics.elementTimingSupported,
		elementTimingCount: metrics.elementTimingCount,
		largestElementRenderTime: metrics.largestElementRenderTime,
		elementTimings: metrics.elementTimings
	}))), /* @__PURE__ */ React.createElement(SideToolbar, null, /* @__PURE__ */ React.createElement(Button, {
		variant: "ghost",
		padding: "small",
		onClick: handleReset,
		ariaLabel: "Reset all metrics"
	}, /* @__PURE__ */ React.createElement(SyncIcon, null))));
}
/**
* Outer panel content - handles storyId gating.
*
* ConnectedPanelContent stores all profiler data and cleans up old entries
* after confirming the new story's profiler is registered. This avoids
* losing mount data due to timing issues with React's key-based remounting.
*
* Uses Storybook lifecycle hooks for:
* - viewMode: Detect docs vs story mode
*
* @component
* @param props.active - Whether the panel tab is currently selected
* @private
*/
function PanelContent({ active }) {
	const { storyId, previewInitialized, viewMode, refId } = useStorybookState();
	if (!active) return null;
	if (!storyId) return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "No story selected"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, "Select a story to view performance metrics"));
	if (viewMode === "docs") return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Docs mode"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, "Performance metrics are optimized for story view. Switch to Canvas view for accurate measurements."), /* @__PURE__ */ React.createElement(EmptyStateHint, null, /* @__PURE__ */ React.createElement("span", null, "Docs mode renders stories in iframes which affects timing accuracy.")));
	if (!previewInitialized) return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Preview not initialized"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, "The preview is still initializing. Please wait..."));
	return /* @__PURE__ */ React.createElement(ConnectedPanelContent, {
		key: refId ?? "local",
		storyId
	});
}
/**
* Main addon panel component for the Performance Monitor.
*
* This is the entry point registered with Storybook's addon API.
* Wraps PanelContent in an AddonPanel for proper Storybook integration.
*
* @component
* @param props - Component props
* @param props.active - Whether the panel is currently visible
*
* @example
* // Registered in manager.tsx via addons.add()
* addons.add(PANEL_ID, {
*   type: types.PANEL,
*   render: ({active}) => <PerformancePanel active={active} />,
* })
*
* @see {@link ./performance-types.tsx} - Where this panel is registered
*/
function PerformancePanel({ active }) {
	return /* @__PURE__ */ React.createElement(ErrorBoundary, null, /* @__PURE__ */ React.createElement(AddonPanel, { active }, /* @__PURE__ */ React.createElement(PanelContent, { active })));
}
var ErrorBoundary = class extends React.Component {
	constructor(props) {
		super(props);
		this.state = { hasError: false };
	}
	static getDerivedStateFromError() {
		return { hasError: true };
	}
	componentDidCatch(error, errorInfo) {
		console.error("Error in PerformancePanel:", error, errorInfo);
	}
	render() {
		if (this.state.hasError) return /* @__PURE__ */ React.createElement(EmptyState, null, /* @__PURE__ */ React.createElement(EmptyStateTitle, null, "Something went wrong"), /* @__PURE__ */ React.createElement(EmptyStateSubtitle, null, "The performance panel failed to load."));
		return this.props.children;
	}
};

//#endregion
//#region manager.tsx
/**
* @fileoverview Manager Entry Point for Performance Monitor Addon
*
* This file registers the addon panel in Storybook's manager UI.
* It is automatically loaded when the addon is added to `.storybook/main.ts`.
*
* @module manager
*/
/**
* Register the Performance Monitor addon with Storybook.
*
* This creates a panel in the addons bar (bottom of the Storybook UI)
* that displays real-time performance metrics for the current story.
*/
addons.register(ADDON_ID, () => {
	addons.add(PANEL_ID, {
		type: types.PANEL,
		title: "⚡ Performance",
		match: ({ viewMode }) => viewMode === "story",
		render: ({ active }) => React.createElement(PerformancePanel, { active: !!active })
	});
});

//#endregion