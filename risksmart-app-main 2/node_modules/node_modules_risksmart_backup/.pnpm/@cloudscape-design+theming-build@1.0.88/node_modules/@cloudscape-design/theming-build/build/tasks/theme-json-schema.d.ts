import { Theme } from '../../shared/theme';
import { ThemeJson } from './theme-json';
interface ThemeJsonSchema extends GenericSchema {
    $schema: string;
}
interface GenericSchema {
    type: string;
    pattern?: string;
    patternProperties?: Record<string, GenericSchema>;
    properties?: Record<string, GenericSchema>;
    additionalProperties?: boolean;
    required?: Array<string>;
}
export declare function getThemeJSONSchema(theme: Theme): ThemeJsonSchema;
export declare function validateJson(themeJson: ThemeJson, themeJsonSchema: ThemeJsonSchema): boolean;
export {};
