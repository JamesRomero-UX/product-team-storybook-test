import type { SegmentItem, HiddenUnchangedLinesInfo } from './get-segments';
declare const calculatePlaceholderHeight: (segments: Array<SegmentItem | HiddenUnchangedLinesInfo>, accTop: number[], startSegment: number, startLine: number, endSegment: number, endLine: number, itemHeight: number, expandLineHeight: number, totalHeight: number) => number[];
export default calculatePlaceholderHeight;
