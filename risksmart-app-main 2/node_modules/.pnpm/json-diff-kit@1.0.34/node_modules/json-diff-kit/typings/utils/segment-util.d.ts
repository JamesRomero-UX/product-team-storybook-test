import type { InlineDiffResult } from './get-inline-diff';
import type { InlineHighlightResult } from './get-inline-syntax-highlight';
import type { SegmentItem, HiddenUnchangedLinesInfo } from './get-segments';
export declare const isExpandLine: (segment: SegmentItem | HiddenUnchangedLinesInfo) => segment is HiddenUnchangedLinesInfo;
export declare const getSegmentHeight: (segment: SegmentItem | HiddenUnchangedLinesInfo, itemHeight: number, expandLineHeight: number) => number;
export declare type InlineRenderInfo = InlineDiffResult & InlineHighlightResult;
/**
 * Merge two segments array into one, divide the segment if necessary.
 */
export declare const mergeSegments: (tokens: InlineHighlightResult[], diffs: InlineDiffResult[]) => InlineRenderInfo[];
