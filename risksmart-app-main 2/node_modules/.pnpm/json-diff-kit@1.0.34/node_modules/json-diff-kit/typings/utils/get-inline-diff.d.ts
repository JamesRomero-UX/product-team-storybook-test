export interface InlineDiffOptions {
    mode?: 'char' | 'word';
    wordSeparator?: string;
}
export interface InlineDiffResult {
    type?: 'add' | 'remove';
    start: number;
    end: number;
}
declare const getInlineDiff: (l: string, r: string, options: InlineDiffOptions) => [
    InlineDiffResult[],
    InlineDiffResult[]
];
export default getInlineDiff;
