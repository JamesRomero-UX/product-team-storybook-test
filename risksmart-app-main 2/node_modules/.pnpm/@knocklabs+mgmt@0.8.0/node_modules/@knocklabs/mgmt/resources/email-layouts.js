"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.EmailLayouts = void 0;
const resource_1 = require("../core/resource.js");
const pagination_1 = require("../core/pagination.js");
const path_1 = require("../internal/utils/path.js");
class EmailLayouts extends resource_1.APIResource {
    /**
     * Retrieve an email layout by its key, in a given environment.
     *
     * @example
     * ```ts
     * const emailLayout = await client.emailLayouts.retrieve(
     *   'email_layout_key',
     *   { environment: 'development' },
     * );
     * ```
     */
    retrieve(emailLayoutKey, query, options) {
        return this._client.get((0, path_1.path) `/v1/email_layouts/${emailLayoutKey}`, { query, ...options });
    }
    /**
     * Returns a paginated list of email layouts available in a given environment.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const emailLayout of client.emailLayouts.list({
     *   environment: 'development',
     * })) {
     *   // ...
     * }
     * ```
     */
    list(query, options) {
        return this._client.getAPIList('/v1/email_layouts', (pagination_1.EntriesCursor), { query, ...options });
    }
    /**
     * Updates an email layout, or creates a new one if it does not yet exist.
     *
     * Note: this endpoint only operates in the "development" environment.
     *
     * @example
     * ```ts
     * const response = await client.emailLayouts.upsert(
     *   'email_layout_key',
     *   {
     *     environment: 'development',
     *     email_layout: {
     *       html_layout:
     *         '<html><body>Hello, world!</body></html>',
     *       name: 'Transactional',
     *       text_layout: 'Hello, world!',
     *     },
     *   },
     * );
     * ```
     */
    upsert(emailLayoutKey, params, options) {
        const { environment, annotate, commit, commit_message, ...body } = params;
        return this._client.put((0, path_1.path) `/v1/email_layouts/${emailLayoutKey}`, {
            query: { environment, annotate, commit, commit_message },
            body,
            ...options,
        });
    }
    /**
     * Validates an email layout payload without persisting it.
     *
     * Note: this endpoint only operates in the "development" environment.
     *
     * @example
     * ```ts
     * const response = await client.emailLayouts.validate(
     *   'email_layout_key',
     *   {
     *     environment: 'development',
     *     email_layout: {
     *       html_layout:
     *         '<html><body>Hello, world!</body></html>',
     *       name: 'Transactional',
     *       text_layout: 'Hello, world!',
     *     },
     *   },
     * );
     * ```
     */
    validate(emailLayoutKey, params, options) {
        const { environment, ...body } = params;
        return this._client.put((0, path_1.path) `/v1/email_layouts/${emailLayoutKey}/validate`, {
            query: { environment },
            body,
            ...options,
        });
    }
}
exports.EmailLayouts = EmailLayouts;
//# sourceMappingURL=email-layouts.js.map