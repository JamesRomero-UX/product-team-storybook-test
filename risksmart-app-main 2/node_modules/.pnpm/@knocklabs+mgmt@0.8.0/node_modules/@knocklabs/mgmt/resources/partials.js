"use strict";
// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
Object.defineProperty(exports, "__esModule", { value: true });
exports.Partials = void 0;
const resource_1 = require("../core/resource.js");
const pagination_1 = require("../core/pagination.js");
const path_1 = require("../internal/utils/path.js");
class Partials extends resource_1.APIResource {
    /**
     * Get a partial by its key.
     *
     * @example
     * ```ts
     * const partial = await client.partials.retrieve(
     *   'partial_key',
     *   { environment: 'development' },
     * );
     * ```
     */
    retrieve(partialKey, query, options) {
        return this._client.get((0, path_1.path) `/v1/partials/${partialKey}`, { query, ...options });
    }
    /**
     * List all partials for a given environment.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const partial of client.partials.list({
     *   environment: 'development',
     * })) {
     *   // ...
     * }
     * ```
     */
    list(query, options) {
        return this._client.getAPIList('/v1/partials', (pagination_1.EntriesCursor), { query, ...options });
    }
    /**
     * Updates a partial of a given key, or creates a new one if it does not yet exist.
     *
     * Note: this endpoint only operates on partials in the “development” environment.
     *
     * @example
     * ```ts
     * const response = await client.partials.upsert(
     *   'partial_key',
     *   {
     *     environment: 'development',
     *     partial: {
     *       content: '<p>Hello, world!</p>',
     *       name: 'My Partial',
     *       type: 'html',
     *     },
     *   },
     * );
     * ```
     */
    upsert(partialKey, params, options) {
        const { environment, annotate, commit, commit_message, ...body } = params;
        return this._client.put((0, path_1.path) `/v1/partials/${partialKey}`, {
            query: { environment, annotate, commit, commit_message },
            body,
            ...options,
        });
    }
    /**
     * Validates a partial payload without persisting it.
     *
     * Note: this endpoint only operates on partials in the “development” environment.
     *
     * @example
     * ```ts
     * const response = await client.partials.validate(
     *   'partial_key',
     *   {
     *     environment: 'development',
     *     partial: {
     *       content: '<p>Hello, world!</p>',
     *       name: 'My Partial',
     *       type: 'html',
     *     },
     *   },
     * );
     * ```
     */
    validate(partialKey, params, options) {
        const { environment, ...body } = params;
        return this._client.put((0, path_1.path) `/v1/partials/${partialKey}/validate`, {
            query: { environment },
            body,
            ...options,
        });
    }
}
exports.Partials = Partials;
//# sourceMappingURL=partials.js.map