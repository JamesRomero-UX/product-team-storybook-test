"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const testcontainers_1 = require("testcontainers");
const timeout = 5 * 60 * 1000;
const startTestContainer = async (image) => 
// Starting this with withReuse() enabled will spin up the container
// on the first call and then reuse it on subsequent calls.
new testcontainers_1.GenericContainer(image)
    .withReuse()
    .withExposedPorts(5432)
    .withEnvironment({ POSTGRES_PASSWORD: "postgres" })
    .withStartupTimeout(timeout)
    .start();
exports.default = startTestContainer;
