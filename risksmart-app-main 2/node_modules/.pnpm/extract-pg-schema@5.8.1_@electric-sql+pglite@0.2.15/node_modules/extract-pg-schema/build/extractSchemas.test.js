"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const extractSchemas_1 = __importDefault(require("./extractSchemas"));
const useSchema_1 = __importDefault(require("./tests/useSchema"));
const useTestKnex_1 = __importDefault(require("./tests/useTestKnex"));
(0, vitest_1.describe)("extractSchemas", () => {
    const [getKnex] = (0, useTestKnex_1.default)();
    (0, useSchema_1.default)(getKnex, "test_schema");
    (0, vitest_1.it)("should extract all schemas with default options", async () => {
        const db = getKnex();
        // Set up test data
        await db.schema
            .withSchema("test_schema")
            .createTable("test_table", (table) => {
            table.increments("id");
            table.string("name");
        });
        const connection = db.client.config.connection;
        const result = await (0, extractSchemas_1.default)(connection);
        (0, vitest_1.expect)(result).toHaveProperty("test_schema");
        (0, vitest_1.expect)(result.test_schema.tables).toHaveLength(1);
        (0, vitest_1.expect)(result.test_schema.tables[0].name).toBe("test_table");
    });
    (0, vitest_1.it)("Issue #618 - should not filter out schemas like 'pgboss'", async () => {
        const db = getKnex();
        // Set up test data
        await db.schema.createSchemaIfNotExists("pgboss");
        await db.schema
            .withSchema("pgboss")
            .createTable("test_table", (table) => table.increments("id"));
        const connection = db.client.config.connection;
        const result = await (0, extractSchemas_1.default)(connection);
        (0, vitest_1.expect)(result).toHaveProperty("pgboss");
        (0, vitest_1.expect)(result.pgboss.tables).toHaveLength(1);
        (0, vitest_1.expect)(result.pgboss.tables[0].name).toBe("test_table");
        await db.schema.dropTableIfExists("pgboss.test_table");
        await db.schema.dropSchemaIfExists("pgboss");
    });
});
