export type ViewReference = {
    viewColumn: string;
    source: {
        schema: string;
        table: string;
        column: string;
    } | undefined;
};
declare function parseViewDefinition(selectStatement: string, defaultSchema: string): Promise<ViewReference[]>;
export default parseViewDefinition;
