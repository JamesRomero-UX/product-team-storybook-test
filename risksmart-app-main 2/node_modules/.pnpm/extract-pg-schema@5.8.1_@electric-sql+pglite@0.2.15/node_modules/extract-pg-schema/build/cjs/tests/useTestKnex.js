"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const knex_1 = __importDefault(require("knex"));
const vitest_1 = require("vitest");
const usePostgresContainer_1 = __importDefault(require("./usePostgresContainer"));
const useTestKnex = () => {
    let knexInstance;
    const databaseName = `test_${Math.ceil(Math.random() * 1000)}`;
    const getContainer = (0, usePostgresContainer_1.default)();
    (0, vitest_1.beforeAll)(async () => {
        const container = getContainer();
        const connection = {
            host: container.getHost(),
            port: container.getMappedPort(5432),
            password: "postgres",
            user: "postgres",
        };
        const setupKnexInstance = (0, knex_1.default)({
            client: "postgres",
            connection: { ...connection, database: "postgres" },
        });
        await setupKnexInstance.raw("create database ??", [databaseName]);
        await setupKnexInstance.destroy();
        knexInstance = (0, knex_1.default)({
            client: "postgres",
            connection: { ...connection, database: databaseName },
        });
    });
    (0, vitest_1.afterAll)(async () => {
        const container = getContainer();
        const connection = {
            host: container.getHost(),
            port: container.getMappedPort(5432),
            password: "postgres",
            user: "postgres",
        };
        const setupKnexInstance = (0, knex_1.default)({
            client: "postgres",
            connection: { ...connection, database: "postgres" },
        });
        setupKnexInstance
            .raw(`drop database ${databaseName} with (force)`)
            .then(() => setupKnexInstance.destroy());
        await knexInstance.destroy();
    });
    return [() => knexInstance, databaseName];
};
exports.default = useTestKnex;
