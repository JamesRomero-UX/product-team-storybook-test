import type { ConnectionConfig } from "pg";
/** @deprecated - use extractSchemas instead */
declare const extractSchema: (schemaName: string, connectionConfig: string | ConnectionConfig, resolveViews: boolean, tables?: string[]) => Promise<{
    tables: any[];
    views: any[];
    types: any[];
}>;
export default extractSchema;
