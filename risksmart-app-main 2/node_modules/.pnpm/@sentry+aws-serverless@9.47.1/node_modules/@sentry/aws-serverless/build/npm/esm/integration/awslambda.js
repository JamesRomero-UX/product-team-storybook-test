import { AwsLambdaInstrumentation } from '@opentelemetry/instrumentation-aws-lambda';
import { SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, SEMANTIC_ATTRIBUTE_SENTRY_OP, defineIntegration } from '@sentry/core';
import { generateInstrumentOnce } from '@sentry/node';
import { eventContextExtractor } from '../utils.js';

const instrumentAwsLambda = generateInstrumentOnce(
  'AwsLambda',
  AwsLambdaInstrumentation,
  (options) => {
    return {
      disableAwsContextPropagation: true,
      ...options,
      eventContextExtractor,
      requestHook(span) {
        span.setAttribute(SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, 'auto.otel.aws-lambda');
        span.setAttribute(SEMANTIC_ATTRIBUTE_SENTRY_OP, 'function.aws.lambda');
      },
    };
  },
);

const _awsLambdaIntegration = ((options = {}) => {
  return {
    name: 'AwsLambda',
    setupOnce() {
      instrumentAwsLambda(options);
    },
  };
}) ;

/**
 * Instrumentation for aws-sdk package
 */
const awsLambdaIntegration = defineIntegration(_awsLambdaIntegration);

export { awsLambdaIntegration, instrumentAwsLambda };
//# sourceMappingURL=awslambda.js.map
