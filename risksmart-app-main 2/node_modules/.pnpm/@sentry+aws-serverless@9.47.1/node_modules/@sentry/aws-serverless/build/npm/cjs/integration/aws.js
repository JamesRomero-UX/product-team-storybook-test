Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const instrumentation = require('@opentelemetry/instrumentation');
const instrumentationAwsSdk = require('@opentelemetry/instrumentation-aws-sdk');
const core = require('@sentry/core');

/**
 * Instrumentation for aws-sdk package
 */
const awsIntegration = core.defineIntegration(() => {
  return {
    name: 'Aws',
    setupOnce() {
      instrumentation.registerInstrumentations({
        instrumentations: [
          new instrumentationAwsSdk.AwsInstrumentation({
            preRequestHook(span) {
              span.setAttribute(core.SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, 'auto.otel.aws');
            },
          }),
        ],
      });
    },
  };
});

exports.awsIntegration = awsIntegration;
//# sourceMappingURL=aws.js.map
