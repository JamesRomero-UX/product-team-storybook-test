"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const chalk_1 = __importDefault(require("chalk"));
// @ts-ignore
const optionator_1 = __importDefault(require("optionator"));
const path_1 = __importDefault(require("path"));
const package_json_1 = require("../package.json");
const engine_1 = require("./engine");
async function main() {
    const o = (0, optionator_1.default)({
        prepend: "Usage: schemalint [options]",
        append: `Version ${package_json_1.version}`,
        options: [
            {
                option: "help",
                alias: "h",
                type: "Boolean",
                description: "displays help",
            },
            {
                option: "version",
                alias: "v",
                type: "Boolean",
                description: "displays version",
            },
            {
                option: "config",
                alias: "c",
                type: "path::String",
                description: "Use this configuration, overriding .schemalintrc.* config options if present",
            },
        ],
    });
    let options;
    try {
        options = o.parseArgv(process.argv);
    }
    catch (error) {
        console.error(error.message);
        process.exit(1);
    }
    if (options.help) {
        console.info(o.generateHelp());
        process.exit(0);
    }
    if (options.version) {
        console.info(package_json_1.version);
        process.exit(0);
    }
    console.info(`${chalk_1.default.greenBright("schema-lint")}`);
    const configFile = path_1.default.join(process.cwd(), options.config || ".schemalintrc.js");
    try {
        // eslint-disable-next-line @typescript-eslint/no-require-imports
        const config = require(configFile);
        const exitCode = await (0, engine_1.processDatabase)(config);
        process.exit(exitCode);
    }
    catch (error) {
        console.error(error);
        process.exit(1);
    }
}
main();
