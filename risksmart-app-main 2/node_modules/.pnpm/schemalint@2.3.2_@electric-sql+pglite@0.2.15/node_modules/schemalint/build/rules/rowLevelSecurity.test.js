"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vitest_1 = require("vitest");
const rowLevelSecurity_1 = require("./rowLevelSecurity");
(0, vitest_1.describe)("rowLevelSecurity", () => {
    (0, vitest_1.describe)("no tables", () => {
        (0, vitest_1.it)("should pass when no tables exist", () => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                tables: [],
                views: [],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [{}],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(0);
        });
    });
    (0, vitest_1.describe)("option is not provided", () => {
        vitest_1.test.each([
            {
                enforced: false,
            },
            {
                enforced: true,
            },
        ])("should pass when row-level security is enabled: {enforced: $enforced}", ({ enforced }) => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: true,
                        isRowLevelSecurityEnforced: enforced,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(0);
        });
        vitest_1.test.each([
            {
                enforced: false,
            },
            {
                enforced: true,
            },
        ])("should report when row-level security is disabled: {enforced: $enforced}", ({ enforced }) => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: false,
                        isRowLevelSecurityEnforced: enforced,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(1);
            (0, vitest_1.expect)(mockReporter).toBeCalledWith(vitest_1.expect.objectContaining({
                rule: "row-level-security",
                identifier: `schema.test`,
                message: `Row-level security is disabled`,
                suggestedMigration: `ALTER TABLE "schema"."test" ENABLE ROW LEVEL SECURITY;`,
            }));
        });
    });
    (0, vitest_1.describe)("option is {enforced: true}", () => {
        (0, vitest_1.it)("should pass when row-level security is enabled and enforced", () => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: true,
                        isRowLevelSecurityEnforced: true,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [{ enforced: true }],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(0);
        });
        (0, vitest_1.it)("should report when row-level security is disabled and not enforced", () => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: false,
                        isRowLevelSecurityEnforced: false,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [{ enforced: true }],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(2);
            (0, vitest_1.expect)(mockReporter).toBeCalledWith(vitest_1.expect.objectContaining({
                rule: "row-level-security",
                identifier: `schema.test`,
                message: `Row-level security is disabled`,
                suggestedMigration: `ALTER TABLE "schema"."test" ENABLE ROW LEVEL SECURITY;`,
            }));
            (0, vitest_1.expect)(mockReporter).toBeCalledWith(vitest_1.expect.objectContaining({
                rule: "row-level-security",
                identifier: `schema.test`,
                message: `Row-level security is not enforced`,
                suggestedMigration: `ALTER TABLE "schema"."test" FORCE ROW LEVEL SECURITY;`,
            }));
        });
        (0, vitest_1.it)("should report when row-level security is enforced but disabled", () => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: false,
                        isRowLevelSecurityEnforced: true,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [{ enforced: true }],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(1);
            (0, vitest_1.expect)(mockReporter).toBeCalledWith(vitest_1.expect.objectContaining({
                rule: "row-level-security",
                identifier: `schema.test`,
                message: `Row-level security is disabled`,
                suggestedMigration: `ALTER TABLE "schema"."test" ENABLE ROW LEVEL SECURITY;`,
            }));
        });
        (0, vitest_1.it)("should report when row-level security is enabled but not enforced", () => {
            const mockReporter = vitest_1.vi.fn();
            const schemaObject = {
                name: "schema",
                tables: [
                    {
                        name: "test",
                        isRowLevelSecurityEnabled: true,
                        isRowLevelSecurityEnforced: false,
                    },
                ],
            };
            rowLevelSecurity_1.rowLevelSecurity.process({
                options: [{ enforced: true }],
                schemaObject: schemaObject,
                report: mockReporter,
            });
            (0, vitest_1.expect)(mockReporter).toBeCalledTimes(1);
            (0, vitest_1.expect)(mockReporter).toBeCalledWith(vitest_1.expect.objectContaining({
                rule: "row-level-security",
                identifier: `schema.test`,
                message: `Row-level security is not enforced`,
                suggestedMigration: `ALTER TABLE "schema"."test" FORCE ROW LEVEL SECURITY;`,
            }));
        });
    });
});
