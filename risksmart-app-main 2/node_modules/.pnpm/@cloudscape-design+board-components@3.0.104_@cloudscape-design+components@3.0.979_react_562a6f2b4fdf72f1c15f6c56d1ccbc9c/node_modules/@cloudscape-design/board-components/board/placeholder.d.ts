import { GridContext } from "../internal/grid/interfaces";
export type PlaceholderState = "default" | "active" | "hover";
export interface PlaceholderProps {
    id: string;
    state: PlaceholderState;
    gridContext: GridContext;
    columns: number;
}
export default function Placeholder({ id, state, gridContext, columns }: PlaceholderProps): import("react/jsx-runtime").JSX.Element;
