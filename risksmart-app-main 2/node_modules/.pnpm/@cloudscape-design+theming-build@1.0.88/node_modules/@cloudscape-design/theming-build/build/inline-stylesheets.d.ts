import { SpecificResolution, Theme } from '../shared/theme';
import { InlineStylesheet } from './tasks/style';
/**
 * Generates a list of inline stylesheets used during the SCSS compilation. The primary
 * theme defines the mappings the environment.
 * @returns inline stylesheets
 */
export declare function getInlineStylesheets(primary: Theme, secondary: Theme[], resolution: SpecificResolution, variablesMap: Record<string, string>, propertiesMap: Record<string, string>, neededTokens: string[]): InlineStylesheet[];
