import { T as TagBadgeParameters, B as Badge } from './TagBadgeParameters-DDiJL0yJ.js';
export { a as BadgeOrBadgeFn, D as Display, M as MDXDisplayOptions, S as SidebarDisplayOptions, c as TagPattern, d as TagPatterns, b as ToolbarDisplayOptions, g as getTagParts, e as getTagPrefix, f as getTagSuffix, m as matchTag, h as matchTags } from './TagBadgeParameters-DDiJL0yJ.js';
import React, { FC, ReactNode } from 'react';
import { API_HashEntry } from 'storybook/internal/types';
import { Of } from '@storybook/addon-docs/blocks';
import 'storybook/internal/components';
import 'storybook/manager-api';
import 'storybook/theming';

declare const defaultConfig: TagBadgeParameters;

declare const DISPLAY_DEFAULTS: {
    mdx: ("component" | "story")[];
    sidebar: ({
        type: "story";
        skipInherited: true;
    } | {
        type: "docs";
        skipInherited: true;
    } | {
        type: "component";
        skipInherited: false;
    } | {
        type: "group";
        skipInherited: false;
    })[];
    toolbar: ("docs" | "story")[];
};

declare function renderLabel(item: API_HashEntry): React.JSX.Element | undefined;

interface SidebarProps {
    children: ReactNode;
    item: API_HashEntry;
    hasStatusWithUI?: boolean;
}
declare const Sidebar: FC<SidebarProps>;

declare const CustomBadge: React.FC<Badge>;

declare global {
    interface Window {
        tagBadges: TagBadgeParameters;
    }
}
interface MDXBadgesProps {
    /**
     * Specify where to get the Badge tags from. Must be a CSF file's default export or a CSF story.
     * If not specified, the tags will be extracted from the meta of the attached CSF file.
     */
    of?: Of;
}
declare const MDXBadges: FC<MDXBadgesProps>;

export { Badge, CustomBadge, MDXBadges, Sidebar, TagBadgeParameters, defaultConfig, DISPLAY_DEFAULTS as defaultDisplay, renderLabel };
