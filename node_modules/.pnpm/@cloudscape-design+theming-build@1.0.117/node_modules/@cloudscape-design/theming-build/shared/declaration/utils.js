"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getReferencedVar = exports.compact = void 0;
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
function compact(arr) {
    const result = [];
    for (const item of arr) {
        if (item !== undefined) {
            result.push(item);
        }
    }
    return result;
}
exports.compact = compact;
/**
 * Extracts the CSS variable name from a var() reference.
 * @param value - Token value that may contain a var() reference
 * @returns The variable name (e.g., '--color-primary') or null if not a var() reference
 */
function getReferencedVar(value) {
    const match = value.match(/var\((--[^)]+)\)/);
    return match ? match[1] : null;
}
exports.getReferencedVar = getReferencedVar;
