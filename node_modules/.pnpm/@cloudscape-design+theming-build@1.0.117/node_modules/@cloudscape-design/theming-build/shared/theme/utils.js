"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isValidPaletteStep = exports.getDefaultState = exports.getMode = exports.collectReferencedTokens = exports.getReference = exports.isOptionalState = exports.areAssignmentsEqual = exports.isModeValue = exports.isReference = exports.isValue = exports.generateReferenceTokenName = exports.flattenReferenceTokens = exports.generateCamelCaseName = exports.flattenObject = exports.isReferenceToken = void 0;
function isReferenceToken(category, theme, token) {
    const categoryTokens = theme.referenceTokens?.[category];
    if (!categoryTokens)
        return false;
    return Object.entries(categoryTokens).some(([type, set]) => {
        if (!set)
            return false;
        return Object.keys(set).some((step) => generateReferenceTokenName(category, type, step) === token);
    });
}
exports.isReferenceToken = isReferenceToken;
function flattenObject(obj, prefix = []) {
    const result = {};
    if (!obj || typeof obj !== 'object') {
        return result;
    }
    for (const [key, value] of Object.entries(obj)) {
        const path = [...prefix, key];
        if (typeof value === 'string') {
            result[generateCamelCaseName(...path)] = value;
        }
        else if (isModeValue(value)) {
            // Stop flattening at mode values - preserve them as Assignment
            result[generateCamelCaseName(...path)] = value;
        }
        else if (value && typeof value === 'object') {
            Object.assign(result, flattenObject(value, path));
        }
    }
    return result;
}
exports.flattenObject = flattenObject;
function generateCamelCaseName(...segments) {
    return segments.reduce((acc, segment, index) => acc + (index === 0 ? segment : segment.charAt(0).toUpperCase() + segment.slice(1)), '');
}
exports.generateCamelCaseName = generateCamelCaseName;
function flattenReferenceTokens(theme) {
    return theme.referenceTokens?.color ? flattenObject(theme.referenceTokens.color, ['color']) : {};
}
exports.flattenReferenceTokens = flattenReferenceTokens;
function generateReferenceTokenName(category, type, step) {
    return generateCamelCaseName(category, type, step);
}
exports.generateReferenceTokenName = generateReferenceTokenName;
function isValue(val) {
    return typeof val === 'string' && !isReference(val);
}
exports.isValue = isValue;
function isReference(val) {
    return typeof val === 'string' && val.charAt(0) === '{' && val.charAt(val.length - 1) === '}';
}
exports.isReference = isReference;
function isModeValue(val) {
    return (typeof val === 'object' &&
        val !== null &&
        !Array.isArray(val) &&
        // Exclude objects with numeric keys (palette steps like '500', '900')
        !Object.keys(val).some((key) => !isNaN(Number(key))) &&
        !Object.keys(val).some((state) => !(isValue(val[state]) || isReference(val[state]))));
}
exports.isModeValue = isModeValue;
function areAssignmentsEqual(valueA, valueB) {
    return (valueA === valueB ||
        (typeof valueA === 'object' &&
            typeof valueB == 'object' &&
            Object.keys(valueA).length === Object.keys(valueB).length &&
            Object.keys(valueA).every((key) => valueA[key] === valueB[key])));
}
exports.areAssignmentsEqual = areAssignmentsEqual;
function isOptionalState(val) {
    return 'selector' in val;
}
exports.isOptionalState = isOptionalState;
function getReference(reference) {
    return reference.slice(1, reference.length - 1);
}
exports.getReference = getReference;
function collectReferencedTokens(theme, tokens) {
    const referenced = new Set();
    const visited = new Set();
    const addReferences = (value) => {
        if (isReference(value)) {
            referenced.add(getReference(value));
        }
        else if (isModeValue(value)) {
            Object.values(value).forEach(addReferences);
        }
    };
    const processToken = (token) => {
        if (visited.has(token))
            return;
        visited.add(token);
        const value = theme.tokens[token];
        if (value)
            addReferences(value);
        Object.values(theme.contexts).forEach((context) => {
            const contextValue = context.tokens[token];
            if (contextValue)
                addReferences(contextValue);
        });
    };
    // Initial pass
    tokens.forEach(processToken);
    // Recursive passes until no new tokens found
    let previousSize = 0;
    let iterations = 0;
    while (referenced.size > previousSize && iterations < 10) {
        previousSize = referenced.size;
        const newTokens = Array.from(referenced).filter((t) => !visited.has(t));
        newTokens.forEach(processToken);
        iterations++;
    }
    return Array.from(referenced);
}
exports.collectReferencedTokens = collectReferencedTokens;
function getMode(theme, token) {
    const modeId = theme.tokenModeMap[token];
    return theme.modes[modeId] ?? null;
}
exports.getMode = getMode;
function getDefaultState(mode) {
    const states = Object.keys(mode.states);
    for (let index = 0; index < states.length; index++) {
        const state = states[index];
        const option = mode.states[state];
        if (option && 'default' in option && option.default) {
            return state;
        }
    }
    throw new Error(`Mode ${JSON.stringify(mode)} does not have a default state`);
}
exports.getDefaultState = getDefaultState;
function isValidPaletteStep(step) {
    return step >= 50 && step <= 1000 && step % 50 === 0;
}
exports.isValidPaletteStep = isValidPaletteStep;
