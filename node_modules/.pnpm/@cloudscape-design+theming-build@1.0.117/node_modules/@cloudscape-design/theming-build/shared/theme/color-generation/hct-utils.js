"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Hct = exports.argbFromHex = exports.hexFromArgb = exports.createHct = exports.hexToHct = exports.hctToHex = void 0;
// Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
// SPDX-License-Identifier: Apache-2.0
const material_color_utilities_1 = require("@material/material-color-utilities");
Object.defineProperty(exports, "argbFromHex", { enumerable: true, get: function () { return material_color_utilities_1.argbFromHex; } });
Object.defineProperty(exports, "Hct", { enumerable: true, get: function () { return material_color_utilities_1.Hct; } });
Object.defineProperty(exports, "hexFromArgb", { enumerable: true, get: function () { return material_color_utilities_1.hexFromArgb; } });
function isValidHex(hex) {
    return /^#?([A-Fa-f0-9]{6}|[A-Fa-f0-9]{3})$/.test(hex);
}
/**
 * Parses CSS color formats and converts to ARGB.
 * Supports: #hex and rgb()/rgba()
 */
function parseColorToArgb(color) {
    const trimmed = color.trim();
    // Hex format
    if (trimmed.startsWith('#')) {
        if (!isValidHex(trimmed)) {
            throw new Error(`Invalid hex color: ${color}`);
        }
        return (0, material_color_utilities_1.argbFromHex)(trimmed);
    }
    // rgb() or rgba() format
    const rgbMatch = trimmed.match(/rgba?\s*\(\s*(\d+)\s*,\s*(\d+)\s*,\s*(\d+)/);
    if (rgbMatch) {
        const r = parseInt(rgbMatch[1]);
        const g = parseInt(rgbMatch[2]);
        const b = parseInt(rgbMatch[3]);
        return (0, material_color_utilities_1.argbFromRgb)(r, g, b);
    }
    throw new Error(`Unsupported color format: ${color}. Supported formats: #hex, rgb(), rgba()`);
}
function hctToHex(hctColor) {
    return (0, material_color_utilities_1.hexFromArgb)(hctColor.toInt());
}
exports.hctToHex = hctToHex;
function hexToHct(color) {
    const argb = parseColorToArgb(color);
    return material_color_utilities_1.Hct.fromInt(argb);
}
exports.hexToHct = hexToHct;
function createHct(hue, chroma, tone) {
    return material_color_utilities_1.Hct.from(hue, chroma, tone);
}
exports.createHct = createHct;
