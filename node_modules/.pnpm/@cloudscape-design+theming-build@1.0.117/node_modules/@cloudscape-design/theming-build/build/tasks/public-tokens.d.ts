import { ThemePreset, SpecificResolution } from '../../shared/theme';
import { Token } from '../../shared/theme/interfaces';
interface PublicTokensTaskParams {
    resolution: SpecificResolution;
    preset: ThemePreset;
    outputDir: string;
    fileName: string;
    descriptions?: Record<string, string>;
    jsonSchema?: boolean;
}
export declare function createPublicTokenFiles(params: PublicTokensTaskParams): Promise<void>;
export declare function renderSCSS(resolution: SpecificResolution, variablesMap: ThemePreset['variablesMap'], propertiesMap: ThemePreset['propertiesMap'], tokens: Token[]): string;
export declare function renderJS(resolution: SpecificResolution, propertiesMap: ThemePreset['propertiesMap'], tokens: Token[]): string;
export declare function writeJSONfiles(preset: ThemePreset, outputDir: string, fileName: string, descriptions?: Record<string, string>, jsonSchema?: boolean): Promise<void>;
export declare function renderTS(tokens: Token[]): string;
export {};
