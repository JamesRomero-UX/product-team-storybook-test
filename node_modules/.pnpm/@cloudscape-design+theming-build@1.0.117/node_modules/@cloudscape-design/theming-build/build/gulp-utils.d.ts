/**
 * Run a gulp task and returns a promise. The promise will resolve on completion or
 * reject with an error.
 *
 * @param task gulp task
 */
export declare function runTask(task: (cb: (err?: Error | null) => void) => void): Promise<void>;
export declare const createNoop: () => () => Promise<void>;
