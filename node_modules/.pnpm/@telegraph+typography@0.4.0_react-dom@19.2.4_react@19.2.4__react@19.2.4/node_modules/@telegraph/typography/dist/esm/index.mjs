import { jsx as g } from "react/jsx-runtime";
import { Box as c } from "@telegraph/layout";
import { useStyleEngine as d } from "@telegraph/style-engine";
import f from "clsx";
const V = {
  color: {
    cssVar: "--color",
    value: "var(--tgph-VARIABLE)"
  },
  fontSize: {
    cssVar: "--font-size",
    value: "var(--tgph-text-VARIABLE)"
  },
  weight: {
    cssVar: "--weight",
    value: "var(--tgph-weight-VARIABLE)"
  },
  leading: {
    cssVar: "--leading",
    value: "var(--tgph-leading-VARIABLE)"
  },
  tracking: {
    cssVar: "--tracking",
    value: "var(--tgph-tracking-VARIABLE)"
  },
  align: {
    cssVar: "--text-align",
    value: "VARIABLE"
  },
  family: {
    cssVar: "--font-family",
    value: "var(--tgph-family-VARIABLE)"
  },
  textOverflow: {
    cssVar: "--text-overflow",
    value: "VARIABLE"
  }
}, u = V, y = {
  default: "gray-12",
  gray: "gray-11",
  red: "red-11",
  beige: "beige-11",
  blue: "blue-11",
  green: "green-11",
  yellow: "yellow-11",
  purple: "purple-11",
  accent: "accent-11",
  white: "white",
  black: "black",
  contrast: "gray-1",
  disabled: "gray-9"
}, E = ({
  as: t,
  size: e = "2",
  weight: o = "semi-bold",
  color: r,
  align: a,
  className: l,
  // Remove this from props to avoid passing to DOM element
  internal_optionalAs: i,
  ...n
}) => {
  if (!t) throw new Error("as prop is required");
  const { styleProp: p, otherProps: s } = d({
    props: {
      color: y[r],
      fontSize: e,
      tracking: e,
      leading: e,
      weight: o,
      align: a,
      ...n
    },
    cssVars: u
  });
  return /* @__PURE__ */ g(
    c,
    {
      as: t,
      className: f("tgph-heading", l),
      display: "inline",
      m: "0",
      style: p,
      ...s
    }
  );
}, R = ({
  as: t,
  size: e = "2",
  weight: o = "regular",
  align: r = "left",
  color: a,
  className: l,
  // Remove this from props to avoid passing to DOM element
  internal_optionalAs: i,
  ...n
}) => {
  if (!t) throw new Error("as prop is required");
  const { styleProp: p, otherProps: s } = d({
    props: {
      color: y[a],
      fontSize: e,
      tracking: e,
      leading: e,
      weight: o,
      align: r,
      ...n
    },
    cssVars: u
  });
  return /* @__PURE__ */ g(
    c,
    {
      as: t,
      className: f("tgph-text", l),
      display: "inline",
      style: p,
      ...s
    }
  );
}, m = {
  default: "gray-4",
  gray: "gray-4",
  red: "red-4",
  beige: "beige-4",
  blue: "blue-4",
  green: "green-4",
  yellow: "yellow-4",
  purple: "purple-4",
  accent: "accent-4"
}, v = ({
  as: t,
  size: e = "1",
  weight: o = "regular",
  variant: r = "soft",
  align: a = "left",
  family: l = "mono",
  color: i,
  className: n,
  // Remove this from props to avoid passing to DOM element
  internal_optionalAs: p,
  ...s
}) => {
  if (!t) throw new Error("as prop is required");
  const { styleProp: A, otherProps: h } = d({
    props: {
      color: y[i],
      fontSize: `code-${e}`,
      leading: `code-${e}`,
      weight: o,
      align: a,
      family: l,
      ...s
    },
    cssVars: u
  });
  return /* @__PURE__ */ g(
    c,
    {
      as: t,
      className: f("tgph-code", n),
      bg: r === "soft" ? "gray-2" : "transparent",
      display: "inline",
      m: "0",
      px: r === "soft" ? "0_5" : "0",
      pt: r === "soft" ? "0_5" : "0",
      pb: r === "soft" ? "px" : "0",
      border: "px",
      borderColor: r === "soft" ? m[i] : "transparent",
      rounded: "1",
      style: A,
      ...h
    }
  );
};
export {
  v as Code,
  E as Heading,
  R as Text
};
//# sourceMappingURL=index.mjs.map
