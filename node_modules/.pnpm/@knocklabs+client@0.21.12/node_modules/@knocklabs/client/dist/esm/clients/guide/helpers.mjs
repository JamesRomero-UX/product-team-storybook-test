const y = (e) => `status=${e.status}, resolved=${e.resolved}`, h = (e) => `loc=${e.location}`, g = (e = {}) => [
  e.key && `key=${e.key}`,
  e.type && `type=${e.type}`
].filter((t) => t).join(", "), _ = (e) => e.reduce((t, n) => ({ ...t, [n.key]: n }), {}), d = (e) => [...e].sort(
  (t, n) => new Date(t.inserted_at).getTime() - new Date(n.inserted_at).getTime()
), c = "default", u = "$default", k = (e = []) => {
  const t = /* @__PURE__ */ new Date();
  return {
    __typename: "GuideGroup",
    key: u,
    display_sequence: d(e).map((n) => n.key),
    display_interval: null,
    inserted_at: t.toISOString(),
    updated_at: t.toISOString()
  };
}, l = (e) => e.find(
  (t) => t.key === c || t.key === u
), w = (e) => {
  var o;
  if ((o = e.debug) != null && o.ignoreDisplayInterval)
    return !1;
  const t = l(e.guideGroups), n = e.guideGroupDisplayLogs[c];
  return t && t.display_interval && n ? f(
    n,
    t.display_interval
  ) : !1;
}, f = (e, t) => {
  const n = new Date(e);
  if (isNaN(n.getTime()))
    return !1;
  const o = n.getTime() + t * 1e3;
  return (/* @__PURE__ */ new Date()).getTime() <= o;
}, v = (e) => {
  try {
    return new URL(e);
  } catch {
    return;
  }
}, i = (e, t) => t.variable === "pathname" ? t.operator === "equal_to" ? (t.argument.startsWith("/") ? t.argument : `/${t.argument}`) === e.pathname : t.operator === "contains" ? e.pathname.includes(t.argument) : !1 : !1, D = (e, t) => {
  const o = t.every((r) => r.directive === "block") ? !0 : void 0;
  return t.reduce((r, a) => {
    if (r === !1) return !1;
    switch (a.directive) {
      case "allow":
        return r === !0 || i(e, a) ? !0 : void 0;
      case "block":
        return i(e, a) ? !1 : r;
    }
  }, o);
}, T = (e, t) => {
  const o = t.every(
    (r) => r.directive === "block"
  ) ? !0 : void 0;
  return t.reduce((r, a) => {
    if (r === !1) return !1;
    switch (a.directive) {
      case "allow":
        return r === !0 || a.pattern.test(e) ? !0 : void 0;
      case "block":
        return a.pattern.test(e) ? !1 : r;
    }
  }, o);
}, m = "knock_guide_toolbar", p = "focused_guide_key", b = () => {
  const e = { isVisible: !1 };
  if (typeof window > "u" || !window.location)
    return e;
  const t = new URLSearchParams(window.location.search), n = t.get(m), o = t.get(p);
  if (n === null)
    return e;
  const r = {
    isVisible: n === "true"
  };
  return o && (r.focusedGuideKeys = { [o]: !0 }), r;
};
export {
  c as DEFAULT_GROUP_KEY,
  _ as byKey,
  w as checkStateIfThrottled,
  i as evaluateUrlRule,
  l as findDefaultGroup,
  g as formatFilters,
  y as formatGroupStage,
  h as formatState,
  b as getToolbarRunConfigFromUrl,
  k as mockDefaultGroup,
  v as newUrl,
  T as predicateUrlPatterns,
  D as predicateUrlRules
};
//# sourceMappingURL=helpers.mjs.map
