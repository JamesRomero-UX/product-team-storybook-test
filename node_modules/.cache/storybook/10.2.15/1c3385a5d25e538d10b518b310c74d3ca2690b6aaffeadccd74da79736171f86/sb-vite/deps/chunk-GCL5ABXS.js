import {
  _defineProperty,
  addDays,
  addMonths,
  differenceInYears,
  isAfter,
  isBefore,
  isSameDay,
  isSameMonth,
  isSameYear,
  startOfMonth,
  startOfYear,
  subMonths
} from "./chunk-FWATUNHA.js";
import {
  warnOnce
} from "./chunk-CMYEOKCY.js";
import {
  require_react
} from "./chunk-YHCPMRL2.js";
import {
  __toESM
} from "./chunk-PR4QN5HX.js";

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/utils/locale/merge-locales.js
function mergeLocales(locale, fullLocale) {
  const isShort = locale.length === 2;
  if (isShort && fullLocale.indexOf(locale) === 0) {
    return fullLocale;
  }
  return locale;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/utils/locale/normalize-locale.js
function normalizeLocale(component, locale) {
  var _a;
  locale = checkLocale(component, locale);
  const browserLocale = getBrowserLocale();
  if (locale) {
    return mergeLocales(locale, browserLocale);
  }
  const htmlLocale = checkLocale(component, (_a = getHtmlElement()) === null || _a === void 0 ? void 0 : _a.getAttribute("lang"));
  if (htmlLocale) {
    return mergeLocales(htmlLocale, browserLocale);
  }
  return browserLocale;
}
function checkLocale(component, locale) {
  if (!locale || locale === "") {
    return "";
  }
  locale = locale && locale.replace(/^([a-z]{2})_/, "$1-");
  if (locale && !locale.match(/^[a-z]{2}(-[A-Z]{2})?$/)) {
    warnOnce(component, `Invalid locale provided: ${locale}. Falling back to default`);
    locale = "";
  }
  return locale;
}
function getHtmlElement() {
  return typeof document !== "undefined" ? document.querySelector("html") : null;
}
function getBrowserLocale() {
  return new Intl.DateTimeFormat().resolvedOptions().locale;
}

// node_modules/.pnpm/weekstart@1.1.0/node_modules/weekstart/dist/es-module/api.js
function getWeekStartByRegion(regionCode, regionDayMap2) {
  var code = regionDayMap2[typeof regionCode === "string" ? regionCode.toUpperCase() : regionCode];
  return typeof code === "number" ? code : 1;
}
function getWeekStartByLocale(locale, langRegionMap2, regionDayMap2) {
  if (locale) {
    var data = locale.toLowerCase().split(/[-_]/);
    var language = data[0];
    var country;
    if (data[1] && data[1].length === 4) {
      language += "_" + data[1];
      country = data[2];
    } else {
      country = data[1];
    }
    if (!country) {
      country = langRegionMap2[language];
    }
    if (country) {
      return getWeekStartByRegion(country.match(/^\d+$/) ? Number(country) : country, regionDayMap2);
    }
  }
  return 1;
}

// node_modules/.pnpm/weekstart@1.1.0/node_modules/weekstart/dist/es-module/langRegionMap.js
var langRegionMap = {
  en: "US",
  zh: "CN",
  zh_hans: "CN",
  hans: "CN",
  wuu: "CN",
  hsn: "CN",
  hak: "CN",
  nan: "CN",
  gan: "CN",
  hi: "IN",
  te: "IN",
  mr: "IN",
  ta: "IN",
  gu: "IN",
  kn: "IN",
  or: "IN",
  ml: "IN",
  pa_guru: "IN",
  bho: "IN",
  awa: "IN",
  as: "IN",
  mwr: "IN",
  mai: "IN",
  mag: "IN",
  bgc: "IN",
  hne: "IN",
  dcc: "IN",
  dz: "BT",
  tn: "BW",
  am: "ET",
  om: "ET",
  quc: "GT",
  id: "ID",
  jv: "ID",
  su: "ID",
  mad: "ID",
  ms_arab: "ID",
  ga: "IE",
  he: "IL",
  jam: "JM",
  ja: "JP",
  km: "KH",
  ko: "KR",
  lo: "LA",
  mh: "MH",
  my: "MM",
  mt: "MT",
  ne: "NP",
  fil: "PH",
  ceb: "PH",
  ilo: "PH",
  ur: "PK",
  pa: "PK",
  pa_arab: "PK",
  arab: "PK",
  lah: "PK",
  ps: "PK",
  sd: "PK",
  sd_arab: "PK",
  skr: "PK",
  gn: "PY",
  th: "TH",
  tts: "TH",
  aeb: "TN",
  zh_hant: "TW",
  hant: "TW",
  sm: "WS",
  zu: "ZA",
  sn: "ZW",
  arq: "DZ",
  ar: "EG",
  arz: "EG",
  fa: "IR",
  az_arab: "IR",
  ary: "MA",
  bn: "BD",
  rkt: "BD",
  dv: "MV"
};
var langRegionMap_default = langRegionMap;

// node_modules/.pnpm/weekstart@1.1.0/node_modules/weekstart/dist/es-module/regionDayMap.js
var regionDayMap = {
  AG: 0,
  ATG: 0,
  28: 0,
  AR: 0,
  ARG: 0,
  32: 0,
  AS: 0,
  ASM: 0,
  16: 0,
  AU: 0,
  AUS: 0,
  36: 0,
  BR: 0,
  BRA: 0,
  76: 0,
  BS: 0,
  BHS: 0,
  44: 0,
  BT: 0,
  BTN: 0,
  64: 0,
  BW: 0,
  BWA: 0,
  72: 0,
  BZ: 0,
  BLZ: 0,
  84: 0,
  CA: 0,
  CAN: 0,
  124: 0,
  CN: 0,
  CHN: 0,
  156: 0,
  CO: 0,
  COL: 0,
  170: 0,
  DM: 0,
  DMA: 0,
  212: 0,
  DO: 0,
  DOM: 0,
  214: 0,
  ET: 0,
  ETH: 0,
  231: 0,
  GT: 0,
  GTM: 0,
  320: 0,
  GU: 0,
  GUM: 0,
  316: 0,
  HK: 0,
  HKG: 0,
  344: 0,
  HN: 0,
  HND: 0,
  340: 0,
  ID: 0,
  IDN: 0,
  360: 0,
  IE: 0,
  IRL: 0,
  372: 0,
  IL: 0,
  ISR: 0,
  376: 0,
  IN: 0,
  IND: 0,
  356: 0,
  JM: 0,
  JAM: 0,
  388: 0,
  JP: 0,
  JPN: 0,
  392: 0,
  KE: 0,
  KEN: 0,
  404: 0,
  KH: 0,
  KHM: 0,
  116: 0,
  KR: 0,
  KOR: 0,
  410: 0,
  LA: 0,
  LA0: 0,
  418: 0,
  MH: 0,
  MHL: 0,
  584: 0,
  MM: 0,
  MMR: 0,
  104: 0,
  MO: 0,
  MAC: 0,
  446: 0,
  MT: 0,
  MLT: 0,
  470: 0,
  MX: 0,
  MEX: 0,
  484: 0,
  MZ: 0,
  MOZ: 0,
  508: 0,
  NI: 0,
  NIC: 0,
  558: 0,
  NP: 0,
  NPL: 0,
  524: 0,
  NZ: 0,
  NZL: 0,
  554: 0,
  PA: 0,
  PAN: 0,
  591: 0,
  PE: 0,
  PER: 0,
  604: 0,
  PH: 0,
  PHL: 0,
  608: 0,
  PK: 0,
  PAK: 0,
  586: 0,
  PR: 0,
  PRI: 0,
  630: 0,
  PY: 0,
  PRY: 0,
  600: 0,
  SA: 0,
  SAU: 0,
  682: 0,
  SG: 0,
  SGP: 0,
  702: 0,
  SV: 0,
  SLV: 0,
  222: 0,
  TH: 0,
  THA: 0,
  764: 0,
  TN: 0,
  TUN: 0,
  788: 0,
  TT: 0,
  TTO: 0,
  780: 0,
  TW: 0,
  TWN: 0,
  158: 0,
  UM: 0,
  UMI: 0,
  581: 0,
  US: 0,
  USA: 0,
  840: 0,
  VE: 0,
  VEN: 0,
  862: 0,
  VI: 0,
  VIR: 0,
  850: 0,
  WS: 0,
  WSM: 0,
  882: 0,
  YE: 0,
  YEM: 0,
  887: 0,
  ZA: 0,
  ZAF: 0,
  710: 0,
  ZW: 0,
  ZWE: 0,
  716: 0,
  AE: 6,
  ARE: 6,
  784: 6,
  AF: 6,
  AFG: 6,
  4: 6,
  BH: 6,
  BHR: 6,
  48: 6,
  DJ: 6,
  DJI: 6,
  262: 6,
  DZ: 6,
  DZA: 6,
  12: 6,
  EG: 6,
  EGY: 6,
  818: 6,
  IQ: 6,
  IRQ: 6,
  368: 6,
  IR: 6,
  IRN: 6,
  364: 6,
  JO: 6,
  JOR: 6,
  400: 6,
  KW: 6,
  KWT: 6,
  414: 6,
  LY: 6,
  LBY: 6,
  434: 6,
  MA: 6,
  MAR: 6,
  504: 6,
  OM: 6,
  OMN: 6,
  512: 6,
  QA: 6,
  QAT: 6,
  634: 6,
  SD: 6,
  SDN: 6,
  729: 6,
  SY: 6,
  SYR: 6,
  760: 6,
  BD: 5,
  BGD: 5,
  50: 5,
  MV: 5,
  MDV: 5,
  462: 5
};
var regionDayMap_default = regionDayMap;

// node_modules/.pnpm/weekstart@1.1.0/node_modules/weekstart/dist/es-module/main.js
function getWeekStartByLocale$1(locale) {
  return getWeekStartByLocale(locale, langRegionMap_default, regionDayMap_default);
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/utils/locale/normalize-start-of-week.js
function normalizeStartOfWeek(startOfWeek, locale) {
  return typeof startOfWeek === "number" ? startOfWeek % 7 : getWeekStartByLocale$1(locale);
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/hooks/use-date-cache/index.js
var import_react = __toESM(require_react());
function useDateCache() {
  const cacheRef = (0, import_react.useRef)(/* @__PURE__ */ new Map());
  return (date) => {
    const current = cacheRef.current.get(date.getTime());
    if (current) {
      return current;
    }
    cacheRef.current.set(date.getTime(), date);
    return date;
  };
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/calendar/utils/intl.js
function setDayIndex(date, dayIndex) {
  const diff = dayIndex - date.getDay();
  date.setDate(date.getDate() + diff);
}
function renderDayName(locale, dayIndex, mode) {
  const tempDate = /* @__PURE__ */ new Date();
  setDayIndex(tempDate, dayIndex);
  return tempDate.toLocaleDateString(locale, { weekday: mode });
}
function renderMonthAndYear(locale, baseDate) {
  const result = baseDate.toLocaleDateString(locale, {
    year: "numeric",
    month: "long"
  });
  return result;
}
function renderYear(locale, date) {
  return date.toLocaleDateString(locale, {
    year: "numeric"
  });
}
var dayLabelCache = /* @__PURE__ */ new Map();
function getDateLabel(locale, date, mode = "full") {
  const cacheKey = locale + date.getTime() + mode;
  const cachedValue = dayLabelCache.get(cacheKey);
  if (cachedValue) {
    return cachedValue;
  }
  const value = date.toLocaleDateString(locale, {
    weekday: mode === "full" ? "long" : void 0,
    month: "long",
    day: "numeric",
    year: "numeric"
  });
  dayLabelCache.set(cacheKey, value);
  return value;
}
function renderTimeLabel(locale, date, format) {
  let options = {};
  if (format === "hh") {
    options = { hour: "2-digit" };
  }
  if (format === "hh:mm") {
    options = { hour: "2-digit", minute: "2-digit" };
  }
  const value = date.toLocaleTimeString(locale, options);
  return value;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/calendar/utils/navigation-day.js
function moveNextDay(startDate, isDateFocusable) {
  return moveDay(startDate, isDateFocusable, 1);
}
function movePrevDay(startDate, isDateFocusable) {
  return moveDay(startDate, isDateFocusable, -1);
}
function moveNextWeek(startDate, isDateFocusable) {
  return moveDay(startDate, isDateFocusable, 7);
}
function movePrevWeek(startDate, isDateFocusable) {
  return moveDay(startDate, isDateFocusable, -7);
}
function getBaseDay(date, isDateFocusable) {
  const startDate = startOfMonth(date);
  if (isDateFocusable(startDate)) {
    return startDate;
  }
  const firstEnabledDate = moveDay(startDate, isDateFocusable, 1);
  return isSameMonth(startDate, firstEnabledDate) ? firstEnabledDate : startDate;
}
function moveDay(startDate, isDateFocusable, step) {
  const limitYears = 1;
  let current = addDays(startDate, step);
  while (!isDateFocusable(current)) {
    if (Math.abs(differenceInYears(startDate, current)) > limitYears) {
      return startDate;
    }
    current = addDays(current, step);
  }
  return current;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/calendar/utils/navigation-month.js
function moveNextMonth(startDate, isDateFocusable) {
  return moveMonth(startDate, isDateFocusable, 1);
}
function movePrevMonth(startDate, isDateFocusable) {
  return moveMonth(startDate, isDateFocusable, -1);
}
function moveMonthDown(startDate, isDateFocusable) {
  return moveMonth(startDate, isDateFocusable, 3);
}
function moveMonthUp(startDate, isDateFocusable) {
  return moveMonth(startDate, isDateFocusable, -3);
}
function getBaseMonth(date, isDateFocusable) {
  const startDate = startOfYear(date);
  if (isDateFocusable(startDate)) {
    return startDate;
  }
  const firstEnabledDate = moveMonth(startDate, isDateFocusable, 1);
  return isSameYear(startDate, firstEnabledDate) ? firstEnabledDate : startDate;
}
function moveMonth(startDate, isDateFocusable, step) {
  const limitYears = 10;
  let current = addMonths(startDate, step);
  while (!isDateFocusable(current)) {
    if (Math.abs(differenceInYears(startDate, current)) > limitYears) {
      return startDate;
    }
    current = addMonths(current, step);
  }
  return current;
}

// node_modules/.pnpm/mnth@2.0.0/node_modules/mnth/web/add-days.js
var addDays2 = function addDays3(date, days) {
  var result = new Date(date);
  result.setDate(result.getDate() + days);
  return result;
};

// node_modules/.pnpm/mnth@2.0.0/node_modules/mnth/web/set-date.js
var setDate = function setDate2(date, day) {
  var result = new Date(date);
  result.setDate(day);
  return result;
};

// node_modules/.pnpm/mnth@2.0.0/node_modules/mnth/web/index.js
function ownKeys(object, enumerableOnly) {
  var keys = Object.keys(object);
  if (Object.getOwnPropertySymbols) {
    var symbols = Object.getOwnPropertySymbols(object);
    if (enumerableOnly) symbols = symbols.filter(function(sym) {
      return Object.getOwnPropertyDescriptor(object, sym).enumerable;
    });
    keys.push.apply(keys, symbols);
  }
  return keys;
}
function _objectSpread(target) {
  for (var i = 1; i < arguments.length; i++) {
    var source = arguments[i] != null ? arguments[i] : {};
    if (i % 2) {
      ownKeys(Object(source), true).forEach(function(key) {
        _defineProperty(target, key, source[key]);
      });
    } else if (Object.getOwnPropertyDescriptors) {
      Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
      ownKeys(Object(source)).forEach(function(key) {
        Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
      });
    }
  }
  return target;
}
var DAYS_IN_WEEK = 7;
var getCalendarMonth = function getCalendarMonth2(date, options) {
  var _firstDayOfWeek$optio = _objectSpread({
    firstDayOfWeek: 1
  }, options), firstDayOfWeek = _firstDayOfWeek$optio.firstDayOfWeek;
  var firstDateOfMonth = new Date(date.getFullYear(), date.getMonth(), 1);
  var lastDateOfMonth = new Date(date.getFullYear(), date.getMonth() + 1, 0);
  var firstWeekdayOfMonth = firstDateOfMonth.getDay();
  var lastWeekdayOfMonth = lastDateOfMonth.getDay();
  var daysInMonth = lastDateOfMonth.getDate();
  var daysToPrepend = (firstWeekdayOfMonth - firstDayOfWeek + DAYS_IN_WEEK) % DAYS_IN_WEEK;
  var daysToAppend = (DAYS_IN_WEEK - 1 - lastWeekdayOfMonth + firstDayOfWeek) % DAYS_IN_WEEK;
  var month = [];
  var week = [];
  for (var i = 1 - daysToPrepend; i <= daysInMonth + daysToAppend + 1; i++) {
    if (i <= 0) {
      week.push(addDays2(firstDateOfMonth, i - 1));
    } else if (i > daysInMonth) {
      week.push(addDays2(lastDateOfMonth, i - daysInMonth));
    } else {
      week.push(setDate(date, i));
    }
    if (week.length === 7) {
      month.push(week);
      week = [];
    }
  }
  return month;
};

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/utils/date-time/calendar.js
function getCalendarYear(date) {
  const year = date.getFullYear();
  return new Array(4).fill(0).map((_, i) => new Array(3).fill(0).map((_2, j) => new Date(year, i * 3 + j)));
}
var MonthCalendar = class {
  constructor({ padDates, baseDate, startOfWeek, selection }) {
    this.padDates = padDates;
    this.weeks = [];
    this.range = [baseDate, baseDate];
    const allCalendarDates = getCalendarMonthWithSixRows(baseDate, { startOfWeek, padDates });
    this.weekdays = allCalendarDates[0].map((date) => date.getDay());
    const isDateVisible = (weekIndex, dayIndex) => {
      const week = allCalendarDates[weekIndex];
      const date = week === null || week === void 0 ? void 0 : week[dayIndex];
      if (!date) {
        return false;
      }
      switch (padDates) {
        case "before":
          return isSameMonth(date, baseDate) || isBefore(date, baseDate);
        case "after":
          return isSameMonth(date, baseDate) || isAfter(date, baseDate);
      }
    };
    const isDateInRange = (weekIndex, dayIndex) => {
      const week = allCalendarDates[weekIndex];
      const date = week === null || week === void 0 ? void 0 : week[dayIndex];
      return !!(date && selection && checkDateIsInRange(date, selection[0], selection[1]));
    };
    const getWeekTestIndex = (weekIndex) => {
      var _a;
      const week = allCalendarDates[weekIndex];
      if (!week) {
        return void 0;
      }
      if (!isSameMonth(week[0], baseDate) && !isSameMonth(week[week.length - 1], baseDate)) {
        return void 0;
      }
      return ((_a = getWeekTestIndex(weekIndex - 1)) !== null && _a !== void 0 ? _a : 0) + 1;
    };
    for (let weekIndex = 0; weekIndex < allCalendarDates.length; weekIndex++) {
      const daysOfWeek = allCalendarDates[weekIndex];
      const week = { days: [], testIndex: getWeekTestIndex(weekIndex) };
      for (let dayIndex = 0; dayIndex < daysOfWeek.length; dayIndex++) {
        const date = daysOfWeek[dayIndex];
        const isVisible = isDateVisible(weekIndex, dayIndex);
        const isSelected = !!(selection && (isSameDay(date, selection[0]) || isSameDay(date, selection[1])));
        const isInRange = isDateInRange(weekIndex, dayIndex);
        const isTop = isVisible && !isDateVisible(weekIndex - 1, dayIndex);
        const isBottom = isVisible && !isDateVisible(weekIndex + 1, dayIndex);
        const isLeft = isVisible && !isDateVisible(weekIndex, dayIndex - 1);
        const isRight = isVisible && !isDateVisible(weekIndex, dayIndex + 1);
        const isRangeTop = isInRange && !isDateInRange(weekIndex - 1, dayIndex);
        const isRangeBottom = isInRange && !isDateInRange(weekIndex + 1, dayIndex);
        const isRangeLeft = isInRange && !isDateInRange(weekIndex, dayIndex - 1);
        const isRangeRight = isInRange && !isDateInRange(weekIndex, dayIndex + 1);
        week.days.push({
          date,
          isVisible,
          isSelected,
          isInRange,
          isSelectionTop: isTop || isRangeTop,
          isSelectionBottom: isBottom || isRangeBottom,
          isSelectionLeft: isLeft || isRangeLeft,
          isSelectionRight: isRight || isRangeRight
        });
      }
      this.weeks.push(week);
    }
  }
};
var YearCalendar = class {
  constructor({ baseDate, selection }) {
    this.quarters = [];
    this.range = [baseDate, baseDate];
    const allCalendarMonths = getCalendarYear(baseDate);
    const isMonthInRange = (quarterIndex, monthIndex) => {
      const quarter = allCalendarMonths[quarterIndex];
      const month = quarter === null || quarter === void 0 ? void 0 : quarter[monthIndex];
      return !!(month && selection && checkMonthIsInRange(month, selection[0], selection[1]));
    };
    const isMonthVisible = (quarterIndex, monthIndex) => {
      const quarter = allCalendarMonths[quarterIndex];
      const month = quarter === null || quarter === void 0 ? void 0 : quarter[monthIndex];
      if (!month) {
        return false;
      }
      return isSameYear(month, baseDate);
    };
    for (let quarterIndex = 0; quarterIndex < allCalendarMonths.length; quarterIndex++) {
      const monthsOfQuarter = allCalendarMonths[quarterIndex];
      const quarter = { months: [] };
      for (let monthIndex = 0; monthIndex < monthsOfQuarter.length; monthIndex++) {
        const month = monthsOfQuarter[monthIndex];
        const isVisible = isMonthVisible(quarterIndex, monthIndex);
        const isSelected = !!(selection && (isSameMonth(month, selection[0]) || isSameMonth(month, selection[1])));
        const isInRange = isMonthInRange(quarterIndex, monthIndex);
        const isTop = isVisible && !isMonthVisible(quarterIndex - 1, monthIndex);
        const isBottom = isVisible && !isMonthVisible(quarterIndex + 1, monthIndex);
        const isLeft = isVisible && !isMonthVisible(quarterIndex, monthIndex - 1);
        const isRight = isVisible && !isMonthVisible(quarterIndex, monthIndex + 1);
        const isRangeTop = isInRange && !isMonthInRange(quarterIndex - 1, monthIndex);
        const isRangeBottom = isInRange && !isMonthInRange(quarterIndex + 1, monthIndex);
        const isRangeLeft = isInRange && !isMonthInRange(quarterIndex, monthIndex - 1);
        const isRangeRight = isInRange && !isMonthInRange(quarterIndex, monthIndex + 1);
        quarter.months.push({
          date: month,
          isVisible,
          isSelected,
          isInRange,
          isSelectionTop: isTop || isRangeTop,
          isSelectionBottom: isBottom || isRangeBottom,
          isSelectionLeft: isLeft || isRangeLeft,
          isSelectionRight: isRight || isRangeRight
        });
      }
      this.quarters.push(quarter);
    }
  }
};
function getCalendarMonthWithSixRows(date, { startOfWeek, padDates }) {
  switch (padDates) {
    case "before":
      return [...getPrevMonthRows(date, startOfWeek), ...getCurrentMonthRows(date, startOfWeek)].slice(-6);
    case "after":
      return [...getCurrentMonthRows(date, startOfWeek), ...getNextMonthRows(date, startOfWeek)].slice(0, 6);
  }
}
function checkDateIsInRange(date, dateOne, dateTwo) {
  if (!dateOne || !dateTwo || isSameDay(dateOne, dateTwo)) {
    return false;
  }
  const inRange = isAfter(date, dateOne) && isBefore(date, dateTwo) || isAfter(date, dateTwo) && isBefore(date, dateOne);
  return inRange || isSameDay(date, dateOne) || isSameDay(date, dateTwo);
}
function checkMonthIsInRange(date, dateOne, dateTwo) {
  if (!dateOne || !dateTwo || isSameMonth(dateOne, dateTwo)) {
    return false;
  }
  const inRange = isAfter(date, dateOne) && isBefore(date, dateTwo) || isAfter(date, dateTwo) && isBefore(date, dateOne);
  return inRange || isSameMonth(date, dateOne) || isSameMonth(date, dateTwo);
}
function getCurrentMonthRows(date, firstDayOfWeek) {
  return getCalendarMonth(date, { firstDayOfWeek });
}
function getPrevMonthRows(date, firstDayOfWeek) {
  const rows = getCalendarMonth(subMonths(date, 1), { firstDayOfWeek });
  const lastDay = rows[rows.length - 1][rows[rows.length - 1].length - 1];
  return !isSameMonth(date, lastDay) ? rows : rows.slice(0, -1);
}
function getNextMonthRows(date, firstDayOfWeek) {
  const rows = getCalendarMonth(addMonths(date, 1), { firstDayOfWeek });
  const firstDay = rows[0][0];
  return !isSameMonth(date, firstDay) ? rows : rows.slice(1);
}

export {
  useDateCache,
  normalizeLocale,
  normalizeStartOfWeek,
  renderDayName,
  renderMonthAndYear,
  renderYear,
  getDateLabel,
  renderTimeLabel,
  moveNextDay,
  movePrevDay,
  moveNextWeek,
  movePrevWeek,
  getBaseDay,
  moveNextMonth,
  movePrevMonth,
  moveMonthDown,
  moveMonthUp,
  getBaseMonth,
  getCalendarYear,
  MonthCalendar,
  YearCalendar,
  getCalendarMonthWithSixRows
};
//# sourceMappingURL=chunk-GCL5ABXS.js.map
