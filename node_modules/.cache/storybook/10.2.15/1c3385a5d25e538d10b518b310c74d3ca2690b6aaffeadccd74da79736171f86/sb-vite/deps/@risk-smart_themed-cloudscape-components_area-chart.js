import {
  AsyncStore,
  useReaction,
  useSelector
} from "./chunk-FGGVOXKR.js";
import {
  throttle
} from "./chunk-HM4DTRJ5.js";
import {
  CartesianChartContainer,
  ChartScale,
  NumericChartScale,
  axis_label_default,
  block_end_labels_default,
  createXTicks,
  createYTicks,
  emphasized_baseline_default,
  getXTickCount,
  getYTickCount,
  highlighted_point_default,
  inline_start_labels_default,
  labels_measure_default,
  useBLockEndLabels,
  vertical_marker_default
} from "./chunk-O6Q6X3CQ.js";
import {
  ChartStatusContainer,
  ChartWrapper,
  area_default,
  chart_filter_default,
  chart_legend_default,
  chart_plot_default,
  chart_popover_default,
  chart_series_details_default,
  chart_series_marker_default,
  createCategoryColorScale,
  getChartStatus,
  line_default,
  useHeightMeasure
} from "./chunk-WPMMI74K.js";
import "./chunk-ZN7Q6JBX.js";
import "./chunk-5QZRTO2M.js";
import "./chunk-JTTTGOK7.js";
import "./chunk-BVLN4GVJ.js";
import {
  circleIndex
} from "./chunk-XVMNXRED.js";
import "./chunk-FWATUNHA.js";
import "./chunk-H7XOVI53.js";
import "./chunk-RSPX7ILA.js";
import "./chunk-BRZGKKI5.js";
import "./chunk-4BPVAR5R.js";
import "./chunk-7EV5QVO4.js";
import "./chunk-NM2UFPHB.js";
import "./chunk-S5DE7ROA.js";
import "./chunk-VAKUJE6P.js";
import "./chunk-KEN4GW4C.js";
import "./chunk-YR6QDLZD.js";
import "./chunk-2QKXW3TF.js";
import "./chunk-DROBD2RG.js";
import "./chunk-X6AS5CV5.js";
import "./chunk-G5QK7KVI.js";
import "./chunk-Z7SMG3UP.js";
import "./chunk-AGU3Y3FJ.js";
import "./chunk-CHQYBZGX.js";
import "./chunk-EG3L6CGG.js";
import "./chunk-MVYXSD5C.js";
import "./chunk-FHKRNWSU.js";
import "./chunk-ZIK6ZP5D.js";
import "./chunk-VAZCZFSH.js";
import "./chunk-7LZWD7WQ.js";
import "./chunk-NZVEIX2D.js";
import "./chunk-7RZQNPHL.js";
import "./chunk-2JWHQWEQ.js";
import {
  useContainerWidth
} from "./chunk-BHF4AY7K.js";
import "./chunk-DFAFZNCO.js";
import {
  handleKey
} from "./chunk-AJHPKFY3.js";
import "./chunk-PBSKSJXF.js";
import {
  useControllable
} from "./chunk-IJHQZZNK.js";
import "./chunk-3REGT5OA.js";
import "./chunk-QHUEVSAF.js";
import "./chunk-JPVH3OCQ.js";
import "./chunk-ZHEGEKED.js";
import "./chunk-74T3HYB2.js";
import "./chunk-P6JAMUWB.js";
import "./chunk-FBDHOJS3.js";
import "./chunk-UR5B5LXM.js";
import "./chunk-SBBKGBX6.js";
import "./chunk-MOSKSJRE.js";
import "./chunk-6EHERSIB.js";
import "./chunk-VBZQI6EI.js";
import "./chunk-NYZXY5XT.js";
import "./chunk-YW7RACHJ.js";
import "./chunk-FUGZCOSR.js";
import "./chunk-UL2YV4UJ.js";
import "./chunk-EN3KSVNS.js";
import "./chunk-4YGTG4S2.js";
import "./chunk-BTOJ6BB7.js";
import "./chunk-RK5MFOAL.js";
import {
  useInternalI18n
} from "./chunk-SCG5NBRN.js";
import {
  KeyCode
} from "./chunk-RQYQ76PG.js";
import "./chunk-QIVFBN6U.js";
import "./chunk-FMTTQ2N7.js";
import "./chunk-J5NXX4C5.js";
import {
  fireNonCancelableEvent
} from "./chunk-QRSSPSED.js";
import "./chunk-WQWUW5P3.js";
import "./chunk-J5N6WJQC.js";
import "./chunk-XAJCD2Q5.js";
import "./chunk-RVZ7X5AK.js";
import {
  nodeBelongs
} from "./chunk-BJH6AXTS.js";
import "./chunk-JXM4BF5H.js";
import {
  useUniqueId
} from "./chunk-Z3FGJ2XJ.js";
import {
  useMergeRefs
} from "./chunk-4UVHBYQ2.js";
import {
  applyDisplayName,
  getBaseProps,
  isDevelopment,
  useBaseComponent
} from "./chunk-A5JTL534.js";
import {
  clsx_default
} from "./chunk-HPPI4HBL.js";
import {
  getIsRtl,
  nodeContains,
  useStableCallback,
  warnOnce
} from "./chunk-CMYEOKCY.js";
import {
  __rest
} from "./chunk-XSNQIJR6.js";
import "./chunk-YXFYVPI7.js";
import {
  require_react
} from "./chunk-YHCPMRL2.js";
import {
  __toESM
} from "./chunk-PR4QN5HX.js";

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/index.js
var import_react14 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/internal.js
var import_react13 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/chart-container.js
var import_react7 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/chart-popover.js
var import_react = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/styles.css.js
import "/Users/jamesromero/Library/CloudStorage/OneDrive-RiskSmart/Product - Product Dream Team/Product-team-storybook/node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/styles.scoped.css";
var styles_css_default = {
  "series": "awsui_series_y1yrh_195jp_189",
  "root": "awsui_root_y1yrh_195jp_203",
  "series--highlighted": "awsui_series--highlighted_y1yrh_195jp_215",
  "series--dimmed": "awsui_series--dimmed_y1yrh_195jp_219",
  "series--threshold": "awsui_series--threshold_y1yrh_195jp_223",
  "popover-divider": "awsui_popover-divider_y1yrh_195jp_228"
};

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/chart-popover.js
function AreaChartPopover({ model, highlightDetails, dismissAriaLabel, footer, size, onBlur }) {
  if (!highlightDetails) {
    return null;
  }
  const popoverProps = {
    title: highlightDetails.formattedX,
    trackRef: model.refs.verticalMarker,
    trackKey: highlightDetails.highlightIndex,
    dismissButton: highlightDetails.isPopoverPinned,
    onDismiss: model.handlers.onPopoverDismiss,
    onMouseLeave: model.handlers.onPopoverLeave,
    ref: model.refs.popoverRef
  };
  return import_react.default.createElement(
    chart_popover_default,
    Object.assign({}, popoverProps, { container: model.refs.container.current, dismissAriaLabel, size, onBlur, footer }),
    import_react.default.createElement(chart_series_details_default, { details: highlightDetails.seriesDetails }),
    import_react.default.createElement("div", { className: styles_css_default["popover-divider"] }),
    import_react.default.createElement(chart_series_details_default, { details: highlightDetails.totalDetails })
  );
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/data-series.js
var import_react4 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/area-series.js
var import_react2 = __toESM(require_react());
var area_series_default = (0, import_react2.memo)(AreaSeries);
function AreaSeries({ data, color, chartAreaClipPath }) {
  const areaGenerator = area_default().x((p) => p.scaled.x).y0((p) => p.scaled.y0).y1((p) => p.scaled.y1);
  const areaPath = areaGenerator(data) || "";
  const lineGenerator = line_default().x((p) => p.scaled.x).y((p) => p.scaled.y1);
  const linePath = lineGenerator(data) || "";
  return import_react2.default.createElement(
    import_react2.default.Fragment,
    null,
    import_react2.default.createElement("path", { "aria-hidden": true, fill: color, stroke: color, style: { opacity: 0.4 }, clipPath: `url(#${chartAreaClipPath})`, d: areaPath }),
    import_react2.default.createElement("path", { "aria-hidden": true, stroke: color, clipPath: `url(#${chartAreaClipPath})`, d: linePath })
  );
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/threshold-series.js
var import_react3 = __toESM(require_react());
var threshold_series_default = (0, import_react3.memo)(ThresholdSeries);
function ThresholdSeries({ data, xScale, color, chartAreaClipPath }) {
  const range = xScale.d3Scale.range();
  const y = data[0].scaled.y0;
  const path = { x1: range[0], x2: range[1], y1: y, y2: y };
  return import_react3.default.createElement("line", Object.assign({ "aria-hidden": true, stroke: color, clipPath: `url(#${chartAreaClipPath})` }, path));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/data-series.js
var data_series_default = (0, import_react4.memo)(DataSeries);
function DataSeries({ model }) {
  const chartAreaClipPath = useUniqueId("awsui-area-chart__chart-area-");
  const highlightedX = useSelector(model.interactions, (state) => state.highlightedX);
  const highlightedSeries = useSelector(model.interactions, (state) => state.highlightedSeries);
  const useHighlightDimmed = !highlightedX;
  const seriesData = [];
  for (let seriesIndex = model.series.length - 1; seriesIndex >= 0; seriesIndex--) {
    if (model.computed.plot.sx[seriesIndex]) {
      seriesData.push([model.series[seriesIndex], model.computed.plot.sx[seriesIndex] || []]);
    }
  }
  return import_react4.default.createElement(
    import_react4.default.Fragment,
    null,
    import_react4.default.createElement(
      "defs",
      { "aria-hidden": "true" },
      import_react4.default.createElement(
        "clipPath",
        { id: chartAreaClipPath },
        import_react4.default.createElement("rect", { x: 0, y: 0, width: model.width, height: model.height })
      )
    ),
    import_react4.default.createElement("g", { role: "group" }, seriesData.map(([series, data]) => {
      const isHighlighted = series === highlightedSeries;
      const isDimmed = !!highlightedSeries && !isHighlighted;
      return import_react4.default.createElement("g", { key: series.title, role: "group", "aria-label": series.title, className: clsx_default(styles_css_default.series, styles_css_default[`series--${series.type}`], {
        [styles_css_default["series--highlighted"]]: isHighlighted,
        [styles_css_default["series--dimmed"]]: useHighlightDimmed && isDimmed
      }) }, series.type === "area" ? import_react4.default.createElement(area_series_default, { data, color: model.getInternalSeries(series).color, chartAreaClipPath }) : import_react4.default.createElement(threshold_series_default, { data, xScale: model.computed.xScale, color: model.getInternalSeries(series).color, chartAreaClipPath }));
    }))
  );
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/highlighted-point.js
var import_react5 = __toESM(require_react());
var highlighted_point_default2 = (0, import_react5.memo)((0, import_react5.forwardRef)(AreaHighlightedPoint));
function AreaHighlightedPoint({ model, ariaLabel }, ref) {
  const highlightedPoint = useSelector(model.interactions, (state) => state.highlightedPoint);
  const isPopoverPinned = useSelector(model.interactions, (state) => state.isPopoverPinned);
  const point = highlightedPoint ? {
    key: `${highlightedPoint.index.x}:${highlightedPoint.index.s}`,
    x: highlightedPoint.scaled.x,
    y: highlightedPoint.scaled.y1,
    color: model.getInternalSeries(model.series[highlightedPoint.index.s]).color
  } : null;
  return import_react5.default.createElement(highlighted_point_default, { ref, point, role: "button", ariaLabel, ariaHasPopup: true, ariaExpanded: isPopoverPinned });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/use-highlight-details.js
function useHighlightDetails({ model, xTickFormatter, yTickFormatter, detailTotalFormatter, detailTotalLabel }) {
  const i18n = useInternalI18n("area-chart");
  const hX = useSelector(model.interactions, (state) => state.highlightedX);
  const hPoint = useSelector(model.interactions, (state) => state.highlightedPoint);
  const isPopoverPinned = useSelector(model.interactions, (state) => state.isPopoverPinned);
  if (!hX) {
    return null;
  }
  const [firstPoint] = hX;
  const highlightIndex = firstPoint.index.x;
  const seriesPoints = [...model.computed.plot.xs[highlightIndex]];
  const detailsTotal = seriesPoints.reduce((total, point) => total + point.value, 0);
  const formattedX = xTickFormatter ? xTickFormatter(firstPoint.x) : firstPoint.x.toString();
  const seriesTitle = hPoint ? getInternalSeries(hPoint).title : "";
  const formattedY = hPoint ? getInternalSeries(hPoint).formatValue(hPoint.value, hPoint.x) : "";
  const seriesDetails = seriesPoints.map((point) => {
    const { title, formatValue, color, markerType } = getInternalSeries(point);
    const isDimmed = Boolean(hPoint) && point !== hPoint;
    return { key: title, value: formatValue(point.value, point.x), color, markerType, isDimmed };
  });
  const totalDetails = [
    {
      key: i18n("i18nStrings.detailTotalLabel", detailTotalLabel) || "",
      value: detailTotalFormatter ? detailTotalFormatter(detailsTotal) : yTickFormatter ? yTickFormatter(detailsTotal) : detailsTotal
    }
  ];
  const pointDetails = hPoint ? seriesDetails[hPoint.index.s] : null;
  const activeLabel = `${seriesTitle} ${formattedX} ${formattedY}, ${totalDetails[0].key} ${totalDetails[0].value}`;
  return {
    isPopoverPinned,
    highlightIndex,
    formattedX,
    seriesTitle,
    formattedY,
    activeLabel,
    seriesDetails,
    totalDetails,
    pointDetails
  };
  function getInternalSeries(point) {
    return model.getInternalSeries(model.series[point.index.s]);
  }
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/vertical-marker.js
var import_react6 = __toESM(require_react());
var vertical_marker_default2 = (0, import_react6.memo)(AreaVerticalMarker);
function AreaVerticalMarker({ model }) {
  const highlightedX = useSelector(model.interactions, (state) => state.highlightedX);
  const verticalMarker = (highlightedX || []).map((point) => ({
    key: `${point.index.x}:${point.index.s}`,
    x: point.scaled.x,
    y: point.scaled.y1,
    color: model.getInternalSeries(model.series[point.index.s]).color
  }));
  return import_react6.default.createElement(vertical_marker_default, { height: model.height, points: verticalMarker, ref: model.refs.verticalMarker });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/chart-container.js
var DEFAULT_CHART_WIDTH = 500;
var INLINE_START_LABELS_MARGIN = 16;
var BLOCK_END_LABELS_OFFSET = 12;
var chart_container_default = (0, import_react7.memo)(ChartContainer);
function ChartContainer({ model, autoWidth, xTitle, yTitle, detailPopoverSize, detailPopoverFooter, ariaLabel, ariaLabelledby, ariaDescription, i18nStrings: { xTickFormatter: deprecatedXTickFormatter, yTickFormatter: deprecatedYTickFormatter, detailTotalFormatter: deprecatedDetailTotalFormatter, detailTotalLabel, chartAriaRoleDescription, xAxisAriaRoleDescription, yAxisAriaRoleDescription, detailPopoverDismissAriaLabel } = {}, fitHeight, hasFilters, minHeight, xTickFormatter = deprecatedXTickFormatter, yTickFormatter = deprecatedYTickFormatter, detailTotalFormatter = deprecatedDetailTotalFormatter, isRTL }) {
  const [inlineStartLabelsWidth, setInlineStartLabelsWidth] = (0, import_react7.useState)(0);
  const [containerWidth, containerWidthRef] = useContainerWidth(DEFAULT_CHART_WIDTH);
  const maxInlineStartLabelsWidth = Math.round(containerWidth / 2);
  const blockEndLabelsProps = useBLockEndLabels({
    ticks: model.computed.xTicks,
    scale: model.computed.xScale,
    tickFormatter: xTickFormatter
  });
  const plotWidth = Math.max(0, containerWidth - inlineStartLabelsWidth - INLINE_START_LABELS_MARGIN);
  (0, import_react7.useEffect)(() => {
    autoWidth(plotWidth);
  }, [autoWidth, plotWidth]);
  const highlightDetails = useHighlightDetails({
    model,
    xTickFormatter,
    yTickFormatter,
    detailTotalFormatter,
    detailTotalLabel
  });
  const highlightedPointRef = (0, import_react7.useRef)(null);
  const mergedRef = useMergeRefs(containerWidthRef, model.refs.container);
  const isPointHighlighted = model.interactions.get().highlightedPoint !== null;
  const highlightedX = useSelector(model.interactions, (state) => state.highlightedX);
  const detailPopoverFooterContent = (0, import_react7.useMemo)(() => detailPopoverFooter && highlightedX ? detailPopoverFooter(highlightedX[0].x) : null, [detailPopoverFooter, highlightedX]);
  return import_react7.default.createElement(CartesianChartContainer, { ref: mergedRef, minHeight: minHeight + blockEndLabelsProps.height, fitHeight: !!fitHeight, hasFilters, leftAxisLabel: import_react7.default.createElement(axis_label_default, { axis: "y", position: "left", title: yTitle }), leftAxisLabelMeasure: import_react7.default.createElement(labels_measure_default, { scale: model.computed.yScale, ticks: model.computed.yTicks, tickFormatter: yTickFormatter, autoWidth: setInlineStartLabelsWidth, maxLabelsWidth: maxInlineStartLabelsWidth }), bottomAxisLabel: import_react7.default.createElement(axis_label_default, { axis: "x", position: "bottom", title: xTitle }), chartPlot: import_react7.default.createElement(
    chart_plot_default,
    { ref: model.refs.plot, width: "100%", height: fitHeight ? `calc(100% - ${blockEndLabelsProps.height}px)` : model.height, offsetBottom: blockEndLabelsProps.height, ariaLabel, ariaLabelledby, ariaDescription, ariaRoleDescription: chartAriaRoleDescription, activeElementKey: !(highlightDetails === null || highlightDetails === void 0 ? void 0 : highlightDetails.isPopoverPinned) && (highlightDetails === null || highlightDetails === void 0 ? void 0 : highlightDetails.activeLabel), activeElementRef: isPointHighlighted ? highlightedPointRef : model.refs.verticalMarker, activeElementFocusOffset: isPointHighlighted ? 3 : { x: 8, y: 0 }, isClickable: !(highlightDetails === null || highlightDetails === void 0 ? void 0 : highlightDetails.isPopoverPinned), onMouseMove: model.handlers.onSVGMouseMove, onMouseOut: model.handlers.onSVGMouseOut, onClick: model.handlers.onSVGMouseDown, onKeyDown: model.handlers.onSVGKeyDown, onApplicationFocus: model.handlers.onApplicationFocus, onApplicationBlur: model.handlers.onApplicationBlur },
    import_react7.default.createElement("line", { ref: model.refs.plotMeasure, x1: "0", x2: "0", y1: "0", y2: "100%", stroke: "transparent", strokeWidth: 1, style: { pointerEvents: "none" } }),
    import_react7.default.createElement(inline_start_labels_default, { plotWidth: model.width, plotHeight: model.height, scale: model.computed.yScale, ticks: model.computed.yTicks, tickFormatter: yTickFormatter, title: yTitle, ariaRoleDescription: yAxisAriaRoleDescription, maxLabelsWidth: maxInlineStartLabelsWidth }),
    import_react7.default.createElement(data_series_default, { model }),
    import_react7.default.createElement(block_end_labels_default, Object.assign({}, blockEndLabelsProps, { width: model.width, height: model.height, scale: model.computed.xScale, title: xTitle, ariaRoleDescription: xAxisAriaRoleDescription, offsetLeft: inlineStartLabelsWidth + BLOCK_END_LABELS_OFFSET, offsetRight: BLOCK_END_LABELS_OFFSET, isRTL })),
    import_react7.default.createElement(emphasized_baseline_default, { width: model.width, height: model.height, scale: model.computed.yScale }),
    import_react7.default.createElement(vertical_marker_default2, { model }),
    import_react7.default.createElement(highlighted_point_default2, { ref: highlightedPointRef, model, ariaLabel: highlightDetails === null || highlightDetails === void 0 ? void 0 : highlightDetails.activeLabel })
  ), popover: import_react7.default.createElement(AreaChartPopover, { model, highlightDetails, dismissAriaLabel: detailPopoverDismissAriaLabel, size: detailPopoverSize, footer: detailPopoverFooterContent, onBlur: model.handlers.onApplicationBlur }) });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/area-chart-filter.js
var import_react8 = __toESM(require_react());
var area_chart_filter_default = (0, import_react8.memo)(AreaChartFilter);
function AreaChartFilter({ model, filterLabel, filterPlaceholder, filterSelectedAriaLabel }) {
  const filterItems = model.allSeries.map((s) => {
    const { title, color, markerType } = model.getInternalSeries(s);
    return {
      label: title,
      marker: import_react8.default.createElement(chart_series_marker_default, { type: markerType, color }),
      datum: s
    };
  });
  return import_react8.default.createElement(chart_filter_default, { series: filterItems, onChange: model.handlers.onFilterSeries, selectedSeries: model.series, i18nStrings: {
    filterLabel,
    filterPlaceholder,
    filterSelectedAriaLabel
  } });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/elements/area-chart-legend.js
var import_react9 = __toESM(require_react());
var area_chart_legend_default = (0, import_react9.memo)(AreaChartLegend);
function AreaChartLegend({ model, legendTitle, ariaLabel, plotContainerRef }) {
  const legendItems = (0, import_react9.useMemo)(() => model.series.map((s) => {
    const { title, color, markerType } = model.getInternalSeries(s);
    return { label: title, marker: import_react9.default.createElement(chart_series_marker_default, { color, type: markerType }), datum: s };
  }), [model]);
  const legendSeries = useSelector(model.interactions, (state) => state.legendSeries);
  return import_react9.default.createElement(chart_legend_default, { series: legendItems, highlightedSeries: legendSeries, onHighlightChange: model.handlers.onLegendHighlight, legendTitle, ariaLabel, plotContainerRef });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/use-chart-model.js
var import_react10 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/utils.js
var EPSILON = 1e-13;
function computeDomainX(series) {
  const xValues = getXValues(series);
  if (xValues.length === 0) {
    return [];
  }
  if (typeof xValues[0] === "string") {
    return uniq(xValues);
  }
  return xValues.reduce(([min, max], x) => [x < min ? x : min, max < x ? x : max], [xValues[0], xValues[0]]);
}
function computeDomainY(series, scaleType) {
  let min = Number.POSITIVE_INFINITY;
  let max = Number.NEGATIVE_INFINITY;
  series.forEach((s) => {
    if (s.type === "threshold") {
      min = Math.min(min, s.y);
      max = Math.max(max, s.y);
    }
  });
  getXValues(series).forEach((_, xIndex) => {
    var _a;
    let stackY = scaleType === "linear" ? 0 : EPSILON;
    for (const s of series) {
      if (s.type === "area") {
        stackY = stackY + (((_a = s.data[xIndex]) === null || _a === void 0 ? void 0 : _a.y) || 0);
        min = Math.min(min, stackY);
        max = Math.max(max, stackY);
      }
    }
  });
  if (min === Number.POSITIVE_INFINITY) {
    return [];
  }
  if (scaleType === "log" && min === 0 && max > 1) {
    return [1, max];
  }
  return [min, max];
}
function computePlotPoints(series, xScale, yScale) {
  const xValues = getXValues(series);
  const xy = [];
  const xs = [];
  const sx = [];
  getVisibleData(xValues, xScale).forEach(({ x, scaledX }, xIndex) => {
    let stackY = yScale.scaleType === "linear" ? 0 : EPSILON;
    const points = [];
    series.forEach((s, sIndex) => {
      var _a;
      if (s.type === "threshold") {
        const scaledY = yScale.d3Scale(s.y) || 0;
        points.push({
          x,
          y0: s.y,
          y1: s.y,
          scaled: { x: scaledX, y0: scaledY, y1: scaledY },
          index: { x: xIndex, s: sIndex, y: 0 },
          value: 0
        });
      } else {
        const value = ((_a = s.data[xIndex]) === null || _a === void 0 ? void 0 : _a.y) || 0;
        const y0 = stackY;
        const y1 = stackY + value;
        points.push({
          x,
          y0,
          y1,
          scaled: { x: scaledX, y0: yScale.d3Scale(y0) || 0, y1: yScale.d3Scale(y1) || 0 },
          index: { x: xIndex, s: sIndex, y: 0 },
          value
        });
        stackY = y1;
      }
    });
    points.sort((p1, p2) => p1.y1 - p2.y1).forEach((point, index) => {
      point.index.y = index;
      insertIntoMatrix(xy, point.index.x, point.index.y, point);
      insertIntoMatrix(xs, point.index.x, point.index.s, point);
      insertIntoMatrix(sx, point.index.s, point.index.x, point);
    });
  });
  return { xy, xs, sx };
}
function findClosest(sortedArray, target, getter) {
  if (sortedArray.length === 0) {
    throw new Error("Invariant violation: array is empty.");
  }
  const isAscending = getter(sortedArray[0]) < getter(sortedArray[sortedArray.length - 1]);
  const compare = (x) => isAscending ? getter(x) < target : getter(x) > target;
  const delta = (x) => Math.abs(getter(x) - target);
  let lo = 0;
  let hi = sortedArray.length - 1;
  while (hi - lo > 1) {
    const mid = Math.floor((lo + hi) / 2);
    if (compare(sortedArray[mid])) {
      lo = mid;
    } else {
      hi = mid;
    }
  }
  return delta(sortedArray[lo]) < delta(sortedArray[hi]) ? sortedArray[lo] : sortedArray[hi];
}
function isSeriesValid(series) {
  var _a;
  const sampleXValues = getXValues(series);
  for (const s of series) {
    if (s.type === "area") {
      for (let i = 0; i < Math.max(s.data.length, sampleXValues.length); i++) {
        if (((_a = s.data[i]) === null || _a === void 0 ? void 0 : _a.x) !== sampleXValues[i]) {
          return false;
        }
      }
    }
  }
  return true;
}
function getXValues(series) {
  for (const s of series) {
    if (s.type === "area") {
      return s.data.map(({ x }) => x);
    }
  }
  return [];
}
function getVisibleData(data, xScale) {
  const scaledOffsetX = xScale.isCategorical() ? Math.max(0, xScale.d3Scale.bandwidth() - 1) / 2 : 0;
  const visibleData = [];
  for (const x of data) {
    const scaledX = xScale.d3Scale(x);
    if (scaledX !== void 0) {
      visibleData.push({ x, scaledX: scaledX + scaledOffsetX });
    }
  }
  return visibleData;
}
function insertIntoMatrix(matrix, row, col, value) {
  if (!matrix[row]) {
    matrix[row] = [];
  }
  matrix[row][col] = value;
}
function uniq(arr) {
  const set = /* @__PURE__ */ new Set();
  const uniqArray = [];
  for (const value of arr) {
    if (!set.has(value)) {
      set.add(value);
      uniqArray.push(value);
    }
  }
  return uniqArray;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/compute-chart-props.js
function computeChartProps({ isRtl, series, xDomain: externalXDomain, yDomain: externalYDomain, xScaleType, yScaleType, height, width }) {
  const xDomain = externalXDomain ? [...externalXDomain] : computeDomainX(series);
  const xTickCount = getXTickCount(width);
  const xScale = new ChartScale(xScaleType, xDomain, !isRtl ? [0, width] : [width, 0]);
  const xTicks = xScale.domain.length > 0 ? createXTicks(xScale, xTickCount) : [];
  const yDomain = externalYDomain || computeDomainY(series, yScaleType);
  const yTickCount = getYTickCount(height);
  const yScale = new NumericChartScale(yScaleType, yDomain, [height, 0], externalYDomain ? null : yTickCount);
  const yTicks = createYTicks(yScale, yTickCount);
  const plot = computePlotPoints(series, xScale, yScale);
  return { xDomain, yDomain, xScale, yScale, xTicks, yTicks, plot };
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/create-series-decorator.js
function createSeriesDecorator(externalSeries) {
  const colorScale = createCategoryColorScale(externalSeries, (s) => s.type === "threshold", (s) => s.color || null);
  const decorateSeries = (s, index) => {
    const title = s.title;
    const color = colorScale[index];
    const markerType = s.type === "area" ? "hollow-rectangle" : "dashed";
    const formatValue = s.type === "threshold" ? () => s.valueFormatter ? s.valueFormatter(s.y) : s.y : (y, x) => s.valueFormatter ? s.valueFormatter(y, x) : y;
    return { series: s, title, color, markerType, formatValue };
  };
  const mapping = externalSeries.reduce((map, series, index) => {
    map.set(series, decorateSeries(series, index));
    return map;
  }, /* @__PURE__ */ new Map());
  const seriesDecorator = (series) => mapping.get(series) || decorateSeries(series, externalSeries.length);
  return seriesDecorator;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/interactions-store.js
var initialState = Object.freeze({
  highlightedX: null,
  highlightedPoint: null,
  highlightedSeries: null,
  legendSeries: null,
  isPopoverPinned: false
});
var InteractionsStore = class extends AsyncStore {
  constructor(series, plot) {
    super(initialState);
    this.series = series;
    this.plot = plot;
  }
  highlightPoint(point) {
    this.set((state) => Object.assign(Object.assign({}, state), { highlightedX: this.plot.xy[point.index.x], highlightedPoint: point, highlightedSeries: this.series[point.index.s], legendSeries: this.series[point.index.s] }));
  }
  highlightX(points) {
    this.set((state) => Object.assign(Object.assign({}, state), { highlightedX: points, highlightedPoint: null, highlightedSeries: null, legendSeries: null }));
  }
  highlightFirstPoint() {
    this.set((state) => {
      const series = state.legendSeries || state.highlightedSeries;
      const firstSeriesPoint = series && this._getFirstSeriesPoint(series);
      const point = state.highlightedPoint || firstSeriesPoint || this.plot.sx[0][0];
      return Object.assign(Object.assign({}, state), { highlightedX: this.plot.xy[point.index.x], highlightedPoint: point, highlightedSeries: this.series[point.index.s], legendSeries: this.series[point.index.s] });
    });
  }
  highlightSeries(s) {
    this.set((state) => Object.assign(Object.assign({}, state), { highlightedSeries: s, legendSeries: s }));
  }
  clearHighlight() {
    this.set((state) => Object.assign(Object.assign({}, state), { highlightedX: null, highlightedPoint: null, highlightedSeries: null }));
  }
  clearHighlightedLegend() {
    this.set((state) => Object.assign(Object.assign({}, state), { legendSeries: null }));
  }
  clearState() {
    this.set(() => initialState);
  }
  pinPopover() {
    this.set((state) => Object.assign(Object.assign({}, state), { isPopoverPinned: true }));
  }
  unpinPopover() {
    this.set((state) => Object.assign(Object.assign({}, state), { isPopoverPinned: false }));
  }
  togglePopoverPin() {
    this.set((state) => Object.assign(Object.assign({}, state), { isPopoverPinned: !state.isPopoverPinned }));
  }
  _getFirstSeriesPoint(s) {
    const seriesIndex = this.series.indexOf(s);
    const [firstSeriesPoint] = this.plot.sx[seriesIndex] || [];
    return firstSeriesPoint || null;
  }
};

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/use-chart-model.js
var MAX_HOVER_MARGIN = 6;
var SVG_HOVER_THROTTLE = 25;
var POPOVER_DEADZONE = 12;
function useChartModel({ isRtl, fitHeight, externalSeries: allSeries, visibleSeries: series, setVisibleSeries, highlightedSeries, setHighlightedSeries, xDomain, yDomain, xScaleType, yScaleType, height: explicitHeight, width, popoverRef, statusType }) {
  var _a;
  const plotRef = (0, import_react10.useRef)(null);
  const containerRef = (0, import_react10.useRef)(null);
  const verticalMarkerRef = (0, import_react10.useRef)(null);
  const plotMeasureRef = (0, import_react10.useRef)(null);
  const hasVisibleSeries = series.length > 0;
  const height = (_a = useHeightMeasure(() => plotMeasureRef.current, !fitHeight, [hasVisibleSeries, statusType])) !== null && _a !== void 0 ? _a : explicitHeight;
  const stableSetVisibleSeries = useStableCallback(setVisibleSeries);
  const model = (0, import_react10.useMemo)(() => {
    const computed = computeChartProps({
      isRtl,
      series,
      xDomain,
      yDomain,
      xScaleType,
      yScaleType,
      height,
      width
    });
    const interactions = new InteractionsStore(series, computed.plot);
    const containsMultipleSeries = interactions.series.length > 1;
    const getInternalSeries = createSeriesDecorator(allSeries);
    const isMouseOverPopover = (clientX, clientY) => {
      var _a2;
      if ((_a2 = popoverRef.current) === null || _a2 === void 0 ? void 0 : _a2.firstChild) {
        const popoverPosition = popoverRef.current.firstChild.getBoundingClientRect();
        if (clientX > popoverPosition.x - POPOVER_DEADZONE && clientX < popoverPosition.x + popoverPosition.width + POPOVER_DEADZONE && clientY > popoverPosition.y - POPOVER_DEADZONE && clientY < popoverPosition.y + popoverPosition.height + POPOVER_DEADZONE) {
          return true;
        }
      }
      return false;
    };
    const onSVGMouseMoveThrottled = throttle((clientX, clientY) => {
      if (interactions.get().isPopoverPinned || !plotRef.current || interactions.plot.xy.length === 0 || isMouseOverPopover(clientX, clientY)) {
        return;
      }
      const svgRect = plotRef.current.svg.getBoundingClientRect();
      const offsetX = clientX - svgRect.left;
      const offsetY = clientY - svgRect.top;
      const closestX = findClosest(interactions.plot.xy, offsetX, (xPoints) => xPoints[0].scaled.x);
      const closestPoint = findClosest(closestX, offsetY, (point) => point.scaled.y1);
      if (Math.abs(offsetX - closestPoint.scaled.x) < MAX_HOVER_MARGIN && Math.abs(offsetY - closestPoint.scaled.y1) < MAX_HOVER_MARGIN) {
        interactions.highlightPoint(closestPoint);
      } else {
        interactions.highlightX(closestX);
      }
    }, SVG_HOVER_THROTTLE);
    const onSVGMouseMove = ({ clientX, clientY }) => onSVGMouseMoveThrottled(clientX, clientY);
    const onSVGMouseOut = (event) => {
      onSVGMouseMoveThrottled.cancel();
      if (interactions.get().isPopoverPinned || isMouseOverPopover(event.clientX, event.clientY)) {
        return;
      }
      if (!nodeContains(plotRef.current.svg, event.relatedTarget)) {
        interactions.clearHighlightedLegend();
        interactions.clearHighlight();
      }
    };
    const onSVGMouseDown = (event) => {
      interactions.togglePopoverPin();
      event.preventDefault();
    };
    const moveWithinXAxis = (direction) => {
      if (interactions.get().highlightedPoint) {
        return moveWithinSeries(direction);
      } else if (containsMultipleSeries) {
        const { highlightedX } = interactions.get();
        if (highlightedX) {
          const currentXIndex = highlightedX[0].index.x;
          const nextXIndex = circleIndex(currentXIndex + direction, [0, interactions.plot.xy.length - 1]);
          interactions.highlightX(interactions.plot.xy[nextXIndex]);
        }
      }
    };
    const moveWithinSeries = (direction) => {
      const point = interactions.get().highlightedPoint;
      if (!point) {
        return;
      }
      const sIndex = point.index.s;
      const xIndex = circleIndex(point.index.x + direction, [0, interactions.plot.xs.length - 1]);
      interactions.highlightPoint(interactions.plot.xs[xIndex][sIndex]);
    };
    const moveBetweenSeries = (direction) => {
      const point = interactions.get().highlightedPoint;
      if (!point) {
        const { highlightedX } = interactions.get();
        if (highlightedX) {
          const xIndex2 = highlightedX[0].index.x;
          const points = interactions.plot.xy[xIndex2];
          const yIndex = direction === 1 ? 0 : points.length - 1;
          interactions.highlightPoint(points[yIndex]);
        }
        return;
      }
      const xIndex = point.index.x;
      const currentYIndex = point.index.y;
      if (containsMultipleSeries && (currentYIndex === 0 && direction === -1 || currentYIndex === interactions.plot.xy[xIndex].length - 1 && direction === 1)) {
        interactions.highlightX(interactions.plot.xy[xIndex]);
      } else {
        const nextYIndex = circleIndex(currentYIndex + direction, [0, interactions.plot.xy[xIndex].length - 1]);
        interactions.highlightPoint(interactions.plot.xy[xIndex][nextYIndex]);
      }
    };
    const onSVGKeyDown = (event) => {
      const keyCode = event.keyCode;
      if (keyCode !== KeyCode.up && keyCode !== KeyCode.right && keyCode !== KeyCode.down && keyCode !== KeyCode.left && keyCode !== KeyCode.space && keyCode !== KeyCode.enter) {
        return;
      }
      event.preventDefault();
      if (interactions.get().isPopoverPinned) {
        return;
      }
      handleKey(event, {
        onBlockEnd: () => moveBetweenSeries(-1),
        onBlockStart: () => moveBetweenSeries(1),
        onInlineStart: () => moveWithinXAxis(-1),
        onInlineEnd: () => moveWithinXAxis(1),
        onActivate: () => interactions.pinPopover()
      });
    };
    const highlightFirstX = () => {
      interactions.highlightX(interactions.plot.xy[0]);
    };
    const onApplicationFocus = (_event, trigger) => {
      if (trigger === "keyboard") {
        const { highlightedX, highlightedPoint, highlightedSeries: highlightedSeries2, legendSeries } = interactions.get();
        if (containsMultipleSeries && !highlightedX && !highlightedPoint && !highlightedSeries2 && !legendSeries) {
          highlightFirstX();
        } else if (!highlightedX) {
          interactions.highlightFirstPoint();
        }
      }
    };
    const onApplicationBlur = (event) => {
      if (!nodeBelongs(containerRef.current, event.relatedTarget) && !interactions.get().isPopoverPinned) {
        interactions.clearHighlight();
      }
    };
    const onFilterSeries = (series2) => {
      stableSetVisibleSeries(series2);
    };
    const onLegendHighlight = (series2) => {
      interactions.highlightSeries(series2);
    };
    const onPopoverDismiss = (outsideClick) => {
      interactions.unpinPopover();
      if (!outsideClick) {
        setTimeout(() => {
          if (interactions.get().highlightedPoint || interactions.get().highlightedX) {
            plotRef.current.focusApplication();
          } else {
            interactions.clearHighlight();
            plotRef.current.focusPlot();
          }
        }, 0);
      }
    };
    const onContainerBlur = () => {
      interactions.clearState();
    };
    const onDocumentKeyDown = (event) => {
      if (event.key === "Escape") {
        interactions.clearHighlight();
        interactions.clearHighlightedLegend();
      }
    };
    const onPopoverLeave = (event) => {
      if (nodeContains(plotRef.current.svg, event.relatedTarget) || interactions.get().isPopoverPinned) {
        return;
      }
      interactions.clearHighlight();
      interactions.clearHighlightedLegend();
    };
    return {
      width,
      height,
      series,
      allSeries,
      getInternalSeries,
      computed,
      interactions,
      handlers: {
        onSVGMouseMove,
        onSVGMouseOut,
        onSVGMouseDown,
        onSVGKeyDown,
        onApplicationFocus,
        onApplicationBlur,
        onFilterSeries,
        onLegendHighlight,
        onPopoverDismiss,
        onContainerBlur,
        onDocumentKeyDown,
        onPopoverLeave
      },
      refs: {
        plot: plotRef,
        plotMeasure: plotMeasureRef,
        container: containerRef,
        verticalMarker: verticalMarkerRef,
        popoverRef
      }
    };
  }, [
    allSeries,
    series,
    xDomain,
    yDomain,
    xScaleType,
    yScaleType,
    height,
    width,
    stableSetVisibleSeries,
    popoverRef,
    isRtl
  ]);
  useReaction(model.interactions, (state) => state.highlightedSeries, setHighlightedSeries);
  (0, import_react10.useEffect)(() => {
    if (highlightedSeries !== model.interactions.get().highlightedSeries) {
      model.interactions.highlightSeries(highlightedSeries);
    }
  }, [model, highlightedSeries]);
  return model;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/use-filter-props.js
var import_react11 = __toESM(require_react());
function useFilterProps(series, controlledVisibleSeries, controlledOnVisibleChange) {
  const [visibleSeries = [], setVisibleSeriesState] = useControllable(controlledVisibleSeries, controlledOnVisibleChange, series, {
    componentName: "AreaChart",
    controlledProp: "visibleSeries",
    changeHandler: "onFilterChange"
  });
  const setVisibleSeries = (0, import_react11.useCallback)((selectedSeries) => {
    setVisibleSeriesState(selectedSeries);
    fireNonCancelableEvent(controlledOnVisibleChange, { visibleSeries: selectedSeries });
  }, [controlledOnVisibleChange, setVisibleSeriesState]);
  (0, import_react11.useEffect)(() => {
    const newVisibleSeries = visibleSeries.filter((s) => series.indexOf(s) !== -1);
    if (newVisibleSeries.length !== visibleSeries.length) {
      setVisibleSeries(newVisibleSeries);
    }
  }, [series, visibleSeries, setVisibleSeries]);
  return [visibleSeries, setVisibleSeries];
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/model/use-highlight-props.js
var import_react12 = __toESM(require_react());
function useHighlightProps(series, controlledHighlightedSeries, controlledOnHighlightChange) {
  const [highlightedSeries = null, setHighlightedSeries] = useControllable(controlledHighlightedSeries, controlledOnHighlightChange, null, {
    componentName: "AreaChart",
    controlledProp: "highlightedSeries",
    changeHandler: "onHighlightChange"
  });
  const notifyHighlightedSeries = (0, import_react12.useCallback)((s) => {
    fireNonCancelableEvent(controlledOnHighlightChange, { highlightedSeries: s });
  }, [controlledOnHighlightChange]);
  (0, import_react12.useEffect)(() => {
    if (controlledHighlightedSeries) {
      const highlightedSeriesIndex = series.indexOf(controlledHighlightedSeries);
      if (highlightedSeriesIndex === -1) {
        setHighlightedSeries(null);
        notifyHighlightedSeries(null);
      }
    }
  }, [series, controlledHighlightedSeries, setHighlightedSeries, notifyHighlightedSeries]);
  return [highlightedSeries, notifyHighlightedSeries];
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/internal.js
function InternalAreaChart(_a) {
  var { fitHeight, height, xScaleType, yScaleType, xDomain, yDomain, xTickFormatter, yTickFormatter, detailTotalFormatter, highlightedSeries: controlledHighlightedSeries, visibleSeries: controlledVisibleSeries, series: externalSeries, onFilterChange: controlledOnVisibleChange, onHighlightChange: controlledOnHighlightChange, i18nStrings, ariaLabel, ariaLabelledby, ariaDescription, xTitle, yTitle, hideFilter, additionalFilters, hideLegend, legendTitle, statusType, detailPopoverSize, detailPopoverFooter, empty, noMatch, errorText, loadingText, recoveryText, onRecoveryClick, __internalRootRef = null } = _a, props = __rest(_a, ["fitHeight", "height", "xScaleType", "yScaleType", "xDomain", "yDomain", "xTickFormatter", "yTickFormatter", "detailTotalFormatter", "highlightedSeries", "visibleSeries", "series", "onFilterChange", "onHighlightChange", "i18nStrings", "ariaLabel", "ariaLabelledby", "ariaDescription", "xTitle", "yTitle", "hideFilter", "additionalFilters", "hideLegend", "legendTitle", "statusType", "detailPopoverSize", "detailPopoverFooter", "empty", "noMatch", "errorText", "loadingText", "recoveryText", "onRecoveryClick", "__internalRootRef"]);
  const baseProps = getBaseProps(props);
  const containerRef = (0, import_react13.useRef)(null);
  const popoverRef = (0, import_react13.useRef)(null);
  if (isDevelopment) {
    (0, import_react13.useEffect)(() => {
      if (!isSeriesValid(externalSeries)) {
        warnOnce("AreaChart", "The `series` property violates the component's constraints: all `area` series must have `data` arrays of the same length and with the same x-values.");
      }
    }, [externalSeries]);
  }
  const [width, setWidth] = (0, import_react13.useState)(0);
  const [visibleSeries, setVisibleSeries] = useFilterProps(externalSeries, controlledVisibleSeries, controlledOnVisibleChange);
  const [highlightedSeries, setHighlightedSeries] = useHighlightProps(externalSeries, controlledHighlightedSeries, controlledOnHighlightChange);
  const isRtl = containerRef.current ? getIsRtl(containerRef.current) : false;
  const model = useChartModel({
    isRtl,
    fitHeight,
    externalSeries,
    visibleSeries,
    setVisibleSeries,
    highlightedSeries,
    setHighlightedSeries,
    xDomain,
    yDomain,
    xScaleType,
    yScaleType,
    height,
    width,
    popoverRef,
    statusType
  });
  const { isEmpty, isNoMatch, showChart } = getChartStatus({
    externalData: externalSeries,
    visibleData: visibleSeries,
    statusType
  });
  const showFilters = statusType === "finished" && (!isEmpty || isNoMatch) && (additionalFilters || !hideFilter);
  const showLegend = !hideLegend && !isEmpty && statusType === "finished";
  const reserveLegendSpace = !showChart && !hideLegend;
  const reserveFilterSpace = !showChart && !isNoMatch && (!hideFilter || additionalFilters);
  (0, import_react13.useEffect)(() => {
    const onKeyDown = model.handlers.onDocumentKeyDown;
    document.addEventListener("keydown", onKeyDown);
    return () => document.removeEventListener("keydown", onKeyDown);
  }, [model.handlers.onDocumentKeyDown]);
  const onBlur = (event) => {
    if (event.relatedTarget && !nodeBelongs(containerRef.current, event.relatedTarget)) {
      model.handlers.onContainerBlur();
    }
  };
  const mergedRef = useMergeRefs(containerRef, __internalRootRef);
  return import_react13.default.createElement(ChartWrapper, Object.assign({ ref: mergedRef }, baseProps, { className: clsx_default(baseProps.className, styles_css_default.root), fitHeight: !!fitHeight, contentMinHeight: height, defaultFilter: showFilters && !hideFilter ? import_react13.default.createElement(area_chart_filter_default, { model, filterLabel: i18nStrings.filterLabel, filterPlaceholder: i18nStrings.filterPlaceholder, filterSelectedAriaLabel: i18nStrings.filterSelectedAriaLabel }) : null, additionalFilters: showFilters ? additionalFilters : null, reserveFilterSpace: !!reserveFilterSpace, reserveLegendSpace: !!reserveLegendSpace, chartStatus: import_react13.default.createElement(ChartStatusContainer, { isEmpty, isNoMatch, showChart, statusType, empty, noMatch, loadingText, errorText, recoveryText, onRecoveryClick }), chart: showChart ? import_react13.default.createElement(chart_container_default, { model, autoWidth: setWidth, detailPopoverSize, detailPopoverFooter, xTitle, yTitle, xTickFormatter, yTickFormatter, detailTotalFormatter, ariaLabel, ariaLabelledby, ariaDescription, i18nStrings, fitHeight, hasFilters: !!showFilters, minHeight: height, isRTL: isRtl }) : null, legend: showLegend ? import_react13.default.createElement(area_chart_legend_default, { plotContainerRef: containerRef, model, legendTitle, ariaLabel: i18nStrings.legendAriaLabel }) : null, onBlur }));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/area-chart/index.js
function AreaChart(_a) {
  var { height = 500, xScaleType = "linear", yScaleType = "linear", statusType = "finished", detailPopoverSize = "medium", i18nStrings = {} } = _a, props = __rest(_a, ["height", "xScaleType", "yScaleType", "statusType", "detailPopoverSize", "i18nStrings"]);
  const baseComponentProps = useBaseComponent("AreaChart", {
    props: {
      detailPopoverSize,
      hideLegend: props.hideLegend,
      hideFilter: props.hideFilter,
      fitHeight: props.fitHeight,
      xScaleType,
      yScaleType
    }
  });
  return import_react14.default.createElement(InternalAreaChart, Object.assign({ height, xScaleType, yScaleType, statusType, detailPopoverSize, i18nStrings }, props, baseComponentProps));
}
applyDisplayName(AreaChart, "AreaChart");
var area_chart_default = AreaChart;
export {
  area_chart_default as default
};
//# sourceMappingURL=@risk-smart_themed-cloudscape-components_area-chart.js.map
