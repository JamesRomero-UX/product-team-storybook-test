import {
  CartesianChartContainer,
  ChartScale,
  NumericChartScale,
  axis_label_default,
  block_end_labels_default,
  createXTicks,
  createYTicks,
  emphasized_baseline_default,
  getXTickCount,
  getYTickCount,
  highlighted_point_default,
  inline_start_labels_default,
  labels_measure_default,
  styles_css_default,
  useBLockEndLabels,
  vertical_marker_default
} from "./chunk-O6Q6X3CQ.js";
import {
  ChartStatusContainer,
  ChartWrapper,
  chart_filter_default,
  chart_legend_default,
  chart_plot_default,
  chart_popover_default,
  chart_series_details_default,
  chart_series_marker_default,
  createCategoryColorScale,
  getChartStatus,
  line_default,
  useHeightMeasure
} from "./chunk-WPMMI74K.js";
import {
  circleIndex
} from "./chunk-XVMNXRED.js";
import {
  usePrevious
} from "./chunk-NZVEIX2D.js";
import {
  useContainerWidth
} from "./chunk-BHF4AY7K.js";
import {
  useControllable
} from "./chunk-IJHQZZNK.js";
import {
  Transition
} from "./chunk-YW7RACHJ.js";
import {
  KeyCode
} from "./chunk-RQYQ76PG.js";
import {
  fireNonCancelableEvent
} from "./chunk-QRSSPSED.js";
import {
  nodeBelongs
} from "./chunk-BJH6AXTS.js";
import {
  useUniqueId
} from "./chunk-Z3FGJ2XJ.js";
import {
  useMergeRefs
} from "./chunk-4UVHBYQ2.js";
import {
  getBaseProps,
  isDevelopment,
  useVisualRefresh
} from "./chunk-A5JTL534.js";
import {
  clsx_default
} from "./chunk-HPPI4HBL.js";
import {
  getIsRtl,
  nodeContains,
  warnOnce
} from "./chunk-CMYEOKCY.js";
import {
  __rest
} from "./chunk-XSNQIJR6.js";
import {
  require_react
} from "./chunk-YHCPMRL2.js";
import {
  __toESM
} from "./chunk-PR4QN5HX.js";

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/internal.js
var import_react11 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/chart-container.js
var import_react9 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/internal/components/cartesian-chart/vertical-grid-lines.js
var import_react = __toESM(require_react());
var vertical_grid_lines_default = (0, import_react.memo)(VerticalGridLines);
function VerticalGridLines({ ticks, scale, height }) {
  return import_react.default.createElement("g", { "aria-hidden": "true" }, ticks.map((tick) => {
    var _a;
    const x = (_a = scale.d3Scale(tick)) !== null && _a !== void 0 ? _a : NaN;
    return isFinite(x) && import_react.default.createElement("line", { key: tick, className: styles_css_default.grid, x1: x, y1: 0, x2: x, y2: height });
  }));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/bar-groups.js
var import_react2 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/styles.css.js
import "/Users/jamesromero/Library/CloudStorage/OneDrive-RiskSmart/Product - Product Dream Team/Product-team-storybook/node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/styles.scoped.css";
var styles_css_default2 = {
  "series": "awsui_series_1yz9c_v68e6_189",
  "series--bar": "awsui_series--bar_1yz9c_v68e6_190",
  "series__rect": "awsui_series__rect_1yz9c_v68e6_190",
  "exiting": "awsui_exiting_1yz9c_v68e6_207",
  "awsui-motion-fade-out-0": "awsui_awsui-motion-fade-out-0_1yz9c_v68e6_1",
  "root": "awsui_root_1yz9c_v68e6_229",
  "series--highlighted": "awsui_series--highlighted_1yz9c_v68e6_242",
  "series__bar--highlighted": "awsui_series__bar--highlighted_1yz9c_v68e6_243",
  "series--dimmed": "awsui_series--dimmed_1yz9c_v68e6_247",
  "series--threshold": "awsui_series--threshold_1yz9c_v68e6_251",
  "bar-group": "awsui_bar-group_1yz9c_v68e6_256"
};

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/bar-groups.js
var OUTLINE_PADDING = 2;
function BarGroups({ ariaLabel, isRefresh, isPopoverPinned, barGroups, highlightedGroupIndex, highlightedGroupRef }) {
  return import_react2.default.createElement("g", { role: "group", "aria-hidden": "true" }, barGroups.map((group, index) => import_react2.default.createElement("rect", { key: index, x: Math.max(0, group.position.x - OUTLINE_PADDING), y: Math.max(0, group.position.y - OUTLINE_PADDING), width: group.position.width + 2 * OUTLINE_PADDING, height: group.position.height + 2 * OUTLINE_PADDING, ref: index === highlightedGroupIndex ? highlightedGroupRef : void 0, rx: isRefresh ? 4 : 2, role: "button", "aria-label": ariaLabel, "aria-haspopup": true, "aria-expanded": isPopoverPinned, fill: "none", className: styles_css_default2["bar-group"] })));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/chart-popover.js
var import_react3 = __toESM(require_react());
var chart_popover_default2 = import_react3.default.forwardRef(MixedChartPopover);
function MixedChartPopover({ containerRef, trackRef, isOpen, isPinned, highlightDetails, footer, onDismiss, size = "medium", dismissAriaLabel, onMouseEnter, onMouseLeave, onBlur, setPopoverText }, popoverRef) {
  const [expandedSeries, setExpandedSeries] = (0, import_react3.useState)({});
  return import_react3.default.createElement(Transition, { in: isOpen }, (state, ref) => import_react3.default.createElement("div", { ref, className: clsx_default(state === "exiting" && styles_css_default2.exiting) }, (isOpen || state !== "exited") && highlightDetails && import_react3.default.createElement(
    chart_popover_default,
    { ref: popoverRef, title: highlightDetails.position, trackRef, trackKey: highlightDetails.position, dismissButton: isPinned, dismissAriaLabel, onDismiss, container: containerRef.current, size, onMouseEnter, onMouseLeave, onBlur, footer },
    import_react3.default.createElement(chart_series_details_default, { key: highlightDetails.position, details: highlightDetails.details, setPopoverText, expandedSeries: expandedSeries[highlightDetails.position], setExpandedState: (id, isExpanded) => setExpandedSeries((oldState) => {
      const expandedSeriesInCurrentCoordinate = new Set(oldState[highlightDetails.position]);
      if (isExpanded) {
        expandedSeriesInCurrentCoordinate.add(id);
      } else {
        expandedSeriesInCurrentCoordinate.delete(id);
      }
      return Object.assign(Object.assign({}, oldState), { [highlightDetails.position]: expandedSeriesInCurrentCoordinate });
    }) })
  )));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/data-series.js
var import_react6 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/bar-series.js
var import_react4 = __toESM(require_react());

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/create-one-side-rounded-rect-path.js
function createOneSideRoundedRectPath({ x, y, width, height }, radius, side) {
  const coordinates = [
    { x, y },
    { x: x + width, y },
    { x: x + width, y: y + height },
    { x, y: y + height }
  ];
  const startIndex = { left: 2, right: 0, top: 3, bottom: 1 }[side];
  let path = "";
  for (let i = startIndex; i < startIndex + coordinates.length + 1; i++) {
    const start = coordinates[i % coordinates.length];
    const end = coordinates[(i + 1) % coordinates.length];
    const c = radius && i < startIndex + 3 ? Math.min(radius / Math.hypot(end.x - start.x, end.y - start.y), 0.5) : 0;
    if (i === startIndex) {
      path += `M${start.x * (1 - c) + end.x * c},${start.y * (1 - c) + end.y * c}`;
    }
    if (i > startIndex) {
      path += `Q${start.x},${start.y} ${start.x * (1 - c) + end.x * c},${start.y * (1 - c) + end.y * c}`;
    }
    if (i < startIndex + coordinates.length) {
      path += `L${start.x * c + end.x * (1 - c)},${start.y * c + end.y * (1 - c)}`;
    }
  }
  return path + "Z";
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/utils.js
var chartLegendMap = {
  line: "line",
  bar: "rectangle",
  threshold: "dashed"
};
var nextValidDomainIndex = (nextGroupIndex, barGroups, direction = 1) => {
  let index = nextGroupIndex;
  if (index < 0 || index >= barGroups.length) {
    index = 0;
  }
  do {
    if (barGroups[index].isValid && barGroups[index].hasData) {
      return index;
    }
    index += direction;
    if (index >= barGroups.length) {
      index = 0;
    } else if (index < 0) {
      index = barGroups.length - 1;
    }
  } while (index !== nextGroupIndex);
  return 0;
};
function findNavigableSeries(series) {
  const navigableSeries = [];
  let navigableBarSeriesIndex = -1;
  series.forEach((internalSeries) => {
    if (internalSeries.series.type === "bar") {
      if (navigableBarSeriesIndex === -1) {
        navigableBarSeriesIndex = navigableSeries.length;
        navigableSeries.push(internalSeries.series);
      }
    } else {
      navigableSeries.push(internalSeries.series);
    }
  });
  return { navigableSeries, navigableBarSeriesIndex };
}
var matchesX = (x1, x2) => {
  if (x1 instanceof Date && x2 instanceof Date) {
    return x1.getTime() === x2.getTime();
  }
  return x1 === x2;
};
function calculateStackedBarValues(dataBySeries) {
  var _a, _b, _c, _d;
  const negativeValues = /* @__PURE__ */ new Map();
  const positiveValues = /* @__PURE__ */ new Map();
  const values = /* @__PURE__ */ new Map();
  for (let seriesIndex = 0; seriesIndex < dataBySeries.length; seriesIndex++) {
    for (const datum of dataBySeries[seriesIndex]) {
      const key = getKeyValue(datum.x);
      if (datum.y < 0) {
        negativeValues.set(key, ((_a = negativeValues.get(key)) !== null && _a !== void 0 ? _a : 0) + datum.y);
      } else {
        positiveValues.set(key, ((_b = positiveValues.get(key)) !== null && _b !== void 0 ? _b : 0) + datum.y);
      }
      const seriesValue = (_c = datum.y < 0 ? negativeValues.get(key) : positiveValues.get(key)) !== null && _c !== void 0 ? _c : 0;
      const valuesByIndex = (_d = values.get(key)) !== null && _d !== void 0 ? _d : /* @__PURE__ */ new Map();
      valuesByIndex.set(seriesIndex, seriesValue);
      values.set(key, valuesByIndex);
    }
  }
  return values;
}
var getKeyValue = (key) => key instanceof Date ? key.getTime() : key;
function isYThreshold(series) {
  return series.type === "threshold" && "y" in series;
}
function isXThreshold(series) {
  return series.type === "threshold" && "x" in series;
}
function isDataSeries(series) {
  return series.type === "line" || series.type === "bar";
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/bar-series.js
function BarSeries({ axis, series, color, xScale, yScale, highlighted, dimmed, highlightedGroupIndex, totalSeriesCount, seriesIndex, plotSize, chartAreaClipPath, stackedBarValues, isRtl }) {
  const isRefresh = useVisualRefresh();
  const isStacked = !!stackedBarValues;
  const isVertical = axis === "x";
  const xCoordinates = (() => {
    var _a;
    if (series.type !== "bar" || !xScale.isCategorical()) {
      return [];
    }
    const yContinuosScale = yScale.d3Scale;
    const xPoints = series.data.map(({ x }) => xScale.d3Scale(x) || NaN);
    const zeroY = (_a = yScale.d3Scale(0)) !== null && _a !== void 0 ? _a : NaN;
    const baseY = isFinite(zeroY) ? Math.min(plotSize, zeroY) : plotSize;
    let barWidth = xScale.d3Scale.bandwidth();
    const PADDING = 4;
    const MINWIDTH = 4;
    if (!isStacked && totalSeriesCount > 1) {
      barWidth = (barWidth - (totalSeriesCount - 1) * PADDING) / totalSeriesCount;
      barWidth = Math.max(barWidth, MINWIDTH);
    }
    return xPoints.map((x, i) => {
      var _a2, _b, _c, _d;
      const d = series.data[i];
      const key = getKeyValue(d.x);
      let barX = x;
      let yValue = d.y;
      let isRoundedStart = !isStacked;
      let isRoundedEnd = !isStacked;
      if (isStacked) {
        const allXValues = (_a2 = stackedBarValues.get(key)) !== null && _a2 !== void 0 ? _a2 : /* @__PURE__ */ new Map();
        yValue = (_b = allXValues.get(seriesIndex)) !== null && _b !== void 0 ? _b : 0;
        const allXValuesSorted = Array.from(allXValues.values()).sort((a, b) => a - b);
        isRoundedStart = yValue === allXValuesSorted[0];
        isRoundedEnd = yValue === allXValuesSorted[allXValuesSorted.length - 1];
      } else if (totalSeriesCount > 1) {
        barX += seriesIndex * (barWidth + PADDING);
      }
      yValue = yValue < 0 ? yValue - d.y : yValue;
      return {
        x: barX,
        y: (_c = yContinuosScale(yValue)) !== null && _c !== void 0 ? _c : NaN,
        width: barWidth,
        height: Math.abs(((_d = yContinuosScale(d.y)) !== null && _d !== void 0 ? _d : NaN) - baseY),
        isRoundedStart,
        isRoundedEnd
      };
    });
  })();
  const highlightedXValue = highlightedGroupIndex !== null ? xScale.domain[highlightedGroupIndex] : null;
  return import_react4.default.createElement("g", { "aria-label": series.title, clipPath: `url(#${chartAreaClipPath})`, className: clsx_default(styles_css_default2.series, styles_css_default2["series--bar"], {
    [styles_css_default2["series--highlighted"]]: highlighted,
    [styles_css_default2["series--dimmed"]]: dimmed
  }) }, xCoordinates.map(({ x, y, width, height, isRoundedStart, isRoundedEnd }, i) => {
    if (!isFinite(x) || !isFinite(height)) {
      return;
    }
    const baseHeightOffset = isStacked ? 3 : 0;
    const isSmallBar = height < 4;
    const heightOffset = isSmallBar ? 0 : baseHeightOffset;
    const widthOffset = 2;
    const rx = isRefresh ? isSmallBar ? 2 : 4 : 0;
    const placement = isVertical ? {
      x: x + widthOffset / 2,
      y: y + heightOffset / 2,
      width: width - widthOffset,
      height: height - heightOffset
    } : {
      x: y - (!isRtl ? height : 0) + heightOffset / 2,
      y: x + widthOffset / 2,
      width: height - heightOffset,
      height: width - widthOffset
    };
    const className = clsx_default(styles_css_default2.series__rect, {
      [styles_css_default2["series--dimmed"]]: highlightedXValue !== null && !matchesX(highlightedXValue, series.data[i].x)
    });
    const styleProps = { fill: color, className };
    let side = "none";
    if (isRoundedStart && isRoundedEnd) {
      side = "all";
    } else if (!isRoundedStart && !isRoundedEnd) {
      side = "none";
    } else if (isVertical) {
      side = isRoundedStart ? "bottom" : "top";
    } else if (!isRtl) {
      side = isRoundedStart ? "left" : "right";
    } else {
      side = isRoundedStart ? "right" : "left";
    }
    if (side === "all") {
      return import_react4.default.createElement("rect", Object.assign({ key: i }, placement, styleProps, { rx }));
    }
    if (side === "none") {
      return import_react4.default.createElement("rect", Object.assign({ key: i }, placement, styleProps, { rx: 0 }));
    }
    return import_react4.default.createElement("path", Object.assign({ key: i, d: createOneSideRoundedRectPath(placement, rx, side) }, styleProps));
  }));
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/line-series.js
var import_react5 = __toESM(require_react());
function LineSeries({ axis, series, color, xScale, yScale, chartAreaClipPath }) {
  var _a;
  const commonProps = { "aria-hidden": true, stroke: color, clipPath: `url(#${chartAreaClipPath})` };
  if (series.type === "line") {
    const lineGenerator = line_default().x((d) => {
      let x = xScale.d3Scale(d.x) || 0;
      if (xScale.isCategorical()) {
        const offset = Math.max(0, xScale.d3Scale.bandwidth() - 1) / 2;
        x += offset;
      }
      return x;
    }).y((d) => yScale.d3Scale(d.y) || 0);
    const visibleData = series.data.filter(({ x }) => xScale.d3Scale(x) !== void 0);
    return import_react5.default.createElement("path", Object.assign({}, commonProps, { d: lineGenerator(visibleData) || "" }));
  } else if (isYThreshold(series)) {
    const [x1, x2] = xScale.d3Scale.range();
    const y = yScale.d3Scale(series.y);
    const coordinates = axis === "x" ? { x1, x2, y1: y, y2: y } : { x1: y, x2: y, y1: x1, y2: x2 };
    return import_react5.default.createElement("line", Object.assign({}, commonProps, coordinates));
  } else if (isXThreshold(series)) {
    const [y1, y2] = yScale.d3Scale.range();
    const xOffset = xScale.isCategorical() ? Math.max(0, xScale.d3Scale.bandwidth() - 1) / 2 : 0;
    const x = ((_a = xScale.d3Scale(series.x)) !== null && _a !== void 0 ? _a : NaN) + xOffset;
    const coordinates = axis === "x" ? { x1: x, x2: x, y1, y2 } : { x1: y1, x2: y2, y1: x, y2: x };
    return import_react5.default.createElement("line", Object.assign({}, commonProps, coordinates));
  } else {
    return null;
  }
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/data-series.js
var STROKE_WIDTH = 2;
function DataSeries({ axis, plotHeight, plotWidth, highlightedGroupIndex, highlightedSeries, stackedBars, isGroupNavigation, visibleSeries, xScale, yScale, isRtl }) {
  const chartAreaClipPath = useUniqueId("awsui-mixed-line-bar-chart__chart-area-");
  const lineAreaClipPath = useUniqueId("awsui-line-chart__chart-area-");
  const stackedBarValues = (0, import_react6.useMemo)(() => {
    if (!stackedBars) {
      return void 0;
    }
    const barData = [];
    visibleSeries.forEach(({ series }) => {
      if (series.type === "bar") {
        barData.push(series.data);
      } else {
        barData.push([]);
      }
    });
    return calculateStackedBarValues(barData);
  }, [visibleSeries, stackedBars]);
  return import_react6.default.createElement(
    import_react6.default.Fragment,
    null,
    import_react6.default.createElement(
      "defs",
      { "aria-hidden": "true" },
      import_react6.default.createElement(
        "clipPath",
        { id: chartAreaClipPath },
        import_react6.default.createElement("rect", { x: 0, y: 0, width: plotWidth, height: plotHeight })
      ),
      import_react6.default.createElement(
        "clipPath",
        { id: lineAreaClipPath },
        import_react6.default.createElement("rect", { x: 0, y: -STROKE_WIDTH / 2, width: plotWidth, height: plotHeight + STROKE_WIDTH })
      )
    ),
    import_react6.default.createElement("g", { "aria-hidden": isGroupNavigation ? true : void 0, role: "group" }, visibleSeries.map(({ series, color }, index) => {
      const isHighlighted = series === highlightedSeries;
      const isDimmed = !!highlightedSeries && !isHighlighted;
      switch (series.type) {
        case "line":
        case "threshold": {
          return import_react6.default.createElement(
            "g",
            { key: index, role: "group", "aria-label": series.title, className: clsx_default(styles_css_default2.series, styles_css_default2[`series--${series.type}`], {
              [styles_css_default2["series--highlighted"]]: isHighlighted,
              [styles_css_default2["series--dimmed"]]: isDimmed
            }) },
            import_react6.default.createElement(LineSeries, { axis, series, color, xScale, yScale, chartAreaClipPath: lineAreaClipPath })
          );
        }
        case "bar":
          return import_react6.default.createElement(BarSeries, { key: index, axis, series, color, totalSeriesCount: visibleSeries.filter((s) => s.series.type === "bar").length, seriesIndex: index, xScale, yScale, plotSize: axis === "y" ? plotWidth : plotHeight, highlighted: isHighlighted, dimmed: isDimmed, chartAreaClipPath, stackedBarValues, highlightedGroupIndex, isRtl });
      }
    }))
  );
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/domain.js
function computeDomainX(series, xScaleType) {
  if (xScaleType === "categorical") {
    return series.reduce((acc, s) => {
      if (isDataSeries(s.series)) {
        s.series.data.forEach(({ x }) => {
          if (acc.indexOf(x) === -1) {
            acc.push(x);
          }
        });
      }
      if (isXThreshold(s.series)) {
        if (acc.indexOf(s.series.x) === -1) {
          acc.push(s.series.x);
        }
      }
      return acc;
    }, []);
  }
  return series.reduce((acc, curr) => {
    if (isYThreshold(curr.series)) {
      return acc;
    }
    if (isXThreshold(curr.series)) {
      const [min, max] = acc;
      const newMin = min === void 0 || min === null || curr.series.x < min ? curr.series.x : min;
      const newMax = max === void 0 || max === null || max < curr.series.x ? curr.series.x : max;
      return [newMin, newMax];
    }
    if (isDataSeries(curr.series)) {
      return curr.series.data.reduce(([min, max], { x }) => {
        const newMin = min === void 0 || min === null || x < min ? x : min;
        const newMax = max === void 0 || max === null || max < x ? x : max;
        return [newMin, newMax];
      }, acc);
    }
    return acc;
  }, []);
}
function find(arr, func) {
  for (let i = 0; i < arr.length; i++) {
    const found = func(arr[i]);
    if (found) {
      return arr[i];
    }
  }
  return null;
}
function computeDomainY(series, scaleType, stackedBars) {
  let _series = series;
  if (stackedBars) {
    const { positiveData, negativeData } = series.reduce((acc, curr) => {
      if (curr.series.type === "bar") {
        curr.series.data.forEach(({ x, y }) => {
          const data = y < 0 ? acc.negativeData : acc.positiveData;
          const stackedDatum = find(data, (el) => matchesX(el.x, x));
          if (stackedDatum) {
            stackedDatum.y += y;
          } else {
            data.push({ x, y });
          }
          return acc;
        });
      }
      return acc;
    }, {
      positiveData: [],
      negativeData: []
    });
    const stackedSeries = [
      {
        color: "",
        index: NaN,
        series: {
          type: "bar",
          title: "positive",
          data: positiveData
        }
      },
      {
        color: "",
        index: NaN,
        series: {
          type: "bar",
          title: "negative",
          data: negativeData
        }
      }
    ];
    _series = [...stackedSeries, ..._series.filter((s) => s.series.type !== "bar")];
  }
  const domain = _series.reduce((acc, curr) => {
    if (isYThreshold(curr.series)) {
      const [min, max] = acc;
      const newMin = min === void 0 || curr.series.y < min ? curr.series.y : min;
      const newMax = max === void 0 || max < curr.series.y ? curr.series.y : max;
      return [newMin, newMax];
    }
    if (isXThreshold(curr.series)) {
      return acc;
    }
    if (isDataSeries(curr.series)) {
      return curr.series.data.reduce(([min, max], { y }) => {
        const newMin = min === void 0 || y < min ? y : min;
        const newMax = max === void 0 || max < y ? y : max;
        return [newMin, newMax];
      }, acc);
    }
    return acc;
  }, [0, 0]);
  if (scaleType === "log" && domain[0] === 0 && domain[1] > 1) {
    return [1, domain[1]];
  }
  return domain;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/format-highlighted.js
function formatHighlighted({ position, series, xTickFormatter, detailPopoverSeriesContent }) {
  const formattedPosition = xTickFormatter ? xTickFormatter(position) : position.toString();
  const details = [];
  series.forEach((s) => {
    const detail = getSeriesDetail({ internalSeries: s, targetX: position, detailPopoverSeriesContent });
    if (detail) {
      details.push(detail);
    }
  });
  return { position: formattedPosition, details };
}
function getSeriesDetail({ internalSeries, targetX, detailPopoverSeriesContent }) {
  var _a;
  const { series, color } = internalSeries;
  if (isXThreshold(series)) {
    return series.x === targetX ? {
      key: series.title,
      value: "",
      color,
      markerType: "dashed"
    } : null;
  }
  if (isYThreshold(series)) {
    return {
      key: series.title,
      value: series.valueFormatter ? series.valueFormatter(series.y) : series.y,
      color,
      markerType: "dashed"
    };
  }
  if (isDataSeries(series)) {
    for (const datum of series.data) {
      if (matchesX(targetX, datum.x)) {
        const customContent = detailPopoverSeriesContent ? detailPopoverSeriesContent({ series, x: targetX, y: datum.y }) : void 0;
        const hasSubItems = !!((_a = customContent === null || customContent === void 0 ? void 0 : customContent.subItems) === null || _a === void 0 ? void 0 : _a.length);
        const isExpandable = (customContent === null || customContent === void 0 ? void 0 : customContent.expandable) && hasSubItems;
        const isKeyString = typeof (customContent === null || customContent === void 0 ? void 0 : customContent.key) === "string";
        const key = (customContent === null || customContent === void 0 ? void 0 : customContent.key) && (!isExpandable || isKeyString) ? customContent.key : series.title;
        if ((customContent === null || customContent === void 0 ? void 0 : customContent.expandable) && !hasSubItems) {
          warnOnce("MixedLineBarChart", "`expandable` was set to `true` for a series without sub-items. This property will be ignored.");
        }
        if (isExpandable && !isKeyString) {
          warnOnce("MixedLineBarChart", "A ReactNode was used for the key of an expandable series. The series title will be used instead because nested interactive elements can cause accessiblity issues.");
        }
        if (!isKeyString && !isExpandable && (customContent === null || customContent === void 0 ? void 0 : customContent.value) && typeof customContent.value !== "string") {
          warnOnce("MixedLineBarChart", "Use a ReactNode for the key or the value of a series, but not for both. It is not recommended to use links for key and value at the same time.");
        }
        return {
          key,
          value: (customContent === null || customContent === void 0 ? void 0 : customContent.value) || (series.valueFormatter ? series.valueFormatter(datum.y, targetX) : datum.y),
          color,
          markerType: series.type === "line" ? "line" : "rectangle",
          subItems: customContent === null || customContent === void 0 ? void 0 : customContent.subItems,
          expandableId: isExpandable ? series.title : void 0
        };
      }
    }
  }
  return null;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/hooks/use-mouse-hover.js
var MAX_HOVER_MARGIN = 6;
var POPOVER_DEADZONE = 12;
function useMouseHover({ plotRef, popoverRef, scaledSeries, barGroups, highlightPoint, highlightGroup, clearHighlightedSeries, isGroupNavigation, isHandlersDisabled, highlightX }) {
  const isMouseOverPopover = (event) => {
    var _a;
    if ((_a = popoverRef.current) === null || _a === void 0 ? void 0 : _a.firstChild) {
      const popoverPosition = popoverRef.current.firstChild.getBoundingClientRect();
      if (event.clientX > popoverPosition.x - POPOVER_DEADZONE && event.clientX < popoverPosition.x + popoverPosition.width + POPOVER_DEADZONE && event.clientY > popoverPosition.y - POPOVER_DEADZONE && event.clientY < popoverPosition.y + popoverPosition.height + POPOVER_DEADZONE) {
        return true;
      }
    }
    return false;
  };
  const onSeriesMouseMove = (event) => {
    var _a, _b;
    const svgRect = event.target.getBoundingClientRect();
    const offsetX = event.clientX - svgRect.left;
    const closestX = scaledSeries.map((v) => v.x).reduce((prev, curr) => Math.abs(curr - offsetX) < Math.abs(prev - offsetX) ? curr : prev, -Infinity);
    if (isFinite(closestX)) {
      const offsetY = event.clientY - svgRect.top;
      const closestY = scaledSeries.filter((v) => v.x === closestX || isYThreshold(v.series)).map((v) => v.y).reduce((prev, curr) => Math.abs(curr - offsetY) < Math.abs(prev - offsetY) ? curr : prev, -Infinity);
      if (isFinite(closestY) && Math.abs(offsetX - closestX) < MAX_HOVER_MARGIN && Math.abs(offsetY - closestY) < MAX_HOVER_MARGIN) {
        const [{ color, datum, series }] = scaledSeries.filter((s) => (s.x === closestX || isYThreshold(s.series)) && s.y === closestY);
        highlightPoint({ x: closestX, y: closestY, color, datum, series });
      } else {
        let datumX = null;
        for (const point of scaledSeries) {
          if (point.x === closestX) {
            datumX = (_b = (_a = point.datum) === null || _a === void 0 ? void 0 : _a.x) !== null && _b !== void 0 ? _b : null;
            break;
          }
        }
        highlightX({ scaledX: closestX, label: datumX });
      }
    }
  };
  const onGroupMouseMove = (event) => {
    const svgRect = event.target.getBoundingClientRect();
    const offsetX = event.clientX - svgRect.left;
    const offsetY = event.clientY - svgRect.top;
    for (let groupIndex = 0; groupIndex < barGroups.length; groupIndex++) {
      const { position: { x, y, width, height } } = barGroups[groupIndex];
      if (x <= offsetX && offsetX <= x + width && y <= offsetY && offsetY <= y + height) {
        highlightGroup(groupIndex);
        return;
      }
    }
    clearHighlightedSeries();
  };
  const onSVGMouseMove = (event) => {
    if (event.target === plotRef.current.svg && !isHandlersDisabled && !isMouseOverPopover(event)) {
      if (isGroupNavigation) {
        onGroupMouseMove(event);
      } else if (scaledSeries.length > 0) {
        onSeriesMouseMove(event);
      }
    }
  };
  const onSVGMouseOut = (event) => {
    if (isHandlersDisabled || isMouseOverPopover(event)) {
      return;
    }
    if (!nodeContains(plotRef.current.svg, event.relatedTarget) || event.relatedTarget && event.relatedTarget.classList.contains(styles_css_default2.series)) {
      highlightX(null);
      clearHighlightedSeries();
    }
  };
  const onPopoverLeave = (event) => {
    if (!isHandlersDisabled && nodeContains(plotRef.current.svg, event.relatedTarget)) {
      highlightX(null);
      clearHighlightedSeries();
    }
  };
  return { onSVGMouseMove, onSVGMouseOut, onPopoverLeave };
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/hooks/use-navigation.js
var import_react7 = __toESM(require_react());
function useNavigation({ series, visibleSeries, scaledSeries, barGroups, xScale, yScale, highlightedPoint, highlightedGroupIndex, highlightedSeries, isHandlersDisabled, pinPopover, highlightSeries, highlightGroup, highlightPoint, highlightX, verticalMarkerX, isRtl, horizontalBars }) {
  const [targetX, setTargetX] = (0, import_react7.useState)(null);
  const [xIndex, setXIndex] = (0, import_react7.useState)(0);
  const isGroupNavigation = (0, import_react7.useMemo)(() => visibleSeries.some(({ series: series2 }) => series2.type === "bar"), [visibleSeries]);
  const { navigableSeries } = (0, import_react7.useMemo)(() => findNavigableSeries(visibleSeries), [visibleSeries]);
  const containsMultipleSeries = navigableSeries.length > 1;
  const onBarGroupFocus = () => {
    const groupIndex = highlightedGroupIndex !== null && highlightedGroupIndex !== void 0 ? highlightedGroupIndex : 0;
    setTargetX(xScale.domain[groupIndex]);
    highlightGroup(groupIndex);
  };
  const onLineFocus = () => {
    var _a;
    if (verticalMarkerX === null) {
      const index = !isRtl ? 0 : allUniqueX.length - 1;
      if (containsMultipleSeries) {
        moveToLineGroupIndex(index);
      } else {
        moveBetweenSeries(0, (_a = allUniqueX[index].datum) === null || _a === void 0 ? void 0 : _a.x);
      }
    }
  };
  const onFocus = () => {
    if (isGroupNavigation) {
      onBarGroupFocus();
    } else {
      onLineFocus();
    }
  };
  const allUniqueX = (0, import_react7.useMemo)(() => {
    const result = [];
    for (let i = 0; i < scaledSeries.length; i += 1) {
      const point = scaledSeries[i];
      if (point !== void 0 && (!result.length || result[result.length - 1].scaledX !== point.x)) {
        result.push({ scaledX: point.x, datum: point.datum });
      }
    }
    return result;
  }, [scaledSeries]);
  const moveBetweenSeries = (0, import_react7.useCallback)((direction, startFrom) => {
    var _a, _b, _c, _d, _e;
    if (isGroupNavigation) {
      return;
    }
    const xOffset = xScale.isCategorical() ? Math.max(0, xScale.d3Scale.bandwidth() - 1) / 2 : 0;
    const MAX_SERIES_INDEX = navigableSeries.length - 1;
    let previousSeriesIndex = -1;
    if (highlightedSeries) {
      previousSeriesIndex = navigableSeries.indexOf(highlightedSeries);
    }
    const firstPossibleIndex = containsMultipleSeries ? -1 : 0;
    let nextSeriesIndex = 0;
    if (previousSeriesIndex !== null) {
      nextSeriesIndex = previousSeriesIndex + direction;
      if (nextSeriesIndex > MAX_SERIES_INDEX) {
        nextSeriesIndex = firstPossibleIndex;
      } else if (nextSeriesIndex < firstPossibleIndex) {
        nextSeriesIndex = MAX_SERIES_INDEX;
      }
    }
    if (nextSeriesIndex === -1) {
      highlightSeries(null);
      highlightPoint(null);
      return;
    }
    const nextSeries = navigableSeries[nextSeriesIndex];
    const nextInternalSeries = series.filter(({ series: series2 }) => series2 === nextSeries)[0];
    const actualTargetX = (_a = targetX !== null && targetX !== void 0 ? targetX : startFrom) !== null && _a !== void 0 ? _a : null;
    let targetXPoint = ((_b = xScale.d3Scale(actualTargetX)) !== null && _b !== void 0 ? _b : NaN) + xOffset;
    if (!isFinite(targetXPoint)) {
      targetXPoint = 0;
    }
    if (nextSeries.type === "line") {
      const nextScaledSeries = scaledSeries.filter((it) => it.series === nextSeries);
      const closestNextSeriesPoint = nextScaledSeries.reduce((prev, curr) => Math.abs(curr.x - targetXPoint) < Math.abs(prev.x - targetXPoint) ? curr : prev, { x: -Infinity, y: -Infinity });
      highlightPoint(Object.assign(Object.assign({}, closestNextSeriesPoint), { color: nextInternalSeries.color, series: nextSeries }));
    } else if (isYThreshold(nextSeries)) {
      const scaledTargetIndex = scaledSeries.map((it) => {
        var _a2;
        return ((_a2 = it.datum) === null || _a2 === void 0 ? void 0 : _a2.x) || null;
      }).indexOf(actualTargetX);
      highlightPoint({
        x: targetXPoint,
        y: (_c = yScale.d3Scale(nextSeries.y)) !== null && _c !== void 0 ? _c : NaN,
        color: nextInternalSeries.color,
        series: nextSeries,
        datum: (_d = scaledSeries[scaledTargetIndex]) === null || _d === void 0 ? void 0 : _d.datum
      });
    } else if (isXThreshold(nextSeries)) {
      highlightPoint({
        x: (_e = xScale.d3Scale(nextSeries.x)) !== null && _e !== void 0 ? _e : NaN,
        y: yScale.d3Scale.range()[0],
        color: nextInternalSeries.color,
        series: nextSeries,
        datum: { x: nextSeries.x, y: NaN }
      });
    }
  }, [
    isGroupNavigation,
    xScale,
    navigableSeries,
    highlightedSeries,
    containsMultipleSeries,
    highlightSeries,
    highlightPoint,
    series,
    targetX,
    scaledSeries,
    yScale
  ]);
  const moveWithinSeries = (0, import_react7.useCallback)((direction) => {
    var _a;
    const series2 = highlightedSeries || visibleSeries[0].series;
    if (series2.type === "line" || isYThreshold(series2)) {
      const targetScaledSeries = scaledSeries.filter((it) => it.series === series2);
      const previousPoint = highlightedPoint || targetScaledSeries[0];
      const indexOfPreviousPoint = targetScaledSeries.map((it) => it.x).indexOf(previousPoint.x);
      const nextPointIndex = circleIndex(indexOfPreviousPoint + direction, [0, targetScaledSeries.length - 1]);
      const nextPoint = targetScaledSeries[nextPointIndex];
      setTargetX(((_a = nextPoint.datum) === null || _a === void 0 ? void 0 : _a.x) || null);
      setXIndex(nextPointIndex);
      highlightPoint(nextPoint);
    } else if (series2.type === "bar") {
      const xDomain = xScale.domain;
      const MAX_GROUP_INDEX = xDomain.length - 1;
      let nextGroupIndex = 0;
      if (highlightedGroupIndex !== null) {
        if (isRtl && !horizontalBars) {
          direction = -direction;
        }
        nextGroupIndex = highlightedGroupIndex + direction;
        if (nextGroupIndex > MAX_GROUP_INDEX) {
          nextGroupIndex = 0;
        } else if (nextGroupIndex < 0) {
          nextGroupIndex = MAX_GROUP_INDEX;
        }
      }
      const nextDomainIndex = nextValidDomainIndex(nextGroupIndex, barGroups, direction);
      setTargetX(xDomain[nextDomainIndex]);
      highlightGroup(nextDomainIndex);
    }
  }, [
    highlightedSeries,
    visibleSeries,
    scaledSeries,
    highlightedPoint,
    highlightPoint,
    xScale.domain,
    highlightedGroupIndex,
    barGroups,
    highlightGroup,
    isRtl,
    horizontalBars
  ]);
  const moveToLineGroupIndex = (0, import_react7.useCallback)((index) => {
    var _a, _b, _c, _d;
    const point = allUniqueX[index];
    setXIndex(index);
    setTargetX(((_a = point.datum) === null || _a === void 0 ? void 0 : _a.x) || null);
    highlightX({ scaledX: (_b = point === null || point === void 0 ? void 0 : point.scaledX) !== null && _b !== void 0 ? _b : null, label: (_d = (_c = point.datum) === null || _c === void 0 ? void 0 : _c.x) !== null && _d !== void 0 ? _d : null });
  }, [allUniqueX, highlightX]);
  const moveWithinXAxis = (0, import_react7.useCallback)((direction) => {
    if (highlightedSeries || isGroupNavigation) {
      moveWithinSeries(direction);
    } else {
      const nextPointGroupIndex = circleIndex(xIndex + direction, [0, allUniqueX.length - 1]);
      moveToLineGroupIndex(nextPointGroupIndex);
    }
  }, [highlightedSeries, isGroupNavigation, moveWithinSeries, xIndex, allUniqueX.length, moveToLineGroupIndex]);
  const onKeyDown = (0, import_react7.useCallback)((event) => {
    const keyCode = event.keyCode;
    if (keyCode !== KeyCode.up && keyCode !== KeyCode.right && keyCode !== KeyCode.down && keyCode !== KeyCode.left && keyCode !== KeyCode.space && keyCode !== KeyCode.enter) {
      return;
    }
    event.preventDefault();
    if (isHandlersDisabled) {
      return;
    }
    if (keyCode === KeyCode.down || keyCode === KeyCode.up) {
      moveBetweenSeries(keyCode === KeyCode.down ? 1 : -1);
    } else if (keyCode === KeyCode.left || keyCode === KeyCode.right) {
      moveWithinXAxis(keyCode === KeyCode.right ? 1 : -1);
    } else if (keyCode === KeyCode.enter || keyCode === KeyCode.space) {
      pinPopover();
    }
  }, [isHandlersDisabled, moveBetweenSeries, moveWithinXAxis, pinPopover]);
  return { isGroupNavigation, onFocus, onKeyDown, xIndex };
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/hooks/use-popover.js
var import_react8 = __toESM(require_react());
function usePopover() {
  const [state, setState] = (0, import_react8.useState)("closed");
  const isPopoverOpen = state !== "closed";
  const isPopoverPinned = state === "pinned";
  const showPopover = (0, import_react8.useCallback)(() => setState("open"), []);
  const pinPopover = (0, import_react8.useCallback)(() => setState("pinned"), []);
  const dismissPopover = (0, import_react8.useCallback)(() => setState("closed"), []);
  return { isPopoverOpen, isPopoverPinned, showPopover, pinPopover, dismissPopover };
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/make-scaled-bar-groups.js
function makeScaledBarGroups(series, xScale, plotWidth, plotHeight, axis) {
  if (!xScale.isCategorical()) {
    return [];
  }
  return xScale.domain.map((x) => {
    const scaledX = xScale.d3Scale(x);
    const isValid = typeof scaledX !== "undefined" && isFinite(scaledX);
    return {
      x,
      isValid,
      hasData: series.some(({ series: series2 }) => {
        if (isYThreshold(series2)) {
          return true;
        }
        if (isXThreshold(series2)) {
          return false;
        }
        if (isDataSeries(series2)) {
          return series2.data.some((datum) => matchesX(datum.x, x));
        }
        return false;
      }),
      position: axis === "x" ? {
        x: 0,
        y: isValid ? scaledX || 0 : 0,
        width: plotWidth,
        height: xScale.d3Scale.bandwidth()
      } : {
        x: isValid ? scaledX || 0 : 0,
        y: 0,
        width: xScale.d3Scale.bandwidth(),
        height: plotHeight
      }
    };
  });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/make-scaled-series.js
function makeScaledSeries(allSeries, xScale, yScale) {
  const xOffset = xScale.isCategorical() ? Math.max(0, xScale.d3Scale.bandwidth() - 1) / 2 : 0;
  const scaleX = (x) => (xScale.d3Scale(x) || 0) + xOffset;
  const scaleY = (y) => yScale.d3Scale(y) || 0;
  const allX = getAllX(allSeries);
  function mergeLineSeriesPointsWithXThresholds(scaledPoints, xThresholdSeries, xThresholdSeriesColor) {
    var _a, _b;
    const x = scaleX(xThresholdSeries.x);
    let bisectIndex = -1;
    for (let i = 0; i < scaledPoints.length - 1; i++) {
      if (scaledPoints[i].x < x && x < scaledPoints[i + 1].x) {
        bisectIndex = i;
        break;
      }
    }
    if (bisectIndex !== -1) {
      const prevY = ((_a = scaledPoints[bisectIndex].datum) === null || _a === void 0 ? void 0 : _a.y) || 0;
      const nextY = ((_b = scaledPoints[bisectIndex + 1].datum) === null || _b === void 0 ? void 0 : _b.y) || 0;
      const averageY = (prevY + nextY) / 2;
      scaledPoints.push({
        x,
        y: scaleY(averageY),
        datum: { x: xThresholdSeries.x, y: NaN },
        series: scaledPoints[bisectIndex].series,
        color: xThresholdSeriesColor
      });
    }
  }
  const scaledSeriesX = allSeries.map(({ series, color }) => {
    const scaledPoints = [];
    if (series.type === "line") {
      for (const datum of series.data) {
        scaledPoints.push({ x: scaleX(datum.x), y: scaleY(datum.y), datum, series, color });
      }
      scaledPoints.sort((s1, s2) => s1.x - s2.x);
      for (const otherSeries of allSeries) {
        if (isXThreshold(otherSeries.series)) {
          mergeLineSeriesPointsWithXThresholds(scaledPoints, otherSeries.series, otherSeries.color);
        }
      }
    } else if (isYThreshold(series)) {
      for (const x of allX) {
        scaledPoints.push({ x: scaleX(x), y: scaleY(series.y), datum: { x, y: series.y }, series, color });
      }
      if (allX.length === 0) {
        scaledPoints.push({ x: NaN, y: scaleY(series.y), series, color });
      }
    } else if (isXThreshold(series)) {
      scaledPoints.push({ x: scaleX(series.x), y: NaN, datum: { x: series.x, y: NaN }, series, color });
    }
    return scaledPoints;
  });
  return flatten(scaledSeriesX).sort((s1, s2) => s1.x - s2.x);
}
function getAllX(series) {
  const addDataXSet = /* @__PURE__ */ new Set();
  for (const { series: s } of series) {
    switch (s.type) {
      // Add all X values from data series.
      case "bar":
      case "line":
        for (const d of s.data) {
          addDataXSet.add(d.x);
        }
        break;
      case "threshold":
        if (isXThreshold(s)) {
          addDataXSet.add(s.x);
        }
        break;
    }
  }
  const allDataX = [];
  addDataXSet.forEach((x) => allDataX.push(x));
  return allDataX;
}
function flatten(arrays) {
  const merged = [];
  for (const array of arrays) {
    for (const item of array) {
      merged.push(item);
    }
  }
  return merged;
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/chart-container.js
var INLINE_START_LABELS_MARGIN = 16;
var BLOCK_END_LABELS_OFFSET = 12;
var fallbackContainerWidth = 500;
function ChartContainer(_a) {
  var _b, _c;
  var { fitHeight, hasFilters, height: explicitPlotHeight, series, visibleSeries, highlightedSeries, onHighlightChange, highlightedPoint, setHighlightedPoint, highlightedGroupIndex, setHighlightedGroupIndex, detailPopoverFooter, detailPopoverSize = "medium", stackedBars = false, horizontalBars = false, xScaleType, yScaleType, xTickFormatter, yTickFormatter, emphasizeBaselineAxis, xTitle, yTitle, ariaLabel, ariaLabelledby, ariaDescription, i18nStrings = {}, detailPopoverSeriesContent } = _a, props = __rest(_a, ["fitHeight", "hasFilters", "height", "series", "visibleSeries", "highlightedSeries", "onHighlightChange", "highlightedPoint", "setHighlightedPoint", "highlightedGroupIndex", "setHighlightedGroupIndex", "detailPopoverFooter", "detailPopoverSize", "stackedBars", "horizontalBars", "xScaleType", "yScaleType", "xTickFormatter", "yTickFormatter", "emphasizeBaselineAxis", "xTitle", "yTitle", "ariaLabel", "ariaLabelledby", "ariaDescription", "i18nStrings", "detailPopoverSeriesContent"]);
  const plotRef = (0, import_react9.useRef)(null);
  const verticalMarkerRef = (0, import_react9.useRef)(null);
  const [inlineStartLabelsWidth, setInlineStartLabelsWidth] = (0, import_react9.useState)(0);
  const [verticalMarkerX, setVerticalMarkerX] = (0, import_react9.useState)(null);
  const [detailsPopoverText, setDetailsPopoverText] = (0, import_react9.useState)("");
  const [containerWidth, containerMeasureRef] = useContainerWidth(fallbackContainerWidth);
  const maxInlineStartLabelsWidth = Math.round(containerWidth / 2);
  const plotWidth = containerWidth ? (
    // Calculate the minimum between inlineStartLabelsWidth and maxInlineStartLabelsWidth for extra safety because inlineStarteLabelsWidth could be out of date
    Math.max(0, containerWidth - Math.min(inlineStartLabelsWidth, maxInlineStartLabelsWidth) - INLINE_START_LABELS_MARGIN)
  ) : fallbackContainerWidth;
  const containerRefObject = (0, import_react9.useRef)(null);
  const containerRef = useMergeRefs(containerMeasureRef, containerRefObject);
  const popoverRef = (0, import_react9.useRef)(null);
  const isRtl = getIsRtl(containerRefObject.current);
  const xDomain = props.xDomain || computeDomainX(series, xScaleType);
  const yDomain = props.yDomain || computeDomainY(series, yScaleType, stackedBars);
  const linesOnly = series.every(({ series: series2 }) => series2.type === "line" || series2.type === "threshold");
  function getXAxisProps(size, range) {
    const tickCount = getXTickCount(size);
    const scale = new ChartScale(xScaleType, xDomain, range, linesOnly);
    const ticks = createXTicks(scale, tickCount);
    return {
      axis: "x",
      tickCount,
      scale,
      ticks,
      tickFormatter: xTickFormatter,
      title: xTitle,
      ariaRoleDescription: i18nStrings.xAxisAriaRoleDescription
    };
  }
  function getYAxisProps(size, range) {
    const tickCount = getYTickCount(size);
    const scale = new NumericChartScale(yScaleType, yDomain, range, props.yDomain ? null : tickCount);
    const ticks = createYTicks(scale, tickCount);
    return {
      axis: "y",
      tickCount,
      scale,
      ticks,
      tickFormatter: yTickFormatter,
      title: yTitle,
      ariaRoleDescription: i18nStrings.yAxisAriaRoleDescription
    };
  }
  const bottomAxisProps = !horizontalBars ? getXAxisProps(plotWidth, !isRtl ? [0, plotWidth] : [plotWidth, 0]) : getYAxisProps(plotWidth, !isRtl ? [0, plotWidth] : [plotWidth, 0]);
  const blockEndLabelsProps = useBLockEndLabels(Object.assign({}, bottomAxisProps));
  const plotMeasureRef = (0, import_react9.useRef)(null);
  const measuredHeight = useHeightMeasure(() => plotMeasureRef.current, !fitHeight);
  const plotHeight = fitHeight ? measuredHeight !== null && measuredHeight !== void 0 ? measuredHeight : 0 : explicitPlotHeight;
  const leftAxisProps = !horizontalBars ? getYAxisProps(plotHeight, [plotHeight, 0]) : getXAxisProps(plotHeight, [0, plotHeight]);
  const xAxisProps = bottomAxisProps.axis === "x" ? bottomAxisProps : leftAxisProps.axis === "x" ? leftAxisProps : null;
  const yAxisProps = bottomAxisProps.axis === "y" ? bottomAxisProps : leftAxisProps.axis === "y" ? leftAxisProps : null;
  if (!xAxisProps || !yAxisProps) {
    throw new Error("Invariant violation: invalid axis props.");
  }
  const highlightedPointRef = (0, import_react9.useRef)(null);
  const highlightedGroupRef = (0, import_react9.useRef)(null);
  const x = !horizontalBars ? "x" : "y";
  const y = !horizontalBars ? "y" : "x";
  const scaledSeries = makeScaledSeries(visibleSeries, xAxisProps.scale, yAxisProps.scale);
  const barGroups = makeScaledBarGroups(visibleSeries, xAxisProps.scale, plotWidth, plotHeight, y);
  const { isPopoverOpen, isPopoverPinned, showPopover, pinPopover, dismissPopover } = usePopover();
  const [isHandlersDisabled, setHandlersDisabled] = (0, import_react9.useState)(false);
  (0, import_react9.useEffect)(() => {
    if (isPopoverPinned) {
      setHandlersDisabled(true);
    } else {
      const timeoutId = setTimeout(() => setHandlersDisabled(false), 25);
      return () => clearTimeout(timeoutId);
    }
  }, [isPopoverPinned]);
  const highlightSeries = (0, import_react9.useCallback)((series2) => {
    if (series2 !== highlightedSeries) {
      onHighlightChange(series2);
    }
  }, [highlightedSeries, onHighlightChange]);
  const highlightPoint = (0, import_react9.useCallback)((point2) => {
    var _a2, _b2;
    setHighlightedGroupIndex(null);
    setHighlightedPoint(point2);
    if (point2) {
      highlightSeries(point2.series);
      setVerticalMarkerX({
        scaledX: point2.x,
        label: (_b2 = (_a2 = point2.datum) === null || _a2 === void 0 ? void 0 : _a2.x) !== null && _b2 !== void 0 ? _b2 : null
      });
    }
  }, [setHighlightedGroupIndex, setHighlightedPoint, highlightSeries]);
  const clearAllHighlights = (0, import_react9.useCallback)(() => {
    setHighlightedPoint(null);
    highlightSeries(null);
    setHighlightedGroupIndex(null);
  }, [highlightSeries, setHighlightedGroupIndex, setHighlightedPoint]);
  const highlightX = (0, import_react9.useCallback)((marker) => {
    if (marker) {
      clearAllHighlights();
    }
    setVerticalMarkerX(marker);
  }, [clearAllHighlights]);
  const highlightGroup = (0, import_react9.useCallback)((groupIndex) => {
    highlightSeries(null);
    setHighlightedPoint(null);
    setHighlightedGroupIndex(groupIndex);
  }, [highlightSeries, setHighlightedPoint, setHighlightedGroupIndex]);
  const clearHighlightedSeries = (0, import_react9.useCallback)(() => {
    clearAllHighlights();
    dismissPopover();
  }, [dismissPopover, clearAllHighlights]);
  const _d = useNavigation({
    series,
    visibleSeries,
    scaledSeries,
    barGroups,
    xScale: xAxisProps.scale,
    yScale: yAxisProps.scale,
    highlightedPoint,
    highlightedGroupIndex,
    highlightedSeries,
    isHandlersDisabled,
    pinPopover,
    highlightSeries,
    highlightGroup,
    highlightPoint,
    highlightX,
    clearHighlightedSeries,
    verticalMarkerX,
    isRtl: !!isRtl,
    horizontalBars
  }), { isGroupNavigation } = _d, handlers = __rest(_d, ["isGroupNavigation"]);
  const { onSVGMouseMove, onSVGMouseOut, onPopoverLeave } = useMouseHover({
    scaledSeries,
    barGroups,
    plotRef,
    popoverRef,
    highlightPoint,
    highlightGroup,
    clearHighlightedSeries,
    isGroupNavigation,
    isHandlersDisabled,
    highlightX
  });
  const highlightedX = (0, import_react9.useMemo)(() => {
    var _a2, _b2, _c2;
    if (highlightedGroupIndex !== null) {
      return (_a2 = barGroups[highlightedGroupIndex]) === null || _a2 === void 0 ? void 0 : _a2.x;
    }
    if (verticalMarkerX !== null) {
      return verticalMarkerX.label;
    }
    return (_c2 = (_b2 = highlightedPoint === null || highlightedPoint === void 0 ? void 0 : highlightedPoint.datum) === null || _b2 === void 0 ? void 0 : _b2.x) !== null && _c2 !== void 0 ? _c2 : null;
  }, [highlightedPoint, verticalMarkerX, highlightedGroupIndex, barGroups]);
  (0, import_react9.useEffect)(() => {
    const onKeyDown = (event) => {
      if (event.key === "Escape") {
        dismissPopover();
      }
    };
    document.addEventListener("keydown", onKeyDown);
    return () => document.removeEventListener("keydown", onKeyDown);
  }, [dismissPopover]);
  (0, import_react9.useLayoutEffect)(() => {
    if (highlightedX !== null || highlightedPoint !== null) {
      showPopover();
    }
  }, [highlightedX, highlightedPoint, showPopover]);
  const onPopoverDismiss = (outsideClick) => {
    dismissPopover();
    if (!outsideClick) {
      setTimeout(() => {
        var _a2, _b2;
        const isSomeElementHighlighted = !!(highlightedPoint || highlightedGroupIndex !== null || verticalMarkerX);
        if (isSomeElementHighlighted) {
          (_a2 = plotRef.current) === null || _a2 === void 0 ? void 0 : _a2.focusApplication();
        } else {
          (_b2 = plotRef.current) === null || _b2 === void 0 ? void 0 : _b2.focusPlot();
        }
      }, 0);
    } else {
      clearAllHighlights();
      setVerticalMarkerX(null);
    }
  };
  const onSVGClick = (e) => {
    if (isPopoverOpen) {
      if (isPopoverPinned) {
        dismissPopover();
      } else {
        pinPopover();
        e.preventDefault();
      }
    } else {
      showPopover();
    }
  };
  const onApplicationFocus = (event, trigger) => {
    if (trigger === "keyboard") {
      handlers.onFocus();
    } else {
    }
  };
  const onApplicationBlur = (event) => {
    const blurTarget = event.relatedTarget || event.target;
    if (blurTarget === null || !(blurTarget instanceof Element) || !nodeBelongs(containerRefObject.current, blurTarget)) {
      clearHighlightedSeries();
      setVerticalMarkerX(null);
      if (isPopoverOpen && !isPopoverPinned) {
        dismissPopover();
      }
    }
  };
  const onSVGKeyDown = handlers.onKeyDown;
  const xOffset = xAxisProps.scale.isCategorical() ? Math.max(0, xAxisProps.scale.d3Scale.bandwidth() - 1) / 2 : 0;
  let verticalLineX = null;
  if (verticalMarkerX !== null) {
    verticalLineX = verticalMarkerX.scaledX;
  } else if (isGroupNavigation && highlightedGroupIndex !== null) {
    const x2 = (_c = xAxisProps.scale.d3Scale((_b = barGroups[highlightedGroupIndex]) === null || _b === void 0 ? void 0 : _b.x)) !== null && _c !== void 0 ? _c : null;
    if (x2 !== null) {
      verticalLineX = xOffset + x2;
    }
  }
  const point = (0, import_react9.useMemo)(() => highlightedPoint ? {
    key: `${highlightedPoint.x}-${highlightedPoint.y}`,
    x: highlightedPoint.x,
    y: highlightedPoint.y,
    color: highlightedPoint.color
  } : null, [highlightedPoint]);
  const verticalMarkers = (0, import_react9.useMemo)(() => verticalLineX !== null ? scaledSeries.filter(({ x: x2, y: y2 }) => (x2 === verticalLineX || isNaN(x2)) && !isNaN(y2)).map(({ x: x2, y: y2, color }, index) => ({
    key: `${index}-${x2}-${y2}`,
    x: !horizontalBars ? verticalLineX || 0 : y2,
    y: !horizontalBars ? y2 : verticalLineX || 0,
    color
  })) : [], [scaledSeries, verticalLineX, horizontalBars]);
  const highlightedElementRef = isGroupNavigation ? highlightedGroupRef : highlightedPoint ? highlightedPointRef : verticalMarkerRef;
  const highlightDetails = (0, import_react9.useMemo)(() => {
    if (highlightedX === null) {
      return null;
    }
    if (highlightedPoint) {
      const seriesToShow = visibleSeries.filter((series2) => series2.series === (highlightedPoint === null || highlightedPoint === void 0 ? void 0 : highlightedPoint.series) || isXThreshold(series2.series));
      return formatHighlighted({
        position: highlightedX,
        series: seriesToShow,
        xTickFormatter,
        detailPopoverSeriesContent
      });
    }
    return formatHighlighted({
      position: highlightedX,
      series: visibleSeries,
      xTickFormatter,
      detailPopoverSeriesContent
    });
  }, [highlightedX, highlightedPoint, visibleSeries, xTickFormatter, detailPopoverSeriesContent]);
  const detailPopoverFooterContent = (0, import_react9.useMemo)(() => detailPopoverFooter && highlightedX ? detailPopoverFooter(highlightedX) : null, [detailPopoverFooter, highlightedX]);
  const activeAriaLabel = highlightDetails && detailsPopoverText ? `${highlightDetails.position}, ${detailsPopoverText}` : "";
  const activeLiveRegion = activeAriaLabel && !highlightedPoint && highlightedGroupIndex === null ? activeAriaLabel : "";
  const isLineXKeyboardFocused = !highlightedPoint && verticalMarkerX;
  const isRefresh = useVisualRefresh();
  return import_react9.default.createElement(CartesianChartContainer, { ref: containerRef, minHeight: explicitPlotHeight + blockEndLabelsProps.height, fitHeight: !!fitHeight, hasFilters, leftAxisLabel: import_react9.default.createElement(axis_label_default, { axis: y, position: "left", title: leftAxisProps.title }), leftAxisLabelMeasure: import_react9.default.createElement(labels_measure_default, { ticks: leftAxisProps.ticks, scale: leftAxisProps.scale, tickFormatter: leftAxisProps.tickFormatter, autoWidth: setInlineStartLabelsWidth, maxLabelsWidth: maxInlineStartLabelsWidth }), bottomAxisLabel: import_react9.default.createElement(axis_label_default, { axis: x, position: "bottom", title: bottomAxisProps.title }), chartPlot: import_react9.default.createElement(
    chart_plot_default,
    { ref: plotRef, width: "100%", height: fitHeight ? `calc(100% - ${blockEndLabelsProps.height}px)` : plotHeight, offsetBottom: blockEndLabelsProps.height, isClickable: isPopoverOpen && !isPopoverPinned, ariaLabel, ariaLabelledby, ariaDescription, ariaRoleDescription: i18nStrings === null || i18nStrings === void 0 ? void 0 : i18nStrings.chartAriaRoleDescription, ariaLiveRegion: activeLiveRegion, activeElementRef: highlightedElementRef, activeElementKey: activeAriaLabel, activeElementFocusOffset: isGroupNavigation ? 0 : isLineXKeyboardFocused ? { x: 8, y: 0 } : 3, onMouseMove: onSVGMouseMove, onMouseOut: onSVGMouseOut, onClick: onSVGClick, onApplicationFocus, onApplicationBlur, onKeyDown: onSVGKeyDown },
    import_react9.default.createElement("line", { ref: plotMeasureRef, x1: "0", x2: "0", y1: "0", y2: "100%", stroke: "transparent", strokeWidth: 1, style: { pointerEvents: "none" } }),
    import_react9.default.createElement(inline_start_labels_default, { axis: y, ticks: leftAxisProps.ticks, scale: leftAxisProps.scale, tickFormatter: leftAxisProps.tickFormatter, title: leftAxisProps.title, ariaRoleDescription: leftAxisProps.ariaRoleDescription, maxLabelsWidth: maxInlineStartLabelsWidth, plotWidth, plotHeight }),
    horizontalBars && import_react9.default.createElement(vertical_grid_lines_default, { scale: yAxisProps.scale, ticks: yAxisProps.ticks, height: plotHeight }),
    emphasizeBaselineAxis && linesOnly && import_react9.default.createElement(emphasized_baseline_default, { axis: x, scale: yAxisProps.scale, width: plotWidth, height: plotHeight }),
    import_react9.default.createElement(DataSeries, { axis: x, plotWidth, plotHeight, highlightedSeries: highlightedSeries !== null && highlightedSeries !== void 0 ? highlightedSeries : null, highlightedGroupIndex, stackedBars, isGroupNavigation, visibleSeries, xScale: xAxisProps.scale, yScale: yAxisProps.scale, isRtl: !!isRtl }),
    emphasizeBaselineAxis && !linesOnly && import_react9.default.createElement(emphasized_baseline_default, { axis: x, scale: yAxisProps.scale, width: plotWidth, height: plotHeight }),
    import_react9.default.createElement(vertical_marker_default, { key: verticalLineX || "", height: plotHeight, showPoints: highlightedPoint === null, showLine: !isGroupNavigation, points: verticalMarkers, ref: verticalMarkerRef }),
    highlightedPoint && import_react9.default.createElement(highlighted_point_default, { ref: highlightedPointRef, point, role: "button", ariaLabel: activeAriaLabel, ariaHasPopup: true, ariaExpanded: isPopoverPinned }),
    isGroupNavigation && xAxisProps.scale.isCategorical() && import_react9.default.createElement(BarGroups, { ariaLabel: activeAriaLabel, isRefresh, isPopoverPinned, barGroups, highlightedGroupIndex, highlightedGroupRef }),
    import_react9.default.createElement(block_end_labels_default, Object.assign({}, blockEndLabelsProps, { axis: x, scale: bottomAxisProps.scale, title: bottomAxisProps.title, ariaRoleDescription: bottomAxisProps.ariaRoleDescription, height: plotHeight, width: plotWidth, offsetLeft: inlineStartLabelsWidth + BLOCK_END_LABELS_OFFSET, offsetRight: BLOCK_END_LABELS_OFFSET, isRTL: isRtl }))
  ), popover: import_react9.default.createElement(chart_popover_default2, { ref: popoverRef, containerRef: containerRefObject, trackRef: highlightedElementRef, isOpen: isPopoverOpen, isPinned: isPopoverPinned, highlightDetails, onDismiss: onPopoverDismiss, size: detailPopoverSize, footer: detailPopoverFooterContent, dismissAriaLabel: i18nStrings.detailPopoverDismissAriaLabel, onMouseLeave: onPopoverLeave, onBlur: onApplicationBlur, setPopoverText: setDetailsPopoverText }) });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/chart-legend.js
var import_react10 = __toESM(require_react());
function InternalChartLegend({ series, visibleSeries, highlightedSeries, onHighlightChange, legendTitle, ariaLabel, plotContainerRef }) {
  const legendItems = (0, import_react10.useMemo)(() => {
    return series.filter((s) => visibleSeries.indexOf(s.series) !== -1).map(({ series: series2, color }) => ({
      label: series2.title,
      marker: import_react10.default.createElement(chart_series_marker_default, { color, type: chartLegendMap[series2.type] }),
      datum: series2
    }));
  }, [series, visibleSeries]);
  return import_react10.default.createElement(chart_legend_default, { series: legendItems, highlightedSeries: highlightedSeries || null, onHighlightChange, legendTitle, ariaLabel, plotContainerRef });
}

// node_modules/.pnpm/@risk-smart+themed-cloudscape-components@0.1.8_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@risk-smart/themed-cloudscape-components/mixed-line-bar-chart/internal.js
function InternalMixedLineBarChart(_a) {
  var { fitHeight, height, xScaleType, yScaleType, xDomain, yDomain, xTickFormatter, yTickFormatter, highlightedSeries: controlledHighlightedSeries, visibleSeries: controlledVisibleSeries, series: externalSeries, onFilterChange, onHighlightChange: controlledOnHighlightChange, i18nStrings, ariaLabel, ariaLabelledby, ariaDescription, xTitle, yTitle, stackedBars, horizontalBars, hideFilter, additionalFilters, hideLegend, legendTitle, statusType, detailPopoverSize, detailPopoverFooter, detailPopoverSeriesContent, emphasizeBaselineAxis, empty, noMatch, errorText, loadingText, recoveryText, onRecoveryClick, __internalRootRef = null } = _a, props = __rest(_a, ["fitHeight", "height", "xScaleType", "yScaleType", "xDomain", "yDomain", "xTickFormatter", "yTickFormatter", "highlightedSeries", "visibleSeries", "series", "onFilterChange", "onHighlightChange", "i18nStrings", "ariaLabel", "ariaLabelledby", "ariaDescription", "xTitle", "yTitle", "stackedBars", "horizontalBars", "hideFilter", "additionalFilters", "hideLegend", "legendTitle", "statusType", "detailPopoverSize", "detailPopoverFooter", "detailPopoverSeriesContent", "emphasizeBaselineAxis", "empty", "noMatch", "errorText", "loadingText", "recoveryText", "onRecoveryClick", "__internalRootRef"]);
  const baseProps = getBaseProps(props);
  const containerRef = (0, import_react11.useRef)(null);
  (0, import_react11.useEffect)(() => {
    const gotBarSeries = externalSeries.some((s) => s.type === "bar");
    const gotLineSeries = externalSeries.some((s) => s.type === "line");
    if (xScaleType !== "categorical" && gotBarSeries) {
      warnOnce("MixedLineBarChart", `Bar series cannot be used with a ${xScaleType} scale. Use a categorical x axis instead.`);
    }
    if (horizontalBars && gotLineSeries) {
      warnOnce("MixedLineBarChart", `Property horizontalBars can only be used with charts that contain only bar or threshold series.`);
    }
    for (const s of externalSeries) {
      if (s.type === "threshold" && s.x !== void 0 && s.y !== void 0) {
        warnOnce("MixedLineBarChart", `Series of type "threshold" must contain either x or y property.`);
      }
      if (s.type === "threshold" && s.x === void 0 && s.y === void 0) {
        warnOnce("MixedLineBarChart", `Series of type "threshold" must contain either x or y property.`);
      }
    }
  }, [xScaleType, horizontalBars, externalSeries]);
  const series = (0, import_react11.useMemo)(() => {
    const colors = createCategoryColorScale(externalSeries, (it) => isYThreshold(it) || isXThreshold(it), (it) => it.color || null);
    return externalSeries.map((s, i) => ({ index: i, color: colors[i], series: s }));
  }, [externalSeries]);
  const [highlightedPoint, setHighlightedPoint] = (0, import_react11.useState)(null);
  const [highlightedGroupIndex, setHighlightedGroupIndex] = (0, import_react11.useState)(null);
  const [highlightedSeries = null, setHighlightedSeries] = useControllable(controlledHighlightedSeries, controlledOnHighlightChange, null, {
    componentName: "MixedLineBarChart",
    controlledProp: "highlightedSeries",
    changeHandler: "onHighlightChange"
  });
  const [legendSeries, setLegendSeries] = (0, import_react11.useState)(highlightedSeries);
  (0, import_react11.useEffect)(() => {
    setLegendSeries(controlledHighlightedSeries || null);
  }, [controlledHighlightedSeries]);
  const [externalVisibleSeries, setExternalVisibleSeries] = useControllable(controlledVisibleSeries, onFilterChange, externalSeries, {
    componentName: "MixedLineBarChart",
    controlledProp: "visibleSeries",
    changeHandler: "onFilterChange"
  });
  if (isDevelopment) {
    const previousSeries = usePrevious(externalSeries);
    const hasPrevious = !!(previousSeries && previousSeries.length);
    const hasCurrent = !!externalSeries.length;
    if (hasPrevious && hasCurrent && externalSeries !== previousSeries && !controlledVisibleSeries && !hideFilter) {
      warnOnce("MixedLineBarChart", "The `series` value passed into the component changed. This may cause problems with filtering - we recommend that you make the `series` value constant, or provide a `visibleSeries` value that derives from the current `series` value.");
    }
  }
  const visibleSeries = (0, import_react11.useMemo)(() => series.filter((s) => (externalVisibleSeries === null || externalVisibleSeries === void 0 ? void 0 : externalVisibleSeries.indexOf(s.series)) !== -1), [series, externalVisibleSeries]);
  const filterChange = (selectedSeries) => {
    setExternalVisibleSeries(selectedSeries);
    fireNonCancelableEvent(onFilterChange, {
      visibleSeries: selectedSeries
    });
  };
  const onHighlightChange = (series2) => {
    setHighlightedSeries(series2);
    fireNonCancelableEvent(controlledOnHighlightChange, {
      highlightedSeries: series2
    });
    setLegendSeries(series2);
  };
  const onBlur = (event) => {
    if (event.relatedTarget && !nodeBelongs(containerRef.current, event.relatedTarget)) {
      highlightedSeries && onHighlightChange(highlightedSeries);
      setHighlightedPoint(null);
      setHighlightedGroupIndex(null);
      setLegendSeries(null);
    }
  };
  const { isEmpty, isNoMatch, showChart } = getChartStatus({
    externalData: externalSeries,
    visibleData: visibleSeries || [],
    statusType
  });
  const showFilters = statusType === "finished" && (!isEmpty || isNoMatch) && (additionalFilters || !hideFilter);
  const showLegend = !hideLegend && !isEmpty && statusType === "finished";
  const reserveLegendSpace = !showChart && !hideLegend;
  const reserveFilterSpace = !showChart && !isNoMatch && (!hideFilter || additionalFilters);
  const mergedRef = useMergeRefs(containerRef, __internalRootRef);
  const filterItems = series.map(({ series: series2, color }) => ({
    label: series2.title,
    marker: import_react11.default.createElement(chart_series_marker_default, { type: chartLegendMap[series2.type], color }),
    datum: series2
  }));
  return import_react11.default.createElement(ChartWrapper, Object.assign({ ref: mergedRef }, baseProps, { className: clsx_default(baseProps.className, styles_css_default2.root), fitHeight: !!fitHeight, contentMinHeight: height, defaultFilter: showFilters && !hideFilter ? import_react11.default.createElement(chart_filter_default, { series: filterItems, onChange: filterChange, selectedSeries: externalVisibleSeries || [], i18nStrings }) : null, additionalFilters: showFilters ? additionalFilters : null, reserveFilterSpace: !!reserveFilterSpace, reserveLegendSpace: !!reserveLegendSpace, chartStatus: import_react11.default.createElement(ChartStatusContainer, { isEmpty, isNoMatch, showChart, statusType, empty, noMatch, loadingText, errorText, recoveryText, onRecoveryClick }), chart: showChart ? import_react11.default.createElement(ChartContainer, { fitHeight, hasFilters: !!showFilters, height, xScaleType, yScaleType, xDomain, yDomain, xTickFormatter: xTickFormatter !== null && xTickFormatter !== void 0 ? xTickFormatter : i18nStrings === null || i18nStrings === void 0 ? void 0 : i18nStrings.xTickFormatter, yTickFormatter: yTickFormatter !== null && yTickFormatter !== void 0 ? yTickFormatter : i18nStrings === null || i18nStrings === void 0 ? void 0 : i18nStrings.yTickFormatter, emphasizeBaselineAxis, stackedBars, horizontalBars, series, visibleSeries, highlightedSeries, onHighlightChange, highlightedPoint, setHighlightedPoint, highlightedGroupIndex, setHighlightedGroupIndex, detailPopoverSize, detailPopoverFooter, detailPopoverSeriesContent, xTitle, yTitle, ariaLabel, ariaLabelledby, ariaDescription, i18nStrings }) : null, legend: showLegend ? import_react11.default.createElement(InternalChartLegend, { series, visibleSeries: externalVisibleSeries || [], highlightedSeries: legendSeries, onHighlightChange, legendTitle, ariaLabel: i18nStrings === null || i18nStrings === void 0 ? void 0 : i18nStrings.legendAriaLabel, plotContainerRef: containerRef }) : null, onBlur }));
}

export {
  InternalMixedLineBarChart
};
//# sourceMappingURL=chunk-S6T7KN7B.js.map
